# Server-only — never NEXT_PUBLIC_ (CONTEXT D-05 + PATTERNS Pattern B).
# Browser must never see this; the FastAPI URL is read inside Next route
# handlers (apps/web/app/api/*/route.ts) and never exposed to the client bundle.
FASTAPI_URL=http://localhost:8000
