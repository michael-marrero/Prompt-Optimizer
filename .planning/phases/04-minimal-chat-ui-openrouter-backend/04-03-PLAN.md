---
phase: 04
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - apps/web/app/api/chat/route.ts
  - apps/web/app/api/threads/route.ts
  - apps/web/app/api/threads/[id]/route.ts
  - apps/web/app/api/settings/route.ts
  - apps/web/app/api/health/route.ts
autonomous: true
requirements:
  - UI-17
user_setup: []

must_haves:
  truths:
    - "Every file in apps/web/app/api/**/route.ts declares `export const runtime = 'nodejs'` AND `export const dynamic = 'force-dynamic'` at module top — Pattern C is non-negotiable (RESEARCH Critical Finding #3, Pitfall 1)"
    - "FASTAPI_URL is read via `process.env.FASTAPI_URL ?? 'http://localhost:8000'` ONLY inside the route handler files; never re-exported; never referenced from any client component or lib file"
    - "apps/web/app/api/chat/route.ts strips the AI SDK v5+ {messages:[...]} body to FastAPI's {message: <latest user text>} per D-08 — FastAPI's get_thread_messages is the single source of history truth"
    - "apps/web/app/api/chat/route.ts forwards `signal: req.signal` to its upstream fetch — D-09 AbortController chain is wired"
    - "apps/web/app/api/chat/route.ts returns the response IMMEDIATELY after wrapping the upstream body in translateNamedSSEToUIMessageStream — never `await upstream.text()` (Pitfall 2)"
    - "apps/web/app/api/chat/route.ts sets `x-vercel-ai-ui-message-stream: v1` response header — Pitfall 7 (AI SDK v6 client requires this)"
    - "apps/web/app/api/chat/route.ts returns 503 with body `{error: 'API unavailable — is uvicorn running?'}` on ECONNREFUSED — UI-SPEC §17 network-down banner text"
    - "apps/web/app/api/settings/route.ts scrubs the plaintext key from EVERY error path via `.replace(key, '***')` — D-18 belt-and-suspenders (Pattern G)"
    - "apps/web/app/api/settings/route.ts NEVER logs the key string (no `console.log(req.body)`, no `console.error(key)`)"
    - "apps/web/app/api/health/route.ts is a pure passthrough returning the FastAPI healthz body verbatim so apps/web/lib/api-client.ts:getHealth() returns the documented HealthResponse shape"
    - "apps/web/app/api/threads/route.ts (POST) creates a new thread with body `{title}` → upstream {title}; apps/web/app/api/threads/[id]/route.ts (GET) returns the upstream thread + messages"
  artifacts:
    - path: "apps/web/app/api/chat/route.ts"
      provides: "SSE proxy — D-07 named-event → AI SDK v6 UI Message Stream translation; D-09 abort chain; D-08 body stripping; Pitfall 7 header"
      exports: ["POST", "runtime", "dynamic"]
    - path: "apps/web/app/api/threads/route.ts"
      provides: "Thread CRUD proxy — POST creates default thread for the Plan 07 first-run flow"
      exports: ["POST", "runtime", "dynamic"]
    - path: "apps/web/app/api/threads/[id]/route.ts"
      provides: "Single-thread fetch — GET /api/threads/<id>"
      exports: ["GET", "runtime", "dynamic"]
    - path: "apps/web/app/api/settings/route.ts"
      provides: "Key submission proxy — D-18 + Pattern G scrubbing; translates browser-friendly {provider,key} to FastAPI's {keys:{provider:key}}"
      exports: ["POST", "runtime", "dynamic"]
    - path: "apps/web/app/api/health/route.ts"
      provides: "FINAL /api/health proxy — overwrites Plan 01's Wave-0 placeholder with the real upstream passthrough"
      exports: ["GET", "runtime", "dynamic"]
  key_links:
    - from: "apps/web/app/api/chat/route.ts"
      to: "apps/web/lib/sse-translate.ts"
      via: "translateNamedSSEToUIMessageStream(upstream.body)"
      pattern: "translateNamedSSEToUIMessageStream"
    - from: "apps/web/app/api/chat/route.ts"
      to: "apps/api/routes/turn.py"
      via: "POST /api/v1/threads/{thread_id}/turn upstream fetch"
      pattern: "/api/v1/threads/.*turn"
    - from: "apps/web/app/api/settings/route.ts"
      to: "apps/api/routes/settings.py"
      via: "PATCH /api/v1/settings with {keys: {provider: key}} body"
      pattern: "PATCH.*api/v1/settings"
    - from: "apps/web/app/api/health/route.ts"
      to: "apps/api/routes/health.py"
      via: "GET /api/v1/healthz passthrough"
      pattern: "/api/v1/healthz"
    - from: "apps/web/app/api/threads/route.ts"
      to: "apps/api/routes/threads.py"
      via: "POST /api/v1/threads with {title} body"
      pattern: "POST.*api/v1/threads"
---

<objective>
Wave 2 — Next.js route handlers. Author the five server-side proxy routes under `apps/web/app/api/`: chat (SSE proxy + translation), settings (key submission with scrubbing), health (passthrough for D-16 boot check), threads (POST for default-thread auto-create), threads/[id] (GET). Every route handler runs on Node runtime (NOT Edge — RESEARCH Critical Finding #3), declares `dynamic = "force-dynamic"`, and reads FASTAPI_URL from server-only env. This wave is the load-bearing UI-17 satisfaction — the browser only ever talks to `/api/*` on the Next.js origin.

Purpose: Land the proxy layer so Plan 05's Chat UI has working backend endpoints and Plan 07's first-run modal can post keys. Plan 04 (Phase 3 amendment) and this wave can run in parallel (different files); Plan 05's chat surface DEPENDS on both.

Output: 5 route handler files; `pnpm --dir apps/web run build` exits 0; manual curl smoke check against a running uvicorn proves SSE bytes flow through end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-VALIDATION.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-02-SUMMARY.md
@apps/api/routes/turn.py
@apps/api/routes/health.py
@apps/api/routes/settings.py
@apps/api/routes/threads.py
@apps/web/lib/sse-translate.ts
@apps/web/lib/types.ts

<interfaces>
<!-- Wave 1's library code the route handlers import. -->

From apps/web/lib/sse-translate.ts:
```typescript
export function translateNamedSSEToUIMessageStream(
  upstream: ReadableStream<Uint8Array>,
): ReadableStream<Uint8Array>;
```

From apps/web/lib/types.ts:
```typescript
export type Backend = "openrouter" | "claude_code" | "computer_use";
export type AdapterStatus = "ready" | "missing_key" | "opt_out" | "error";
```

<!-- Phase 3 upstream contracts the route handlers proxy. -->

From apps/api/routes/turn.py:
- POST /api/v1/threads/{thread_id}/turn
- Body: {message: string, override_backend?: string, max_cost_usd?: number}
- Returns: text/event-stream with `event: <type>\ndata: <JSON>\n\n` blocks (8 event types after Plan 04's D-15 amendment)

From apps/api/routes/health.py:
- GET /api/v1/healthz
- Returns: {adapters: {openrouter: {status: "ready"|"missing_key"|"opt_out"|"error"}, claude_code: {...}, computer_use: {...}}, ...}

From apps/api/routes/settings.py:
- PATCH /api/v1/settings
- Body: {keys: {openrouter?: string, anthropic?: string}, ...}
- Returns: {keys: {openrouter: {present: bool, masked: string}, ...}} — masked only

From apps/api/routes/threads.py:
- POST /api/v1/threads with {title: string} → returns Thread
- GET /api/v1/threads/{id} → returns Thread + messages
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the SSE proxy chat route + the health passthrough — the two routes Plan 05 wires useChat against</name>
  <files>apps/web/app/api/chat/route.ts, apps/web/app/api/health/route.ts</files>
  <read_first>
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md (§"Pattern 2" lines 642-697 — the EXACT route-handler skeleton this task implements; Critical Finding #3 — Node runtime mandatory; Critical Finding #4 — return Response immediately; Pitfalls 1 + 2 + 7),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md (apps/web/app/api/chat/route.ts section — full pattern with line-by-line citation; apps/web/app/api/health/route.ts section — passthrough shape; Pattern B + Pattern C),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md (D-07 D-08 D-09 D-16; Claude's Discretion "Network error handling on the proxy" — 503 ECONNREFUSED behavior),
    apps/api/routes/turn.py (lines 350-590 — full handler the proxy talks to; lines 481-484 — wire format; lines 590 — EventSourceResponse with ping=15),
    apps/api/routes/health.py (full file — the exact JSON shape that flows through),
    apps/web/lib/sse-translate.ts (Wave 1's translator the chat route mounts),
    apps/web/lib/thread-id.ts (Wave 1's getOrCreateDefaultThread — chat route does NOT call this; the BROWSER calls it, then sends the resolved thread_id in the body or path. Plan 03 reads thread_id from the request body header per body shape)
  </read_first>
  <action>Author apps/web/app/api/chat/route.ts. Top-of-file mandatory declarations: `export const runtime = "nodejs";` `export const dynamic = "force-dynamic";` `const FASTAPI_URL = process.env.FASTAPI_URL ?? "http://localhost:8000";`. Author `export async function POST(req: Request)`. Body shape: `const body = await req.json()` returns `{messages: UIMessage[], threadId?: string}` (Plan 05's useChatRuntime sends both — `messages` per AI SDK v6 default, and `threadId` injected via `prepareSendMessagesRequest`). Strip the messages array to the last user text per D-08: `const lastUserMessage = [...(body.messages ?? [])].reverse().find((m: any) => m.role === "user"); const userText = lastUserMessage?.parts?.find((p: any) => p.type === "text")?.text ?? lastUserMessage?.content ?? "";`. Read `const threadId = body.threadId;` and validate it's a string (return 400 if not — the browser is required to send a valid thread id; getOrCreateDefaultThread guarantees one exists). Upstream fetch with abort propagation: `let upstream: Response; try { upstream = await fetch(\`${FASTAPI_URL}/api/v1/threads/${threadId}/turn\`, {method: "POST", headers: {"Content-Type": "application/json"}, body: JSON.stringify({message: userText}), signal: req.signal}); } catch (err: any) { if (err?.name === "AbortError") return new Response(null, {status: 499}); return new Response(JSON.stringify({error: "API unavailable — is uvicorn running?"}), {status: 503, headers: {"Content-Type": "application/json"}}); }`. Status check: `if (!upstream.ok || !upstream.body) { const errText = await upstream.text().catch(() => ""); return new Response(JSON.stringify({error: \`Upstream returned ${upstream.status}\`, detail: errText}), {status: upstream.status, headers: {"Content-Type": "application/json"}}); }`. Return-immediately-then-translate per Pitfall 2 + RESEARCH lines 686-697: `const translated = translateNamedSSEToUIMessageStream(upstream.body); return new Response(translated, {headers: {"Content-Type": "text/event-stream", "Cache-Control": "no-cache, no-transform", "Connection": "keep-alive", "X-Accel-Buffering": "no", "x-vercel-ai-ui-message-stream": "v1"}});`. Import `translateNamedSSEToUIMessageStream` from `@/lib/sse-translate`. Author apps/web/app/api/health/route.ts OVERWRITING the Plan 01 Wave-0 placeholder. Same top-of-file declarations (runtime nodejs + force-dynamic + FASTAPI_URL). `export async function GET()` per PATTERNS snippet: try fetch `${FASTAPI_URL}/api/v1/healthz`, on non-ok return `{error: "upstream unhealthy"}` with the upstream status, on ECONNREFUSED catch and return `{error: "API unavailable — is uvicorn running?"}` with 503, on success return `Response.json(await upstream.json())`. No transformation — the UI consumer reads `body.adapters.openrouter.status` directly. Run `pnpm --dir apps/web run build` and assert exit 0 — Next 16's route discovery validates the route handler shape at build time.</action>
  <verify>
    <automated>pnpm --dir apps/web run build 2>&amp;1 | tail -20 &amp;&amp; node -e "const fs=require('fs'); const chat=fs.readFileSync('apps/web/app/api/chat/route.ts','utf8'); const health=fs.readFileSync('apps/web/app/api/health/route.ts','utf8'); for (const [name, src] of [['chat',chat],['health',health]]) { if (!src.includes('export const runtime = \"nodejs\"')) {console.error(name+': missing runtime=nodejs');process.exit(1)} if (!src.includes('export const dynamic = \"force-dynamic\"')) {console.error(name+': missing force-dynamic');process.exit(1)} if (!src.includes('process.env.FASTAPI_URL')) {console.error(name+': missing FASTAPI_URL');process.exit(1)} } if (!chat.includes('translateNamedSSEToUIMessageStream')) {console.error('chat: must import translator');process.exit(1)} if (!chat.includes('signal: req.signal')) {console.error('chat: must forward req.signal');process.exit(1)} if (!chat.includes('x-vercel-ai-ui-message-stream')) {console.error('chat: missing AI SDK v6 header — Pitfall 7');process.exit(1)} if (!chat.includes('API unavailable — is uvicorn running?')) {console.error('chat: missing 503 body text');process.exit(1)} console.log('chat + health route OK')" &amp;&amp; cd apps/web &amp;&amp; pnpm exec tsc --noEmit --strict app/api/chat/route.ts app/api/health/route.ts 2>&amp;1 | grep -v 'node_modules' | (grep -i 'error' &amp;&amp; exit 1 || echo 'tsc strict OK')</automated>
  </verify>
  <done>Both files have the three top-of-file declarations; chat route imports the Wave-1 translator and forwards req.signal; the AI SDK v6 header is present; the 503 body text exactly matches UI-SPEC §17; build exits 0; tsc strict clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author the settings proxy with D-18 key scrubbing + the threads proxy routes (POST create + GET single)</name>
  <files>apps/web/app/api/settings/route.ts, apps/web/app/api/threads/route.ts, apps/web/app/api/threads/[id]/route.ts</files>
  <read_first>
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md (D-18 — every key-handling rule; D-19 — post-entry unblock; Claude's Discretion "Thread creation in minimal mode"),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md (§"Pattern 8" lines 992-1023 — settings handler with key scrubbing; Pitfall 6 — plaintext key leakage),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md (apps/web/app/api/settings/route.ts section — full code; apps/web/app/api/threads/route.ts section — POST shape; Pattern G — Secret-scrub on every error path; Patterns B + C),
    apps/api/routes/settings.py (lines 238-317 — the upstream contract — body shape `{keys: {openrouter?: string, anthropic?: string}}`; response shape `{keys: {openrouter: {present: bool, masked: string}, ...}}`; comments on lines 286-291 explaining why keys must never be logged),
    apps/api/routes/threads.py (lines 118-130 post_thread; lines 175-194 get_single_thread — return shape),
    apps/web/lib/api-client.ts (the matching browser-side wrappers — must use the same body shapes)
  </read_first>
  <action>Author apps/web/app/api/settings/route.ts. Top-of-file: `export const runtime = "nodejs"; export const dynamic = "force-dynamic"; const FASTAPI_URL = process.env.FASTAPI_URL ?? "http://localhost:8000";`. `export async function POST(req: Request)`. Read `const {provider, key} = await req.json();`. Validate provider: `if (provider !== "openrouter" && provider !== "anthropic") return Response.json({error: "unknown provider"}, {status: 400});`. Validate key is a non-empty string: `if (typeof key !== "string" || key.length === 0) return Response.json({error: "key is required"}, {status: 400});`. Forward to upstream: `try { const upstream = await fetch(\`${FASTAPI_URL}/api/v1/settings\`, {method: "PATCH", headers: {"Content-Type": "application/json"}, body: JSON.stringify({keys: {[provider]: key}})}); if (!upstream.ok) { const text = await upstream.text(); const scrubbed = text.replace(key, "***"); return new Response(scrubbed, {status: upstream.status, headers: {"Content-Type": "application/json"}}); } return Response.json(await upstream.json()); } catch (err: any) { const errMsg = String(err?.message ?? err).replace(key, "***"); return Response.json({error: "Could not save key", detail: errMsg}, {status: 503}); }`. Critical: NEVER call `console.log` / `console.error` with anything that could contain `key` — this file's only side effect is the upstream PATCH. Add a top-of-file comment: `// D-18: this file must never log the key. The .replace(key, "***") scrubs every error path. The Playwright secure-key.spec.ts test (Plan 07) enforces zero literal-key matches across Next stdout/stderr.`. Author apps/web/app/api/threads/route.ts: same three declarations; `export async function POST(req: Request)` reads `const body = await req.json()` expecting `{title: string}`; default title to "Untitled" if missing; forward via `fetch(\`${FASTAPI_URL}/api/v1/threads\`, {method: "POST", headers: {"Content-Type": "application/json"}, body: JSON.stringify({title})})`; on non-ok return text + status; on ECONNREFUSED return 503 with `{error: "API unavailable — is uvicorn running?"}`. Author apps/web/app/api/threads/[id]/route.ts: same declarations; `export async function GET(req: Request, {params}: {params: Promise<{id: string}>})` (Next 16 makes route params a Promise — `const {id} = await params;`); forward `fetch(\`${FASTAPI_URL}/api/v1/threads/${id}\`)`; pass through status + body. Both threads routes catch ECONNREFUSED and return 503. Run `pnpm --dir apps/web run build` and assert exit 0.</action>
  <verify>
    <automated>pnpm --dir apps/web run build 2>&amp;1 | tail -10 &amp;&amp; node -e "const fs=require('fs'); const settings=fs.readFileSync('apps/web/app/api/settings/route.ts','utf8'); const threads=fs.readFileSync('apps/web/app/api/threads/route.ts','utf8'); const single=fs.readFileSync('apps/web/app/api/threads/[id]/route.ts','utf8'); for (const [name, src] of [['settings',settings],['threads',threads],['threads-id',single]]) { if (!src.includes('runtime = \"nodejs\"')) {console.error(name+': missing runtime');process.exit(1)} if (!src.includes('force-dynamic')) {console.error(name+': missing force-dynamic');process.exit(1)} } if (!settings.includes('.replace(key, \"***\")')) {console.error('settings: missing key scrub');process.exit(1)} if (!settings.includes('PATCH')) {console.error('settings: must use PATCH upstream');process.exit(1)} if (settings.includes('console.log') || settings.includes('console.error')) {console.error('settings: NO console.log / console.error allowed');process.exit(1)} if (!threads.includes('/api/v1/threads')) {console.error('threads: must forward to /api/v1/threads');process.exit(1)} if (!single.includes('await params')) {console.error('threads[id]: Next 16 params is a Promise');process.exit(1)} console.log('settings + threads routes OK')" &amp;&amp; cd apps/web &amp;&amp; pnpm exec tsc --noEmit --strict app/api/settings/route.ts app/api/threads/route.ts 'app/api/threads/[id]/route.ts' 2>&amp;1 | grep -v 'node_modules' | (grep -i 'error' &amp;&amp; exit 1 || echo 'tsc strict OK')</automated>
  </verify>
  <done>All three files have the three top-of-file declarations; settings route scrubs key from every error path AND has zero console.log/error calls; threads POST forwards body verbatim with title default; threads/[id] GET handles Next 16 Promise-shaped params; build + tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end smoke check — boot uvicorn + next dev + curl the proxy + verify the SSE bytes flow through with the right header</name>
  <files>apps/web/scripts/smoke-routes.sh</files>
  <read_first>
    apps/web/playwright/mock-fastapi.py (Wave 0's canned-SSE FastAPI used as the upstream for this smoke check — does not need a real OpenRouter key),
    apps/web/app/api/chat/route.ts (Task 1's output),
    apps/web/app/api/health/route.ts (Task 1's output),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md (§"Pattern 2" — the AI SDK v6 chunks the smoke check assert appear in the curl output)
  </read_first>
  <action>Author apps/web/scripts/smoke-routes.sh as a shell script that: (1) starts the mock-fastapi.py from Wave 0 in the background on port 8001 (`FASTAPI_URL=http://localhost:8001`); (2) starts `pnpm --dir apps/web dev --port 3000` in the background; (3) waits up to 30 seconds for both `curl http://localhost:8001/api/v1/healthz` and `curl http://localhost:3000/api/health` to return 200; (4) curls `POST http://localhost:3000/api/chat` with body `{"messages":[{"role":"user","parts":[{"type":"text","text":"hello"}]}],"threadId":"test-thread"}` and asserts the response: HTTP 200, response header `x-vercel-ai-ui-message-stream: v1` present, body contains the substrings `"type":"start"`, `"type":"data-routing"`, `"type":"text-delta"`, `"type":"data-metrics"`, `"type":"finish"`, and `data: [DONE]` (one per AI SDK v6 chunk the translator emits from the mock-fastapi canned sequence); (5) curls `GET http://localhost:3000/api/health` and asserts body contains `"adapters"` and `"openrouter"`; (6) tears down both background processes via `kill $PID`. Use `trap "kill $PID1 $PID2 2>/dev/null" EXIT` for cleanup. Make the script `chmod +x`. The script is NOT run from CI (CI uses Playwright); it's a developer-facing smoke check the executor runs once after Task 1+2 to prove the wire works end-to-end against the mock upstream. Run the script and confirm it exits 0.</action>
  <verify>
    <automated>chmod +x apps/web/scripts/smoke-routes.sh &amp;&amp; bash apps/web/scripts/smoke-routes.sh 2>&amp;1 | tail -30 &amp;&amp; (ls apps/web/scripts/smoke-routes.sh &amp;&amp; head -1 apps/web/scripts/smoke-routes.sh | grep -q '^#!/')</automated>
  </verify>
  <done>smoke-routes.sh is a chmod-executable shell script that boots mock-fastapi + next dev, curls the chat proxy, asserts the AI SDK v6 chunk substrings appear in the SSE body and the x-vercel-ai-ui-message-stream header is set, and tears down cleanly. The script exits 0 against the Wave-0 mock-fastapi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/chat | Trusted source-of-truth path; route handler is the boundary |
| browser → /api/settings | Untrusted body containing plaintext key; route MUST scrub on every error path |
| Next.js route → FastAPI | Server-side fetch with FASTAPI_URL env var; never reaches the browser |
| Next.js stdout/stderr | Logged in development; D-18 prohibits the key from ever appearing here |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Information Disclosure | settings route logs plaintext key | mitigate | Zero `console.log` / `console.error` in settings/route.ts (grep gate in Task 2 verify); `.replace(key, "***")` on every error path; Plan 07's secure-key.spec.ts enforces with Playwright |
| T-04-15 | Information Disclosure | settings route error body echoes the key from FastAPI | mitigate | `const text = await upstream.text(); const scrubbed = text.replace(key, "***");` before returning (RESEARCH Pattern 8 belt-and-suspenders) |
| T-04-16 | Denial of Service | Edge runtime breaks SSE in production | mitigate | Every route has `export const runtime = "nodejs"` (Pattern C); grep gate enforces |
| T-04-17 | Information Disclosure | FASTAPI_URL leaks to the browser bundle | mitigate | FASTAPI_URL is read via `process.env` (server-only) — never `process.env.NEXT_PUBLIC_*`; Pattern B + Plan 07's browser-isolation.spec.ts enforces (no localhost:8000 in browser requests) |
| T-04-18 | Spoofing | Browser injects an arbitrary threadId to read another thread | accept | Single-user local-only app per PROJECT.md; no auth; future v2 multi-tenant work would add auth at the FastAPI layer |
| T-04-19 | Tampering | Body stripping in chat route picks the wrong "user" message | mitigate | D-08 dictates the strip semantics (last role:"user" wins); explicit reverse + find logic; if no user message exists, userText = "" and FastAPI gets a sensible empty-message error response |
| T-04-20 | Denial of Service | Upstream SSE stalls and the proxy holds the connection forever | accept | Phase 3 already enforces request.is_disconnected() polling at the FastAPI layer; the AbortController chain D-09 cancels via req.signal |
| T-04-21 | Information Disclosure | settings route is called from a CSRF-style cross-origin attacker | mitigate | FastAPI's CORSMiddleware (Phase 3 OSS-05) is configured for `http://localhost:3000` only; same-origin policy on the Next side prevents cross-origin POSTs to /api/settings in standard browsers |
</threat_model>

<verification>
- `pnpm --dir apps/web run build` exits 0
- Every route handler declares runtime=nodejs + dynamic=force-dynamic + reads process.env.FASTAPI_URL
- chat route forwards req.signal and sets the AI SDK v6 header
- settings route never logs (grep zero matches for console.log/error)
- settings route scrubs key from every error path
- threads[id] handles Next 16 Promise-shaped params
- smoke-routes.sh exits 0 against mock-fastapi — proves the SSE pipe is wired
- `tsc --strict` clean on all 5 route handler files
</verification>

<success_criteria>
- Wave 2 complete: 5 proxy routes live and tested against the mock upstream
- Plan 05's useChatRuntime can POST to /api/chat and consume the AI SDK v6 stream
- Plan 07's FirstRunModal can POST to /api/settings and read /api/health
- The Plan 04 (Phase 3 amendment) and Plan 05 (chat surface) can run in parallel — different files
- VALIDATION.md row "UI-17" automated path is reachable (Plan 07 lands the Playwright assertion that confirms it)
</success_criteria>

<output>
After completion, create `.planning/phases/04-minimal-chat-ui-openrouter-backend/04-03-SUMMARY.md` recording:
- The five route handler files and their public exports
- The smoke-routes.sh verified output (curl response headers + AI SDK chunk substrings)
- Any deviations from RESEARCH §Pattern 2 / §Pattern 8 (e.g., if the Next 16 params Promise shape needed extra handling)
- Confirmation that grep -rE 'console\\.(log|error|warn)' apps/web/app/api/settings/route.ts returns zero matches
</output>
</content>
</invoke>