---
phase: 04
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/pnpm-lock.yaml
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/postcss.config.mjs
  - apps/web/components.json
  - apps/web/.env.example
  - apps/web/.env.local
  - apps/web/.gitignore
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/app/globals.css
  - apps/web/app/api/health/route.ts
  - apps/web/components/ui/button.tsx
  - apps/web/components/ui/dialog.tsx
  - apps/web/components/ui/input.tsx
  - apps/web/components/ui/sonner.tsx
  - apps/web/lib/cn.ts
  - apps/web/vitest.config.ts
  - apps/web/playwright/playwright.config.ts
  - apps/web/tests/setup.ts
  - apps/web/tests/sse-translate.test.ts
  - apps/web/tests/chunk-schemas.test.ts
  - apps/web/tests/routing-chip.test.tsx
  - apps/web/tests/metrics-footer.test.tsx
  - apps/web/tests/chat-bubble.test.tsx
  - apps/web/playwright/no-flicker.spec.ts
  - apps/web/playwright/cancel-budget.spec.ts
  - apps/web/playwright/first-run.spec.ts
  - apps/web/playwright/secure-key.spec.ts
  - apps/web/playwright/browser-isolation.spec.ts
  - apps/web/playwright/routing-chip.spec.ts
  - apps/web/playwright/mock-fastapi.py
  - .github/workflows/web-test.yml
  - .gitignore
  - ReadMe.md
autonomous: true
requirements:
  - UI-01
user_setup: []

must_haves:
  truths:
    - "pnpm --dir apps/web install completes; node_modules/ present; pnpm-lock.yaml committed"
    - "pnpm --dir apps/web run build completes (Next 16 builds an empty chat surface without runtime/type errors)"
    - "pnpm --dir apps/web test exits 0 with the test stubs (Vitest unit suite: every it.todo / test.skip is recognized)"
    - "pnpm --dir apps/web test:e2e exits 0 with the test stubs (Playwright suite: every test.skip is recognized, mock-fastapi spawnable)"
    - "Wave-0 spike resolved: which of Pattern 5 (built-in fence detection) vs Pattern 5b (StreamingCodeBlock fallback) ships in Plan 06 — answer recorded in apps/web/lib/MARKDOWN-DECISION.md committed by this plan"
    - "Every Wave-0 frontmatter version pin is the RESEARCH-recommended current: Next 16.x, React 19.2.x, AI SDK v6 (ai ^6.0.175+), @ai-sdk/react ^3.x, @assistant-ui/react ^0.14.x, @assistant-ui/react-ai-sdk ^1.3.x, @assistant-ui/react-markdown ^0.14.x, shiki ^4.x, zod ^4.x, tailwindcss ^4.x, vitest ^4.1.x, @playwright/test ^1.60.x — per RESEARCH Critical Finding #1 (CONTEXT D-07 pins are stale floors)"
    - "Every Wave-0 task that creates a test file leaves it with placeholder it.todo/test.skip referencing the VALIDATION.md row it will satisfy"
    - "apps/web/.env.local is in .gitignore (D-05 belt-and-suspenders); FASTAPI_URL=http://localhost:8000 is in apps/web/.env.example"
    - "The shadcn registry vetting gate is satisfied by shipping only the 4 official-registry components: button, dialog, input, sonner (UI-SPEC §18)"
  artifacts:
    - path: "apps/web/package.json"
      provides: "dependency manifest with every RESEARCH Standard Stack pin"
      contains: "\"next\""
    - path: "apps/web/vitest.config.ts"
      provides: "jsdom + RTL setup; alias @ → apps/web/"
      contains: "jsdom"
    - path: "apps/web/playwright/playwright.config.ts"
      provides: "multi-server webServer config (uvicorn + next dev) per RESEARCH Example 3"
      contains: "webServer"
    - path: "apps/web/tests/setup.ts"
      provides: "RTL cleanup + @testing-library/jest-dom matchers"
      contains: "@testing-library/jest-dom"
    - path: "apps/web/app/layout.tsx"
      provides: "root server-component layout with <Toaster /> from sonner and global font"
      contains: "<html"
    - path: "apps/web/app/page.tsx"
      provides: "placeholder client chat surface (mounts <ChatShellPlaceholder /> until Wave 4)"
      contains: "use client"
    - path: "apps/web/app/globals.css"
      provides: "Tailwind v4 CSS-first config: @import 'tailwindcss';"
      contains: "@import"
    - path: "apps/web/app/api/health/route.ts"
      provides: "Wave-0 placeholder /api/health route exposing nodejs runtime + force-dynamic; returns a stub adapter-status payload so Plan 03's overwrite has a known shape"
      contains: "runtime"
    - path: "apps/web/lib/cn.ts"
      provides: "shadcn cn helper composing clsx + tailwind-merge"
      contains: "twMerge"
    - path: "apps/web/playwright/mock-fastapi.py"
      provides: "tiny canned-SSE FastAPI server used by Playwright in CI (no real OpenRouter call)"
      contains: "event: routing_decision"
    - path: ".github/workflows/web-test.yml"
      provides: "CI workflow running pnpm test + pnpm test:e2e; pnpm cache wired to apps/web/pnpm-lock.yaml"
      contains: "pnpm"
    - path: "apps/web/lib/MARKDOWN-DECISION.md"
      provides: "Wave-0 spike outcome — whether @assistant-ui/react-markdown's code-block primitive auto-detects fence state (Pattern 5) OR Plan 06 uses Pattern 5b (StreamingCodeBlock)"
      contains: "Pattern"
  key_links:
    - from: "apps/web/playwright/playwright.config.ts"
      to: "apps/api/main.py (uvicorn) and apps/web (next dev)"
      via: "webServer multi-process spawn"
      pattern: "webServer.*\\[.*uvicorn.*pnpm dev"
    - from: ".github/workflows/web-test.yml"
      to: "apps/web/playwright/playwright.config.ts"
      via: "pnpm --dir apps/web test:e2e"
      pattern: "pnpm.*test:e2e"
    - from: "apps/web/.gitignore (or root .gitignore)"
      to: "apps/web/.env.local"
      via: "explicit .env.local exclusion (D-05 belt-and-suspenders)"
      pattern: "apps/web/.env.local"
---

<objective>
Wave 0 — Toolchain bootstrap for the brand-new Next.js workspace under apps/web/, plus full test scaffolding so every later plan can claim a real test-file path in its acceptance criteria.

Purpose: Establish the deterministic, reproducible JS toolchain (pnpm + Next 16 + React 19.2 + AI SDK v6 + assistant-ui 0.14 + shiki 4 + zod 4 + Tailwind 4 + Vitest 4.1 + Playwright 1.60) under apps/web/, install the 4 shadcn components allowed by UI-SPEC §18 (button, dialog, input, sonner), commit a CI workflow that runs both unit and E2E suites, and resolve the Wave-0 spike about whether @assistant-ui/react-markdown's code-block primitive auto-detects fence state (Pattern 5 vs Pattern 5b — RESEARCH Open Question 3).

Output: A green `pnpm --dir apps/web install && pnpm --dir apps/web test && pnpm --dir apps/web test:e2e` from the repo root, with every test file existing as an it.todo/test.skip stub that maps to a VALIDATION.md row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-UI-SPEC.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-VALIDATION.md
@CLAUDE.md
@.github/workflows/ci.yml
@.gitignore
@ReadMe.md

<interfaces>
<!-- Wave 0 establishes new TS interfaces — there are no existing TS interfaces to extract. -->
<!-- The closed-vocabulary contracts the Wave-1 schemas will mirror come from the existing Python files: -->
<!-- apps/api/backends/chunks.py — ChatChunk union (7 variants); Wave 1 ports to Zod -->
<!-- apps/api/routes/turn.py lines 481-484 — SSE wire format (event: <type>\ndata: <JSON>\n\n) -->
<!-- src/routing/schema.py — RoutingDecision dataclass; Wave 1 ports to TS interface -->
<!-- config/model_mapping.json — 16-entry display_name map; consumed at compile time by Wave 4's chip -->
<!-- Wave 0 only needs to scaffold the workspace; concrete interface ports happen in Plan 02. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold apps/web/ Next 16 workspace + install dependency set + commit Wave-0 build artifacts</name>
  <files>apps/web/package.json, apps/web/pnpm-lock.yaml, apps/web/tsconfig.json, apps/web/next.config.ts, apps/web/postcss.config.mjs, apps/web/components.json, apps/web/.env.example, apps/web/.env.local, apps/web/.gitignore, apps/web/app/layout.tsx, apps/web/app/page.tsx, apps/web/app/globals.css, apps/web/lib/cn.ts, apps/web/components/ui/button.tsx, apps/web/components/ui/dialog.tsx, apps/web/components/ui/input.tsx, apps/web/components/ui/sonner.tsx, .gitignore, ReadMe.md</files>
  <read_first>
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md (D-01 D-02 D-03 D-04 D-05 — scaffolding decisions; canonical_refs "External Dependencies (NEW in Phase 4)" enumerates every package),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md (§"Standard Stack" Core + AI/Streaming + UI Primitives + Testing tables; §"Recommended Project Structure"; Critical Finding #1 — pin uplift to current; §"Installation" block lines 220-237),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-UI-SPEC.md (§1 Design System; §18 Registry Safety — only 4 shadcn components),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md (Wave 0 — Workspace scaffolding table; "No Analog Found" table; Pattern D — Client component declaration; "ReadMe.md MODIFY" snippet),
    .gitignore (existing 30-line file to append to — verify .env*.local rule already there),
    ReadMe.md (append a new "Running the chat UI" H2 section)
  </read_first>
  <action>Run `pnpm create next-app@latest apps/web --typescript --tailwind --app --src-dir=false --use-pnpm --import-alias '@/*' --no-eslint --skip-install` (the `--src-dir=false` matches CONTEXT D-01 because Phase 4 puts app routes at `apps/web/app/...` not `apps/web/src/app/...`; if the scaffolder mishears, manually flatten src/ into the root after). Add the RESEARCH Standard Stack dependencies via `pnpm add ai@^6.0.175 @ai-sdk/react@^3 @assistant-ui/react@^0.14.5 @assistant-ui/react-ai-sdk@^1.3.26 @assistant-ui/react-markdown@^0.14.0 shiki@^4 zod@^4 lucide-react clsx tailwind-merge class-variance-authority` from inside apps/web/. Pin Tailwind to ^4.3 and the @tailwindcss/postcss plugin. Run `pnpm dlx shadcn@latest init` selecting style=new-york baseColor=slate per UI-SPEC §1; then `pnpm dlx shadcn@latest add button dialog input sonner` — ONLY these 4 components (UI-SPEC §18); commit the generated `components/ui/*.tsx` files verbatim with no edits. Author `apps/web/lib/cn.ts` per PATTERNS Pattern E (clsx + tailwind-merge one-liner). Author `apps/web/app/layout.tsx` as a server component (NO "use client") that imports apps/web/app/globals.css, sets `<html lang="en">` + `<body>` with the default font stack from UI-SPEC §1, and mounts `<Toaster />` from the shadcn-installed sonner component; set Next metadata `{title: "Prompt-Optimizer", description: "Quality-first prompt router that picks the right LLM for every question."}` (UI-SPEC §16). Author `apps/web/app/page.tsx` with "use client" header containing only a placeholder div `<main>Phase 4 chat surface — wave 0 placeholder</main>` so Wave 4 has a known overwrite target. Author `apps/web/app/globals.css` with `@import "tailwindcss";` (Tailwind v4 CSS-first). Author `apps/web/.env.example` with single line `FASTAPI_URL=http://localhost:8000` (D-05) and a comment "# Server-only — never NEXT_PUBLIC_". Author empty `apps/web/.env.local` and add it to `apps/web/.gitignore` (D-05 belt-and-suspenders; the file may already be covered by the existing root `.env*.local` rule but the explicit line is the documented behavior per CONTEXT). Append the 6-line "Next.js (Phase 4)" block to root `.gitignore` per PATTERNS ".gitignore MODIFY" snippet covering apps/web/node_modules/, apps/web/.next/, apps/web/coverage/, apps/web/playwright-report/, apps/web/test-results/, apps/web/.env.local. Append the "Running the chat UI" H2 section to ReadMe.md per PATTERNS "ReadMe.md MODIFY" snippet — the 4-line code block from CONTEXT specifics lines 317-331. Run `pnpm --dir apps/web install` and `pnpm --dir apps/web run build` from the repo root; both MUST exit 0.</action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm install --frozen-lockfile &amp;&amp; pnpm run build &amp;&amp; cd - &amp;&amp; node -e "const pkg=require('./apps/web/package.json'); const required=['next','react','react-dom','@ai-sdk/react','ai','@assistant-ui/react','@assistant-ui/react-ai-sdk','@assistant-ui/react-markdown','shiki','zod','tailwindcss','lucide-react','clsx','tailwind-merge','class-variance-authority']; const deps={...pkg.dependencies,...pkg.devDependencies}; for(const r of required){if(!deps[r]){console.error('MISSING:',r);process.exit(1)}} if(!deps.next.match(/(\\^|~)?16\\./)){console.error('Next must be 16.x:',deps.next);process.exit(1)} if(!deps.ai.match(/(\\^|~)?6\\./)){console.error('ai must be 6.x:',deps.ai);process.exit(1)} console.log('versions OK')" &amp;&amp; ls apps/web/components/ui/{button,dialog,input,sonner}.tsx apps/web/app/layout.tsx apps/web/app/page.tsx apps/web/app/globals.css apps/web/lib/cn.ts apps/web/.env.example &amp;&amp; grep -q 'apps/web/node_modules' .gitignore &amp;&amp; grep -q 'Running the chat UI' ReadMe.md</automated>
  </verify>
  <done>`pnpm install` and `pnpm run build` both exit 0; package.json contains every Standard Stack dep at the RESEARCH-current major; ONLY 4 shadcn components present (button, dialog, input, sonner); apps/web/app/{layout.tsx,page.tsx,globals.css} + apps/web/lib/cn.ts exist; .gitignore has the 6-line Next.js block; ReadMe.md ends with the "Running the chat UI" H2 section.</done>
</task>

<task type="auto">
  <name>Task 2: Install Vitest + Playwright + RTL; write all test-framework configs; create Wave-0 test stubs + mock-FastAPI</name>
  <files>apps/web/vitest.config.ts, apps/web/playwright/playwright.config.ts, apps/web/tests/setup.ts, apps/web/tests/sse-translate.test.ts, apps/web/tests/chunk-schemas.test.ts, apps/web/tests/routing-chip.test.tsx, apps/web/tests/metrics-footer.test.tsx, apps/web/tests/chat-bubble.test.tsx, apps/web/playwright/no-flicker.spec.ts, apps/web/playwright/cancel-budget.spec.ts, apps/web/playwright/first-run.spec.ts, apps/web/playwright/secure-key.spec.ts, apps/web/playwright/browser-isolation.spec.ts, apps/web/playwright/routing-chip.spec.ts, apps/web/playwright/mock-fastapi.py, apps/web/package.json</files>
  <read_first>
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-VALIDATION.md (full file — the 12-row Per-Task Verification Map locks each stub's responsibility),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md (§"Validation Architecture" lines 1259-1307; §"Recommended Project Structure" — tests/ + playwright/ layout; §"Example 3 — Playwright webServer config" lines 1152-1164; §"Pattern 2" lines 470-700 — shape the sse-translate.test.ts fixtures will eventually exercise),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md (Wave 0 Test framework setup table; "apps/web/vitest.config.ts" snippet; "apps/web/tests/setup.ts" snippet; "apps/web/playwright/no-flicker.spec.ts" + cancel-budget + secure-key + browser-isolation snippets — each is the eventual shape; Wave 0 only stubs them),
    apps/api/tests/conftest.py (analog: pytest fixture shim → port the "import shim before tests" concept to tests/setup.ts),
    apps/api/tests/test_turn_streaming.py (analog: SSE-consumer test → port the "consume aiter_lines, assert event order" shape to the eventual sse-translate.test.ts; Wave 0 stubs only),
    apps/api/tests/test_secure_no_key_in_logs.py (analog for the eventual secure-key.spec.ts disclosure-regression shape; Wave 0 stubs only)
  </read_first>
  <action>Add dev dependencies via `pnpm --dir apps/web add -D vitest@^4.1 @vitejs/plugin-react jsdom @testing-library/react @testing-library/jest-dom @playwright/test@^1.60`; then `pnpm --dir apps/web exec playwright install chromium` (this downloads ~150MB — may need network; if sandbox blocks, document the install command in apps/web/playwright/playwright.config.ts as a Wave-0 manual step). Add npm scripts in apps/web/package.json: `"test": "vitest run"`, `"test:watch": "vitest"`, `"test:e2e": "playwright test --config=playwright/playwright.config.ts"`, `"dev": "next dev"`, `"build": "next build"`, `"start": "next start"`. Author apps/web/vitest.config.ts using the PATTERNS snippet verbatim — jsdom environment, setupFiles=['./tests/setup.ts'], globals=true, alias `@` → `__dirname`. Author apps/web/tests/setup.ts importing `@testing-library/jest-dom/vitest` and registering `afterEach(cleanup)` per PATTERNS. Author apps/web/playwright/playwright.config.ts using RESEARCH Example 3 verbatim — `webServer: [{command: "uvicorn apps.api.main:app --port 8000", port: 8000, cwd: "../../", reuseExistingServer: !process.env.CI}, {command: "pnpm dev --port 3000", port: 3000, reuseExistingServer: !process.env.CI}]`; `use.baseURL = "http://localhost:3000"`; ALSO add a SECOND webServer entry gated by `process.env.USE_MOCK_FASTAPI` that spawns `python apps/web/playwright/mock-fastapi.py --port 8001` and unsets the uvicorn entry — this is the CI path that does not need an OpenRouter key. Author apps/web/playwright/mock-fastapi.py as a tiny FastAPI app that exposes `/api/v1/healthz` (returns `{adapters: {openrouter: {status: "ready"}}}`), `/api/v1/threads` (returns `{id: "test-thread"}`), `/api/v1/settings` PATCH (returns `{keys: {openrouter: {present: true, masked: "sk-or-…ABC"}}}`), and `/api/v1/threads/{id}/turn` (returns an EventSourceResponse emitting a canned sequence: `event: routing_decision\ndata: {"backend":"openrouter","model_or_agent":"openai/gpt-5","rationale":"Test routing"}` → `event: text_delta\ndata: {"type":"text_delta","text":"Hello"}` → `event: text_delta\ndata: {"type":"text_delta","text":" world"}` → `event: done\ndata: {"type":"done","tokens_in":3,"tokens_out":2,"cost_usd":0.0001,"latency_ms":42}`) — depend on fastapi+sse-starlette which are already in the root pyproject.toml. Author every test stub file in the table below — each contains ONE `import {describe, it} from "vitest"` (or `import {test} from "@playwright/test"`) line + ONE `it.todo(...)` or `test.skip(...)` per VALIDATION.md row referencing the row's behavior verbatim. Stub file → VALIDATION row mapping: apps/web/tests/sse-translate.test.ts → "D-07 contract"; apps/web/tests/chunk-schemas.test.ts → "Schema contract"; apps/web/tests/routing-chip.test.tsx → "UI-04 unit"; apps/web/tests/metrics-footer.test.tsx → "UI-07 unit"; apps/web/tests/chat-bubble.test.tsx → "UI-08 unit"; apps/web/playwright/no-flicker.spec.ts → "UI-03"; apps/web/playwright/cancel-budget.spec.ts → "UI-06"; apps/web/playwright/first-run.spec.ts → "UI-01 + UI-13"; apps/web/playwright/secure-key.spec.ts → "D-18 belt"; apps/web/playwright/browser-isolation.spec.ts → "UI-17"; apps/web/playwright/routing-chip.spec.ts → "UI-04 E2E". Each stub's it.todo/test.skip text MUST contain the exact requirement ID and a one-line description matching the VALIDATION row. Finally run `pnpm --dir apps/web test` (Vitest collects stubs as todo — exit 0) and `pnpm --dir apps/web test:e2e --list` (Playwright lists skip stubs — exit 0).</action>
  <verify>
    <automated>pnpm --dir apps/web test 2>&amp;1 | tail -5 &amp;&amp; pnpm --dir apps/web test:e2e --list 2>&amp;1 | tail -5 &amp;&amp; ls apps/web/tests/{sse-translate,chunk-schemas,routing-chip,metrics-footer,chat-bubble}.test.* apps/web/playwright/{no-flicker,cancel-budget,first-run,secure-key,browser-isolation,routing-chip}.spec.ts apps/web/playwright/mock-fastapi.py apps/web/vitest.config.ts apps/web/playwright/playwright.config.ts apps/web/tests/setup.ts &amp;&amp; for f in apps/web/playwright/{no-flicker,cancel-budget,first-run,secure-key,browser-isolation,routing-chip}.spec.ts; do grep -q 'test.skip\|test\\.todo' "$f" || (echo "$f missing skip stub" &amp;&amp; exit 1); done &amp;&amp; for f in apps/web/tests/{sse-translate,chunk-schemas,routing-chip,metrics-footer,chat-bubble}.test.*; do grep -q 'it.todo\|it.skip' "$f" || (echo "$f missing it.todo stub" &amp;&amp; exit 1); done &amp;&amp; python -c "import ast; ast.parse(open('apps/web/playwright/mock-fastapi.py').read())"</automated>
  </verify>
  <done>`pnpm --dir apps/web test` and `pnpm --dir apps/web test:e2e --list` both exit 0; vitest.config.ts + playwright.config.ts + tests/setup.ts exist; all 11 test stub files exist and contain at least one `it.todo` or `test.skip` referencing the VALIDATION.md row; mock-fastapi.py is valid Python that imports fastapi/sse_starlette and emits the canned 4-event SSE sequence.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 spike — verify @assistant-ui/react-markdown code-block primitive fence-state behavior + author MARKDOWN-DECISION.md + author placeholder /api/health route + commit CI workflow</name>
  <files>apps/web/lib/MARKDOWN-DECISION.md, apps/web/app/api/health/route.ts, .github/workflows/web-test.yml</files>
  <read_first>
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md (§"Pattern 5" lines 778-829 — primitive-vs-Pattern-5b decision; §"Pattern 5b" lines 831-855 — StreamingCodeBlock fallback shape; Open Question 3; Assumption A1; §"Pattern 8" lines 928-989 — eventual /api/health shape Wave-0 placeholder mirrors),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md (Pattern C — runtime declarations; "apps/web/app/api/health/route.ts" snippet — full eventual shape Plan 03 will land; ".github/workflows/web-test.yml" snippet — full eventual CI workflow),
    apps/api/routes/health.py (analog for the eventual healthz proxy passthrough shape; Wave-0 placeholder returns a stub shape so Plan 03 can overwrite cleanly),
    .github/workflows/ci.yml (analog: existing Python CI workflow this new YAML lives beside; mirror its checkout + setup boilerplate)
  </read_first>
  <action>Spike step: read the installed `node_modules/@assistant-ui/react-markdown/dist/` source (look for any `CodeBlock` / `CodeFence` / fence-state hook); write a one-screen scratch component that renders `<MarkdownTextPrimitive>` with a streamed string containing an unclosed code fence, observe whether the rendered `<code>` tree changes once or many times during streaming. The spike does NOT need to be committed as a separate test; the OUTPUT is a single Markdown file `apps/web/lib/MARKDOWN-DECISION.md` that records: (a) the exact module/export inspected, (b) whether fence-state detection exists in the primitive, (c) the decision — "Plan 06 uses Pattern 5 (built-in primitive)" OR "Plan 06 uses Pattern 5b (custom StreamingCodeBlock at apps/web/lib/code-block.tsx)", (d) a one-line note for the Plan 06 executor referencing whichever Pattern section of RESEARCH.md to implement. Commit MARKDOWN-DECISION.md verbatim — this is the source-of-truth Plan 06 reads. Then author `apps/web/app/api/health/route.ts` as a Wave-0 placeholder using PATTERNS Pattern C declarations: top of file `export const runtime = "nodejs"; export const dynamic = "force-dynamic";` then `export async function GET() { return Response.json({adapters: {openrouter: {status: "missing_key"}}}); }` — a stub shape that matches Phase 3's healthz contract enough for Plan 07's modal-trigger logic to function; Plan 03 OVERWRITES this with the real proxy. The point of this Wave-0 placeholder: keep the `apps/web/app/api/` directory non-empty so Next 16's route discovery wires up before Wave 2. Finally author `.github/workflows/web-test.yml` using the PATTERNS snippet verbatim — paths filter on `apps/web/**` + `apps/api/**` + the workflow file itself; uses pnpm/action-setup@v3 + actions/setup-node@v4 with `cache: "pnpm"` and `cache-dependency-path: apps/web/pnpm-lock.yaml`; runs `uv sync --locked` (for the uvicorn webServer), `pnpm --dir apps/web install --frozen-lockfile`, `pnpm --dir apps/web exec playwright install --with-deps chromium`, `pnpm --dir apps/web test`, then `pnpm --dir apps/web test:e2e` with `USE_MOCK_FASTAPI=1` so CI does NOT need an OpenRouter key. Workflow must include the existing repo `actions/checkout@v4` step (no LFS needed because apps/web has no LFS-tracked files).</action>
  <verify>
    <automated>test -f apps/web/lib/MARKDOWN-DECISION.md &amp;&amp; grep -E 'Pattern 5|Pattern 5b' apps/web/lib/MARKDOWN-DECISION.md &amp;&amp; test -f apps/web/app/api/health/route.ts &amp;&amp; grep -q 'runtime = "nodejs"' apps/web/app/api/health/route.ts &amp;&amp; grep -q 'force-dynamic' apps/web/app/api/health/route.ts &amp;&amp; test -f .github/workflows/web-test.yml &amp;&amp; grep -q 'pnpm --dir apps/web test' .github/workflows/web-test.yml &amp;&amp; grep -q 'pnpm --dir apps/web test:e2e' .github/workflows/web-test.yml &amp;&amp; grep -q 'USE_MOCK_FASTAPI' .github/workflows/web-test.yml &amp;&amp; cd apps/web &amp;&amp; pnpm exec next lint --max-warnings 999 --dir app/api 2>&amp;1 | grep -v 'No ESLint' || true</automated>
  </verify>
  <done>MARKDOWN-DECISION.md committed with the Pattern 5 / Pattern 5b verdict; apps/web/app/api/health/route.ts exists with the two `export const` declarations and returns a stub adapter payload; .github/workflows/web-test.yml exists with pnpm cache + Vitest + Playwright + USE_MOCK_FASTAPI env var; Plan 06 has a binding decision file to read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo root → apps/web/ install | Untrusted npm registry — every `pnpm add` introduces transitive third-party code |
| apps/web/.env.local → committed history | Risk of accidentally committing a real key during dev |
| Playwright `webServer` → uvicorn process | The mock-fastapi process emits canned SSE — must not invoke real OpenRouter |
| CI workflow → secrets | Workflow MUST run with USE_MOCK_FASTAPI=1 so no OPENROUTER_API_KEY is required |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | apps/web/.env.local | mitigate | .gitignore explicit `apps/web/.env.local` line AND root `.env*.local` rule (D-05 belt-and-suspenders); verified by `grep` in task verify |
| T-04-02 | Tampering | npm dependency tree | mitigate | `pnpm install --frozen-lockfile` in CI; pnpm-lock.yaml committed; integrity checksums enforced by pnpm |
| T-04-03 | Information Disclosure | CI workflow secret leakage | mitigate | `USE_MOCK_FASTAPI=1` set in workflow — mock-fastapi.py never calls real providers; no OPENROUTER_API_KEY env in workflow |
| T-04-04 | Denial of Service | shadcn CLI registry fetch at install | accept | Components are downloaded once and committed; subsequent `pnpm install` does not re-fetch |
| T-04-05 | Information Disclosure | Wave-0 placeholder /api/health response shape | accept | Stub returns a generic `missing_key` status with no secret content; Plan 03 overwrites with real proxy |
| T-04-06 | Spoofing | Edge runtime SSE proxy | mitigate | Wave-0 placeholder /api/health ALREADY declares `runtime = "nodejs"` (Pattern C) so the pattern is established from the first route handler — RESEARCH Critical Finding #3 |
| T-04-07 | Tampering | Third-party shadcn registry block addition | mitigate | UI-SPEC §18 + this plan's must_haves lock the registry to the official 4 components; any future block requires re-running the registry vetting gate |
</threat_model>

<verification>
- `pnpm --dir apps/web install --frozen-lockfile` exits 0
- `pnpm --dir apps/web run build` exits 0 (Next 16 builds the placeholder page)
- `pnpm --dir apps/web test` exits 0 with `# todo` count > 0 (the it.todo stubs)
- `pnpm --dir apps/web test:e2e --list` lists at least 6 skipped tests (one per Playwright spec stub)
- Every Standard Stack package in apps/web/package.json matches the RESEARCH-recommended current major
- `apps/web/lib/MARKDOWN-DECISION.md` records the spike outcome that Plan 06 will read
- `.github/workflows/web-test.yml` declares pnpm cache and USE_MOCK_FASTAPI env
- `.gitignore` (root) has the 6-line Next.js block
- `ReadMe.md` ends with the "Running the chat UI" H2 section
- `apps/web/.env.example` contains exactly `FASTAPI_URL=http://localhost:8000`
- All 4 shadcn primitives present (button, dialog, input, sonner) — no extra components (UI-SPEC §18)
</verification>

<success_criteria>
- Wave 0 complete: workspace scaffolded, every test file scaffolded as it.todo/test.skip, CI workflow committed, Pattern 5 vs 5b decision recorded
- Downstream plans can claim test-file paths in their `<acceptance_criteria>` because every path Plan 02..Plan 07 reference now exists
- A green `pnpm --dir apps/web install && pnpm --dir apps/web test && pnpm --dir apps/web test:e2e --list` from the repo root is the gate
- VALIDATION.md frontmatter `wave_0_complete` flips to `true` (Plan 02 or the verifier updates it; Wave 0 itself just guarantees the file paths exist)
</success_criteria>

<output>
After completion, create `.planning/phases/04-minimal-chat-ui-openrouter-backend/04-01-SUMMARY.md` recording:
- the resolved versions of every Standard Stack package (the actual pinned versions, not just "latest")
- the spike outcome from MARKDOWN-DECISION.md (Pattern 5 or Pattern 5b)
- any deviations from the RESEARCH Standard Stack (if a peer-dep conflict forced a downgrade, document it)
- the exact command sequence that produced a green local + CI run
- the count of test stubs created (target: 11 — 5 Vitest + 6 Playwright)
</output>
</content>
</invoke>