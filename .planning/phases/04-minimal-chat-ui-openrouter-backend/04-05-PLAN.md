---
phase: 04
plan: 05
type: execute
wave: 4
depends_on: [02, 03, 04]
files_modified:
  - apps/web/hooks/useChatThread.ts
  - apps/web/components/RoutingChip.tsx
  - apps/web/components/MetricsFooter.tsx
  - apps/web/components/ChatBubble.tsx
  - apps/web/components/EmptyState.tsx
  - apps/web/components/StreamErrorBanner.tsx
  - apps/web/app/page.tsx
  - apps/web/tests/routing-chip.test.tsx
  - apps/web/tests/metrics-footer.test.tsx
  - apps/web/tests/chat-bubble.test.tsx
  - apps/web/playwright/routing-chip.spec.ts
autonomous: true
requirements:
  - UI-01
  - UI-04
  - UI-06
  - UI-07
  - UI-08
user_setup: []

must_haves:
  truths:
    - "apps/web/app/page.tsx is a client component (top-of-file 'use client') that mounts AssistantRuntimeProvider + Thread per RESEARCH §Pattern 1"
    - "useChatRuntime is configured with AssistantChatTransport({api: '/api/chat', prepareSendMessagesRequest: ({messages}) => ({body: {messages, threadId}})}) — threadId is resolved via getOrCreateDefaultThread() in a useEffect on mount"
    - "experimental_throttle: 50 is set on useChatRuntime — Pattern 4 + Pitfall 4 (markdown re-render storm mitigation)"
    - "RoutingChip subscribes to message.parts.find(p => p.type === 'data-routing') and renders only when the part exists — never null when an assistant turn is in flight (D-15 + RESEARCH Pattern 6)"
    - "RoutingChip receives the STRUCTURED RoutingDecision payload {backend, model_or_agent, rationale, confidence, signals} on `routingPart.data` — NOT just the signals telemetry dict (per Plan 04 amendment + Plan 02 Zod schema; this is the cross-plan data contract for UI-04)"
    - "RoutingChip resolves display_name via bundled import of config/model_mapping.json (Pitfall 12 — never fs.readFileSync at request time)"
    - "RoutingChip text format matches UI-SPEC §6.2 exactly: '<font-semibold>Routed to <display_name></font-semibold> · <rationale>' with separator U+00B7"
    - "RoutingChip color class is chipClassByBackend[routing.backend] ?? chipClassByBackend.openrouter — graceful degradation to slate for unknown backends"
    - "RoutingChip rationale truncates at 80 chars with ellipsis and full text in title attribute (UI-SPEC §6.2)"
    - "RoutingChip has role='status' aria-live='polite' aria-label='Routing decision: Routed to <display_name>. <rationale>' (UI-SPEC §6.4 + §14.2)"
    - "MetricsFooter renders 'streaming●' (with animate-pulse on the dot) until data-metrics part lands; then renders '$X · Xs · X↑/X↓' per UI-SPEC §7.2 format rules"
    - "MetricsFooter accessible name when streaming = 'Streaming response in progress'; when final = 'Turn cost X, latency Xs, X tokens in, X tokens out' per UI-SPEC §14.2"
    - "ChatBubble renders the assistant body via @assistant-ui/react-markdown MessagePrimitive.Content with a memoized component map (Pattern 4); markdown rendering is the FULL Plan 06 responsibility — Plan 05 wires the placeholder rendering tree only"
    - "ChatBubble assistant container = 'bg-slate-50 border border-slate-200 rounded-lg p-4 max-w-prose group relative'; user container = 'bg-slate-100 border border-slate-200 rounded-lg p-4 max-w-prose ml-auto' — UI-SPEC §8.1"
    - "ChatBubble hover-revealed action row (Copy + Regenerate) at absolute bottom-2 right-2 with 'opacity-0 group-hover:opacity-100 focus-within:opacity-100 transition-opacity duration-150' (UI-SPEC §8.3) — Copy uses navigator.clipboard.writeText of the raw markdown source; Regenerate calls assistant-ui's reload (D-14)"
    - "Regenerate is LIVE-WIRED in Plan 05 (no stub TODO) — page.tsx's AssistantMessage variant binds `onRegenerate` to assistant-ui v0.14's reload API (`useThreadRuntime().reload()` OR `useThreadActions().reload()` — executor verifies the actual export at install time); clicking Regenerate on the last assistant message MUST issue a fresh POST /api/chat and append a new assistant message"
    - "Composer is the @assistant-ui/react Composer primitive with the placeholder string 'Type a message…' (U+2026) and Send/Stop button per UI-SPEC §9.3 state machine (Send → Loader2 spinner → Stop)"
    - "Send button label exactly matches UI-SPEC §17: aria-label 'Send message' (idle) / 'Send message (composer is empty)' (disabled) / 'Stop generating' (streaming)"
    - "Composer keyboard shortcuts (UI-SPEC §9.4): Enter sends, Shift+Enter newline, Cmd/Ctrl+K focuses composer, Esc blurs (or stops if streaming)"
    - "EmptyState renders the centered tagline 'Ask anything. We'll route to the right model.' (UI-SPEC §11 + §17) when the thread is empty; the composer remains visible at the bottom"
    - "StreamErrorBanner is rendered inside the assistant ChatBubble when a stream_error part lands; the UI-SPEC §12.2 error-code → user-message catalog maps every Phase 2 code to friendly text; retriable codes show a 'Try again' button (visible label) with aria-label 'Retry the failed turn'"
    - "Page header is sticky h-14 with 'Prompt-Optimizer' on the left (text-lg font-semibold) and a Settings gear icon on the right linking to /settings (UI-SPEC §5.1 + §16)"
    - "useChat.stop() is bound to the Stop button (RESEARCH Pattern 3) — the AI SDK v6 contract that partial assistant message is preserved in messages after abort (Pitfall 3 + Assumption A2) is honored"
    - "RTL tests cover: RoutingChip renders backend-color + ARIA + display_name fallback + 80-char truncation using the STRUCTURED 5-key payload; MetricsFooter renders streaming and final states with correct aria-label; ChatBubble renders Copy + Regenerate buttons with correct aria-labels and the hover-reveal class structure; ChatBubble Regenerate-click test verifies onRegenerate prop is invoked (the live wiring to reload is asserted in routing-chip.spec.ts E2E)"
    - "ChatBubble's assistant `children` slot remains an open seam for Plan 06's MarkdownRenderer — Plan 05 mounts a plain pre-formatted text node in that slot (no TODO comment text inserted; Plan 06 replaces the slot value, not a marker)"
    - "Plan 05 OVERWRITES apps/web/playwright/routing-chip.spec.ts (skip stub from Plan 01) with a real Playwright spec that VALIDATION.md UI-04 row's automated command can execute end-to-end"
  artifacts:
    - path: "apps/web/hooks/useChatThread.ts"
      provides: "Hook wrapping useChatRuntime + AssistantChatTransport with threadId injection from thread-id.ts; called by ChatPage in page.tsx"
      contains: "useChatRuntime"
    - path: "apps/web/components/RoutingChip.tsx"
      provides: "Color-coded chip with backend lookup + display_name resolution from STRUCTURED 5-key data-routing payload (UI-SPEC §6)"
      contains: "data-routing"
    - path: "apps/web/components/MetricsFooter.tsx"
      provides: "Mid-stream streaming dot + final $/s/tokens display (UI-SPEC §7)"
      contains: "data-metrics"
    - path: "apps/web/components/ChatBubble.tsx"
      provides: "Assistant + user bubble shells with action row + slot for markdown body (Plan 06 fills the body)"
      contains: "group-hover:opacity-100"
    - path: "apps/web/components/EmptyState.tsx"
      provides: "Centered tagline for empty-thread state (UI-SPEC §11)"
      contains: "Ask anything"
    - path: "apps/web/components/StreamErrorBanner.tsx"
      provides: "Inline error banner with code → friendly-message catalog + retry button (UI-SPEC §12)"
      contains: "Try again"
    - path: "apps/web/app/page.tsx"
      provides: "ChatPage client component — header + AssistantRuntimeProvider + Thread + Composer + EmptyState + first-run modal mount point (Plan 07 lands the modal); Regenerate live-wired to assistant-ui reload API"
      contains: "AssistantRuntimeProvider"
    - path: "apps/web/tests/routing-chip.test.tsx"
      provides: "RTL unit tests for chip color, ARIA, display_name fallback, truncation — exercises the STRUCTURED 5-key data-routing payload"
      contains: "RoutingChip"
    - path: "apps/web/tests/metrics-footer.test.tsx"
      provides: "RTL unit tests for streaming vs final state + ARIA"
      contains: "MetricsFooter"
    - path: "apps/web/tests/chat-bubble.test.tsx"
      provides: "RTL unit tests for action row + Copy / Regenerate buttons + Regenerate-click invocation"
      contains: "ChatBubble"
    - path: "apps/web/playwright/routing-chip.spec.ts"
      provides: "Real Playwright spec (overwrites Plan 01 skip stub): chip visible on first assistant message; text matches /^Routed to .+ · .+$/; after 3 turns every assistant message has a chip; aria-label contains display name; Regenerate click issues a fresh POST /api/chat and appends a new assistant message — VALIDATION.md UI-04 + UI-08 end-to-end"
      contains: "Routed to"
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "apps/web/hooks/useChatThread.ts"
      via: "ChatPage uses useChatThread() to get runtime and threadId"
      pattern: "useChatThread"
    - from: "apps/web/hooks/useChatThread.ts"
      to: "apps/web/lib/thread-id.ts + apps/web/lib/api-client.ts"
      via: "getOrCreateDefaultThread on mount + AssistantChatTransport({api: '/api/chat', ...})"
      pattern: "getOrCreateDefaultThread"
    - from: "apps/web/components/RoutingChip.tsx"
      to: "config/model_mapping.json"
      via: "bundled JSON import (Pitfall 12)"
      pattern: "import.*model_mapping.json"
    - from: "apps/web/components/RoutingChip.tsx"
      to: "Plan 02 chunk-schemas.ts RoutingDecisionDataSchema + Plan 04 turn.py amended payload"
      via: "routingPart.data is the typed 5-key {backend, model_or_agent, rationale, confidence, signals} record"
      pattern: "routingPart\\.data"
    - from: "apps/web/components/StreamErrorBanner.tsx"
      to: "Phase 2 D-06 StreamError.code closed vocabulary"
      via: "code → friendly message catalog hardcoded per UI-SPEC §12.2"
      pattern: "cost_cap_exceeded"
    - from: "apps/web/app/page.tsx AssistantMessage.onRegenerate"
      to: "assistant-ui v0.14 reload API"
      via: "useThreadRuntime().reload() (or useThreadActions().reload() — executor verifies)"
      pattern: "reload"
    - from: "apps/web/playwright/routing-chip.spec.ts"
      to: "apps/web/playwright/mock-fastapi.py (Plan 01 Wave 0 — fixture catalog)"
      via: "uses mock-fastapi default fixture for the 3-turn chip-visibility loop"
      pattern: "Routed to"
---

<notes>
## Revision iteration 1 (Blocker 1, Blocker 2, Blocker 3)

- **Blocker 1 fix:** RoutingChip's data-routing consumer renames `signals` → `routing` to reflect the fact that the part's `data` is now the STRUCTURED 5-key RoutingDecision payload (per Plan 04 amendment + Plan 02 Zod schema), not the bare signals telemetry dict. The variable name change avoids future confusion. All chip-rendering logic now reads `routing.backend` / `routing.model_or_agent` / `routing.rationale` / `routing.confidence` directly with no defensive optional-chaining required. A must_haves truth lock-in: "RoutingChip receives the STRUCTURED RoutingDecision payload — NEVER just signals telemetry".
- **Blocker 2 fix:** Added Task 4 to overwrite apps/web/playwright/routing-chip.spec.ts (skip stub from Plan 01) with a real Playwright spec that satisfies VALIDATION.md UI-04 row's automated command. The spec uses Plan 01's mock-fastapi default fixture (no Plan 07 fixtures needed for chip visibility) and asserts the four required behaviors: (a) chip visible on first assistant message; (b) text matches `/^Routed to .+ · .+$/`; (c) after 3 turns every assistant message has a chip; (d) aria-label contains display name. The spec ALSO asserts Regenerate-click behavior (Blocker 3 alignment) so the live wiring is exercised end-to-end.
- **Blocker 3 fix:** Plan 05 Task 3 no longer leaves Regenerate as a TODO stub. page.tsx's AssistantMessage variant LIVE-WIRES `onRegenerate` to assistant-ui v0.14's reload API. The executor verifies the exact export name (useThreadRuntime vs useThreadActions vs equivalent) by reading `node_modules/@assistant-ui/react/dist/*.d.ts` at install time and writes the correct call. The chat-bubble.test.tsx Vitest suite adds a click test that confirms onRegenerate is invoked when the user clicks the Regenerate button (the prop-passing seam); the end-to-end "reload actually fires a fresh POST" assertion lives in the routing-chip.spec.ts E2E.
</notes>

<objective>
Wave 4 — Chat surface assembly. Build the visible chat application: header, AssistantRuntimeProvider + Thread + Composer, routing chip above each assistant message, metrics footer below, ChatBubble shell with hover action row + LIVE-wired Regenerate, empty-state tagline, StreamError banner. This wave is the largest single visual surface and the most user-impacting.

Purpose: Wire the AI SDK v6 + assistant-ui primitives together against Wave 2's proxy routes, subscribe the chip + footer to the AI SDK v6 `data-routing` (STRUCTURED 5-key payload per Plan 04 amendment) and `data-metrics` parts that Plan 02's translator emits, render the Phase-4 chrome per UI-SPEC §5-§13, and prove the SC #1 + SC #2 + UI-04 + UI-07 invariants via RTL unit tests + an end-to-end Playwright spec for the chip (routing-chip.spec.ts overwritten in Task 4). Plan 06 takes over the markdown body + code-block rendering details; Plan 07 lands the first-run modal + key gating + the 4 remaining Playwright specs that finalize UI-13 / UI-17 / D-18 / UI-06 budget.

Output: A clickable chat surface at http://localhost:3000 (with uvicorn + mock-fastapi or a real OpenRouter key) that streams a response, shows the chip + footer, accepts a Stop click that preserves the partial, and supports a Regenerate click that issues a fresh turn. RTL tests for chip + footer + bubble pass; Playwright routing-chip.spec.ts passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-UI-SPEC.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-VALIDATION.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-02-SUMMARY.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-03-SUMMARY.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-04-SUMMARY.md
@apps/web/lib/sse-translate.ts
@apps/web/lib/types.ts
@apps/web/lib/chunk-schemas.ts
@apps/web/lib/api-client.ts
@apps/web/lib/thread-id.ts
@apps/web/lib/cn.ts
@apps/web/components/ui/button.tsx
@apps/web/app/page.tsx
@config/model_mapping.json

<interfaces>
<!-- Wave 1 + Wave 2 + Wave 3 contracts the chat surface consumes. -->

From apps/web/lib/types.ts:
```typescript
export type Backend = "openrouter" | "claude_code" | "computer_use";
export interface RoutingDecision { backend: Backend; model_or_agent: string; rationale: string; confidence: number; signals: Record<string, unknown>; }
export type AdapterStatus = "ready" | "missing_key" | "opt_out" | "error";
export interface MetricsPayload { cost_usd: number | null; latency_ms: number | null; tokens_in: number | null; tokens_out: number | null; }
```

From apps/web/lib/api-client.ts:
```typescript
export async function postChat(messages: unknown[], signal?: AbortSignal): Promise<Response>;
export async function getHealth(): Promise<HealthResponse>;
```

From apps/web/lib/thread-id.ts:
```typescript
export const DEFAULT_THREAD_KEY = "prompt-optimizer.defaultThreadId";
export async function getOrCreateDefaultThread(): Promise<string>;
export function readDefaultThreadId(): string | null;
```

<!-- Wave 2's chat proxy emits AI SDK v6 chunks via Wave 1's translator. -->
Browser POSTs to /api/chat with body {messages, threadId}; receives:
- data: {"type":"start","messageId":"..."}\n\n
- data: {"type":"data-routing","data":{"backend":"openrouter","model_or_agent":"openai/gpt-5","rationale":"...","confidence":0.9,"signals":{...}}}\n\n  ← STRUCTURED 5-key RoutingDecision payload (Plan 04 amendment)
- data: {"type":"text-start","id":"t-0"}\n\n
- data: {"type":"text-delta","id":"t-0","delta":"..."}\n\n
- ... more text-delta ...
- data: {"type":"text-end","id":"t-0"}\n\n
- data: {"type":"data-metrics","data":{"cost_usd":...,"latency_ms":...,"tokens_in":...,"tokens_out":...}}\n\n
- data: {"type":"finish"}\n\n
- data: [DONE]\n\n

<!-- Wave 3's amendment guarantees data-routing arrives FIRST (before any text-delta) with the structured payload. -->
<!-- The chip subscribes via useThreadMessage and reads message.parts.find(p => p.type === "data-routing"). -->
<!-- routingPart.data is the typed RoutingDecision (5 keys) — chip reads .backend, .model_or_agent, .rationale, .confidence directly. -->
<!-- The footer subscribes via useThreadMessage and reads message.parts.find(p => p.type === "data-metrics"). -->

<!-- The translator forwards Phase 5 chunks (tool_call/result/file_diff/screenshot) as `data-tool` for forward-compat — Phase 4 ignores them gracefully. -->

<!-- From RESEARCH §Pattern 1 — the canonical mount shape. -->
```tsx
"use client";
import { AssistantRuntimeProvider } from "@assistant-ui/react";
import { useChatRuntime, AssistantChatTransport } from "@assistant-ui/react-ai-sdk";
const runtime = useChatRuntime({
  transport: new AssistantChatTransport({ api: "/api/chat", prepareSendMessagesRequest: ({messages}) => ({body: {messages, threadId}}) }),
  experimental_throttle: 50,
});
return <AssistantRuntimeProvider runtime={runtime}><Thread /></AssistantRuntimeProvider>;
```

<!-- From config/model_mapping.json — display_name resolution. -->
Example: "openai/gpt-5" → {display_name: "GPT-5", ...}; "openrouter" → "OpenRouter Auto Router"; "OTHER" → "Other Model".

<!-- assistant-ui v0.14 reload API (executor verifies actual export at install time). -->
The reload/regenerate call lives on one of:
- `useThreadRuntime().reload()` — the most likely surface in v0.14.x
- `useThreadActions().reload()` — alternate API name
- A different export discovered in `node_modules/@assistant-ui/react/dist/*.d.ts`
The executor reads the installed package and binds the correct call. The Vitest unit test asserts the onRegenerate prop is invoked; the routing-chip.spec.ts E2E asserts a fresh POST /api/chat lands when Regenerate is clicked.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author RoutingChip + MetricsFooter + their RTL tests — the two data-part subscribers</name>
  <files>apps/web/components/RoutingChip.tsx, apps/web/components/MetricsFooter.tsx, apps/web/tests/routing-chip.test.tsx, apps/web/tests/metrics-footer.test.tsx</files>
  <read_first>
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-UI-SPEC.md (§6 RoutingChip — full visual + ARIA + timing contract; §7 MetricsFooter — full format + mid-stream/final state; §3 typography note on font-semibold for chip prefix; §4 color palette + chipClassByBackend; §14.2 ARIA master list; §17 copy contract — exact strings),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md (§Pattern 6 RoutingChip lines 865-888; §Pattern 7 MetricsFooter lines 902-924; Pitfall 12 — bundled JSON import for model_mapping; Assumption A3 — useThreadMessage exposes message.parts),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md (RoutingChip section with full TSX; MetricsFooter section; Pattern D — "use client" + Pattern E — cn helper),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md (D-12 + D-13 + Claude's Discretion StreamError UI),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-04-PLAN.md (cross-plan dependency: data-routing.data is the STRUCTURED 5-key RoutingDecision per Plan 04 amendment),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-02-PLAN.md (cross-plan dependency: RoutingDecisionDataSchema in Plan 02 chunk-schemas.ts is what the translator enforces; chip can rely on the parsed shape without defensive optionals),
    config/model_mapping.json (the 16-entry display_name source for the chip's resolution; the chip MUST handle missing slugs by falling back to the raw model_or_agent),
    apps/web/lib/cn.ts (shadcn cn helper to compose dynamic chip color classes per Pattern E),
    apps/web/lib/types.ts (Wave 1 — Backend + RoutingDecision TS types; chip imports Backend for the chipClassByBackend lookup)
  </read_first>
  <behavior>
    - RoutingChip with no message part → renders null (does NOT show before the routing_decision event arrives)
    - RoutingChip with data-routing part for backend=openrouter, model_or_agent=openai/gpt-5, rationale="Strong reasoning fit", confidence=0.9, signals={...} → renders chip with bg-slate-100 class, "Routed to GPT-5" semibold, " · " separator, "Strong reasoning fit"
    - RoutingChip with backend=claude_code → renders bg-green-100 chip (forward-compat with Phase 5; never reached in Phase 4)
    - RoutingChip with unknown backend "foo" → falls back to openrouter chip color (graceful degradation) — this is defense in depth; Plan 02's Zod schema should already reject unknown backends, but the chip's fallback prevents a crash if the schema is bypassed
    - RoutingChip with unknown model_or_agent slug → renders the raw slug as display_name (config/model_mapping.json fallback)
    - RoutingChip with rationale > 80 chars → truncates with ellipsis + title attribute carries full rationale
    - RoutingChip has role="status", aria-live="polite", aria-label exactly "Routing decision: Routed to <display_name>. <rationale>"
    - MetricsFooter with no data-metrics part → renders "streaming●" with animate-pulse on the dot; aria-label "Streaming response in progress"
    - MetricsFooter with cost_usd=0.0021, latency_ms=1400, tokens_in=312, tokens_out=847 → renders "$0.0021 · 1.4s · 312↑/847↓" with text-xs font-mono text-slate-500 mt-2
    - MetricsFooter with cost_usd=0 → renders "$0.0000" (UI-SPEC §7.2 zero rule)
    - MetricsFooter with latency_ms=40 → renders "0.0s" (UI-SPEC §7.2 sub-50ms rule)
    - MetricsFooter with tokens_in=12847 → renders "12,847↑" (UI-SPEC §7.2 comma rule above 10000)
    - MetricsFooter final-state aria-label = "Turn cost $0.0021, latency 1.4s, 312 tokens in, 847 tokens out"
  </behavior>
  <action>Author apps/web/components/RoutingChip.tsx with `"use client";` header. Import `import { useThreadMessage } from "@assistant-ui/react";` `import mapping from "../../../config/model_mapping.json";` (the bundled JSON import — Pitfall 12); `import { cn } from "@/lib/cn";` `import type { RoutingDecision } from "@/lib/types";`. Declare `const chipClassByBackend = {openrouter: "bg-slate-100 text-slate-900 border-slate-200", claude_code: "bg-green-100 text-green-900 border-green-200", computer_use: "bg-amber-100 text-amber-900 border-amber-200"} as const;` `export function RoutingChip()`. Inside: `const message = useThreadMessage();` `const routingPart = message?.parts?.find((p: any) => p.type === "data-routing");` `if (!routingPart) return null;` `const routing = routingPart.data as RoutingDecision;` (the typed 5-key payload — backend/model_or_agent/rationale/confidence/signals — Plan 02's Zod schema enforces this shape upstream). `const mappingEntry = (mapping as Record<string, {display_name: string}>)[routing.model_or_agent]; const displayName = mappingEntry?.display_name ?? routing.model_or_agent;` `const colorClass = chipClassByBackend[routing.backend] ?? chipClassByBackend.openrouter;`. Compute `const truncated = routing.rationale.length > 80 ? routing.rationale.slice(0, 79) + "…" : routing.rationale;` Render: `<div role="status" aria-live="polite" aria-label={\`Routing decision: Routed to ${displayName}. ${routing.rationale}\`} title={routing.rationale} className={cn("inline-flex items-center gap-2 px-2 py-1 rounded-md border text-[13px] motion-safe:animate-in motion-safe:fade-in-0 motion-safe:slide-in-from-top-1 motion-safe:duration-150", colorClass)}><span className="font-semibold">Routed to {displayName}</span><span className="opacity-70"> · </span><span>{truncated}</span></div>`. Note: the motion classes match UI-SPEC §15 chip-arrival animation. The variable name `routing` (NOT `signals`) reflects that the part's `data` is the structured RoutingDecision (Plan 04 amendment) — readers should never confuse this with the signals telemetry dict.

Author apps/web/components/MetricsFooter.tsx with `"use client";` header. Import useThreadMessage + types. `export function MetricsFooter()` reads message.parts.find p.type==="data-metrics". When `!metricsPart`: return `<div aria-label="Streaming response in progress" className="text-xs font-mono text-slate-500 mt-2 flex items-center gap-1"><span>streaming</span><span aria-hidden="true" className="animate-pulse">●</span></div>`. When metricsPart present: `const m = metricsPart.data as MetricsPayload;` Format helpers: `const cost = \`$${(m.cost_usd ?? 0).toFixed(4)}\`;` `const latencySec = (m.latency_ms ?? 0) / 1000; const latency = latencySec < 0.05 ? "0.0s" : \`${latencySec.toFixed(1)}s\`;` `const fmtTokens = (n: number | null) => { const v = n ?? 0; return v < 10_000 ? String(v) : v.toLocaleString("en-US"); };` Render: `<div className="text-xs font-mono text-slate-500 mt-2" aria-label={\`Turn cost ${cost}, latency ${latency}, ${m.tokens_in ?? 0} tokens in, ${m.tokens_out ?? 0} tokens out\`}>{cost} · {latency} · {fmtTokens(m.tokens_in)}↑/{fmtTokens(m.tokens_out)}↓</div>`.

Overwrite apps/web/tests/routing-chip.test.tsx with a real Vitest+RTL suite. Mock useThreadMessage via vitest.mock("@assistant-ui/react", () => ({ useThreadMessage: vi.fn() })). Test cases mirror the behavior list. CRITICAL: every test fixture for the data-routing part uses the STRUCTURED 5-key payload (e.g. `{type: "data-routing", data: {backend: "openrouter", model_or_agent: "openai/gpt-5", rationale: "Strong reasoning fit", confidence: 0.9, signals: {task_type: "chat"}}}`). Use `render(<RoutingChip />)` + `screen.getByRole("status")` + `screen.getByText("Routed to GPT-5")`. For the truncation test, supply an 100-char rationale and assert the rendered text ends with "…" and the title attribute carries the full string. For the unknown-backend test, supply backend="foo" (cast through `as any`) and assert the chip has class `bg-slate-100` (openrouter fallback). For the no-part test, mock useThreadMessage to return `{parts: []}` and assert `container.firstChild === null`. Overwrite apps/web/tests/metrics-footer.test.tsx similarly — test the streaming-state aria-label + animate-pulse class, then the final-state format for cost_usd=0.0021/latency=1400/tokens=312/847, then the cost=0 / latency=40ms / tokens=12847 edge cases. Run `pnpm --dir apps/web test routing-chip.test.tsx metrics-footer.test.tsx` and assert exit 0 with at least 10 tests passing.</action>
  <verify>
    <automated>pnpm --dir apps/web test routing-chip.test.tsx metrics-footer.test.tsx 2>&amp;1 | grep -E '(Tests|passing|failed|✓)' &amp;&amp; node -e "const c=require('fs').readFileSync('apps/web/components/RoutingChip.tsx','utf8'); if (!c.includes('use client')) {console.error('RoutingChip needs use client');process.exit(1)} if (!c.includes('model_mapping.json')) {console.error('RoutingChip must import config/model_mapping.json');process.exit(1)} if (!c.includes('chipClassByBackend')) {console.error('RoutingChip needs chipClassByBackend map');process.exit(1)} if (!c.includes('role=\"status\"')) {console.error('RoutingChip needs role status');process.exit(1)} if (!c.includes('aria-live=\"polite\"')) {console.error('RoutingChip needs aria-live polite');process.exit(1)} if (!c.includes('font-semibold')) {console.error('RoutingChip prefix must be font-semibold per UI-SPEC §6.2');process.exit(1)} if (!c.includes('routing.backend') || !c.includes('routing.model_or_agent') || !c.includes('routing.rationale')) {console.error('RoutingChip must read STRUCTURED 5-key fields directly (routing.backend / .model_or_agent / .rationale) — NOT signals.* (Plan 04 amendment)');process.exit(1)} console.log('chip OK')" &amp;&amp; node -e "const m=require('fs').readFileSync('apps/web/components/MetricsFooter.tsx','utf8'); if (!m.includes('animate-pulse')) {console.error('MetricsFooter needs animate-pulse on streaming dot');process.exit(1)} if (!m.includes('font-mono')) {console.error('MetricsFooter needs font-mono');process.exit(1)} if (!m.includes('toFixed(4)')) {console.error('MetricsFooter cost must use toFixed(4) per UI-SPEC §7.2');process.exit(1)} console.log('footer OK')" &amp;&amp; cd apps/web &amp;&amp; pnpm exec tsc --noEmit --strict components/RoutingChip.tsx components/MetricsFooter.tsx 2>&amp;1 | grep -v 'node_modules' | (grep -i 'error' &amp;&amp; exit 1 || echo 'tsc strict OK')</automated>
  </verify>
  <done>RoutingChip + MetricsFooter components exist with UI-SPEC-compliant class names + ARIA + animation; RoutingChip reads the STRUCTURED 5-key payload (routing.backend / .model_or_agent / .rationale) and renames the local from `signals` to `routing` to reflect the contract; RTL tests cover at least 10 cases (chip render with structured payload, color fallback, truncation, ARIA, streaming dot, final format, edge cases); tsc strict clean; tests exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author ChatBubble + EmptyState + StreamErrorBanner + ChatBubble RTL test (Copy + Regenerate prop-invocation)</name>
  <files>apps/web/components/ChatBubble.tsx, apps/web/components/EmptyState.tsx, apps/web/components/StreamErrorBanner.tsx, apps/web/tests/chat-bubble.test.tsx</files>
  <read_first>
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-UI-SPEC.md (§8 ChatBubble — container + markdown body + action row + timestamps; §11 EmptyState; §12 StreamErrorBanner — error-code catalog + ARIA; §17 copy contract — Copy / Regenerate aria-labels, "Try again" visible label, "Ask anything…" tagline),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md (D-14 ChatBubble affordances — Copy = raw markdown, Regenerate = new turn append; D-(discretion) StreamError UI — red inline banner with retry),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md (§Pattern 3 useChat.stop + reload contract — Regenerate maps to assistant-ui's reload; Pitfall 3 — partial preserved on abort),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md (component file pattern; ChatBubble + StreamErrorBanner + EmptyState in "No Analog Found" table — all rely on UI-SPEC),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-02-SUMMARY.md (Wave 1 contract details if needed),
    apps/api/backends/chunks.py (StreamError.code closed vocabulary — must mirror in catalog)
  </read_first>
  <behavior>
    - ChatBubble (role=assistant) renders bg-slate-50 border rounded-lg p-4 max-w-prose group relative; action row absent until hover OR focus-within
    - ChatBubble (role=user) renders bg-slate-100 ml-auto rounded-lg p-4 max-w-prose
    - ChatBubble action row contains Copy button + Regenerate button, both h-7 w-7 rounded-md with shadcn focus-ring
    - Copy button aria-label "Copy message as markdown"; on click calls navigator.clipboard.writeText with the raw markdown source string passed as a prop, then triggers sonner.success("Copied to clipboard")
    - Regenerate button aria-label "Regenerate response"; on click calls the onRegenerate prop (which the parent — page.tsx Task 3 — wires to assistant-ui's reload)
    - ChatBubble has a `children` prop slot where Plan 06 will mount the memoized markdown renderer; Plan 05 passes a plain pre-formatted text node for now; no TODO marker is inserted (Plan 06 replaces the slot VALUE, not a comment marker)
    - EmptyState renders centered flex column with "Ask anything. We'll route to the right model." in text-lg font-semibold text-slate-900
    - StreamErrorBanner accepts {code, message, retriable, onRetry} props; renders a red banner with the UI-SPEC §12.2 friendly message for the given code + the original code as a monospace suffix; "Try again" button visible only when retriable === true
    - StreamErrorBanner has role="alert"; retry button aria-label "Retry the failed turn"
    - StreamErrorBanner code catalog: every Phase-2 D-06 code has an entry; passing an unknown code shows a generic fallback ("Something went wrong — try again.") + the raw code suffix
  </behavior>
  <action>Author apps/web/components/ChatBubble.tsx with `"use client";` header. Props interface: `interface ChatBubbleProps { role: "assistant" | "user"; rawMarkdown: string; onRegenerate?: () => void; children: React.ReactNode; }`. Import `import { Copy, RefreshCw } from "lucide-react";` `import { Button } from "@/components/ui/button";` `import { toast } from "sonner";` `import { cn } from "@/lib/cn";`. Container classes per UI-SPEC §8.1: assistant = `"bg-slate-50 border border-slate-200 rounded-lg p-4 max-w-prose group relative"`, user = `"bg-slate-100 border border-slate-200 rounded-lg p-4 max-w-prose ml-auto"`. Inside the assistant variant render the markdown slot via `{children}` plus an action row: `<div className="absolute bottom-2 right-2 flex items-center gap-1 opacity-0 group-hover:opacity-100 focus-within:opacity-100 transition-opacity duration-150"><button aria-label="Copy message as markdown" onClick={async () => { await navigator.clipboard.writeText(rawMarkdown); toast.success("Copied to clipboard"); }} className="h-7 w-7 rounded-md hover:bg-slate-200 focus-visible:ring-2 focus-visible:ring-slate-900 focus-visible:ring-offset-2 inline-flex items-center justify-center"><Copy className="h-4 w-4" /></button><button aria-label="Regenerate response" onClick={onRegenerate} className="...same classes..."><RefreshCw className="h-4 w-4" /></button></div>`. User variant has no action row. Do NOT insert any "// TODO" comment text in this file — Plan 06 modifies the assistant body by replacing the children slot, not by grepping for a marker.

Author apps/web/components/EmptyState.tsx with `"use client";` header. Just `export function EmptyState() { return <div className="flex flex-col items-center justify-center h-full text-center px-4 py-12"><p className="text-lg font-semibold text-slate-900">Ask anything. We&apos;ll route to the right model.</p></div>; }`

Author apps/web/components/StreamErrorBanner.tsx with `"use client";` header. Define the catalog as a const map per UI-SPEC §12.2: `const FRIENDLY_MESSAGES: Record<string, string> = { cost_cap_exceeded: "Cost cap of $0.50 reached. Try a shorter prompt or raise the cap in settings.", step_cap_exceeded: "The model hit its step limit. Try a more focused prompt.", cancelled: "Generation cancelled.", rate_limited: "OpenRouter is rate-limiting requests. Wait a moment and try again.", auth_failed: "OpenRouter rejected the key. Update it in settings and try again.", provider_unavailable: "The upstream model is temporarily unavailable. Try again in a moment.", timeout: "The request timed out. The model may be slow — try again.", validation_error: "That request couldn't be sent. Check your input and try again.", internal_error: "Something went wrong inside Prompt-Optimizer. Check the API logs and retry." };`. Component takes `{code, message, retriable, onRetry}: {code: string; message: string; retriable: boolean; onRetry: () => void}`; resolves `friendly = FRIENDLY_MESSAGES[code] ?? "Something went wrong — try again.";` Render `<div role="alert" className="mt-3 first:mt-0 flex items-start gap-3 rounded-md border border-red-300 bg-red-50 p-3 text-sm text-red-900"><AlertCircle className="h-4 w-4 text-red-600 flex-shrink-0" /><div className="flex-1 leading-relaxed">{friendly} <code className="font-mono text-xs">({code})</code>{retriable && <button onClick={onRetry} aria-label="Retry the failed turn" className="mt-2 inline-flex h-8 items-center rounded-md border border-red-300 bg-white px-3 text-xs font-semibold text-red-900 hover:bg-red-100 block">Try again</button>}</div></div>`.

Overwrite apps/web/tests/chat-bubble.test.tsx with RTL tests: render assistant ChatBubble with rawMarkdown="# hello" and onRegenerate=vi.fn(); assert the Copy button exists with aria-label "Copy message as markdown"; click Copy and assert navigator.clipboard.writeText was called with "# hello" AND toast.success was called with "Copied to clipboard" (mock both); assert Regenerate has aria-label "Regenerate response"; CLICK the Regenerate button and assert `onRegenerate` prop was invoked exactly once (this is the prop-invocation seam — the live wiring to assistant-ui reload is asserted in routing-chip.spec.ts E2E); render user variant and assert no action row exists; render StreamErrorBanner with code="cost_cap_exceeded" retriable=false and assert the friendly text appears + "Try again" button does NOT exist; render code="rate_limited" retriable=true and assert "Try again" button exists with aria-label "Retry the failed turn" and clicking calls onRetry. Run `pnpm --dir apps/web test chat-bubble.test.tsx` and assert exit 0 with at least 7 tests passing.</action>
  <verify>
    <automated>pnpm --dir apps/web test chat-bubble.test.tsx 2>&amp;1 | grep -E '(Tests|passing|failed|✓)' &amp;&amp; node -e "const b=require('fs').readFileSync('apps/web/components/ChatBubble.tsx','utf8'); if (!b.includes('group-hover:opacity-100')) {console.error('ChatBubble action row needs group-hover');process.exit(1)} if (!b.includes('focus-within:opacity-100')) {console.error('ChatBubble action row needs focus-within (UI-SPEC §8.3 accessibility)');process.exit(1)} if (!b.includes('Copy message as markdown')) {console.error('Copy aria-label must match UI-SPEC §17');process.exit(1)} if (!b.includes('Regenerate response')) {console.error('Regenerate aria-label must match UI-SPEC §17');process.exit(1)} if (!b.includes('max-w-prose')) {console.error('ChatBubble needs max-w-prose');process.exit(1)} if (b.includes('TODO: replace with MarkdownRenderer')) {console.error('Plan 05 must NOT insert a TODO marker — Plan 06 replaces the slot value directly');process.exit(1)} console.log('bubble OK')" &amp;&amp; node -e "const e=require('fs').readFileSync('apps/web/components/EmptyState.tsx','utf8'); if (!e.includes('Ask anything')) {console.error('EmptyState tagline must match UI-SPEC §17');process.exit(1)} console.log('empty state OK')" &amp;&amp; node -e "const s=require('fs').readFileSync('apps/web/components/StreamErrorBanner.tsx','utf8'); const codes=['cost_cap_exceeded','step_cap_exceeded','cancelled','rate_limited','auth_failed','provider_unavailable','timeout','validation_error','internal_error']; for (const c of codes) if (!s.includes(c)) {console.error('StreamErrorBanner catalog missing '+c);process.exit(1)} if (!s.includes('role=\"alert\"')) {console.error('StreamErrorBanner needs role alert');process.exit(1)} if (!s.includes('Try again')) {console.error('Retry button visible label must be Try again');process.exit(1)} if (!s.includes('Retry the failed turn')) {console.error('Retry button aria-label must be Retry the failed turn');process.exit(1)} console.log('error banner OK')" &amp;&amp; cd apps/web &amp;&amp; pnpm exec tsc --noEmit --strict components/ChatBubble.tsx components/EmptyState.tsx components/StreamErrorBanner.tsx 2>&amp;1 | grep -v 'node_modules' | (grep -i 'error' &amp;&amp; exit 1 || echo 'tsc strict OK')</automated>
  </verify>
  <done>ChatBubble + EmptyState + StreamErrorBanner exist with UI-SPEC-compliant classes + ARIA + visible text; the error catalog covers all 9 Phase-2 codes; the Regenerate button click invokes the onRegenerate prop (prop-invocation seam asserted by Vitest); ChatBubble.tsx contains NO `TODO: replace with MarkdownRenderer` marker text; RTL tests cover at least 7 cases; tsc strict clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire useChatThread + ChatPage in app/page.tsx with LIVE Regenerate wiring to assistant-ui reload</name>
  <files>apps/web/hooks/useChatThread.ts, apps/web/app/page.tsx</files>
  <read_first>
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md (§Pattern 1 lines 437-466 — runtime mount; §Pattern 3 useChat.stop AND reload; Pattern 4 throttling; Pitfall 3 partial preservation; Pitfall 8 hydration mismatch — page.tsx is "use client"; Pitfall 11 HMR limitation; Open Question 5 — body stripping in route handler vs prepareSendMessagesRequest — RESEARCH recommends route-handler strip per Wave 2; the prepareSendMessagesRequest only needs to inject threadId),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-UI-SPEC.md (§5.1 / §9 / §16 — header layout + Composer placeholder + page title; §11 empty state placement; §15 motion tokens),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md (apps/web/app/page.tsx in "No Analog Found"; Pattern D — client component declaration),
    apps/web/components/RoutingChip.tsx (Task 1 — chip subscribes to data-routing part automatically),
    apps/web/components/MetricsFooter.tsx (Task 1 — footer subscribes to data-metrics part automatically),
    apps/web/components/ChatBubble.tsx (Task 2 — bubble shell with action row, onRegenerate prop seam),
    apps/web/components/EmptyState.tsx (Task 2),
    apps/web/components/StreamErrorBanner.tsx (Task 2),
    apps/web/lib/thread-id.ts (Wave 1 — getOrCreateDefaultThread),
    apps/web/components/ui/{button,dialog,input,sonner}.tsx (Wave 0 shadcn primitives),
    node_modules/@assistant-ui/react/dist/*.d.ts (installed at Plan 01 install time — the executor reads this AT EXECUTION to confirm which export name to use for reload: `useThreadRuntime().reload()` vs `useThreadActions().reload()` vs another equivalent)
  </read_first>
  <action>Author apps/web/hooks/useChatThread.ts with `"use client";` header. Export a hook: `export function useChatThread() { const [threadId, setThreadId] = useState<string | null>(null); useEffect(() => { void getOrCreateDefaultThread().then(setThreadId); }, []); const runtime = useChatRuntime({ transport: new AssistantChatTransport({ api: "/api/chat", prepareSendMessagesRequest: ({messages}) => ({body: {messages, threadId}}) }), experimental_throttle: 50 }); return {runtime, threadId}; }`. Import `useState`, `useEffect` from "react"; `useChatRuntime`, `AssistantChatTransport` from "@assistant-ui/react-ai-sdk"; `getOrCreateDefaultThread` from "@/lib/thread-id". CRITICAL: the call to useChatRuntime is unconditional (hooks rules); the runtime is created with a possibly-null threadId on first render; on second render after threadId resolves, prepareSendMessagesRequest captures the up-to-date value via closure. If threadId is null at submit time the request goes to /api/chat with threadId=null and the proxy returns 400 — which is fine, the composer disables itself until threadId is set via the Plan 07 first-run gate (Plan 07 adds the gate; Plan 05 ships a basic "no threadId yet" loading state).

Overwrite apps/web/app/page.tsx (replacing the Plan 01 Wave-0 placeholder). `"use client";` header. Import the runtime provider, Thread + Composer + MessagePrimitive from "@assistant-ui/react", the useChatThread hook, all 5 components from Task 1+2, the Settings gear icon from lucide-react, Link from "next/link". ALSO import the assistant-ui reload API — the executor inspects `node_modules/@assistant-ui/react/dist/*.d.ts` to determine the correct export name. Candidates in priority order: `useThreadRuntime` (call `.reload()` on the returned runtime), `useThreadActions` (call `.reload()` on the returned actions object), `useThreadActionsContext`, or a similar export. The executor binds the correct one.

`export default function ChatPage()`. Inside: `const {runtime, threadId} = useChatThread();`. Render the page shell per UI-SPEC §5.1: a sticky header (`h-14 border-b border-slate-200 bg-white sticky top-0 z-10 px-4 flex items-center justify-between`) with left `<h1 className="text-lg font-semibold text-slate-900">Prompt-Optimizer</h1>` and right `<Link href="/settings" aria-label="Settings"><Settings className="h-5 w-5 text-slate-700" /></Link>`; a main area `<main className="flex-1 overflow-y-auto"><div className="max-w-3xl mx-auto px-4 py-6"><AssistantRuntimeProvider runtime={runtime}><Thread.Root>{/* empty-state OR message list */}<Thread.Messages components={{UserMessage: () => <ChatBubble role="user" rawMarkdown="" children={<MessagePrimitive.Content />} />, AssistantMessage: ({message}) => { const threadRuntime = useThreadRuntime(); /* OR useThreadActions() per executor verification */ return (<><RoutingChip /><ChatBubble role="assistant" rawMarkdown="" onRegenerate={() => threadRuntime.reload()}><MessagePrimitive.Content /></ChatBubble><MetricsFooter /></>); }}} /><Composer.Root className="border-t border-slate-200 bg-white px-4 py-3 sticky bottom-0"><div className="max-w-3xl mx-auto flex items-end gap-2"><Composer.Input placeholder="Type a message…" className="flex-1 min-h-12 max-h-48 resize-none rounded-md border border-slate-300 bg-white px-3 py-2 text-sm leading-normal placeholder:text-slate-500 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-slate-900 focus-visible:ring-offset-2" /><Composer.Send className="h-9 w-9 rounded-md bg-slate-900 text-white hover:bg-slate-800 disabled:bg-slate-300" /><Composer.Cancel className="h-9 w-9 rounded-md bg-slate-900 text-white hover:bg-slate-800" /></div></Composer.Root></Thread.Root></AssistantRuntimeProvider></div></main>`.

CRITICAL Regenerate wiring: the AssistantMessage variant calls `useThreadRuntime()` (or whichever assistant-ui v0.14 export the executor confirms) inside the component function (this is a hook call so it must be inside the component body, not at the top level). The hook returns a runtime object exposing `.reload()`. Wire `<ChatBubble ... onRegenerate={() => threadRuntime.reload()} ... />`. NEVER leave this as a stub TODO — Blocker 3 explicitly requires live wiring.

NOTE: the assistant-ui primitive API names (Thread.Root, Thread.Messages, Composer.Root, Composer.Send, Composer.Cancel) AND the reload export name MUST be cross-checked against the installed @assistant-ui/react@^0.14.5 package at execution time — the executor reads `node_modules/@assistant-ui/react/dist/*.d.ts` and adjusts the JSX names AND the reload binding if the primitive set differs. The intent is clear: a Thread root with a Messages slot rendering different bubbles per role, a Composer root with Input + Send + Stop buttons, and Regenerate live-wired to the runtime's reload action. If the AI SDK v6 message.parts API for routing/metrics requires a different subscription pattern (RESEARCH Open Question 4), the executor adapts the chip + footer wiring to read from `useThread`'s `data` state instead of message.parts — both paths satisfy the same SC.

Render EmptyState only when Thread reports zero messages — the assistant-ui primitive exposes this via `Thread.Empty` or a similar slot; use it if available, else render `EmptyState` conditionally based on `useThread()` state. Wire the Composer's Stop button to useChat.stop() (Pattern 3) — this is what useChatRuntime already binds for the Cancel primitive; no extra code needed.

Run `pnpm --dir apps/web run build` and confirm exit 0. Run `pnpm --dir apps/web dev` and open `http://localhost:3000` (with mock-fastapi.py from Wave 0 running on port 8001 and `FASTAPI_URL=http://localhost:8001 pnpm --dir apps/web dev`) — manually verify a turn streams text, the chip appears with "Routed to GPT-5" or similar, the footer shows streaming dot then final metrics, the Stop button replaces Send during streaming, the Regenerate button appears on hover over the assistant bubble and clicking it issues a fresh /api/chat POST. (The E2E assertion for Regenerate lives in Task 4's routing-chip.spec.ts.)</action>
  <verify>
    <automated>pnpm --dir apps/web run build 2>&amp;1 | tail -10 &amp;&amp; node -e "const p=require('fs').readFileSync('apps/web/app/page.tsx','utf8'); if (!p.startsWith('\"use client\"')) {console.error('page.tsx must start with \"use client\"');process.exit(1)} if (!p.includes('AssistantRuntimeProvider')) {console.error('page.tsx must mount AssistantRuntimeProvider');process.exit(1)} if (!p.includes('RoutingChip')) {console.error('page.tsx must mount RoutingChip');process.exit(1)} if (!p.includes('MetricsFooter')) {console.error('page.tsx must mount MetricsFooter');process.exit(1)} if (!p.includes('ChatBubble')) {console.error('page.tsx must mount ChatBubble');process.exit(1)} if (!p.includes('Prompt-Optimizer')) {console.error('header must show Prompt-Optimizer');process.exit(1)} if (!p.includes('Type a message')) {console.error('composer must use placeholder Type a message…');process.exit(1)} if (!p.includes('reload(')) {console.error('Regenerate MUST be live-wired to assistant-ui reload — no TODO stub allowed (Blocker 3)');process.exit(1)} if (p.includes('Plan 05 leaves this stubbed') || p.includes('Task 3 polish') || p.includes('TODO')) {console.error('Stub TODO for Regenerate MUST be removed (Blocker 3)');process.exit(1)} console.log('page.tsx OK')" &amp;&amp; node -e "const h=require('fs').readFileSync('apps/web/hooks/useChatThread.ts','utf8'); if (!h.includes('useChatRuntime')) {console.error('hook must call useChatRuntime');process.exit(1)} if (!h.includes('experimental_throttle')) {console.error('hook must set experimental_throttle for Pattern 4');process.exit(1)} if (!h.includes('getOrCreateDefaultThread')) {console.error('hook must call getOrCreateDefaultThread');process.exit(1)} console.log('hook OK')" &amp;&amp; cd apps/web &amp;&amp; pnpm exec tsc --noEmit --strict hooks/useChatThread.ts app/page.tsx 2>&amp;1 | grep -v 'node_modules' | grep -v 'Cannot find module' | (grep -i 'error' &amp;&amp; exit 1 || echo 'tsc strict OK')</automated>
  </verify>
  <done>useChatThread hook + ChatPage page exist; page.tsx is a client component mounting AssistantRuntimeProvider + Thread + Composer with chip + bubble + footer wired; header + Settings link present; Regenerate is LIVE-WIRED to assistant-ui's reload API (no TODO stub remaining — Blocker 3 resolved); build exits 0; tsc strict clean (cross-package module-not-found warnings excluded — the executor verifies actual assistant-ui exports at install).</done>
</task>

<task type="auto">
  <name>Task 4: Overwrite apps/web/playwright/routing-chip.spec.ts with a real Playwright spec — VALIDATION.md UI-04 row's automated command + Regenerate E2E</name>
  <files>apps/web/playwright/routing-chip.spec.ts</files>
  <read_first>
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-VALIDATION.md (UI-04 row — `pnpm --dir apps/web test:e2e routing-chip.spec.ts`; UI-08 row — Copy + Regenerate),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-UI-SPEC.md (§6 chip text format + ARIA; §8.3 hover-revealed action row; §17 copy contract),
    apps/web/playwright/mock-fastapi.py (Plan 01 Wave 0 — the canned-SSE upstream; the default fixture sequence emits a routing_decision event with model_or_agent="openai/gpt-5"),
    apps/web/playwright/playwright.config.ts (Plan 01 — multi-server webServer config + USE_MOCK_FASTAPI gating),
    apps/web/components/RoutingChip.tsx (Task 1 — the chip rendering target),
    apps/web/app/page.tsx (Task 3 — the AssistantMessage variant that mounts the chip + bubble + footer)
  </read_first>
  <behavior>
    - Spec replaces the Plan 01 test.skip stub with a real Playwright test
    - Spec asserts (a) chip is visible on the first assistant message after the first turn
    - Spec asserts (b) chip's text matches /^Routed to .+ · .+$/ (the UI-SPEC §6.2 prefix + display_name + separator + rationale format)
    - Spec asserts (c) after sending 2 additional turns (3 total), every assistant message has a chip AND none is collapsed (offsetHeight > 0) or hidden (visibility !== 'hidden')
    - Spec asserts (d) the chip's aria-label contains "Routing decision: Routed to <display_name>"
    - Spec asserts (e) clicking Regenerate on the last assistant message issues a fresh POST /api/chat (verified via page.on("request") interception) AND appends a new assistant message — Blocker 3 E2E enforcement
    - Spec uses USE_MOCK_FASTAPI=1; no real OpenRouter key needed
  </behavior>
  <action>OVERWRITE apps/web/playwright/routing-chip.spec.ts (currently a test.skip stub from Plan 01) with a real Playwright spec. Header comment must reference VALIDATION.md UI-04 + UI-08 rows.

```typescript
// VALIDATION.md rows wired by this spec:
//   - UI-04: Routing chip always visible (never collapsed) on every assistant message
//   - UI-08: ChatBubble action row — Copy + Regenerate; Regenerate issues a fresh turn
//
// Uses Plan 01's mock-fastapi.py default fixture (USE_MOCK_FASTAPI=1 in CI).
// The default fixture emits routing_decision with model_or_agent="openai/gpt-5"
// which resolves to display_name="GPT-5" via config/model_mapping.json.

import {test, expect} from "@playwright/test";

test("UI-04 + UI-08: routing chip visible on every assistant message + Regenerate issues fresh turn", async ({page}) => {
  // Track every POST to /api/chat so we can assert Regenerate fires a fresh one
  const chatPosts: string[] = [];
  page.on("request", (req) => {
    if (req.method() === "POST" && /\/api\/chat/.test(req.url())) {
      chatPosts.push(req.url());
    }
  });

  await page.goto("/");

  // Satisfy first-run modal if it appears (Plan 07 may add the modal; tolerate either path)
  const modalHeading = page.getByText("Connect OpenRouter to get started");
  if (await modalHeading.isVisible({timeout: 2000}).catch(() => false)) {
    await page.getByLabel("OpenRouter API key").fill("sk-or-v1-" + "K".repeat(48));
    await page.getByRole("button", {name: "Save & continue"}).click();
    await expect(modalHeading).toBeHidden({timeout: 5000});
  }

  const composer = page.locator('textarea[placeholder="Type a message…"]');
  await composer.waitFor({state: "visible", timeout: 10_000});

  // Turn 1: send a prompt, wait for the chip
  await composer.fill("hello");
  await page.keyboard.press("Enter");

  const firstChip = page.locator('[role="status"][aria-label*="Routing decision"]').first();
  await firstChip.waitFor({state: "visible", timeout: 10_000});

  // Assertion (a): chip is visible
  await expect(firstChip).toBeVisible();

  // Assertion (b): chip text matches the UI-SPEC §6.2 format
  const chipText = (await firstChip.innerText()).trim();
  expect(chipText).toMatch(/^Routed to .+ · .+$/);

  // Assertion (d): aria-label contains "Routing decision: Routed to <display_name>"
  const ariaLabel = await firstChip.getAttribute("aria-label");
  expect(ariaLabel).toMatch(/^Routing decision: Routed to .+/);

  // Wait for the first turn to fully complete before sending the next
  await page.locator('[aria-label*="Turn cost"]').first().waitFor({timeout: 30_000});

  // Turns 2 and 3: send two more prompts; chip count should grow to 3
  for (let i = 0; i < 2; i++) {
    await composer.fill(`follow up ${i + 2}`);
    await page.keyboard.press("Enter");
    // Wait for the (i+2)-th metrics footer to confirm the turn completed
    await page.locator('[aria-label*="Turn cost"]').nth(i + 1).waitFor({timeout: 30_000});
  }

  // Assertion (c): after 3 turns, every assistant message has a chip AND none is collapsed
  const chips = page.locator('[role="status"][aria-label*="Routing decision"]');
  await expect(chips).toHaveCount(3, {timeout: 10_000});
  const collapseChecks = await chips.evaluateAll((els) =>
    els.map((el) => ({
      h: (el as HTMLElement).offsetHeight,
      v: window.getComputedStyle(el as HTMLElement).visibility,
      d: window.getComputedStyle(el as HTMLElement).display,
    }))
  );
  for (const c of collapseChecks) {
    expect(c.h, "chip height must be > 0 (UI-04: never collapsed)").toBeGreaterThan(0);
    expect(c.v, "chip visibility must not be 'hidden'").not.toBe("hidden");
    expect(c.d, "chip display must not be 'none'").not.toBe("none");
  }

  // Assertion (e): click Regenerate on the LAST assistant message → fresh POST /api/chat fires
  const postsBeforeRegen = chatPosts.length;
  const assistantBubbles = page.locator("text=Routed to").locator("..").locator("..");
  // Hover the last assistant bubble to reveal the action row, then click Regenerate
  const lastBubble = assistantBubbles.last();
  await lastBubble.hover();
  const regenButton = lastBubble.getByRole("button", {name: "Regenerate response"});
  await regenButton.waitFor({state: "visible", timeout: 5_000});
  await regenButton.click();

  // Wait for the new POST to land (allow up to 3s for the round-trip start)
  await page.waitForFunction(
    (before) => (window as any).__POSTS_TRACKED__ === undefined && true,
    postsBeforeRegen,
    {timeout: 100}
  ).catch(() => {/* noop — we use the request listener instead */});
  // Give the request listener time to accumulate
  await page.waitForTimeout(2000);
  expect(chatPosts.length, "Regenerate must fire a fresh POST /api/chat (Blocker 3 E2E)").toBeGreaterThan(postsBeforeRegen);

  // Wait for the new turn to complete (a 4th metrics footer appears)
  await page.locator('[aria-label*="Turn cost"]').nth(3).waitFor({timeout: 30_000});

  // Final sanity: chip count is now 4 (3 prior + 1 regenerated)
  await expect(chips).toHaveCount(4, {timeout: 10_000});
});
```

Run `USE_MOCK_FASTAPI=1 pnpm --dir apps/web test:e2e routing-chip.spec.ts` and assert exit 0. NOTE: this test requires Plan 03's /api/chat route to be live AND Plan 01's mock-fastapi to emit a routing_decision event in its default sequence (which it does per Plan 01 Task 2). If the test fails because the mock-fastapi default fixture doesn't include the routing_decision-then-text-delta-then-done sequence, instead of widening this plan's scope, the executor reports back to the orchestrator — the mock-fastapi.py canned sequence is Plan 01's responsibility and Plan 06/07 also extend it.</action>
  <verify>
    <automated>USE_MOCK_FASTAPI=1 pnpm --dir apps/web test:e2e routing-chip.spec.ts 2>&amp;1 | tail -20 | (grep -E '(passed|FAIL)' || echo 'check Playwright output above') &amp;&amp; node -e "const s=require('fs').readFileSync('apps/web/playwright/routing-chip.spec.ts','utf8'); if (s.includes('test.skip(') || s.includes('test.todo(')) {console.error('routing-chip.spec.ts skip/todo stub must be removed (Blocker 2)');process.exit(1)} if (!s.includes('import {test')) {console.error('routing-chip.spec.ts must import from @playwright/test');process.exit(1)} if (!s.includes('Routing decision')) {console.error('spec must assert chip aria-label contains \"Routing decision\"');process.exit(1)} if (!s.match(/\\/\\^Routed to .\\+ · .\\+\\$/)) {console.error('spec must assert text matches /^Routed to .+ · .+$/');process.exit(1)} if (!s.includes('toHaveCount(3')) {console.error('spec must assert 3 chips after 3 turns');process.exit(1)} if (!s.includes('Regenerate response')) {console.error('spec must click Regenerate response button (UI-08 + Blocker 3 E2E)');process.exit(1)} if (!s.includes('chatPosts')) {console.error('spec must intercept POST /api/chat for Regenerate assertion');process.exit(1)} console.log('routing-chip.spec.ts contract OK')"</automated>
  </verify>
  <done>apps/web/playwright/routing-chip.spec.ts is a real Playwright test (no skip/todo); the spec asserts the 4 chip-visibility requirements (visible, text format, 3-turn count + non-collapsed, aria-label) AND the Regenerate-fires-fresh-POST behavior (Blocker 3 E2E); USE_MOCK_FASTAPI=1 test:e2e routing-chip.spec.ts exits 0; VALIDATION.md UI-04 row's automated command is now backed by a real test (Blocker 2 resolved).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| message.parts → chip render | Trusted AI SDK v6 part; data is the STRUCTURED RoutingDecision (5 keys: backend, model_or_agent, rationale, confidence, signals) — Plan 02 Zod schema validated this at the wire boundary |
| message.parts → footer render | Same trust level; data is the metrics payload from Phase 2 Done chunk |
| rawMarkdown prop → clipboard | Untrusted markdown; navigator.clipboard.writeText is text-only (no exec) — XSS-safe by API contract |
| user input → composer → /api/chat | Untrusted prompt text; FastAPI handles validation downstream |
| Regenerate click → assistant-ui reload | Internal — triggers a known runtime action; no untrusted data flows through here |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-27 | Tampering | XSS via assistant-rendered routing rationale | mitigate | RoutingChip renders rationale as text content via JSX `{truncated}` — React auto-escapes; never dangerouslySetInnerHTML (RESEARCH Security Domain "Reflected XSS via routing rationale") |
| T-04-28 | Tampering | XSS via display_name from config/model_mapping.json | mitigate | model_mapping.json is repo-committed and source-controlled; React auto-escapes the resolved string |
| T-04-29 | Information Disclosure | RoutingChip aria-label could leak signals dict contents to assistive tech | accept | The aria-label is only "Routed to <display_name>. <rationale>" — the full signals dict is NOT exposed (only the rationale). Phase 5's expandable "why this route" panel would reveal more, but it's Out-Of-Scope per V2 ROUTER-V2-01. |
| T-04-30 | Denial of Service | rawMarkdown prop is a 10MB string and clipboard.writeText hangs | accept | Single-user local-only; user's own paste; not an attacker-controlled scenario |
| T-04-31 | Spoofing | Unknown backend renders openrouter color, masking the actual route | mitigate | Plan 02's Zod schema rejects unknown backends at the wire boundary; if a malformed payload bypasses Zod, the chip's openrouter fallback is graceful degradation — better than crashing |
| T-04-32 | Tampering | useChat.stop() leaves a partially-rendered message that contains incomplete markdown | mitigate | RESEARCH Pitfall 3 — AI SDK v6 stop() preserves partial; the assistant bubble shows whatever was streamed; Plan 06's markdown renderer handles incomplete fences gracefully (Pattern 5 / 5b) |
| T-04-33 | Tampering | Composer Stop button mid-stream produces an inconsistent UI state | mitigate | useChatRuntime's Composer.Cancel primitive (or whatever its name is in 0.14.5) is bound to runtime.stop() — single source of truth; UI-SPEC §9.3 state machine ensures Send → Stop transition is atomic on first chunk arrival |
| T-04-34 | Information Disclosure | useChatThread hook accidentally injects threadId via NEXT_PUBLIC_ env | mitigate | threadId comes from getOrCreateDefaultThread (localStorage), never from a server env var |
| T-04-34b | Tampering | Regenerate triggers infinite reload loop on persistent error | accept | assistant-ui's reload action is user-initiated (button click); no automatic retry path; user controls the cadence |
</threat_model>

<verification>
- `pnpm --dir apps/web run build` exits 0
- `pnpm --dir apps/web test routing-chip.test.tsx metrics-footer.test.tsx chat-bubble.test.tsx` exits 0 with at least 17 tests passing (added Regenerate-click-invokes-prop case)
- `USE_MOCK_FASTAPI=1 pnpm --dir apps/web test:e2e routing-chip.spec.ts` exits 0 (Blocker 2 enforcement)
- page.tsx is a client component mounting AssistantRuntimeProvider + Thread + Composer
- page.tsx Regenerate is LIVE-WIRED to assistant-ui's reload API (grep for `.reload(` succeeds; grep for `TODO`/`stubbed` returns zero in the Regenerate area — Blocker 3 enforcement)
- RoutingChip + MetricsFooter + ChatBubble + EmptyState + StreamErrorBanner all exist with UI-SPEC-compliant classes
- RoutingChip reads `routing.backend` / `routing.model_or_agent` / `routing.rationale` directly (NOT `signals.*`) — Blocker 1 enforcement
- ChatBubble.tsx contains NO `TODO: replace with MarkdownRenderer` marker text — Plan 06 uses positive-grep on `MarkdownRenderer` instead (per Blocker 4 fix in Plan 06)
- StreamErrorBanner catalog has every Phase 2 D-06 code
- The smoke check (manual): visit localhost:3000 with mock-fastapi running, send a prompt, observe chip pops first then text streams then metrics finalize then Regenerate fires a fresh turn
</verification>

<success_criteria>
- Wave 4 complete: visible chat surface, chip + footer + bubble all wired
- ROADMAP SC #1 (chip on every assistant message) is observable at localhost:3000 AND enforced by routing-chip.spec.ts E2E (Blocker 2)
- ROADMAP SC #3 first half (Stop preserves partial) is wired — Plan 07's Playwright spec confirms the 2s budget
- UI-04 (chip never collapsed) satisfied by RoutingChip's always-mounted-when-part-exists pattern AND routing-chip.spec.ts E2E
- UI-07 (metrics footer) satisfied by MetricsFooter — Plan 06 doesn't touch this
- UI-08 (Copy + Regenerate) satisfied by ChatBubble action row + LIVE-wired Regenerate to assistant-ui reload (Blocker 3) + routing-chip.spec.ts E2E assertion that Regenerate fires a fresh /api/chat POST
- UI-01 (multi-turn chat scroll) inherited from assistant-ui Thread primitive
- UI-06 (Stop preserves partial) satisfied by Pattern 3 + assistant-ui Composer.Cancel
- VALIDATION.md rows "UI-01 unit", "UI-04 unit", "UI-04 E2E", "UI-07 unit", "UI-08 unit" all flip to ✅
</success_criteria>

<output>
After completion, create `.planning/phases/04-minimal-chat-ui-openrouter-backend/04-05-SUMMARY.md` recording:
- The exact assistant-ui primitive names used (Thread.Root / Thread.Messages / Composer.Root / etc.) — these may differ slightly from the planner's guesses depending on @assistant-ui/react@0.14.5 actual exports
- The exact assistant-ui reload export bound to Regenerate (useThreadRuntime vs useThreadActions vs other) — Plan 06 reads this to wire isStreamingComplete the same way
- Resolution of RESEARCH Open Question 4 (whether routing chip subscribes via useThreadMessage.parts OR useChat.data) — record which path was used
- Test count by file (routing-chip, metrics-footer, chat-bubble, routing-chip.spec.ts E2E)
- Manual smoke test output: time from POST to chip-rendered, time to first-text, time to footer-finalized, Regenerate-click → new turn observed
- Confirmation that page.tsx is "use client" and that the hydration mismatch warning is absent in `pnpm dev`
- Confirmation that the Regenerate stub TODO is gone (Blocker 3) and the routing-chip.spec.ts skip stub is overwritten (Blocker 2) and RoutingChip reads the structured 5-key payload (Blocker 1)
</output>
</content>
