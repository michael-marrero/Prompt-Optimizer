---
phase: 04
plan: 04
type: execute
wave: 3
depends_on: [01]
files_modified:
  - apps/api/routes/turn.py
  - apps/api/tests/test_turn_streaming.py
autonomous: true
requirements:
  - UI-04
user_setup: []

must_haves:
  truths:
    - "apps/api/routes/turn.py event_stream() yields a `routing_decision` named SSE event as its FIRST yield, BEFORE the adapter.stream() async for loop — per D-15 + PATTERNS turn.py amendment guidance"
    - "The routing_decision event's `data` is a STRUCTURED JSON payload with exactly 5 keys: `{backend, model_or_agent, rationale, confidence, signals}` — sourced directly from the in-scope `decision: RoutingDecision`; `data['signals']` equals `Done.routing_signals` byte-for-byte (the per-chip metadata wraps the signals; the signals SUB-FIELD preserves the canonical-persistence equality requirement of D-15)"
    - "The new event's emission survives mid-stream cancellation: yielding routing_decision happens BEFORE the try/except asyncio.CancelledError block, so even an instant cancellation still ships the chip data"
    - "ChatChunk Pydantic union (apps/api/backends/chunks.py) is NOT modified — the routing_decision event is yielded alongside ChatChunks, not as one"
    - "import json is added to apps/api/routes/turn.py's import block (currently uses chunk.model_dump_json() Pydantic helper, not stdlib json)"
    - "apps/api/tests/test_turn_streaming.py contains a new test `test_routing_decision_event_arrives_first_and_matches_done` (or equivalent name) asserting: (a) first event on the wire is 'routing_decision', (b) arrives within 500ms (ASGITransport bound; the 100ms target is for real-network), (c) parsed payload has exactly the 5 keys `{backend, model_or_agent, rationale, confidence, signals}`, (d) `payload['signals']` equals `Done.routing_signals` byte-for-byte"
    - "Whole-repo pytest suite (`pytest apps/api/ src/ -x -q`) is GREEN after the amendment — no Phase 2 / Phase 3 regression"
    - "ROADMAP SC #3 wording reconciled: the stop-status assertion broadens to `status IN ('cancelled','complete','error')` per RESEARCH Critical Finding #2 + Open Question 2 — Phase 3's `cancelled` is honored as canonical"
    - "Wave 4 (Plan 05's chat surface) depends on this plan because the chip cannot render until the wire emits the routing_decision event"
  artifacts:
    - path: "apps/api/routes/turn.py"
      provides: "event_stream() with first-yield routing_decision event before adapter dispatch — payload is the structured {backend, model_or_agent, rationale, confidence, signals} record"
      contains: "event=\"routing_decision\""
    - path: "apps/api/tests/test_turn_streaming.py"
      provides: "contract test asserting routing_decision event arrives first, has the structured 5-key shape, and payload['signals'] matches Done.routing_signals byte-for-byte"
      contains: "test_routing_decision_event"
  key_links:
    - from: "apps/api/routes/turn.py event_stream()"
      to: "the wire format apps/web/lib/sse-translate.ts parses"
      via: "ServerSentEvent(event='routing_decision', data=json.dumps({backend, model_or_agent, rationale, confidence, signals}))"
      pattern: "event=\"routing_decision\""
    - from: "apps/api/tests/test_turn_streaming.py::test_routing_decision_event_arrives_first_and_matches_done"
      to: "Done.routing_signals canonical persistence source (Phase 3 STORE-02)"
      via: "byte-for-byte equality assertion routing_payload['signals'] == done_payload['routing_signals']"
      pattern: "routing_payload\\[.signals.\\] == done_payload\\[.routing_signals.\\]"
---

<notes>
## Revision iteration 1 (Blocker 1, Warning 4)

- **Blocker 1 fix:** The emitted `routing_decision` SSE payload changed from `json.dumps(decision.signals)` (signals telemetry only) to a STRUCTURED record `{backend, model_or_agent, rationale, confidence, signals}`. The chip in Plan 05 needs `backend` (for color), `model_or_agent` (for display_name lookup), `rationale` (for the chip body + tooltip), and `confidence`. Emitting only the signals dict (task_type / agentic_intent / rule_fired) starved the chip of every field it needs. The signals dict is preserved as a SUB-FIELD so Done.routing_signals byte-for-byte equality (D-15's persistence-source guarantee) is honored at `payload['signals']` rather than at `payload` itself.
- **D-15 wording amendment:** Plan 04-CONTEXT.md D-15 should be updated by the orchestrator to reflect the structured payload (heading: "Payload shape: structured record `{backend, model_or_agent, rationale, confidence, signals}` — `signals` sub-field equals `Done.routing_signals` byte-for-byte"). This planner-side amendment is logged here so the orchestrator can land a separate `docs(04): reconcile D-15 payload shape` commit.
- **Warning 4 fix:** Task 1's verify now uses an explicit AssertionError grep on a `tee`'d log so the RED gate FAILS LOUDLY if Task 2's amendment was executed first (which would make the test pass and silently hide the RED-required-first ordering).
</notes>

<objective>
Wave 3 — Phase 3 amendment for D-15. Modify `apps/api/routes/turn.py` to emit a `routing_decision` named SSE event as the FIRST event on the wire (before any text_delta or other ChatChunk), with a STRUCTURED payload `{backend, model_or_agent, rationale, confidence, signals}` so the Plan 05 chip can render backend color + display_name + rationale + confidence; the `signals` sub-field equals `Done.routing_signals` byte-for-byte (preserves D-15's canonical-persistence equality requirement). Add the contract test in `apps/api/tests/test_turn_streaming.py` that asserts arrival ordering, the 5-key shape, and the sub-field equality.

Purpose: The Phase 4 routing chip (Plan 05) cannot render until this wire amendment exists. The chip is the highest-visibility deliverable of Phase 4 (UI-04 — "always visible, never collapsed") and ROADMAP SC #1 requires it to appear on every assistant message. The wave-level dependency: Plan 05 (chat surface + chip) `depends_on: [02, 03, 04]` because the chip must subscribe to a real on-the-wire event with the fields it needs.

[BLOCKING] This task modifies a Phase 3 file (apps/api/routes/turn.py). The amendment is deliberate, single-block-of-code in the event_stream generator, and explicitly anticipated by CONTEXT D-15 ("Phase 3 amendment scope: edit apps/api/routes/turn.py only"). No new Phase 3 contracts are introduced; the ChatChunk union is NOT modified.

Output: Modified turn.py + new contract test; `pytest apps/api/ src/ -x -q` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md
@.planning/phases/03-fastapi-service-persistent-storage/03-CONTEXT.md
@apps/api/routes/turn.py
@apps/api/tests/test_turn_streaming.py
@apps/api/backends/chunks.py
@apps/api/tests/fake_adapter.py
@src/routing/schema.py
@src/routing/decide.py

<interfaces>
<!-- The exact insertion point in apps/api/routes/turn.py per PATTERNS turn.py section. -->

Current turn.py event_stream() at lines 452-590:

```python
async def event_stream():
    """Yield ServerSentEvent chunks, buffer for persist_turn."""
    buffer: list[ChatChunk] = []
    start_t = asyncio.get_event_loop().time()
    try:
        async for chunk in adapter.stream(
            body.message, adapter_history, options
        ):
            # ... existing loop body unchanged ...
```

D-15 insertion: as the FIRST statement inside event_stream(), BEFORE the buffer init and BEFORE the try/except CancelledError wrap. The `decision` local is already in scope from lines 380-385.

The ServerSentEvent class comes from `sse_starlette.sse` — already imported at line 161.

json is NOT currently imported at module-top (the file uses Pydantic's chunk.model_dump_json() everywhere). Add `import json` to the import block at lines 134-141.

`decision: RoutingDecision` is the in-scope local from `decision = await asyncio.to_thread(decide, ...)` at lines 380-385. All five fields (backend, model_or_agent, rationale, confidence, signals) are populated by `decide()` per src/routing/schema.py:RoutingDecision.

Done.routing_signals is set at line 509 in the cancellation handler AND at line 446 via `options.routing_signals=decision.signals` then carried by adapter-emitted Done chunks via the Phase 2 D-04 invariant.

From apps/api/tests/test_turn_streaming.py:
- Lines 108-158: `_fresh_app` helper builds a fresh app instance with monkeypatched lifespan; reuse verbatim
- Lines 161-168: `_create_thread` helper POSTs to /api/v1/threads; reuse verbatim
- Lines 176-254: `test_streams_chatchunks` — the canonical SSE consumer using httpx.AsyncClient + ASGITransport; mirror this shape

From apps/api/tests/fake_adapter.py:
- `FakeStreamingAdapter([chunks...])` — yields the provided list of ChatChunks; reuse for the new test
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 [BLOCKING]: Author the contract test in test_turn_streaming.py asserting the routing_decision event arrives first, has the 5-key structured shape, and payload['signals'] equals Done.routing_signals — RED before implementation</name>
  <files>apps/api/tests/test_turn_streaming.py</files>
  <read_first>
    apps/api/tests/test_turn_streaming.py (full file — lines 108-158 _fresh_app helper, lines 161-168 _create_thread helper, lines 176-254 test_streams_chatchunks as the canonical SSE-consumer shape to mirror),
    apps/api/tests/fake_adapter.py (FakeStreamingAdapter constructor + yield mechanics — the new test uses it to inject a deterministic chunk sequence),
    apps/api/backends/chunks.py (TextDelta + Done shapes used in the fixture chunks),
    src/routing/schema.py (RoutingDecision constructor signature for the monkeypatched fake_decide — 5 fields: backend / model_or_agent / rationale / confidence / signals),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md (D-15 — full byte-for-byte equality contract; revision iteration 1 amendment to the payload shape recorded in this plan's <notes>),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md ("apps/api/tests/test_turn_streaming.py (MODIFY — add contract test)" section — full reference test shape; lines 188-261)
  </read_first>
  <behavior>
    - Test passes RED on the un-amended turn.py (the routing_decision event does not yet exist; first event is text_delta from the FakeStreamingAdapter — assertion #1 fails)
    - Test passes GREEN after Task 2's turn.py amendment (first event is routing_decision; payload has the 5 structured keys; payload['signals'] matches Done.routing_signals)
    - Test detects regression: if a future plan re-orders event_stream to emit text_delta before routing_decision, assertion #1 catches it
    - Test detects payload shape drift: if a future plan changes the routing_decision payload to anything other than the {backend, model_or_agent, rationale, confidence, signals} 5-key record, assertion #3 catches it
    - Test detects signals-sub-field drift: if a future plan stops mirroring decision.signals at payload['signals'], assertion #4 catches it (this preserves D-15's canonical-persistence-source invariant)
  </behavior>
  <action>Append the new async test to apps/api/tests/test_turn_streaming.py. Test name: `async def test_routing_decision_event_arrives_first_and_matches_done(monkeypatch, tmp_path) -> None:`. Use the PATTERNS reference shape (PATTERNS file lines 188-261) verbatim with these adaptations:

(1) `test_signals` payload is a realistic Phase-1 signals dict: `test_signals = {"task_type": "chat", "task_type_confidence": 0.92, "agentic_intent": False, "agentic_intent_confidence": 0.05, "rule_fired": "default"}` — exercise the realistic shape so the byte-for-byte test catches any signals truncation.

(2) FakeStreamingAdapter chunks are `[TextDelta(text="hi"), Done(tokens_in=1, tokens_out=1, cost_usd=0.001, latency_ms=10, routing_signals=test_signals)]` — the Done MUST carry the same signals dict.

(3) The monkeypatch of `apps.api.routes.turn.decide` constructs a RoutingDecision with `backend="openrouter", model_or_agent="openai/gpt-5", rationale="test routing", confidence=0.9, signals=test_signals`. The test holds these expected field values in local constants so the assertions can compare them.

(4) The SSE consumer loop uses `client.stream("POST", f"/api/v1/threads/{thread_id}/turn", json={"message": "hello"})` and aiter_lines to collect `(event, data)` tuples until `done` arrives.

ASSERTIONS — four independent:
- (a) `assert events[0][0] == "routing_decision"` (first event ordering)
- (b) `assert (first_event_t - t0) < 0.5` (ASGITransport latency bound — 500ms not 100ms because ASGITransport has no real network)
- (c) `routing_payload = json.loads(events[0][1]); assert set(routing_payload.keys()) == {"backend", "model_or_agent", "rationale", "confidence", "signals"}; assert routing_payload["backend"] == "openrouter"; assert routing_payload["model_or_agent"] == "openai/gpt-5"; assert routing_payload["rationale"] == "test routing"; assert routing_payload["confidence"] == 0.9` (structured 5-key shape with concrete field values)
- (d) `done_event = next(e for e in events if e[0] == "done"); done_payload = json.loads(done_event[1]); assert routing_payload["signals"] == done_payload["routing_signals"]` (byte-for-byte equality moves to the `signals` SUB-FIELD; preserves D-15 canonical-persistence equality)

Run the test BEFORE implementing Task 2 — it MUST fail with assertion (a) `events[0][0] == "text_delta" != "routing_decision"` (RED gate). Commit the failing test as commit `test(04-04): add D-15 routing_decision contract (RED)`.</action>
  <verify>
    <automated>cd /Users/michaelmarrero/GitHub/Prompt-Optimizer &amp;&amp; grep -q 'test_routing_decision_event' apps/api/tests/test_turn_streaming.py &amp;&amp; pytest apps/api/tests/test_turn_streaming.py::test_routing_decision_event_arrives_first_and_matches_done -x 2>&amp;1 | tee /tmp/04-04-red.log &amp;&amp; grep -qE 'FAILED|AssertionError' /tmp/04-04-red.log || (echo 'TEST DID NOT FAIL — RED gate not honored (Task 2 may have been executed prematurely; the test must FAIL on the un-amended turn.py)' &amp;&amp; exit 1) &amp;&amp; grep -E 'routing_payload\[.signals.\] == done_payload\[.routing_signals.\]' apps/api/tests/test_turn_streaming.py &amp;&amp; grep -E '\"routing_decision\"' apps/api/tests/test_turn_streaming.py &amp;&amp; grep -E '\\{.backend., .model_or_agent., .rationale., .confidence., .signals.\\}|set\\(routing_payload\\.keys\\(\\)\\)' apps/api/tests/test_turn_streaming.py</automated>
  </verify>
  <done>The new test exists with the documented name; first run produces a clear FAILURE on the assertion that first event is routing_decision (RED gate confirmed via the `grep -qE 'FAILED|AssertionError'` short-circuit); the test references the 5-key structured shape assertion AND the byte-for-byte equality between routing_payload["signals"] and done_payload["routing_signals"]; the failure message confirms the un-amended turn.py emits text_delta first.</done>
</task>

<task type="auto">
  <name>Task 2: Amend apps/api/routes/turn.py to yield the routing_decision SSE event as event_stream()'s first yield with the structured 5-key payload — GREEN</name>
  <files>apps/api/routes/turn.py</files>
  <read_first>
    apps/api/routes/turn.py (full file — focus on lines 134-161 import block; lines 380-390 where `decision` is assigned by `await asyncio.to_thread(decide, ...)`; lines 435-446 where `options = AdapterOptions(...)` ends; lines 452-590 the existing event_stream() generator — the insertion point is the FIRST line inside the generator body, before `buffer: list[ChatChunk] = []`),
    src/routing/schema.py (RoutingDecision dataclass — confirms decision.backend / .model_or_agent / .rationale / .confidence / .signals are all in scope),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md (D-15 — full event shape + byte-for-byte requirement; revision iteration 1 amendment to the payload shape recorded in this plan's <notes>),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md (apps/api/routes/turn.py MODIFY section — lines 125-178 — the exact insertion shape, including the comment block explaining why the yield must precede the try/except CancelledError block — adapt the inline `data=json.dumps(decision.signals)` to the new structured payload),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md (specifics lines 360-375 — pseudocode showing where the new event yield lives in the generator)
  </read_first>
  <action>Add `import json` to the import block at lines 134-141 of apps/api/routes/turn.py (alphabetically before `import logging`). Locate the `async def event_stream():` declaration (currently around line 452). Insert the NEW yield as the FIRST statement INSIDE the generator body, BEFORE `buffer: list[ChatChunk] = []` and BEFORE the `try:` for the CancelledError wrapper. The payload is the STRUCTURED 5-key record sourced from the in-scope `decision` local:

```python
payload = {
    "backend": decision.backend,
    "model_or_agent": decision.model_or_agent,
    "rationale": decision.rationale,
    "confidence": decision.confidence,
    "signals": decision.signals,
}
yield ServerSentEvent(
    event="routing_decision",
    data=json.dumps(payload),
)
```

Add a SHORT comment block IMMEDIATELY above the yield, mirroring the existing turn.py docstring style:

```
# D-15 (Phase 4): emit routing_decision event BEFORE adapter dispatch
# so the UI chip renders within ~100ms of POST, well before the
# first text_delta. ChatChunk union is NOT modified — this event
# is yielded alongside ChatChunks via the same SSE pipeline.
# Payload is the structured 5-key record {backend, model_or_agent,
# rationale, confidence, signals}; payload['signals'] equals
# Done.routing_signals byte-for-byte (contract test in
# test_turn_streaming.py::test_routing_decision_event...).
```

Do NOT modify any other behavior of event_stream(). Do NOT modify the try/except block or persist_turn. Do NOT add the new event name to ChatChunk discriminator (Plan 04-CONTEXT explicitly says NOT modified). The yield is yielded as a ServerSentEvent (not a ChatChunk) so the wire format honors `event: routing_decision\ndata: {...}\n\n` per Phase 3 D-07 named-event convention. Run the new test that was RED in Task 1 — assert it now passes GREEN. Then run `pytest apps/api/ src/ -x -q` and assert exit 0 (no Phase 2 / Phase 3 regression). Commit as `feat(04-04): emit routing_decision SSE event with structured payload before adapter dispatch (D-15)`.</action>
  <verify>
    <automated>cd /Users/michaelmarrero/GitHub/Prompt-Optimizer &amp;&amp; grep -E '^import json$' apps/api/routes/turn.py &amp;&amp; grep -B1 -A3 'event="routing_decision"' apps/api/routes/turn.py &amp;&amp; grep -E '\"backend\": decision\.backend' apps/api/routes/turn.py &amp;&amp; grep -E '\"model_or_agent\": decision\.model_or_agent' apps/api/routes/turn.py &amp;&amp; grep -E '\"rationale\": decision\.rationale' apps/api/routes/turn.py &amp;&amp; grep -E '\"confidence\": decision\.confidence' apps/api/routes/turn.py &amp;&amp; grep -E '\"signals\": decision\.signals' apps/api/routes/turn.py &amp;&amp; grep 'json.dumps(payload)' apps/api/routes/turn.py &amp;&amp; pytest apps/api/tests/test_turn_streaming.py::test_routing_decision_event_arrives_first_and_matches_done -x 2>&amp;1 | tail -10 | (grep -E '(passed|PASSED)' &amp;&amp; echo 'GREEN gate confirmed' || (echo 'GREEN gate FAILED' &amp;&amp; exit 1)) &amp;&amp; pytest apps/api/ src/ -x -q 2>&amp;1 | tail -5 | (grep -E '(passed|error)' &amp;&amp; echo 'whole-repo OK' || exit 1)</automated>
  </verify>
  <done>turn.py imports json at module top; the new yield appears as the first line of event_stream() with a D-15 attribution comment; the payload contains all 5 keys ({backend, model_or_agent, rationale, confidence, signals}); the previously-RED contract test passes GREEN; whole-repo pytest suite is green (no Phase 2 / Phase 3 regression).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decide() return → SSE wire | RoutingDecision is a frozen dataclass from Phase 1; the SSE event passes its 5 fields through verbatim |
| ChatChunk Pydantic union | NOT modified — Phase 2 contract preserved |
| Phase 3 test suite | Existing tests must remain green — `pytest apps/api/ src/ -x -q` is the regression gate |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-22 | Information Disclosure | routing_decision payload exposes internal classifier signals + rationale text | accept | signals dict is already persisted to .planning/data/routing_decisions.jsonl per Phase 3 STORE-06 — same content; rationale is also persisted; not a new exposure surface. The rationale travels through React JSX as text content in Plan 05's chip — auto-escaped. |
| T-04-23 | Tampering | Future plan changes payload shape without updating UI chip | mitigate | The 5-key structured assertion + the signals sub-field byte-for-byte equality test fire on ANY drift; Plan 05 chip subscribes to data-routing parts via the AI SDK v6 type — if Phase 5 changes the shape, both this test AND the chip will fail loudly |
| T-04-24 | Denial of Service | The new yield happens inside a try/except CancelledError block — could swallow cancellation | mitigate | The yield is INTENTIONALLY placed BEFORE the try block — per PATTERNS lines 174-178 "Yield happens BEFORE the try/except so a cancellation between adapter creation and adapter.stream() still emits the routing_decision". Cancellation behavior preserved. |
| T-04-25 | Information Disclosure | json.dumps(payload) serializes non-JSON-safe types and crashes | accept | Phase 1's RoutingDecision is constructed from JSON-safe types (Backend Literal, strings, float, dict[str, Any] where Any is JSON-safe by Phase 1 convention). If Phase 1 ever introduces non-serializable values, BOTH the existing Done.routing_signals serialization AND this new yield will fail in the same way — single point of breakage, well covered by tests |
| T-04-26 | Information Disclosure | StreamError(auth_failed) emitted BEFORE the routing_decision event leaves the chip orphaned | accept | The current turn.py emits stream errors INSIDE the adapter.stream() loop (after decide returned successfully). decide() itself does not produce stream errors. A pre-decide failure produces an HTTPException(400) — never reaches event_stream(). Edge case acknowledged; no UI surface impact because the chip simply doesn't render. |
| T-04-26b | Tampering | XSS via rationale text injected into chip aria-label | mitigate | Plan 05 RoutingChip renders rationale as JSX text content (`{routing.rationale}`); React auto-escapes; never dangerouslySetInnerHTML. Backend-side, decide() composes the rationale from closed-vocabulary strings (Phase 1 D-04) — no user-controlled text reaches it. |
</threat_model>

<verification>
- `import json` present at top of apps/api/routes/turn.py
- A single yield-block with `event="routing_decision", data=json.dumps(payload)` exists as event_stream's first yield
- The payload dict literal contains all 5 keys: backend, model_or_agent, rationale, confidence, signals (each sourced from `decision.<field>`)
- The yield is followed by the unchanged `buffer: list[ChatChunk] = []` line then the unchanged try/except block
- `pytest apps/api/tests/test_turn_streaming.py::test_routing_decision_event_arrives_first_and_matches_done` passes
- `pytest apps/api/ src/ -x -q` exits 0 (no regression)
- The Done event still carries the same routing_signals — backward compatibility for any consumer that reads only the final state
- ChatChunk Pydantic union (apps/api/backends/chunks.py) is byte-identical to its pre-amendment state — `git diff apps/api/backends/chunks.py` returns nothing
</verification>

<success_criteria>
- Wave 3 complete: Phase 3 wire format is extended with the D-15 structured event
- Plan 05's chat surface can subscribe to the `data-routing` AI SDK v6 part (produced by Plan 02's translator from this new event) and read backend/model_or_agent/rationale/confidence directly
- The byte-for-byte equality between mid-stream chip data signals sub-field and final Done.routing_signals is provably preserved
- No Phase 3 SUMMARY needs revision — the amendment is documented in this plan's SUMMARY only (per CONTEXT D-15 "Phase 3 amendment scope: edit apps/api/routes/turn.py only")
- VALIDATION.md row "D-15 contract" flips to ✅
</success_criteria>

<output>
After completion, create `.planning/phases/04-minimal-chat-ui-openrouter-backend/04-04-SUMMARY.md` recording:
- The exact line number where the new yield landed in turn.py (post-amendment)
- The test name + assertion count for the contract test (4 independent assertions: ordering, latency, 5-key shape, signals-sub-field equality)
- The RED → GREEN transition commit hashes (Task 1's RED commit + Task 2's GREEN commit)
- Confirmation that `git diff apps/api/backends/chunks.py` returns empty
- Confirmation that `pytest apps/api/ src/ -x -q` exit 0 (Phase 2 + Phase 3 + Phase 1 all green)
- Note for Phase 5 planners: when adding CodeBubble + ComputerUseBubble, the same data-routing part flows through with backend="claude_code" or "computer_use" — no further wire amendment needed
- Note for orchestrator: D-15 wording in 04-CONTEXT.md should be reconciled by a separate `docs(04): reconcile D-15 payload shape` commit (the planner's revision-iteration-1 notes flagged this; this plan ships against the structured payload regardless)
</output>
</content>
