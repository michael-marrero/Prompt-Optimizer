---
phase: 04
plan: 06
type: execute
wave: 5
depends_on: [01, 05]
files_modified:
  - apps/web/lib/shiki.ts
  - apps/web/components/MarkdownRenderer.tsx
  - apps/web/components/StreamingCodeBlock.tsx
  - apps/web/lib/markdown-components.tsx
  - apps/web/components/ChatBubble.tsx
  - apps/web/app/page.tsx
  - apps/web/tests/markdown-renderer.test.tsx
  - apps/web/tests/streaming-code-block.test.tsx
  - apps/web/playwright/no-flicker.spec.ts
  - apps/web/playwright/mock-fastapi.py
autonomous: true
requirements:
  - UI-03
user_setup: []

must_haves:
  truths:
    - "MarkdownRenderer is wrapped in React.memo with a custom equality predicate comparing ONLY (message.id, message.content.length, message.role) — a token append on the same message id MUST NOT re-parse prior tokens (RESEARCH §Pattern 4 + Pitfall 4)"
    - "The shiki Highlighter singleton is created EXACTLY ONCE at module scope in apps/web/lib/shiki.ts via `createHighlighter({themes: ['github-light'], langs: ['python','javascript','typescript','tsx','json','bash','yaml','markdown','sql']})` and reused across every render (Pitfall 11 — no per-render createHighlighter() call)"
    - "shiki.ts exports `highlightCode(code: string, lang: string): Promise<string>` AND `getHighlighter(): Promise<Highlighter>` — every call returns the cached singleton; concurrent first-callers share the same promise (no double-init race)"
    - "A react-markdown fenced code block whose closing ``` has NOT yet arrived renders as `<pre><code class=\"language-${lang}\">${raw}</code></pre>` WITHOUT shiki tokens AND WITHOUT dangerouslySetInnerHTML — partial untrusted markdown is NEVER injected as HTML (XSS belt + no-flicker requirement, RESEARCH Pattern 5b + ASVS V11 + Security Domain XSS row)"
    - "On stream completion (fence closed), every code block is highlighted EXACTLY ONCE — the Playwright no-flicker.spec.ts asserts via MutationObserver that the <code> element's innerHTML mutation count does NOT increase AFTER the metrics-footer aria-label appears (RESEARCH Pattern 5 lines 789-825 + Pitfall 5)"
    - "react-markdown is configured with rehype-sanitize (or @assistant-ui/react-markdown's built-in hardening if it supplies an equivalent — the executor verifies at Wave 0 spike output) AND remark-gfm for GFM tables / strikethrough / task-lists per UI-SPEC §4 markdown heading scale + §8.2 element styles"
    - "ChatBubble's assistant variant renders `<MarkdownRenderer rawMarkdown={...} isStreamingComplete={...} messageId={...} />` in the `children` slot Plan 05 left open — Plan 06 replaces the slot VALUE directly (NOT a TODO comment marker; Plan 05 ships without any TODO text for grep-based hand-off)"
    - "page.tsx (Plan 05's output) is updated to compute `isStreamingComplete` per assistant message (true when assistant-ui v6 indicates the assistant message status is complete — executor reads Plan 05 SUMMARY's documented status API; OR equivalently `message.parts.some(p => p.type === 'data-metrics')` since metrics arrives only on Done) and passes it as a prop to ChatBubble, which forwards it to MarkdownRenderer"
    - "ChatBubble.tsx props interface gains `isStreamingComplete: boolean` and `messageId: string`; assistant variant uses these to mount MarkdownRenderer; user variant ignores them"
    - "The `code` mapper in markdown-components.tsx distinguishes inline vs fenced code by the `inline` prop (react-markdown v9) — inline code renders the UI-SPEC §8.2 inline-style `<code>`; fenced inner code renders with its raw className intact so the parent `pre` mapper can extract the language and mount StreamingCodeBlock (Warning 1 fix; without this distinction the parent `pre` sees an already-replaced inline-styled `<code>` and language extraction breaks)"
    - "The Wave-0 spike output at apps/web/lib/MARKDOWN-DECISION.md (committed by Plan 01) is the FIRST file the executor reads — its verdict (Pattern 5 built-in primitive vs Pattern 5b custom StreamingCodeBlock) determines whether StreamingCodeBlock.tsx is the real fallback implementation OR a thin wrapper around the assistant-ui code-block primitive"
    - "Hot Module Reload during `pnpm dev` does NOT trigger a re-highlight of already-mounted closed code blocks — the highlighter singleton survives HMR because it lives at module top-level (Pitfall 11 acceptance); the executor verifies once with `pnpm dev` + edit-and-save during a manual UAT"
    - "All markdown body element classes match UI-SPEC §8.2 exactly: p='text-sm leading-relaxed text-slate-900 mb-3 last:mb-0'; ul/ol='ml-5 list-disc/decimal'; li='mb-1'; h1/h2/h3 sized per UI-SPEC §3; a='text-slate-900 underline underline-offset-2 hover:text-slate-700'; inline code='text-sm font-mono bg-slate-100 px-1 py-1 rounded-sm'; pre (fenced)='text-sm font-mono leading-relaxed rounded-md overflow-x-auto p-4 my-3'; blockquote='border-l-4 border-slate-300 pl-4 italic text-slate-700'"
    - "VALIDATION.md row UI-03 (Markdown streams + code blocks highlight once on close — `pnpm --dir apps/web test:e2e no-flicker.spec.ts`) is wired by Plan 06 — the apps/web/playwright/no-flicker.spec.ts skip stub from Plan 01 is overwritten with the real Playwright test"
    - "mock-fastapi.py canonical fixture catalog comment at the top lists every named fixture across Plans 01/06/07; the body-prefix mechanism is `body.message.startswith('[fixture:NAME]')` — Plan 06 contributes [fixture:code-block]; Plan 07 will add [fixture:slow], [fixture:missing-key], [fixture:auth-failed]; the prefix is stripped before fixture dispatch (Warning 5 fix)"
  artifacts:
    - path: "apps/web/lib/shiki.ts"
      provides: "Module-scope shiki Highlighter singleton + highlightCode helper (theme=github-light, 9 preloaded langs); NEVER imported by client components directly — only by markdown-components.tsx + StreamingCodeBlock.tsx"
      exports: ["getHighlighter", "highlightCode"]
    - path: "apps/web/components/MarkdownRenderer.tsx"
      provides: "React.memo-wrapped markdown body renderer keyed by (message.id, content.length, role); uses @assistant-ui/react-markdown (per CONTEXT D-10) OR react-markdown+remark-gfm+rehype-sanitize fallback per Wave-0 spike outcome"
      exports: ["MarkdownRenderer"]
    - path: "apps/web/components/StreamingCodeBlock.tsx"
      provides: "Fence-state-aware code block per RESEARCH Pattern 5b — renders plain <pre> until isStreamingComplete=true, then one-shot shiki highlight; OR thin wrapper around assistant-ui code-block primitive per Wave-0 spike outcome"
      exports: ["StreamingCodeBlock"]
    - path: "apps/web/lib/markdown-components.tsx"
      provides: "Component map for @assistant-ui/react-markdown — wires StreamingCodeBlock as the fence renderer + UI-SPEC §8.2 element styles for p/ul/ol/li/h1-h3/a/code/blockquote; the `code` mapper distinguishes inline vs fenced via the react-markdown `inline` prop (Warning 1)"
      exports: ["MarkdownComponents"]
    - path: "apps/web/components/ChatBubble.tsx"
      provides: "MODIFIED — props interface gains isStreamingComplete + messageId; assistant variant mounts <MarkdownRenderer rawMarkdown isStreamingComplete messageId />; positive-grep gate enforces `<MarkdownRenderer` presence (no TODO marker hunt — Blocker 4)"
      contains: "MarkdownRenderer"
    - path: "apps/web/app/page.tsx"
      provides: "MODIFIED — AssistantMessage variant computes isStreamingComplete per message and passes it + messageId to ChatBubble"
      contains: "isStreamingComplete"
    - path: "apps/web/tests/markdown-renderer.test.tsx"
      provides: "Vitest+RTL unit tests asserting React.memo equality predicate, raw-pre rendering during open fence, single-highlight on close, inline-vs-fenced code distinction"
      contains: "MarkdownRenderer"
    - path: "apps/web/tests/streaming-code-block.test.tsx"
      provides: "Vitest+RTL unit tests for StreamingCodeBlock fence-state transitions (open → pre → closed → highlighted)"
      contains: "StreamingCodeBlock"
    - path: "apps/web/playwright/no-flicker.spec.ts"
      provides: "Playwright E2E asserting code-block <code> element mutates exactly once per block on fence close — VALIDATION.md UI-03 row's automated command"
      contains: "MutationObserver"
    - path: "apps/web/playwright/mock-fastapi.py"
      provides: "MODIFIED — canonical fixture catalog comment at top lists every named fixture; body-prefix mechanism (body.message.startswith('[fixture:NAME]')) for fixture selection; [fixture:code-block] emits a streamed fenced code block for the no-flicker spec"
      contains: "fixture:code-block"
  key_links:
    - from: "apps/web/components/MarkdownRenderer.tsx"
      to: "apps/web/components/StreamingCodeBlock.tsx"
      via: "MarkdownComponents map registers StreamingCodeBlock as the fenced-pre renderer (the pre mapper extracts lang from the inner code className — preserved because the code mapper does NOT mutate fenced code)"
      pattern: "StreamingCodeBlock"
    - from: "apps/web/components/StreamingCodeBlock.tsx"
      to: "apps/web/lib/shiki.ts"
      via: "highlightCode(rawCode, lang) called ONLY when isStreamingComplete === true"
      pattern: "highlightCode"
    - from: "apps/web/components/ChatBubble.tsx"
      to: "apps/web/components/MarkdownRenderer.tsx"
      via: "Plan 05 slot now mounts the memoized renderer (positive grep enforces presence — Blocker 4)"
      pattern: "MarkdownRenderer"
    - from: "apps/web/app/page.tsx"
      to: "apps/web/components/ChatBubble.tsx"
      via: "AssistantMessage passes isStreamingComplete (computed from message status / data-metrics presence) + messageId props"
      pattern: "isStreamingComplete"
    - from: "apps/web/playwright/no-flicker.spec.ts"
      to: "apps/web/playwright/mock-fastapi.py (Plan 01 Wave 0)"
      via: "Playwright webServer config + USE_MOCK_FASTAPI=1; mock-fastapi emits a streamed fenced code block when the prompt starts with [fixture:code-block]"
      pattern: "MutationObserver"
---

<notes>
## Revision iteration 1 (Blocker 4, Warning 1, Warning 5)

- **Blocker 4 fix (Option B per checker recommendation):** Plan 06 no longer hunts for a TODO marker in ChatBubble.tsx (Plan 05 now ships ChatBubble without any TODO text). Instead, Plan 06 Task 2 directly REPLACES the assistant variant's `children` slot value with `<MarkdownRenderer ... />`, and the verify gate uses POSITIVE grep on `<MarkdownRenderer` in ChatBubble.tsx. This eliminates the cross-wave coordination fragility where Plan 05 had to plant a marker for Plan 06 to find.
- **Blocker 4 fix (page.tsx prop wiring):** ChatBubble's prop interface grows two props (isStreamingComplete: boolean, messageId: string). page.tsx (Plan 05's output) is modified in this plan to compute `isStreamingComplete` per assistant message and pass it + messageId down. The executor reads Plan 05's SUMMARY to confirm the actual assistant-ui status API; `message.parts.some(p => p.type === 'data-metrics')` is a safe equivalent because the translator only emits data-metrics on Done.
- **Warning 1 fix:** The markdown-components.tsx `code` mapper now distinguishes inline vs fenced code via the react-markdown `inline` prop (v9 API). Inline code gets the UI-SPEC §8.2 inline-style `<code>`; fenced inner code passes through with its original className (`language-xyz`) so the parent `pre` mapper can extract the language and mount StreamingCodeBlock. Without this, the code mapper runs first and replaces the inner `<code>` with an inline-styled one — the pre mapper then sees a className-less child and language extraction breaks. The executor verifies the react-markdown major version at install time and adapts (v10+ may use a different signal — instructions provided to the executor in Task 2's action).
- **Warning 5 fix:** mock-fastapi.py is now LOCKED on the body-prefix mechanism (`body.message.startswith('[fixture:NAME]')`). Plan 06 contributes `[fixture:code-block]`. The top of mock-fastapi.py gains a canonical fixture catalog comment listing every named fixture across Plans 01/06/07 so future contributors don't accidentally invent a second selection mechanism (e.g. query params). Plan 07's Task 2 will also follow the body-prefix mechanism.
</notes>

<objective>
Wave 5 — Streaming markdown rendering + no-flicker syntax-highlighted code blocks. Author the memoized MarkdownRenderer (per RESEARCH §Pattern 4 block-memoization), the singleton shiki Highlighter (per Pitfall 11 — single createHighlighter() call at module scope), the fence-state-aware StreamingCodeBlock (per RESEARCH Pattern 5 if the assistant-ui primitive auto-detects fence state, else Pattern 5b custom component with `isStreamingComplete` prop), wire the assistant-ui-react-markdown component map (with inline-vs-fenced code distinction per Warning 1), and modify ChatBubble + page.tsx to mount the renderer with the required props. Land the unit tests + the Playwright no-flicker E2E that satisfies ROADMAP SC #2 + UI-03. Extend mock-fastapi.py with the canonical fixture catalog + the `[fixture:code-block]` body-prefix fixture.

Purpose: Plan 05 wired the chip + footer + ChatBubble shell with an open `children` slot. Plan 06 fills that slot with the only production-quality solution to streaming markdown: block-memoized rendering + one-shot syntax highlighting on fence close. Re-highlight on every token would (a) burn 100+ms per token in non-trivial code blocks and (b) cause the user-visible flicker SC #2 forbids. The Wave-0 spike at apps/web/lib/MARKDOWN-DECISION.md (committed by Plan 01) is the decision oracle — whether @assistant-ui/react-markdown's built-in code-block primitive handles fence-state detection (Pattern 5) OR Plan 06 ships the custom StreamingCodeBlock (Pattern 5b). Both paths satisfy SC #2; the spike picks which path is least code.

Output: An assistant message containing a python fenced code block streams token-by-token as plain <pre> until the closing ``` arrives, then highlights once with `github-light` colors. RTL tests verify React.memo equality and fence-state transitions. The Playwright no-flicker.spec.ts asserts zero mutations on the <code> element AFTER the metrics footer's final aria-label appears.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-UI-SPEC.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-VALIDATION.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-01-SUMMARY.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-05-SUMMARY.md
@apps/web/lib/MARKDOWN-DECISION.md
@apps/web/components/ChatBubble.tsx
@apps/web/app/page.tsx
@apps/web/lib/cn.ts

<interfaces>
<!-- Wave 0 spike decision the executor reads FIRST. -->
From apps/web/lib/MARKDOWN-DECISION.md (Plan 01 Wave 0 output):
- Verdict: "Pattern 5 (built-in primitive)" OR "Pattern 5b (StreamingCodeBlock fallback)"
- If Pattern 5: the assistant-ui code-block primitive owns fence-state detection; StreamingCodeBlock.tsx in this plan is a thin pass-through that mounts the primitive with the github-light theme prop
- If Pattern 5b: StreamingCodeBlock.tsx is the real implementation per RESEARCH Pattern 5b lines 831-855 (the executor adapts the skeleton)

<!-- Wave 4 contract — Plan 05 left ChatBubble assistant variant with a children slot for the markdown body. -->
From apps/web/components/ChatBubble.tsx (Plan 05 output — post revision):
```tsx
interface ChatBubbleProps {
  role: "assistant" | "user";
  rawMarkdown: string;   // the source-markdown string passed in
  onRegenerate?: () => void;
  children: React.ReactNode;  // <— Plan 05 leaves this open for MarkdownRenderer; NO TODO text marker
}
```
The assistant variant currently renders `{children}` inside the bubble. Plan 06 MODIFIES ChatBubble.tsx: (a) the props interface gains `isStreamingComplete: boolean` and `messageId: string`; (b) the assistant variant replaces the `{children}` rendering site with `<MarkdownRenderer rawMarkdown={rawMarkdown} isStreamingComplete={isStreamingComplete} messageId={messageId} />`. The `children` prop REMAINS in the interface for forward-compat (Phase 5's CodeBubble may pass custom children), but the default rendering path uses MarkdownRenderer.

<!-- Wave 4 contract — page.tsx AssistantMessage variant (Plan 05 output) currently passes children via MessagePrimitive.Content. -->
Plan 06 modifies page.tsx's AssistantMessage variant: compute `const isStreamingComplete = message.parts?.some((p: any) => p.type === "data-metrics") ?? false;` (Plan 02's translator emits data-metrics only on Done) AND pass `messageId={message.id}` to ChatBubble. The children prop stays (still receives MessagePrimitive.Content) but the assistant variant's default path uses MarkdownRenderer.

<!-- @assistant-ui/react-markdown surface (verified at install time by the executor) -->
The package likely exports `MarkdownTextPrimitive` (for streaming text), `MarkdownText` (a higher-level wrapper), and a component-map prop for overriding element renderers. The exact names are verified by inspecting `node_modules/@assistant-ui/react-markdown/dist/index.d.ts` at execution time. If the API differs from RESEARCH Pattern 4's skeleton, the executor adapts.

<!-- react-markdown v9 code mapper API (Warning 1) -->
react-markdown v9 passes an `inline: boolean` prop to the `code` component mapper to distinguish inline `<code>` spans from fenced code's inner `<code>` (which lives inside `<pre>`). The code mapper MUST check this prop:
- `inline === true` → render the UI-SPEC §8.2 inline-style `<code>`
- `inline === false` (or undefined for fenced) → render `<code className={className} {...rest}>{children}</code>` UNCHANGED so the parent `pre` mapper sees the original className with `language-xyz`
react-markdown v10+ may use a different signal (e.g. parent-driven dispatch) — the executor verifies the installed major version (`pnpm --dir apps/web list react-markdown`) and adapts. If the @assistant-ui/react-markdown wrapper hides react-markdown entirely, the executor inspects its API for the inline-vs-fenced distinction.

<!-- shiki API (verified against shiki ^4 at install time) -->
shiki 4.x exports:
- `createHighlighter({themes, langs}): Promise<Highlighter>` — async factory
- `Highlighter.codeToHtml(code, {lang, theme}): string` — synchronous render
- `Highlighter.dispose()`: cleanup (not used in Phase 4)

<!-- AI SDK v6 message state -->
The `isStreamingComplete` signal can be derived two ways the executor picks from:
1. The message has a final `data-metrics` part (Wave 4 contract: metrics arrives ONLY on Done, AFTER the closing fence) — so `isStreamingComplete = message.parts.some(p => p.type === "data-metrics")`.
2. AI SDK v6's message-state API exposes `state: "submitted" | "streaming" | "ready" | "error"` — `isStreamingComplete = state === "ready"`.
The executor uses whichever the assistant-ui runtime surfaces cleanly. Both signals fire at the same wire moment. Plan 05's SUMMARY documents which one the runtime exposes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the shiki singleton + StreamingCodeBlock (fence-aware, one-shot highlight) + unit test</name>
  <files>apps/web/lib/shiki.ts, apps/web/components/StreamingCodeBlock.tsx, apps/web/tests/streaming-code-block.test.tsx</files>
  <read_first>
    apps/web/lib/MARKDOWN-DECISION.md (Plan 01 Wave 0 spike output — Pattern 5 vs Pattern 5b verdict; this file is the FIRST read because it decides whether StreamingCodeBlock.tsx is real-impl or thin-wrapper),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md (§"Pattern 5" lines 778-829 — built-in primitive path; §"Pattern 5b" lines 831-855 — custom StreamingCodeBlock fallback with isStreamingComplete prop; Pitfall 5 — naive `<SyntaxHighlighter>{streamingCode}</SyntaxHighlighter>` re-tokenizes every token; Pitfall 11 — singleton highlighter at module scope; §"Don't Hand-Roll" row "Streaming-safe code blocks"),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-UI-SPEC.md (§8.2 code-block typography row — `text-sm font-mono leading-relaxed rounded-md overflow-x-auto p-4 my-3`; §1 shiki theme = `github-light`; §3 fenced code block uses shiki tokens),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md (D-11 — shiki + assistant-ui code-block primitive; D-04 — light mode only so single theme),
    node_modules/@assistant-ui/react-markdown (read the .d.ts at install time to confirm primitive surface — only if MARKDOWN-DECISION says Pattern 5; if Pattern 5b the executor skips this read),
    node_modules/shiki/dist/index.d.ts (verify createHighlighter signature for shiki ^4 — the executor adapts if a minor API differs from the RESEARCH sketch)
  </read_first>
  <behavior>
    - shiki.ts: getHighlighter() returns the SAME Highlighter instance across N concurrent calls (proved by `Promise.all([getHighlighter(), getHighlighter()]).then(([a, b]) => assert(a === b))`)
    - shiki.ts: highlightCode(code, "python") returns an HTML string containing shiki span tokens with github-light colors
    - shiki.ts: highlightCode(code, "unknown-language") falls back to plaintext (lang="text") without throwing
    - StreamingCodeBlock with isStreamingComplete=false renders `<pre><code class="language-${lang}">${raw}</code></pre>` — the raw code is text content (React JSX child), NEVER dangerouslySetInnerHTML
    - StreamingCodeBlock with isStreamingComplete=true awaits highlightCode then renders the shiki-tokenized HTML (this IS dangerouslySetInnerHTML — but ONLY for content that has been through shiki's tokenizer, which strips unsafe HTML; the input source is the raw fenced text already escaped by react-markdown when rendering pre/code)
    - StreamingCodeBlock transitioning from isStreamingComplete=false to true triggers EXACTLY ONE call to highlightCode (proved by spying on the helper); a subsequent re-render with same props triggers ZERO additional calls
    - StreamingCodeBlock with a changed `children` prop while isStreamingComplete=false does NOT call highlightCode (defers until close)
    - StreamingCodeBlock language prop missing/undefined defaults to "text" — never throws
  </behavior>
  <action>Author apps/web/lib/shiki.ts. Module-scope singleton pattern per Pitfall 11. Import `import {createHighlighter, type Highlighter} from "shiki";` Declare a module-scope promise: `let highlighterPromise: Promise<Highlighter> | null = null;` Export `export function getHighlighter(): Promise<Highlighter> { if (!highlighterPromise) { highlighterPromise = createHighlighter({themes: ["github-light"], langs: ["python","javascript","typescript","tsx","json","bash","yaml","markdown","sql"]}); } return highlighterPromise; }`. Export `export async function highlightCode(code: string, lang: string): Promise<string> { const h = await getHighlighter(); const supported = h.getLoadedLanguages(); const safeLang = supported.includes(lang as any) ? lang : "text"; return h.codeToHtml(code, {lang: safeLang, theme: "github-light"}); }`. The "text" fallback path requires no preload because shiki treats "text" specially (plaintext rendering, no tokenizer). Critical: NEVER call `createHighlighter` inside a component render — that's Pitfall 11. The singleton survives HMR because module-scope state is preserved across React fast-refresh.

Read apps/web/lib/MARKDOWN-DECISION.md. IF its verdict is "Pattern 5 (built-in primitive)": author apps/web/components/StreamingCodeBlock.tsx as a THIN WRAPPER that mounts the assistant-ui code-block primitive (the exact import name is recorded in MARKDOWN-DECISION.md by Plan 01's spike) with the github-light theme prop. The wrapper still accepts `{language, children, isStreamingComplete}: {language?: string; children: string; isStreamingComplete: boolean}` so MarkdownRenderer's component map has a stable interface; internally it passes those props through to the primitive (the primitive will ignore isStreamingComplete because it does its own fence-state detection, but the prop stays in the API contract for forward-compat).

ELSE IF MARKDOWN-DECISION says "Pattern 5b (StreamingCodeBlock fallback)": author apps/web/components/StreamingCodeBlock.tsx as the REAL implementation per RESEARCH Pattern 5b lines 831-855. `"use client";` header. Props: `interface Props { language?: string; children: string; isStreamingComplete: boolean }`. Import `import {useEffect, useState} from "react";` `import {highlightCode} from "@/lib/shiki";` `import {cn} from "@/lib/cn";`. Body: `const [html, setHtml] = useState<string | null>(null);` `useEffect(() => { if (!isStreamingComplete) { setHtml(null); return; } let cancelled = false; highlightCode(children, language ?? "text").then(h => { if (!cancelled) setHtml(h); }).catch(() => { if (!cancelled) setHtml(null); }); return () => { cancelled = true; }; }, [isStreamingComplete, children, language]);`. Pre-highlight or fallback render: `if (!isStreamingComplete || !html) { return <pre className={cn("text-sm font-mono leading-relaxed rounded-md overflow-x-auto p-4 my-3 bg-slate-50 border border-slate-200")}><code className={\`language-${language ?? "text"}\`}>{children}</code></pre>; }`. Highlighted render: `return <div className="text-sm font-mono leading-relaxed rounded-md overflow-x-auto p-4 my-3" dangerouslySetInnerHTML={{__html: html}} />;`. Note: shiki's codeToHtml output is the trusted HTML — shiki escapes its inputs and only emits its own `<pre><code><span>` structure with class attributes; it does NOT pass through arbitrary HTML from the source. dangerouslySetInnerHTML on the OUTPUT is therefore safe — UNLIKE dangerouslySetInnerHTML on the raw source which would be the XSS vector RESEARCH Pattern 5b forbids.

Author apps/web/tests/streaming-code-block.test.tsx overwriting any prior stub. Mock `@/lib/shiki`: `vi.mock("@/lib/shiki", () => ({highlightCode: vi.fn().mockResolvedValue("<pre class=shiki><span>hi</span></pre>"), getHighlighter: vi.fn()}))`. Test cases mirror the behavior list: render with isStreamingComplete=false + children="print('hi')" + language="python" — assert the rendered DOM contains `<pre>` with a `<code class="language-python">` whose textContent is "print('hi')" AND that highlightCode was NOT called yet. Then rerender with isStreamingComplete=true and await a microtask — assert highlightCode was called ONCE with ("print('hi')", "python") AND the rendered DOM now contains a `<div>` with the mocked HTML. Rerender once more with the SAME props — assert highlightCode call count is still 1 (no re-highlight). Test the unknown-language path: render with language=undefined — assert NO throw AND highlightCode called with (children, "text"). Run `pnpm --dir apps/web test streaming-code-block.test.tsx` and assert exit 0 with at least 4 tests passing.</action>
  <verify>
    <automated>pnpm --dir apps/web test streaming-code-block.test.tsx 2>&amp;1 | grep -E '(Tests|passing|failed|✓)' &amp;&amp; node -e "const s=require('fs').readFileSync('apps/web/lib/shiki.ts','utf8'); if (!s.includes('createHighlighter')) {console.error('shiki.ts must import createHighlighter');process.exit(1)} const noComments=s.split('\\n').filter(l=>!l.trim().startsWith('//') &amp;&amp; !l.trim().startsWith('*')).join('\\n'); if ((noComments.match(/createHighlighter/g) || []).length !== 1) {console.error('shiki.ts must call createHighlighter EXACTLY ONCE (Pitfall 11) — comment occurrences excluded');process.exit(1)} if (!s.includes('github-light')) {console.error('shiki.ts must preload github-light theme per UI-SPEC §1');process.exit(1)} for (const lang of ['python','javascript','typescript','tsx','json','bash','yaml','markdown','sql']) if (!s.includes(\"'\"+lang+\"'\") &amp;&amp; !s.includes('\"'+lang+'\"')) {console.error('shiki.ts missing langs preload: '+lang);process.exit(1)} console.log('shiki.ts singleton OK')" &amp;&amp; node -e "const c=require('fs').readFileSync('apps/web/components/StreamingCodeBlock.tsx','utf8'); if (!c.includes('use client')) {console.error('StreamingCodeBlock needs use client');process.exit(1)} if (!c.includes('isStreamingComplete')) {console.error('StreamingCodeBlock must accept isStreamingComplete prop');process.exit(1)} const danger=(c.match(/dangerouslySetInnerHTML/g) || []).length; if (danger > 1) {console.error('dangerouslySetInnerHTML appears more than once — Pattern 5b only uses it on shiki OUTPUT');process.exit(1)} console.log('StreamingCodeBlock contract OK')" &amp;&amp; cd apps/web &amp;&amp; pnpm exec tsc --noEmit --strict lib/shiki.ts components/StreamingCodeBlock.tsx 2>&amp;1 | grep -v 'node_modules' | (grep -i 'error' &amp;&amp; exit 1 || echo 'tsc strict OK')</automated>
  </verify>
  <done>shiki.ts exports getHighlighter+highlightCode with EXACTLY ONE module-scope createHighlighter call (comment occurrences excluded from the count to allow header docstrings) across 9 preloaded langs + github-light theme; StreamingCodeBlock either thin-wraps the primitive (Pattern 5) or implements the isStreamingComplete-gated highlight (Pattern 5b) per MARKDOWN-DECISION verdict; dangerouslySetInnerHTML appears AT MOST ONCE and ONLY on shiki output (never on raw source); RTL tests cover fence-state transitions + single-highlight invariant + unknown-language fallback; tsc strict clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author MarkdownRenderer (React.memo block-memoized) + markdown-components.tsx (UI-SPEC §8.2 element styles + inline-vs-fenced code distinction) + unit test + modify ChatBubble + page.tsx</name>
  <files>apps/web/components/MarkdownRenderer.tsx, apps/web/lib/markdown-components.tsx, apps/web/components/ChatBubble.tsx, apps/web/app/page.tsx, apps/web/tests/markdown-renderer.test.tsx</files>
  <read_first>
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md (§"Pattern 4" lines 733-776 — block-memoized markdown body skeleton; Pitfall 4 — markdown re-render storm; Pitfall 7 not relevant here; Open Question 5 not relevant; §"Don't Hand-Roll" row "Markdown parsing" — use @assistant-ui/react-markdown; row "Schema validation for incoming SSE events" — not this task),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md (D-10 — @assistant-ui/react-markdown is the canonical wrapper; D-11 wired by Task 1; deferred — streamdown rejected),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-UI-SPEC.md (§3 Typography — h1/h2/h3 inside markdown sized text-xl/text-lg/text-base font-semibold; §4 inline `code` background bg-slate-100; §8.2 element styles table — p/ul/ol/li/h1/h2/h3/a/code/pre/blockquote canonical Tailwind classes; §14.5 screen reader live regions),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md ("apps/web/lib/markdown-components.tsx" in "No Analog Found" — RESEARCH Pattern 4 + assistant-ui-react-markdown component-map convention; Pattern D — "use client"),
    apps/web/lib/MARKDOWN-DECISION.md (whether to use MarkdownTextPrimitive or a raw react-markdown wrapper; the executor follows whichever path the spike validated),
    apps/web/components/ChatBubble.tsx (Plan 05's output — the assistant variant's `children` slot is the seam; Plan 06 modifies the props interface + the assistant variant body — NO TODO text to find, the modification is direct),
    apps/web/app/page.tsx (Plan 05's output — AssistantMessage variant; Plan 06 modifies it to compute isStreamingComplete and pass messageId + isStreamingComplete to ChatBubble),
    apps/web/components/StreamingCodeBlock.tsx (Task 1 — register as the fenced-pre renderer in markdown-components.tsx),
    apps/web/lib/cn.ts (shadcn cn helper),
    node_modules/react-markdown/package.json (executor reads the major version at install time to determine the inline-vs-fenced API: v9 uses `inline` prop; v10+ may use a different signal — adapt accordingly)
  </read_first>
  <behavior>
    - MarkdownRenderer with the same (id, contentLength, role) tuple across two renders does NOT re-parse — proved by a render-count probe component placed inside the memo body (the probe's effect fires ONCE for the initial render and zero additional times)
    - MarkdownRenderer with a changed contentLength (token appended) on the same id DOES re-render (the memo equality predicate returns false)
    - MarkdownRenderer with the same contentLength but a different id DOES re-render (different message)
    - MarkdownRenderer with `# Heading\n\nparagraph text` renders an `<h1>` with text-xl font-semibold + a `<p>` with text-sm leading-relaxed text-slate-900 mb-3
    - MarkdownRenderer with an open fenced code block (rawMarkdown ends with "```python\nprint('hi')" — no closing fence yet) renders the StreamingCodeBlock with isStreamingComplete=false
    - MarkdownRenderer with a closed fenced code block AND isStreamingComplete=true renders the StreamingCodeBlock with isStreamingComplete=true (one-shot highlight)
    - MarkdownRenderer with a `<script>alert(1)</script>` payload renders the literal text (rehype-sanitize / assistant-ui-hardening strips the script tag) — XSS regression
    - MarkdownRenderer with a GFM table (`| a | b |\n|---|---|\n| 1 | 2 |`) renders a `<table>` element when remark-gfm is enabled
    - MarkdownRenderer with an inline `` `code` `` span renders `<code class="text-sm font-mono bg-slate-100 px-1 py-1 rounded-sm">` (Warning 1: inline-detection works)
    - MarkdownRenderer with a fenced ```` ```python ```` block renders the StreamingCodeBlock with language="python" — the language extraction does NOT break (Warning 1: code mapper does NOT replace fenced inner code)
    - MarkdownRenderer with a link renders `<a class="text-slate-900 underline underline-offset-2">` — auto-escaped by React
  </behavior>
  <action>Author apps/web/lib/markdown-components.tsx with `"use client";` header. Export a `MarkdownComponents` factory mapping HTML element names to React components per UI-SPEC §8.2:
  - `p: ({children}) => <p className="text-sm leading-relaxed text-slate-900 mb-3 last:mb-0">{children}</p>`
  - `ul: ({children}) => <ul className="text-sm leading-relaxed text-slate-900 mb-3 ml-5 list-disc">{children}</ul>`
  - `ol: ({children}) => <ol className="text-sm leading-relaxed text-slate-900 mb-3 ml-5 list-decimal">{children}</ol>`
  - `li: ({children}) => <li className="mb-1">{children}</li>`
  - `h1: ({children}) => <h1 className="text-xl font-semibold text-slate-900 mb-2 mt-4 first:mt-0">{children}</h1>`
  - `h2: ({children}) => <h2 className="text-lg font-semibold text-slate-900 mb-2 mt-4 first:mt-0">{children}</h2>`
  - `h3: ({children}) => <h3 className="text-base font-semibold text-slate-900 mb-2 mt-4 first:mt-0">{children}</h3>`
  - `a: ({href, children}) => <a href={href} target="_blank" rel="noopener noreferrer" className="text-slate-900 underline underline-offset-2 hover:text-slate-700">{children}</a>`
  - `code: ({inline, className, children, ...rest}) => inline ? <code className="text-sm font-mono bg-slate-100 px-1 py-1 rounded-sm">{children}</code> : <code className={className} {...rest}>{children}</code>` — CRITICAL Warning 1 fix: inline code gets the UI-SPEC §8.2 inline style; fenced inner code passes through UNCHANGED so the parent `pre` mapper can read `className="language-xyz"` and extract the language. If the installed react-markdown major (`pnpm --dir apps/web list react-markdown`) is v10+ AND the `inline` prop is no longer passed, the executor adapts: use the `node.tagName === "code" && node.parent?.tagName !== "pre"` heuristic via the `node` prop, OR fall back to checking `className?.startsWith("language-")` (fenced inner code carries this; inline code does not). The verify gate enforces both branches of the conditional exist.
  - `pre: ({children, ...rest}) => { /* react-markdown renders fenced code as <pre><code class="language-foo">RAW</code></pre>; we extract the lang from the inner <code>'s className and the raw text from its children, then mount StreamingCodeBlock */ const inner = (children as any)?.props; const lang = (inner?.className ?? "").replace("language-", "") || "text"; const code = String(inner?.children ?? ""); return <StreamingCodeBlock language={lang} isStreamingComplete={isStreamingComplete}>{code}</StreamingCodeBlock>; }` — the `pre` mapper needs `isStreamingComplete` in its closure; the MarkdownComponents factory is a function that takes `isStreamingComplete` as a closure arg.
  - `blockquote: ({children}) => <blockquote className="border-l-4 border-slate-300 pl-4 italic text-slate-700">{children}</blockquote>`
  Export the map as a FACTORY: `export function getMarkdownComponents(isStreamingComplete: boolean) { return { p: ..., ul: ..., ol: ..., li: ..., h1: ..., h2: ..., h3: ..., a: ..., code: ..., pre: (...) => ..., blockquote: ... }; }`. Import StreamingCodeBlock from `@/components/StreamingCodeBlock`.

Author apps/web/components/MarkdownRenderer.tsx with `"use client";` header. Per MARKDOWN-DECISION verdict: IF Pattern 5 use `import {MarkdownTextPrimitive} from "@assistant-ui/react-markdown";` (or whatever Plan 01's spike recorded); IF Pattern 5b use `import ReactMarkdown from "react-markdown"; import remarkGfm from "remark-gfm"; import rehypeSanitize from "rehype-sanitize";` (and add these to package.json: `pnpm --dir apps/web add react-markdown remark-gfm rehype-sanitize` — note: if @assistant-ui/react-markdown wraps these, the direct deps may not be needed; the executor verifies). Props: `interface Props { rawMarkdown: string; isStreamingComplete: boolean; messageId: string }`. Body for the Pattern 5 path: `const components = getMarkdownComponents(isStreamingComplete); return <MarkdownTextPrimitive components={components}>{rawMarkdown}</MarkdownTextPrimitive>;`. Body for the Pattern 5b path: `const components = getMarkdownComponents(isStreamingComplete); return <ReactMarkdown remarkPlugins={[remarkGfm]} rehypePlugins={[rehypeSanitize]} components={components}>{rawMarkdown}</ReactMarkdown>;`. Wrap the export in React.memo with a CUSTOM equality predicate per RESEARCH Pattern 4: `function arePropsEqual(prev: Props, next: Props): boolean { return prev.messageId === next.messageId && prev.rawMarkdown.length === next.rawMarkdown.length && prev.isStreamingComplete === next.isStreamingComplete; }`. `export const MarkdownRenderer = memo(MarkdownRendererBase, arePropsEqual); MarkdownRenderer.displayName = "MarkdownRenderer";`. CRITICAL note for the executor: the RESEARCH §Pattern 4 skeleton also describes BLOCK-LEVEL memoization (splitting markdown by `\n\n` and memoizing each block). The simpler whole-message memo with the (id, length) predicate is acceptable for Phase 4 — block-level memo is a Phase 5+ optimization if profiling shows long-message render storms. Document the decision in the SUMMARY.

Modify apps/web/components/ChatBubble.tsx (Plan 05's output — post revision; the file contains NO TODO marker text). Update the props interface to add `isStreamingComplete: boolean` and `messageId: string`: `interface ChatBubbleProps { role: "assistant" | "user"; rawMarkdown: string; isStreamingComplete: boolean; messageId: string; onRegenerate?: () => void; children: React.ReactNode; }`. Import `import {MarkdownRenderer} from "./MarkdownRenderer";`. In the assistant variant, REPLACE the `{children}` rendering site with `<MarkdownRenderer rawMarkdown={rawMarkdown} isStreamingComplete={isStreamingComplete} messageId={messageId} />`. The `children` prop REMAINS in the interface (forward-compat for Phase 5's CodeBubble) but the default rendering path uses MarkdownRenderer. The user variant continues to ignore isStreamingComplete + messageId (those props are unused but accepted — the interface is a contract for the assistant path).

Modify apps/web/app/page.tsx (Plan 05's output — AssistantMessage variant). For the AssistantMessage path: compute `const isStreamingComplete = message.parts?.some((p: any) => p.type === "data-metrics") ?? false;` (the translator emits data-metrics ONLY on Done — Plan 02 contract). Pass `isStreamingComplete={isStreamingComplete}` AND `messageId={message.id}` to ChatBubble. Pass `rawMarkdown={/* the assistant message's source markdown — extract from message.content or whatever assistant-ui exposes; the executor reads Plan 05 SUMMARY to confirm */}` — the executor confirms the actual prop accessor at install time. If Plan 05's SUMMARY documents that the assistant-ui runtime exposes `message.status?.type === 'complete'` as the cleaner signal, use that instead; both produce the same boolean for Phase 4.

Author apps/web/tests/markdown-renderer.test.tsx overwriting any prior stub. Mock `@/components/StreamingCodeBlock` with a vi.fn-backed stub so the test can spy on prop-passing. Render `<MarkdownRenderer rawMarkdown="# hi\n\npara" isStreamingComplete={false} messageId="m1" />` then assert: an h1 with class containing "text-xl" + "font-semibold" exists with text "hi"; a p with class "text-sm" + "leading-relaxed" + "mb-3" exists with text "para". Rerender with the SAME props — assert MarkdownRenderer's inner subtree was NOT remounted (use a render-count probe child OR `screen.getByText("hi")` returns the same DOM node via `===`). Rerender with rawMarkdown="# hi\n\npara more" (longer content) — assert the rerender happened (memo predicate returned false). Rerender with messageId="m2" — assert rerender happened. Render with rawMarkdown=`"\`inline code\` and text"` + isStreamingComplete=true — assert an inline `<code>` exists with class containing "bg-slate-100" (Warning 1: inline-vs-fenced distinction works). Render with rawMarkdown=`"\`\`\`python\nprint('hi')"` (open fence) + isStreamingComplete=false — assert the mocked StreamingCodeBlock was called with language="python" AND isStreamingComplete=false (Warning 1: fenced code language extraction NOT broken by the code mapper). Render with the same rawMarkdown + ```\``` closing + isStreamingComplete=true — assert StreamingCodeBlock was called with isStreamingComplete=true. Render with rawMarkdown="<script>alert(1)</script>" + isStreamingComplete=true — assert the rendered DOM contains the literal text "alert(1)" but NO actual `<script>` element (sanitization works). Run `pnpm --dir apps/web test markdown-renderer.test.tsx` and assert exit 0 with at least 8 tests passing.</action>
  <verify>
    <automated>pnpm --dir apps/web test markdown-renderer.test.tsx 2>&amp;1 | grep -E '(Tests|passing|failed|✓)' &amp;&amp; node -e "const r=require('fs').readFileSync('apps/web/components/MarkdownRenderer.tsx','utf8'); if (!r.includes('use client')) {console.error('MarkdownRenderer needs use client');process.exit(1)} if (!r.includes('memo(')) {console.error('MarkdownRenderer must use React.memo');process.exit(1)} if (!r.includes('messageId') || !r.includes('rawMarkdown.length')) {console.error('memo predicate must compare messageId and rawMarkdown.length');process.exit(1)} console.log('renderer memo OK')" &amp;&amp; node -e "const m=require('fs').readFileSync('apps/web/lib/markdown-components.tsx','utf8'); if (!m.includes('StreamingCodeBlock')) {console.error('markdown-components must mount StreamingCodeBlock for pre');process.exit(1)} for (const el of ['text-sm leading-relaxed text-slate-900 mb-3','text-xl font-semibold','text-lg font-semibold','text-base font-semibold','underline underline-offset-2','bg-slate-100 px-1 py-1 rounded-sm','border-l-4 border-slate-300']) if (!m.includes(el)) {console.error('markdown-components missing UI-SPEC §8.2 class: '+el);process.exit(1)} if (!m.includes('inline')) {console.error('code mapper must check inline prop (Warning 1) — inline-vs-fenced distinction required');process.exit(1)} if (!m.match(/inline\\s*\\?/)) {console.error('code mapper must use ternary on inline prop (inline ? styled : passthrough) — Warning 1 fix');process.exit(1)} console.log('markdown-components class map + inline-vs-fenced OK')" &amp;&amp; node -e "const b=require('fs').readFileSync('apps/web/components/ChatBubble.tsx','utf8'); if (!b.includes('MarkdownRenderer')) {console.error('ChatBubble must import + mount MarkdownRenderer (Blocker 4 positive grep)');process.exit(1)} if (!b.includes('isStreamingComplete')) {console.error('ChatBubble props must include isStreamingComplete (Blocker 4 prop wiring)');process.exit(1)} if (!b.includes('messageId')) {console.error('ChatBubble props must include messageId (Blocker 4 prop wiring)');process.exit(1)} console.log('bubble wired (positive grep)')" &amp;&amp; node -e "const p=require('fs').readFileSync('apps/web/app/page.tsx','utf8'); if (!p.includes('isStreamingComplete')) {console.error('page.tsx must compute isStreamingComplete per assistant message and pass to ChatBubble (Blocker 4 page wiring)');process.exit(1)} if (!p.includes('messageId={message.id}') &amp;&amp; !p.includes('messageId: message.id')) {console.error('page.tsx must pass messageId={message.id} to ChatBubble (Blocker 4 page wiring)');process.exit(1)} console.log('page.tsx wired')" &amp;&amp; cd apps/web &amp;&amp; pnpm exec tsc --noEmit --strict components/MarkdownRenderer.tsx lib/markdown-components.tsx components/ChatBubble.tsx app/page.tsx 2>&amp;1 | grep -v 'node_modules' | (grep -i 'error' &amp;&amp; exit 1 || echo 'tsc strict OK')</automated>
  </verify>
  <done>MarkdownRenderer is React.memo-wrapped with the (messageId, contentLength, isStreamingComplete) equality predicate; markdown-components.tsx maps p/ul/ol/li/h1-h3/a/code/pre/blockquote to the exact UI-SPEC §8.2 classes and registers StreamingCodeBlock for fenced code; the code mapper distinguishes inline vs fenced via the `inline` prop (Warning 1) — inline gets the bg-slate-100 chip style; fenced inner code passes through with className intact; ChatBubble.tsx imports + mounts MarkdownRenderer (positive grep — Blocker 4) AND its props interface gained isStreamingComplete + messageId; page.tsx computes isStreamingComplete and passes it + messageId to ChatBubble; RTL tests cover at least 8 cases (memo predicate happy + sad paths, headings/paragraphs styled, inline code, fenced code language extraction, open/closed fence, XSS sanitization); tsc strict clean.</done>
</task>

<task type="auto">
  <name>Task 3: Author the Playwright no-flicker.spec.ts + extend mock-fastapi.py with the canonical fixture catalog + [fixture:code-block]</name>
  <files>apps/web/playwright/no-flicker.spec.ts, apps/web/playwright/mock-fastapi.py</files>
  <read_first>
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md (§"Pattern 5" lines 788-825 — full MutationObserver assertion shape; Pitfall 5 — naive re-tokenize signs; Assumption A10 — MutationObserver reliability on React-batched updates),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-VALIDATION.md (UI-03 row — `pnpm --dir apps/web test:e2e no-flicker.spec.ts`; Manual-Only Verifications "Visual no flicker sanity" row — the human verification that the Playwright assertion proves NO mid-stream re-highlights),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md ("apps/web/playwright/no-flicker.spec.ts" section — full reference test shape),
    apps/web/playwright/mock-fastapi.py (Plan 01 Wave 0 — the canned-SSE upstream; Plan 06 EXTENDS with `[fixture:code-block]` body-prefix dispatch + the canonical fixture catalog comment at the top),
    apps/web/playwright/playwright.config.ts (Plan 01 Wave 0 — webServer config + USE_MOCK_FASTAPI gating),
    apps/web/components/MarkdownRenderer.tsx (Task 2 — the assertion target; the spec uses `page.locator("pre code")` to find the code element)
  </read_first>
  <action>EXTEND apps/web/playwright/mock-fastapi.py with the canonical fixture catalog + the [fixture:code-block] body-prefix dispatch (Warning 5 fix). At the TOP of mock-fastapi.py (after the existing imports and module docstring), insert a CANONICAL FIXTURE CATALOG comment block:

```python
# ============================================================================
# CANONICAL FIXTURE CATALOG (mock-fastapi.py)
# ----------------------------------------------------------------------------
# Tests select named fixtures via a body-prefix mechanism:
#   body.message.startswith("[fixture:NAME]")  →  dispatch to fixture NAME
# The prefix is stripped before the fixture handler sees the rest of the
# message. Adding new fixtures: extend `_resolve_fixture(body)` below; the
# fixture name MUST be added to this catalog comment so future contributors
# do not invent a second selection mechanism (e.g. query params).
#
# Named fixtures across Plans 01/06/07:
#   default                — Plan 01: routing_decision + 2 text_deltas + done
#   [fixture:code-block]   — Plan 06: routing_decision + streamed fenced
#                            ```python ... ``` + done (used by no-flicker.spec.ts)
#   [fixture:slow]         — Plan 07: routing_decision + 1 text_delta every
#                            500ms for 10s + done (used by cancel-budget.spec.ts)
#   [fixture:missing-key]  — Plan 07: healthz returns missing_key until PATCH
#                            /api/v1/settings; used by first-run.spec.ts
#   [fixture:auth-failed]  — Plan 07: routing_decision + stream_error(auth_failed)
#                            + done; used by error-banner manual UAT
# ============================================================================
```

Add the `_resolve_fixture(body) -> str` helper that returns the fixture name (or "default") by reading `body.message.startswith("[fixture:NAME]")` for each catalog entry. The fixture-dispatcher logic in the `/api/v1/threads/{id}/turn` handler reads `fixture_name = _resolve_fixture(body)` then dispatches: `if fixture_name == "code-block": yield from _emit_code_block_fixture(); elif fixture_name == "slow": ...; else: yield from _emit_default_fixture()`.

Implement `_emit_code_block_fixture()` — emits: `event: routing_decision\ndata: {"backend":"openrouter","model_or_agent":"openai/gpt-5","rationale":"Code-block test fixture","confidence":0.9,"signals":{}}\n\n` (the STRUCTURED 5-key payload per Plan 04 amendment), then a sequence of text_delta events that progressively build a fenced Python code block — chunked deliberately to test the no-flicker invariant:
1. `text_delta {"text": "Here is a Python hello world:\\n\\n"}`
2. `text_delta {"text": "\`\`\`python\\n"}` — open fence (chip data already in chip; assistant-ui's MessagePrimitive.Content streams)
3. `text_delta {"text": "def hello():\\n"}`
4. `text_delta {"text": "    print('Hello, world!')\\n"}`
5. `text_delta {"text": "\\nhello()\\n"}`
6. `text_delta {"text": "\`\`\`\\n"}` — close fence (THIS is when StreamingCodeBlock should highlight EXACTLY ONCE)
7. `done {"type":"done","tokens_in":7,"tokens_out":30,"cost_usd":0.0005,"latency_ms":200,"routing_signals":{}}`

Add a short `asyncio.sleep(0.05)` between text_deltas so the no-flicker spec has enough time to install the MutationObserver before the close fence arrives.

OVERWRITE the Plan 01 `test.skip` stub at apps/web/playwright/no-flicker.spec.ts with the real Playwright spec. Import `import {test, expect} from "@playwright/test";`. Test name: `test("UI-03: code block highlights exactly once on fence close", async ({page}) => {...})`.

Test body:
```typescript
test("UI-03: code block highlights exactly once on fence close", async ({page}) => {
  await page.goto("/");

  // Satisfy first-run modal if it appears (Plan 07 may add it; tolerate either path)
  const modalHeading = page.getByText("Connect OpenRouter to get started");
  if (await modalHeading.isVisible({timeout: 2000}).catch(() => false)) {
    await page.getByLabel("OpenRouter API key").fill("sk-or-v1-" + "N".repeat(48));
    await page.getByRole("button", {name: "Save & continue"}).click();
    await expect(modalHeading).toBeHidden({timeout: 5000});
  }

  const composer = page.locator('textarea[placeholder="Type a message…"]');
  await composer.waitFor({state: "visible", timeout: 10_000});
  await composer.fill("[fixture:code-block] please show a hello world");
  await page.keyboard.press("Enter");

  // Wait for the <code> element to appear (the open fence)
  const codeEl = page.locator("pre code").first();
  await codeEl.waitFor({state: "attached", timeout: 10_000});

  // Install MutationObserver in the page context — count childList mutations on the <code>
  await page.evaluate(() => {
    (window as any).__codeMutations = 0;
    const observer = new MutationObserver(() => { (window as any).__codeMutations += 1; });
    const target = document.querySelector("pre code");
    if (target) observer.observe(target, {childList: true, subtree: true, characterData: true});
    (window as any).__observer = observer;
  });

  // Wait for the stream to fully complete — the metrics footer's final aria-label signals Done
  await page.waitForSelector('[aria-label*="Turn cost"]', {timeout: 30_000});

  // Capture the mutation count immediately after Done arrives
  const mutationsAtDone = await page.evaluate(() => (window as any).__codeMutations as number);

  // Wait 500ms past Done — no further mutations should fire (no late re-highlight)
  await page.waitForTimeout(500);
  const mutationsAfterDone = await page.evaluate(() => (window as any).__codeMutations as number);

  // KEY ASSERTION: mutation count is stable after Done — NO late re-highlight
  expect(mutationsAfterDone).toBe(mutationsAtDone);

  // Secondary assertion: shiki tokens are present (the highlight DID fire once)
  const innerHtmlAfter = await codeEl.evaluate((el) => el.innerHTML);
  expect(innerHtmlAfter).toMatch(/<span[^>]+style=|class=/); // shiki emits styled spans

  // Cleanup
  await page.evaluate(() => { (window as any).__observer?.disconnect(); });
});
```

Set `USE_MOCK_FASTAPI=1` in the test environment via the playwright.config.ts webServer entry that Plan 01 wired. Verify the spec passes by running `USE_MOCK_FASTAPI=1 pnpm --dir apps/web test:e2e no-flicker.spec.ts`. Commit the spec + the mock-fastapi.py extension. NOTE: Playwright may need `pnpm exec playwright install chromium` first if Plan 01 didn't run it — re-run is idempotent.</action>
  <verify>
    <automated>USE_MOCK_FASTAPI=1 pnpm --dir apps/web test:e2e no-flicker.spec.ts 2>&amp;1 | tail -20 | (grep -E '(passed|FAIL)' || echo 'check stdout above') &amp;&amp; node -e "const s=require('fs').readFileSync('apps/web/playwright/no-flicker.spec.ts','utf8'); if (!s.includes('MutationObserver')) {console.error('no-flicker.spec.ts must use MutationObserver per RESEARCH Pattern 5');process.exit(1)} if (!s.includes('Turn cost')) {console.error('spec must wait on aria-label*=Turn cost to detect Done');process.exit(1)} if (!s.includes('expect(mutationsAfterDone).toBe(mutationsAtDone)')) {console.error('spec must assert no late re-highlight (mutation count stable after Done)');process.exit(1)} if (s.includes('test.skip(') || s.includes('test.todo(')) {console.error('skip/todo stub must be removed');process.exit(1)} if (!s.includes('[fixture:code-block]')) {console.error('spec must select the code-block fixture via body-prefix mechanism (Warning 5)');process.exit(1)} console.log('no-flicker spec contract OK')" &amp;&amp; node -e "const f=require('fs').readFileSync('apps/web/playwright/mock-fastapi.py','utf8'); if (!f.includes('CANONICAL FIXTURE CATALOG')) {console.error('mock-fastapi.py must include the canonical fixture catalog comment at the top (Warning 5)');process.exit(1)} if (!f.includes('[fixture:code-block]')) {console.error('mock-fastapi.py must support [fixture:code-block] body-prefix dispatch');process.exit(1)} if (!f.includes('\`\`\`python') &amp;&amp; !f.includes('\\\`\\\`\\\`python')) {console.error('mock-fastapi.py must emit a fenced python code block in the code-block fixture');process.exit(1)} if (!f.includes('_resolve_fixture') &amp;&amp; !f.includes('startswith(\"[fixture:')) {console.error('mock-fastapi.py must use the body-prefix mechanism (body.message.startswith(\"[fixture:NAME]\")) — NOT query params (Warning 5 lock-in)');process.exit(1)} console.log('mock-fastapi fixture catalog + code-block OK')"</automated>
  </verify>
  <done>apps/web/playwright/no-flicker.spec.ts contains a real Playwright test using MutationObserver per RESEARCH Pattern 5; the test selects the code-block fixture via the body-prefix mechanism `[fixture:code-block]` (Warning 5); the test waits for the metrics-footer Done aria-label then asserts mutation count is stable; the secondary assertion confirms shiki spans are present (highlight DID fire); mock-fastapi.py includes the canonical fixture catalog comment at the top AND supports the [fixture:code-block] body-prefix dispatch emitting a fenced python code block; `USE_MOCK_FASTAPI=1 pnpm test:e2e no-flicker.spec.ts` exits 0 with the test passing; VALIDATION.md UI-03 row's automated command is now backed by a real test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-streamed markdown → MarkdownRenderer | Untrusted text content; potential XSS via `<script>`, `<iframe>`, `<img onerror>`, javascript: URLs |
| LLM-streamed code fence → StreamingCodeBlock | Raw code text — never executed; rendered as text content until isStreamingComplete then through shiki tokenizer |
| shiki tokenized HTML output → dangerouslySetInnerHTML | Trusted because shiki controls every byte (closed vocabulary of spans + classes + inline style); input was already escaped before shiki saw it |
| HMR-replaced module → singleton highlighter | Module-scope state survives fast-refresh; the singleton never re-initializes — Pitfall 11 acceptance |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-35 | Tampering / Information Disclosure | XSS via LLM-emitted `<script>` tag inside markdown body (V11) | mitigate | MarkdownRenderer uses rehype-sanitize (or @assistant-ui/react-markdown's built-in hardening) to strip script/iframe/object/embed tags; the unit test renders a `<script>alert(1)</script>` payload and asserts no `<script>` element is in the DOM |
| T-04-36 | Tampering | XSS via shiki tokenized HTML output | accept | shiki emits a closed vocabulary of `<pre><code><span class="...">text</span></code></pre>` with no event handlers, no javascript: URLs, no embedded HTML from source — verified by shiki's own test suite and upstream security audits; the dangerouslySetInnerHTML on shiki OUTPUT is the documented safe pattern (RESEARCH Pattern 5b) |
| T-04-37 | Tampering | XSS via raw fenced code rendered into `<code>` without shiki | mitigate | The pre-shiki render uses React JSX text-content (`{children}`) — React auto-escapes; NEVER dangerouslySetInnerHTML on the raw source. The unit test asserts the open-fence path renders text-content, not innerHTML (V11) |
| T-04-38 | Denial of Service | Markdown re-render storm on long streams pegs CPU | mitigate | MarkdownRenderer wrapped in React.memo with `(messageId, contentLength, isStreamingComplete)` equality (RESEARCH Pattern 4 + Pitfall 4); Plan 05 already set experimental_throttle: 50 on the runtime (RESEARCH Pattern 4) |
| T-04-39 | Denial of Service | shiki re-tokenizes on every text-delta — burns 100+ms/token | mitigate | StreamingCodeBlock defers highlight until isStreamingComplete=true (RESEARCH Pattern 5b); Playwright no-flicker.spec.ts asserts mutation count is stable after Done (Pitfall 5) |
| T-04-40 | Denial of Service | createHighlighter() called per render exhausts memory | mitigate | shiki.ts module-scope singleton — exactly ONE call across the process lifetime; grep gate in Task 1 verify enforces (comment occurrences excluded for header docstrings) (Pitfall 11) |
| T-04-41 | Tampering | LLM-emitted `<a href="javascript:..">` link | mitigate | rehype-sanitize default URL allowlist strips javascript:/data:/vbscript: URLs; the markdown-components `a` mapping explicitly sets `rel="noopener noreferrer"` and `target="_blank"` on every external link |
| T-04-42 | Information Disclosure | `target="_blank"` without `rel="noopener"` enables tabnabbing on external links | mitigate | The markdown-components `a` mapping unconditionally sets `rel="noopener noreferrer"` (V14 hardening) |
| T-04-43 | Spoofing | Mock-fastapi.py emits a code-fence payload that includes a fake-looking "highlight DID fire" span — bypasses no-flicker spec | accept | mock-fastapi is dev/CI-only; the spec also checks the mutation count post-Done, not just span presence; secondary assertion `expect(innerHtmlAfter).toMatch(/<span[^>]+style=|class=/)` ensures shiki actually ran (not a hand-crafted span from the upstream) |
| T-04-44 | Tampering | Code mapper incorrectly distinguishes inline vs fenced, breaking either inline styling OR language extraction | mitigate | Warning 1 fix: the code mapper checks the react-markdown `inline` prop; the Vitest test renders BOTH inline `` `code` `` AND fenced ```` ```python ```` and asserts inline gets bg-slate-100 chip AND fenced gets StreamingCodeBlock with language="python" |
| T-04-44b | Tampering | mock-fastapi.py fixture selection drifts to query params over time, creating two parallel mechanisms | mitigate | Warning 5 fix: the canonical fixture catalog comment at the top of mock-fastapi.py LOCKS the body-prefix mechanism; future contributors see "do not invent a second selection mechanism" and follow the convention |
</threat_model>

<verification>
- `pnpm --dir apps/web test streaming-code-block.test.tsx markdown-renderer.test.tsx` exits 0 with at least 12 tests passing
- `USE_MOCK_FASTAPI=1 pnpm --dir apps/web test:e2e no-flicker.spec.ts` exits 0 with the test passing
- `pnpm --dir apps/web run build` exits 0
- `grep -c createHighlighter apps/web/lib/shiki.ts` returns exactly 1 (Pitfall 11 — comment occurrences excluded by the verify-script line filter)
- `grep dangerouslySetInnerHTML apps/web/components/StreamingCodeBlock.tsx` returns at most 1 match — and only inside the `isStreamingComplete && html` branch
- `grep MarkdownRenderer apps/web/components/ChatBubble.tsx` returns at least 1 (positive grep — Blocker 4 enforcement; no TODO marker hunt)
- `grep isStreamingComplete apps/web/components/ChatBubble.tsx` returns at least 1 (props interface grew)
- `grep isStreamingComplete apps/web/app/page.tsx` returns at least 1 (AssistantMessage computes it)
- markdown-components.tsx `code` mapper uses the `inline` prop ternary (Warning 1)
- mock-fastapi.py has the CANONICAL FIXTURE CATALOG comment AND supports [fixture:code-block] body-prefix (Warning 5)
- `tsc --strict` clean on every new + modified file
- VALIDATION.md row UI-03 is now backed by a real Playwright test (no longer a skip stub)
</verification>

<success_criteria>
- Wave 5 complete: assistant messages render streamed markdown with one-shot syntax-highlighted code blocks
- ROADMAP SC #2 ("Code blocks render as plain <pre> while the fence is still open, then receive syntax highlighting on close — verified by a manual no-flicker check AND a Playwright assertion that no re-highlight runs mid-stream") is observable in the running app AND enforced by the Playwright test
- UI-03 (Streaming markdown rendering with code-block syntax highlighting that is streaming-safe, highlight applied on closing fence, no per-token re-highlight flicker) satisfied
- Blocker 4 resolved: ChatBubble cleanly mounts MarkdownRenderer with the required props (isStreamingComplete + messageId) — no TODO-marker hand-off
- Warning 1 resolved: code mapper distinguishes inline vs fenced; both render paths work
- Warning 5 resolved: mock-fastapi.py fixture selection is LOCKED on body-prefix mechanism with a canonical catalog comment
- Plan 07 (final wave) can now run end-to-end Playwright specs against the full visible application — Wave 5 is the last code-producing wave before E2E lockdown
- VALIDATION.md row "UI-03" flips to ✅; the Manual-Only "Visual no flicker sanity" row remains as a Phase-4 UAT gate (developer verifies once with real OpenRouter)
</success_criteria>

<output>
After completion, create `.planning/phases/04-minimal-chat-ui-openrouter-backend/04-06-SUMMARY.md` recording:
- Which path was taken (Pattern 5 built-in primitive vs Pattern 5b custom StreamingCodeBlock) per MARKDOWN-DECISION.md
- The exact @assistant-ui/react-markdown export used (MarkdownTextPrimitive vs MarkdownText vs other) if Pattern 5
- The deps added to apps/web/package.json if Pattern 5b required react-markdown + remark-gfm + rehype-sanitize as direct deps
- The react-markdown major version installed and whether the `inline` prop is available (Warning 1 verification)
- The shiki preloaded langs list (the 9 from this plan, or extended if the executor saw evidence of broader needs)
- The MutationObserver assertion behavior — the actual mutationsAtDone vs mutationsAfterDone numbers observed in the CI run (informational)
- Confirmation that ChatBubble cleanly mounts MarkdownRenderer (positive grep) AND that page.tsx wires isStreamingComplete + messageId (Blocker 4 resolution)
- Confirmation that mock-fastapi.py canonical fixture catalog is at the top + body-prefix mechanism dispatches [fixture:code-block] (Warning 5 resolution)
- Note for Plan 07: the no-flicker.spec.ts test now exists AND the body-prefix mechanism is the LOCKED contract — Plan 07's Task 2 extends mock-fastapi.py with [fixture:slow] / [fixture:missing-key] / [fixture:auth-failed] following the same convention
</output>
</content>
