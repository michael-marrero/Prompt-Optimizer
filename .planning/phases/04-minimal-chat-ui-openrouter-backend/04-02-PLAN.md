---
phase: 04
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - apps/web/lib/types.ts
  - apps/web/lib/chunk-schemas.ts
  - apps/web/lib/sse-translate.ts
  - apps/web/lib/api-client.ts
  - apps/web/lib/thread-id.ts
  - apps/web/tests/sse-translate.test.ts
  - apps/web/tests/chunk-schemas.test.ts
  - apps/web/tests/fixtures/sse-events.ts
  - apps/web/tests/api-client.test.ts
  - apps/web/tests/thread-id.test.ts
autonomous: true
requirements:
  - UI-17
user_setup: []

must_haves:
  truths:
    - "translateNamedSSEToUIMessageStream(upstream: ReadableStream<Uint8Array>): ReadableStream<Uint8Array> is a pure function — no I/O, no module-level state, no global mutation; unit-testable with synthetic ReadableStreams"
    - "NamedSSEEventSchema (Zod) is a discriminated union over 'event' with exactly 8 variants: routing_decision, text_delta, tool_call, tool_result, file_diff, screenshot, stream_error, done — byte-for-byte matching the union in apps/api/backends/chunks.py + the new D-15 event"
    - "The routing_decision variant's `data` schema is the STRUCTURED 5-key object `{backend: Backend enum, model_or_agent: string, rationale: string, confidence: number, signals: Record<string, unknown>}` — NOT z.record(z.unknown()) — so any upstream drift (e.g. missing model_or_agent) fails Zod parse and surfaces as a translator error chunk; this matches the Plan 04 amendment payload shape"
    - "StreamError.code Zod enum lists exactly the 9 codes from apps/api/backends/chunks.py lines 127-137: cost_cap_exceeded, step_cap_exceeded, cancelled, rate_limited, auth_failed, provider_unavailable, timeout, validation_error, internal_error"
    - "Translator's output emits AI SDK v6 UI Message Stream chunks per RESEARCH §Pattern 2 mapping table — routing_decision → start + data-routing (the data-routing chunk's `data` is the parsed structured object; downstream consumers receive {backend, model_or_agent, rationale, confidence, signals}); first text_delta → text-start + text-delta; subsequent text_delta → text-delta; stream_error(cancelled) → text-end + abort; stream_error(other) → text-end + error; done → text-end + data-metrics + finish + [DONE]"
    - "Parse failures inside the translator emit {type:'error', errorText:...} and CONTINUE — the translator never throws uncaught (V13 + V5 ASVS; Pattern F)"
    - "RoutingDecision TS interface (apps/web/lib/types.ts) mirrors src/routing/schema.py field-for-field: backend (Backend union), model_or_agent (string), rationale (string), confidence (number), signals (Record<string, unknown>)"
    - "Backend TS union exactly matches src/routing/schema.py: 'openrouter' | 'claude_code' | 'computer_use'"
    - "Wave 1 unit tests use FIXTURE event strings sampled from the real Phase 3 SSE wire shape (event: <type>\\ndata: <pydantic-JSON>\\n\\n) — captured in apps/web/tests/fixtures/sse-events.ts; the routing_decision fixture uses the structured 5-key payload Plan 04 emits, NOT the bare signals dict"
    - "thread-id.ts persists the default thread ID in localStorage under the documented key 'prompt-optimizer.defaultThreadId' (CONTEXT Claude's Discretion + PATTERNS); SSR-safe: never accesses window outside useEffect / function bodies"
    - "api-client.ts wraps /api/chat, /api/settings, /api/health, /api/threads, /api/threads/[id] with typed functions; every fetch goes to a SAME-ORIGIN path (no FASTAPI_URL leak, no NEXT_PUBLIC_* env var)"
    - "Wave 1 ships unit tests for api-client.ts (postSettings error scrubbing) and thread-id.ts (SSR guard + localStorage caching) — minimum 3 fast Vitest tests across these two files"
  artifacts:
    - path: "apps/web/lib/types.ts"
      provides: "RoutingDecision + Backend + RoutingSignals + AdapterStatus + MetricsPayload TS types"
      contains: "export type Backend"
    - path: "apps/web/lib/chunk-schemas.ts"
      provides: "Zod schemas for the 8-variant NamedSSEEvent discriminated union + each individual variant export; routing_decision uses the STRUCTURED 5-key shape"
      contains: "z.discriminatedUnion"
    - path: "apps/web/lib/sse-translate.ts"
      provides: "translateNamedSSEToUIMessageStream pure function (RESEARCH §Pattern 2 reference implementation)"
      exports: ["translateNamedSSEToUIMessageStream"]
    - path: "apps/web/lib/api-client.ts"
      provides: "Typed wrappers: postChat, postSettings, getHealth, postThread, getThread — every URL is /api/* (same-origin)"
      contains: "export async function"
    - path: "apps/web/lib/thread-id.ts"
      provides: "getOrCreateDefaultThread + readDefaultThreadId helpers — localStorage-backed"
      contains: "prompt-optimizer.defaultThreadId"
    - path: "apps/web/tests/fixtures/sse-events.ts"
      provides: "Fixture event strings sampled from the real Phase 3 wire — one per ChatChunk variant; routing_decision fixture matches Plan 04's structured 5-key payload"
      contains: "export const"
    - path: "apps/web/tests/sse-translate.test.ts"
      provides: "Vitest unit suite — one test per Phase-3 event variant + edge cases (malformed event, cancelled vs error, multi-block buffer split); routing_decision test asserts the structured fields flow into the data-routing chunk's data field"
      contains: "translateNamedSSEToUIMessageStream"
    - path: "apps/web/tests/chunk-schemas.test.ts"
      provides: "Vitest unit suite — every fixture in fixtures/sse-events.ts parses through the Zod schema; unknown event names rejected; routing_decision payload missing model_or_agent is rejected"
      contains: "NamedSSEEventSchema"
    - path: "apps/web/tests/api-client.test.ts"
      provides: "Vitest unit suite — postSettings throws an Error whose .message does NOT contain the literal key string (D-18 belt enforcement at the wrapper layer)"
      contains: "postSettings"
    - path: "apps/web/tests/thread-id.test.ts"
      provides: "Vitest unit suite — SSR guard returns null when window is undefined; getOrCreateDefaultThread writes once and returns cached value on subsequent calls"
      contains: "thread-id"
  key_links:
    - from: "apps/web/lib/sse-translate.ts"
      to: "apps/web/lib/chunk-schemas.ts"
      via: "imports NamedSSEEventSchema and StreamErrorCode"
      pattern: "import.*chunk-schemas"
    - from: "apps/web/lib/chunk-schemas.ts"
      to: "apps/api/backends/chunks.py"
      via: "byte-for-byte port of the Pydantic discriminated union (Pattern A)"
      pattern: "stream_error.*cost_cap_exceeded"
    - from: "apps/web/lib/chunk-schemas.ts"
      to: "Plan 04 amended turn.py payload shape (D-15)"
      via: "routing_decision Zod schema mirrors the structured 5-key record {backend, model_or_agent, rationale, confidence, signals}"
      pattern: "z\\.object\\(\\{.*backend.*model_or_agent.*rationale.*confidence.*signals"
    - from: "apps/web/lib/api-client.ts"
      to: "apps/web/app/api/*/route.ts (Plan 03)"
      via: "same-origin fetch — never the FASTAPI_URL constant"
      pattern: "fetch\\(\"/api/"
    - from: "apps/web/lib/thread-id.ts"
      to: "apps/web/app/api/threads/route.ts (Plan 03)"
      via: "POST /api/threads on first boot"
      pattern: "POST.*api/threads"
---

<notes>
## Revision iteration 1 (Blocker 1, Warning 3)

- **Blocker 1 fix:** The routing_decision Zod schema changed from `data: z.record(z.unknown())` (any record) to a STRUCTURED 5-key object `z.object({backend: BackendEnum, model_or_agent: z.string(), rationale: z.string(), confidence: z.number(), signals: z.record(z.unknown())})`. The fixture `ROUTING_DECISION_EVENT` and the translator now flow the 5 structured fields into the `data-routing` chunk's `data` field; the Plan 05 chip reads `routing.backend`/`routing.model_or_agent`/`routing.rationale`/`routing.confidence` directly. Drift (e.g. upstream omits model_or_agent) fails Zod parse and surfaces as a translator error chunk per Pattern F.
- **Warning 3 fix:** Added Task 3 unit tests (apps/web/tests/api-client.test.ts + apps/web/tests/thread-id.test.ts) — 3 Vitest cases minimum: (a) postSettings error.message does not include the literal key, (b) thread-id SSR guard returns null when window is undefined, (c) getOrCreateDefaultThread caches in localStorage.
</notes>

<objective>
Wave 1 — Wire-format contracts and library code. Author the TypeScript port of the Phase-2 ChatChunk discriminated union (`chunk-schemas.ts`), the pure-function SSE translator (`sse-translate.ts`) per RESEARCH §Pattern 2, the TS interface ports of `src/routing/schema.py` (`types.ts`), the same-origin typed API-client (`api-client.ts`), and the default-thread localStorage helper (`thread-id.ts`). Land the Vitest unit suite alongside — every public export has a test that uses fixtures captured from the real Phase-3 wire.

Purpose: Prove the SSE pipe end-to-end as a unit test BEFORE any UI work. If the translator + Zod contract are wrong, every downstream wave fails. By landing the contracts first, Waves 2-6 implement against a verified shape, not a sketch. The routing_decision schema is STRUCTURED (5 keys) per Plan 04's amendment — the chip can rely on receiving backend/model_or_agent/rationale/confidence directly.

Output: A green `pnpm --dir apps/web test sse-translate.test.ts chunk-schemas.test.ts api-client.test.ts thread-id.test.ts` plus 5 library files committed and importable from `@/lib/*`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-VALIDATION.md
@.planning/phases/04-minimal-chat-ui-openrouter-backend/04-01-SUMMARY.md
@apps/api/backends/chunks.py
@apps/api/routes/turn.py
@src/routing/schema.py

<interfaces>
<!-- The canonical Python contracts the Wave-1 TS files MUST mirror byte-for-byte. -->
<!-- Extracted from apps/api/backends/chunks.py + src/routing/schema.py for executor read-first convenience. -->

From apps/api/backends/chunks.py (the ChatChunk union):

```python
# 7 variants discriminated by `type` Literal:
#   TextDelta       type="text_delta"      text: str
#   ToolCall        type="tool_call"       tool_call_id: str, tool_name: str, arguments: dict
#   ToolResult      type="tool_result"     tool_call_id: str, content: str | dict, is_error: bool
#   FileDiff        type="file_diff"       tool_call_id: str, path: str, diff: str, operation: Literal["create","edit","delete"]
#   Screenshot     type="screenshot"      step: int, image_b64: str | None, image_ref: str | None, image_format: Literal["png","jpeg"]
#   StreamError    type="stream_error"    code: Literal[9 values below], message: str, retriable: bool
#   Done           type="done"            tokens_in/tokens_out/cost_usd/latency_ms: optional numbers; routing_signals: dict | None

# StreamError.code closed vocabulary (lines 127-137):
#   "cost_cap_exceeded","step_cap_exceeded","cancelled","rate_limited",
#   "auth_failed","provider_unavailable","timeout","validation_error","internal_error"
```

From apps/api/routes/turn.py (the SSE wire format yielded by event_stream()):

```
event: <chunk.type>\ndata: <chunk.model_dump_json()>\n\n
```

Plus Phase 4's NEW event (added by Plan 04 — D-15 — STRUCTURED payload per revision iteration 1):

```
event: routing_decision
data: {"backend":"openrouter","model_or_agent":"openai/gpt-5","rationale":"...","confidence":0.9,"signals":{...}}
```

The 5-key payload is sourced from the in-scope `decision: RoutingDecision` in turn.py event_stream(); `payload['signals']` equals `Done.routing_signals` byte-for-byte (D-15 canonical-persistence equality).

From src/routing/schema.py (Phase 1 — the RoutingDecision shape the chip displays):

```python
Backend = Literal["openrouter", "claude_code", "computer_use"]

@dataclass
class RoutingDecision:
    backend: Backend
    model_or_agent: str
    rationale: str
    confidence: float
    signals: dict[str, Any] = field(default_factory=dict)
```

From RESEARCH §Pattern 2 (the AI SDK v6 UI Message Stream emission vocabulary the translator outputs):

```
data: {"type":"start","messageId":"<id>"}\n\n
data: {"type":"text-start","id":"<text-id>"}\n\n
data: {"type":"text-delta","id":"<text-id>","delta":"<text>"}\n\n
data: {"type":"text-end","id":"<text-id>"}\n\n
data: {"type":"data-routing","data":{...structured 5-key record...}}\n\n
data: {"type":"data-metrics","data":{cost_usd,latency_ms,tokens_in,tokens_out}}\n\n
data: {"type":"data-tool","data":<original-chunk>}\n\n        // for tool_call/tool_result/file_diff/screenshot
data: {"type":"error","errorText":"...","code":"...","retriable":bool}\n\n
data: {"type":"abort","reason":"cancelled"}\n\n
data: {"type":"finish"}\n\n
data: [DONE]\n\n
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the closed-vocabulary contracts — types.ts + chunk-schemas.ts + fixture suite + chunk-schemas.test.ts</name>
  <files>apps/web/lib/types.ts, apps/web/lib/chunk-schemas.ts, apps/web/tests/fixtures/sse-events.ts, apps/web/tests/chunk-schemas.test.ts</files>
  <read_first>
    apps/api/backends/chunks.py (full file — every Literal vocabulary on every variant MUST be mirrored; pay special attention to lines 127-137 StreamError.code and the discriminated union at lines 167-178),
    src/routing/schema.py (lines 37-65 — RoutingDecision dataclass shape — the 5-key record the routing_decision Zod schema mirrors),
    apps/api/routes/turn.py (lines 481-484 — the exact SSE wire format the fixtures must mirror; Plan 04 amendment will land the STRUCTURED 5-key routing_decision payload here),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-04-PLAN.md (cross-plan dependency — confirms the routing_decision payload is {backend, model_or_agent, rationale, confidence, signals} per revision iteration 1),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md (Pattern A — closed-vocabulary discriminated union; "apps/web/lib/chunk-schemas.ts" snippet; "apps/web/lib/types.ts" snippet; Pattern F — Pydantic-style validation at the wire boundary),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md (§Pattern 2 lines 510-524 — the canonical NamedSSEEventSchema shape; §"Validation Architecture" — Schema contract row in VALIDATION.md),
    apps/api/tests/test_settings.py (analog: Pydantic body validation tests — same shape Vitest tests will take)
  </read_first>
  <behavior>
    - chunk-schemas: NamedSSEEventSchema.parse({event: "text_delta", data: {type:"text_delta", text:"hi"}}) returns the typed variant
    - chunk-schemas: NamedSSEEventSchema.parse({event: "stream_error", data: {type:"stream_error", code: "cost_cap_exceeded", message:"...", retriable:false}}) returns the typed variant
    - chunk-schemas: NamedSSEEventSchema.parse({event: "unknown_event", data: {}}) throws ZodError
    - chunk-schemas: StreamError.code rejects any string not in the 9-value enum
    - chunk-schemas: Done payload accepts all-optional fields (tokens_in/out/cost_usd/latency_ms/routing_signals can each be null/undefined)
    - chunk-schemas: routing_decision event accepts the STRUCTURED 5-key shape {backend: openrouter|claude_code|computer_use, model_or_agent: string, rationale: string, confidence: number, signals: Record<string, unknown>} — this is the Plan 04 amended payload (NOT the bare signals dict)
    - chunk-schemas: routing_decision event REJECTS a payload missing model_or_agent (catches upstream drift early — Pattern F)
    - chunk-schemas: routing_decision event REJECTS a backend value outside the 3-value enum
    - types.ts: import { RoutingDecision, Backend } from "@/lib/types" — both compile at strict TS
    - fixtures/sse-events.ts: exports one named const per variant, each is the exact bytes of one `event: ...\ndata: ...\n\n` block as a string (suitable for fixtureStream wrapper in Task 2's translator tests)
  </behavior>
  <action>Author apps/web/lib/types.ts exporting: `export type Backend = "openrouter" | "claude_code" | "computer_use";` + `export interface RoutingDecision { backend: Backend; model_or_agent: string; rationale: string; confidence: number; signals: Record<string, unknown>; }` + `export type AdapterStatus = "ready" | "missing_key" | "opt_out" | "error";` (mirrors Phase 3 D-18 healthz vocabulary) + `export interface MetricsPayload { cost_usd: number | null; latency_ms: number | null; tokens_in: number | null; tokens_out: number | null; }`. NO runtime values in types.ts — types only.

Author apps/web/lib/chunk-schemas.ts importing zod (v4). Export `BackendEnum = z.enum(["openrouter", "claude_code", "computer_use"])` so the routing_decision schema can use it. Export `StreamErrorCode = z.enum([...nine values from chunks.py lines 127-137 in the EXACT order...])`; define one `z.object({type: z.literal(...), ...})` schema per chunks.py variant (TextDeltaSchema, ToolCallSchema, ToolResultSchema, FileDiffSchema, ScreenshotSchema, StreamErrorSchema, DoneSchema). Export the STRUCTURED routing_decision data schema: `RoutingDecisionDataSchema = z.object({backend: BackendEnum, model_or_agent: z.string(), rationale: z.string(), confidence: z.number(), signals: z.record(z.unknown())})`. Export `NamedSSEEventSchema = z.discriminatedUnion("event", [ z.object({event: z.literal("routing_decision"), data: RoutingDecisionDataSchema}), z.object({event: z.literal("text_delta"), data: TextDeltaSchema}), z.object({event: z.literal("tool_call"), data: ToolCallSchema}), z.object({event: z.literal("tool_result"), data: ToolResultSchema}), z.object({event: z.literal("file_diff"), data: FileDiffSchema}), z.object({event: z.literal("screenshot"), data: ScreenshotSchema}), z.object({event: z.literal("stream_error"), data: StreamErrorSchema}), z.object({event: z.literal("done"), data: DoneSchema}) ])`. Use `z.literal("text_delta")` (not `z.string()`) for every `type` discriminator so unknown variants fail. For Done: all fields nullable + optional (mirror `int | None = None` in Python). For ToolResult.content: `z.union([z.string(), z.record(z.unknown())])` (mirror `str | dict`). For Screenshot: image_b64 and image_ref both `.nullable().optional()` (mirror `str | None = None`); image_format `z.enum(["png","jpeg"])`.

Author apps/web/tests/fixtures/sse-events.ts exporting ONE const per variant — the EXACT string bytes Phase 3's `ServerSentEvent(event=..., data=chunk.model_dump_json())` produces; each ends with `\n\n`. Examples: `export const TEXT_DELTA_EVENT = 'event: text_delta\\ndata: {"type":"text_delta","text":"Hello"}\\n\\n';` `export const DONE_EVENT = 'event: done\\ndata: {"type":"done","tokens_in":42,"tokens_out":17,"cost_usd":0.0021,"latency_ms":1400,"routing_signals":{"task_type":"chat","agentic_intent":false,"rule_fired":"default"}}\\n\\n';` `export const ROUTING_DECISION_EVENT = 'event: routing_decision\\ndata: {"backend":"openrouter","model_or_agent":"openai/gpt-5","rationale":"Strong reasoning fit","confidence":0.9,"signals":{"task_type":"chat","agentic_intent":false,"rule_fired":"default"}}\\n\\n';` (the routing_decision data is the STRUCTURED 5-key record per Plan 04's amended payload — `signals` is a SUB-FIELD, NOT the whole payload). Provide a STREAM_ERROR_CANCELLED, STREAM_ERROR_AUTH_FAILED, TOOL_CALL, TOOL_RESULT, FILE_DIFF, SCREENSHOT_SMALL, and MALFORMED_EVENT (data fails JSON.parse) fixture. Also provide a `ROUTING_DECISION_EVENT_MISSING_MODEL` fixture (payload omits model_or_agent) used to assert Pattern F rejection in the chunk-schemas test suite.

Overwrite apps/web/tests/chunk-schemas.test.ts (was an it.todo stub from Plan 01) with a real Vitest suite: one `it("parses <variant>", () => ...)` per variant asserting `NamedSSEEventSchema.parse({event, data: JSON.parse(...)})` returns the typed shape; one `it("rejects unknown event names", ...)` asserting ZodError thrown for `{event: "garbage", data: {}}`; one `it("rejects invalid StreamError.code", ...)` asserting ZodError for code outside the 9-value enum; one `it("parses the structured routing_decision payload", ...)` asserting all 5 keys are typed (backend / model_or_agent / rationale / confidence / signals); one `it("rejects routing_decision missing model_or_agent", ...)` parsing ROUTING_DECISION_EVENT_MISSING_MODEL asserts ZodError (Pattern F); one `it("rejects routing_decision with backend outside the 3-value enum", ...)` parsing `{event:"routing_decision", data:{backend:"foo",model_or_agent:"x",rationale:"y",confidence:0.5,signals:{}}}` asserts ZodError. Run `pnpm --dir apps/web test chunk-schemas.test.ts` and assert exit 0 with > 11 tests passing.</action>
  <verify>
    <automated>pnpm --dir apps/web test chunk-schemas.test.ts 2>&amp;1 | grep -E '(Tests|passing|failed)' &amp;&amp; node -e "const fs=require('fs'); const src=fs.readFileSync('apps/web/lib/chunk-schemas.ts','utf8'); const codes=['cost_cap_exceeded','step_cap_exceeded','cancelled','rate_limited','auth_failed','provider_unavailable','timeout','validation_error','internal_error']; for (const c of codes) if (!src.includes(c)) {console.error('Missing StreamError code:',c);process.exit(1)} const events=['routing_decision','text_delta','tool_call','tool_result','file_diff','screenshot','stream_error','done']; for (const e of events) if (!src.includes('z.literal(\"'+e+'\")')) {console.error('Missing event:',e);process.exit(1)} for (const f of ['backend','model_or_agent','rationale','confidence','signals']) if (!src.includes(f)) {console.error('routing_decision Zod schema missing field:',f);process.exit(1)} if (!src.includes('BackendEnum') &amp;&amp; !src.includes('z.enum([\"openrouter\"')) {console.error('Backend Zod enum missing');process.exit(1)} console.log('all closed-vocab present')" &amp;&amp; node -e "const fs=require('fs'); const src=fs.readFileSync('apps/web/lib/types.ts','utf8'); if (!src.includes('\"openrouter\"') || !src.includes('\"claude_code\"') || !src.includes('\"computer_use\"')) process.exit(1); console.log('Backend union OK')" &amp;&amp; node -e "const fs=require('fs'); const src=fs.readFileSync('apps/web/tests/fixtures/sse-events.ts','utf8'); if (!src.includes('ROUTING_DECISION_EVENT')) {console.error('Missing ROUTING_DECISION_EVENT fixture');process.exit(1)} if (!src.includes('\"backend\":\"openrouter\"') || !src.includes('\"model_or_agent\"')) {console.error('ROUTING_DECISION_EVENT fixture must include structured 5-key payload (Plan 04 amendment)');process.exit(1)} if (!src.includes('ROUTING_DECISION_EVENT_MISSING_MODEL')) {console.error('Missing ROUTING_DECISION_EVENT_MISSING_MODEL drift fixture');process.exit(1)} console.log('fixtures OK')" &amp;&amp; cd apps/web &amp;&amp; pnpm exec tsc --noEmit --strict lib/types.ts lib/chunk-schemas.ts 2>&amp;1 | grep -v 'node_modules' | (grep -i 'error' &amp;&amp; exit 1 || echo 'tsc strict OK')</automated>
  </verify>
  <done>chunk-schemas.test.ts passes with at least 13 tests (8 variants + 3 negative + structured-routing-decision + missing-model-drift + bad-backend-enum); chunk-schemas.ts exports NamedSSEEventSchema + StreamErrorCode + BackendEnum + RoutingDecisionDataSchema + 7 per-variant schemas; types.ts exports Backend + RoutingDecision + AdapterStatus + MetricsPayload; `tsc --strict` passes on both; every StreamError.code from chunks.py is present in the enum; every event name from PATTERNS is in the union; the routing_decision fixture uses the structured 5-key payload (Plan 04 amendment).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the pure-function SSE translator + sse-translate.test.ts with end-to-end fixture stream assertions</name>
  <files>apps/web/lib/sse-translate.ts, apps/web/tests/sse-translate.test.ts</files>
  <read_first>
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md (§"Pattern 2" lines 470-637 — full reference implementation skeleton including the translation mapping table lines 498-506; Critical Finding #4 — return-immediately-then-translate pattern; Pitfall 2 — never await upstream.text(); Pitfall 7 — x-vercel-ai-ui-message-stream header is set by Plan 03's route handler, NOT this translator),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md (apps/web/lib/sse-translate.ts section — translation mapping; Pattern F — parse failure handling; apps/web/tests/sse-translate.test.ts section — full test shape),
    apps/web/lib/chunk-schemas.ts (Task 1's NamedSSEEventSchema is the parse boundary; routing_decision parses to the structured 5-key shape),
    apps/web/tests/fixtures/sse-events.ts (Task 1's fixtures are the test inputs),
    apps/api/routes/turn.py (lines 481-484 — the canonical event format the translator parses; lines 467-484 — confirm single-line `data:` blocks since Pydantic's model_dump_json has no embedded newlines)
  </read_first>
  <behavior>
    - Empty input stream → output emits NOTHING then closes cleanly
    - Single routing_decision event (structured 5-key payload) → output: data: {type:"start",messageId:...}\n\n + data: {type:"data-routing",data:{backend, model_or_agent, rationale, confidence, signals}}\n\n — the data field carries all 5 keys verbatim
    - routing_decision + first text_delta → output: start + data-routing + text-start (id:"t-0") + text-delta (id:"t-0",delta:"...")
    - 3 text_deltas in sequence → 1 text-start + 3 text-deltas (all reusing id:"t-0")
    - done event → text-end + data-metrics + finish + literal "data: [DONE]\n\n"
    - stream_error with code="cancelled" → text-end + {type:"abort",reason:"cancelled"}
    - stream_error with code="cost_cap_exceeded" → text-end + {type:"error",errorText:<message>,code:"cost_cap_exceeded",retriable:false}
    - tool_call / tool_result / file_diff / screenshot → forwarded as {type:`data-${event}`, data:<original>} (Phase 5 forward-compat; Phase 4 doesn't render these)
    - Malformed event (data is not valid JSON) → output emits {type:"error",errorText:"Malformed upstream event: <event>"} and CONTINUES (the next valid event still produces correct chunks; the stream does NOT close)
    - Heartbeat lines (lines starting with ":") and empty blocks → skipped silently
    - Input arriving in multiple chunks (a single SSE block split across two Uint8Array reads) → still parsed correctly (buffer accumulation across read() calls)
    - Unknown event name (not in the 8-variant union) → output emits {type:"error",errorText:"Malformed upstream event: <event>"} (Zod parse fails inside the translator) and CONTINUES
    - routing_decision with structurally-invalid payload (e.g. missing model_or_agent) → output emits {type:"error",errorText:"Malformed upstream event: routing_decision"} and CONTINUES (Pattern F)
  </behavior>
  <action>Author apps/web/lib/sse-translate.ts using RESEARCH §Pattern 2 lines 510-637 as the canonical reference implementation (the executor adapts the skeleton: imports `{NamedSSEEventSchema}` from `./chunk-schemas`; sets `messageId = "m-" + crypto.randomUUID()` and `textPartId = "t-0"`; tracks `messageStarted` and `textPartOpen` booleans across reads; uses `new TextDecoder({stream:true})` + `new TextEncoder()` for byte handling; splits incoming buffer on `\n\n` to find complete blocks; skips blocks beginning with ":" or empty; for each parsed event applies the mapping table per RESEARCH lines 498-506). The `emit` helper is `(chunk: object) => encoder.encode(\`data: ${JSON.stringify(chunk)}\\n\\n\`)`.

For the routing_decision mapping: `case "routing_decision": emit({type: "data-routing", data: parsed.data});` — `parsed.data` is the typed 5-key object after Zod parse; downstream consumers receive {backend, model_or_agent, rationale, confidence, signals} verbatim.

Critical: the function signature is `export function translateNamedSSEToUIMessageStream(upstream: ReadableStream<Uint8Array>): ReadableStream<Uint8Array>` — pure, no side effects, no globals. The new ReadableStream's `start(controller)` reads from `upstream.getReader()` in an async loop; on parse failure emit error chunk + `continue` (never throw); on `done` emit text-end (if open) + data-metrics + finish + `data: [DONE]\n\n` (literal — write encoder.encode("data: [DONE]\\n\\n")); on translator-level exception call `controller.error(err)`; on upstream completion call `controller.close()` and `reader.releaseLock()` in finally.

Overwrite apps/web/tests/sse-translate.test.ts (was an it.todo stub from Plan 01) with a real Vitest suite. Helper functions: `function fixtureStream(events: string[]): ReadableStream<Uint8Array>` enqueues each string via TextEncoder; `async function consumeToText(stream): Promise<string>` reads to completion via getReader. Test cases mirroring the behavior list above — one test per behavior. CRITICAL: the routing_decision test asserts the emitted data-routing chunk's `data` field is the structured 5-key object: parse the emitted chunk, assert `chunk.data.backend === "openrouter"`, `chunk.data.model_or_agent === "openai/gpt-5"`, `chunk.data.rationale === "Strong reasoning fit"`, `chunk.data.confidence === 0.9`, `chunk.data.signals` deep-equals the source signals dict. For the "split-block" test, simulate a single SSE block being delivered as two chunks: `fixtureStream(["event: text_delta\\nda", "ta: {\\\"type\\\":\\\"text_delta\\\",\\\"text\\\":\\\"hi\\\"}\\n\\n"])` — assert the output contains exactly one text-delta with delta:"hi" (proves buffer accumulation works). For the malformed-event test, send `event: text_delta\\ndata: not-json\\n\\n` followed by a valid done event — assert output contains `{type:"error",errorText:"Malformed upstream event: text_delta"}` AND the data-metrics from done (proves the translator continues). For the routing-decision-drift test, feed ROUTING_DECISION_EVENT_MISSING_MODEL — assert output contains an error chunk (Pattern F) and the stream continues. Run `pnpm --dir apps/web test sse-translate.test.ts`; assert exit 0 with at least 11 tests passing.</action>
  <verify>
    <automated>pnpm --dir apps/web test sse-translate.test.ts 2>&amp;1 | grep -E '(Tests|passing|failed)' &amp;&amp; node -e "const fs=require('fs'); const src=fs.readFileSync('apps/web/lib/sse-translate.ts','utf8'); const required=['translateNamedSSEToUIMessageStream','NamedSSEEventSchema','TextDecoder','TextEncoder','data: [DONE]','data-routing','data-metrics','text-start','text-delta','text-end','finish','abort']; for (const r of required) if (!src.includes(r)) {console.error('Missing:',r);process.exit(1)} console.log('all symbols present')" &amp;&amp; cd apps/web &amp;&amp; pnpm exec tsc --noEmit --strict lib/sse-translate.ts 2>&amp;1 | grep -v 'node_modules' | (grep -i 'error' &amp;&amp; exit 1 || echo 'tsc strict OK')</automated>
  </verify>
  <done>sse-translate.test.ts passes with at least 11 tests (one per behavior list item + Pattern F drift); sse-translate.ts is a pure function with no module-level state; output stream emits AI SDK v6 chunks matching the mapping table; the data-routing chunk's data field carries the structured 5-key routing payload verbatim; parse failures produce error chunks and CONTINUE; multi-read buffer accumulation works; `tsc --strict` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author api-client.ts + thread-id.ts as the typed same-origin client surface for Plan 03's route handlers + 3 fast Vitest tests</name>
  <files>apps/web/lib/api-client.ts, apps/web/lib/thread-id.ts, apps/web/tests/api-client.test.ts, apps/web/tests/thread-id.test.ts</files>
  <read_first>
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-CONTEXT.md (D-08 — body shape Next route sends to FastAPI; D-16 — healthz boot trigger; D-17 — settings page reuse; D-18 — key-handling rules; D-19 — post-entry unblock sequence; Claude's Discretion "Thread creation in minimal mode" — localStorage key 'prompt-optimizer.defaultThreadId'),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-RESEARCH.md (§Pattern 8 — boot sequence + submitKey helper shape; §Open Question 6 — server-vs-browser thread creation; planner picks browser-driven path),
    .planning/phases/04-minimal-chat-ui-openrouter-backend/04-PATTERNS.md (Pattern B — server-only env var, NO NEXT_PUBLIC; apps/web/lib/thread-id.ts "No Analog Found" entry referencing CONTEXT discretion),
    apps/api/routes/health.py (the upstream contract Plan 03 proxies — getHealth() must return that shape),
    apps/api/routes/settings.py (the upstream contract — postSettings forwards {provider,key} which Plan 03's route translates to {keys:{provider:key}}),
    apps/api/routes/threads.py (the upstream contract — postThread accepts {title:string}),
    apps/web/lib/types.ts (Task 1's RoutingDecision + AdapterStatus types)
  </read_first>
  <behavior>
    - postSettings throws an Error on non-200; the thrown Error.message does NOT include the literal key string (D-18 belt-and-suspenders at the wrapper layer; verified by regex search on Error.message)
    - postSettings on 200 returns the typed {keys: {...}} response
    - thread-id readDefaultThreadId returns null when typeof window === "undefined" (SSR-safe; no ReferenceError)
    - thread-id getOrCreateDefaultThread on first call (empty localStorage): calls postThread, stores returned id in localStorage, returns it
    - thread-id getOrCreateDefaultThread on second call (localStorage has cached id): returns cached value WITHOUT calling postThread again
  </behavior>
  <action>Author apps/web/lib/api-client.ts exporting typed functions whose URLs are all SAME-ORIGIN paths (never the FASTAPI_URL constant — that lives server-side only in Plan 03's route handlers, per PATTERNS Pattern B + D-18). Functions: `export async function postChat(messages: unknown[], signal?: AbortSignal): Promise<Response>` — POSTs to `/api/chat` with `{messages}` body and returns the raw Response (caller is useChat which consumes the stream); `export async function postSettings(provider: "openrouter" | "anthropic", key: string): Promise<{keys: Record<string, {present: boolean, masked: string}>}>` — POSTs to `/api/settings` with `{provider, key}`; CRITICAL: this function does NOT log the key, does NOT include the key in any thrown error message (D-18 belt-and-suspenders), and on non-200 throws an Error whose .message does NOT include the key string (scrub via `.replace(key, "***")` before throw). `export interface HealthResponse { adapters: { openrouter: { status: AdapterStatus }, [k: string]: { status: AdapterStatus } } }` + `export async function getHealth(): Promise<HealthResponse>` — GETs `/api/health`. `export async function postThread(title: string): Promise<{id: string, title: string}>` — POSTs to `/api/threads` with `{title}`. `export async function getThread(id: string): Promise<{id: string, messages: unknown[]}>` — GETs `/api/threads/${id}`. Every function uses `fetch(..., {signal})` where applicable so callers can wire AbortController.

Author apps/web/lib/thread-id.ts exporting: `export const DEFAULT_THREAD_KEY = "prompt-optimizer.defaultThreadId";` `export function readDefaultThreadId(): string | null` (returns `null` if `typeof window === "undefined"` — SSR safe; returns localStorage value otherwise); `export async function getOrCreateDefaultThread(): Promise<string>` (on first call when localStorage is empty: calls `postThread("Untitled")` from api-client, stores returned id in localStorage, returns it; subsequent calls return cached value via readDefaultThreadId). Both functions throw if window is undefined when called outside SSR-safe contexts — actually readDefaultThreadId is SSR-safe (returns null); getOrCreateDefaultThread requires window because it writes localStorage — callers must invoke from useEffect / event handlers.

Author apps/web/tests/api-client.test.ts — Vitest unit suite. Mock global `fetch` with `vi.stubGlobal("fetch", vi.fn())`. Test cases: (1) `postSettings("openrouter", "sk-or-v1-LITERAL_KEY")` when fetch returns a 500 with body text containing the key — assert the thrown Error.message does NOT contain "sk-or-v1-LITERAL_KEY" (regex `expect(err.message).not.toMatch(/sk-or-v1-LITERAL_KEY/)`); (2) `postSettings` happy-path returns the typed `{keys: {...}}` response. NEVER log the literal key from within the test (the test imports the key from a const and asserts on it; vitest's assertion failure messages may include it — that's acceptable because tests are not production code paths).

Author apps/web/tests/thread-id.test.ts — Vitest unit suite. (1) `readDefaultThreadId` SSR path — temporarily override `globalThis.window` via `vi.stubGlobal("window", undefined)` (or use vitest's `jsdom`-vs-`node` env switching) and assert the function returns null cleanly without ReferenceError. (2) `getOrCreateDefaultThread` cache path — mock `postThread` from api-client via `vi.mock("@/lib/api-client", () => ({postThread: vi.fn().mockResolvedValue({id: "test-tid-1", title: "Untitled"})}))`; clear localStorage; call getOrCreateDefaultThread — assert postThread called ONCE AND localStorage now has the value "test-tid-1"; call getOrCreateDefaultThread AGAIN — assert postThread call count is still 1 (cache hit) AND returned value is "test-tid-1".

Run `pnpm --dir apps/web test api-client.test.ts thread-id.test.ts` and assert exit 0 with at least 3 tests passing. Run `tsc --strict` on the source files; assert clean.</action>
  <verify>
    <automated>pnpm --dir apps/web test api-client.test.ts thread-id.test.ts 2>&amp;1 | grep -E '(Tests|passing|failed|✓)' &amp;&amp; cd apps/web &amp;&amp; pnpm exec tsc --noEmit --strict lib/api-client.ts lib/thread-id.ts 2>&amp;1 | grep -v 'node_modules' | (grep -i 'error' &amp;&amp; exit 1 || echo 'tsc strict OK') &amp;&amp; node -e "const src=require('fs').readFileSync('apps/web/lib/api-client.ts','utf8'); if (src.includes('FASTAPI_URL') || src.includes('localhost:8000') || src.includes('NEXT_PUBLIC')) { console.error('api-client must NOT reference server-only env var'); process.exit(1); } if (!src.includes('\"/api/chat\"') || !src.includes('\"/api/settings\"') || !src.includes('\"/api/health\"') || !src.includes('\"/api/threads\"')) { console.error('api-client must use same-origin /api/* paths'); process.exit(1); } if (!src.includes('.replace(key')) { console.error('postSettings must scrub key from errors'); process.exit(1); } console.log('api-client hygiene OK')" &amp;&amp; node -e "const src=require('fs').readFileSync('apps/web/lib/thread-id.ts','utf8'); if (!src.includes('prompt-optimizer.defaultThreadId')) {console.error('Missing localStorage key constant');process.exit(1)} if (!src.includes('typeof window')) {console.error('Missing SSR guard');process.exit(1)} console.log('thread-id SSR-safe OK')"</automated>
  </verify>
  <done>api-client.ts + thread-id.ts compile under `tsc --strict`; api-client uses ONLY `/api/*` same-origin paths — never FASTAPI_URL or NEXT_PUBLIC_*; postSettings scrubs the key string from any thrown error; thread-id.ts has SSR guards (typeof window check) and uses the documented localStorage key; api-client.test.ts + thread-id.test.ts pass with at least 3 tests covering the key-scrub regression, SSR guard, and localStorage caching.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-3 SSE wire → translator | Untrusted bytes — translator MUST Zod-parse and never crash the stream |
| browser fetch → api-client | All paths are same-origin; FASTAPI_URL never appears in browser bundle |
| api-client error path → caller | Plaintext key never reaches the caller's error message or log line |
| localStorage write → defaultThreadId | Non-secret value; D-18 prohibits secret material in localStorage |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Denial of Service | Malformed upstream SSE event crashes the translator | mitigate | translator try/catch wraps NamedSSEEventSchema.parse; failure emits {type:"error"} and continues — RESEARCH §"Known Threat Patterns" + Pitfall in sse-translate.test.ts behavior list |
| T-04-09 | Information Disclosure | api-client.ts accidentally references FASTAPI_URL | mitigate | grep gate in Task 3 verify; the constant only exists in Plan 03's server-side route handlers |
| T-04-10 | Information Disclosure | postSettings error message contains plaintext key | mitigate | postSettings catches every error path and calls .replace(key, "***") before throwing — defense in depth before Plan 03's server-side scrub; api-client.test.ts asserts this with a regex |
| T-04-11 | Tampering | Backend union receives an unknown string from upstream | mitigate | Backend is `Literal[3]`; the routing_decision Zod schema's BackendEnum rejects unknowns at the wire boundary; translator emits an error chunk on rejection (Pattern F); the chip never receives an undefined backend |
| T-04-12 | Information Disclosure | localStorage write of a sensitive value | mitigate | thread-id.ts writes ONLY the thread-id string (non-secret); a comment in thread-id.ts forbids ever writing key material to localStorage (D-18) |
| T-04-13 | Information Disclosure | Translator emits the raw upstream chunk content in error fallback | accept | Error fallback emits `errorText: "Malformed upstream event: <event>"` — only the event NAME, never the data payload; the Pattern 2 reference code is deliberately conservative |
| T-04-13b | Tampering | upstream routing_decision payload missing required fields silently produces a half-rendered chip | mitigate | Task 1's structured Zod schema rejects missing fields; the test suite includes ROUTING_DECISION_EVENT_MISSING_MODEL to enforce the rejection path; the translator emits an error chunk instead of forwarding a malformed data-routing chunk |
</threat_model>

<verification>
- `pnpm --dir apps/web test sse-translate.test.ts chunk-schemas.test.ts api-client.test.ts thread-id.test.ts` exits 0 with at least 24 tests passing
- `tsc --strict` clean on all 5 new source files
- No fixture in fixtures/sse-events.ts uses content outside the Phase-3 wire shape; ROUTING_DECISION_EVENT uses the structured 5-key payload (Plan 04 amendment)
- api-client.ts has zero references to FASTAPI_URL / NEXT_PUBLIC_*
- chunk-schemas.ts mirrors apps/api/backends/chunks.py byte-for-byte on every Literal; routing_decision is a discriminated-union member with the structured 5-key data schema
- sse-translate.ts is a pure function (grep proves no module-level mutation outside the function body)
- api-client.test.ts proves postSettings error path scrubs the key from Error.message
- thread-id.test.ts proves SSR guard returns null + localStorage cache hit avoids the second POST
</verification>

<success_criteria>
- Wave 1 complete: the wire-format contract is provably correct via unit tests BEFORE any route handler or UI work
- Plan 03's route handlers can `import { translateNamedSSEToUIMessageStream } from "@/lib/sse-translate"` and trust the output shape
- Plan 05's useChat integration can rely on every chunk type the translator emits being a documented AI SDK v6 chunk
- Plan 05's chip subscribes to a `data-routing` part whose `data` field is the STRUCTURED 5-key routing record — chip reads backend/model_or_agent/rationale/confidence directly with no defensive optional-chaining required
- Plan 05's footer subscribes to a `data-metrics` part that the translator produces deterministically
- VALIDATION.md row "D-07 contract" + "Schema contract" both flip to ✅
</success_criteria>

<output>
After completion, create `.planning/phases/04-minimal-chat-ui-openrouter-backend/04-02-SUMMARY.md` recording:
- The exact translation mapping verified by test (one row per Phase-3 event → AI SDK v6 chunk)
- The Zod schema variants and their parent union shape; emphasize that routing_decision is structured (5 keys), not free-form
- Any deviations from RESEARCH §Pattern 2 (e.g., if a chunk shape needed adjustment for AI SDK v6 actual behavior)
- The fixture count in fixtures/sse-events.ts and which Phase-3 events are covered
- Confirmation that api-client.ts uses zero server-only env references
- The test count for api-client.test.ts + thread-id.test.ts (target: ≥3 between them)
</output>
