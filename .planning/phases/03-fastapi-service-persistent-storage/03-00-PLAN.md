---
phase: 03
plan: 00
plan_id: "03-00-scaffolding"
type: execute
wave: 0
depends_on: []
files_modified:
  - "pyproject.toml"
  - "uv.lock"
  - "apps/api/paths.py"
  - "apps/api/tests/__init__.py"
  - "apps/api/tests/conftest.py"
  - "apps/api/tests/fake_adapter.py"
  - "apps/api/tests/fixtures/schema_v0_seed.sql"
  - "apps/api/tests/test_smoke.py"
  - ".env.example"
  - ".gitignore"
  - ".planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md"
autonomous: true
requirements: [API-08]
user_setup: []
must_haves:
  truths:
    - "All Phase 3 source files use `pathlib.Path(__file__).resolve().parents[N]` for path resolution; no `sys.path.append` anywhere (CONTEXT anti-pattern; codebase ARCHITECTURE.md)."
    - "All Phase 3 modules import `apps.api.*` first to trigger `dotenv.load_dotenv()` + `install_redaction_filter()` from `apps/api/__init__.py`; no module re-installs the redaction filter (D-19 / Phase 2 D-11)."
    - "`grep -rE 'from fastapi.testclient|fastapi\\.testclient\\.TestClient' apps/api/tests/` returns zero matches — API-08 / D-20 negative-grep guard is enforceable."
    - "`uv run python -c 'from fastapi import FastAPI; from sse_starlette.sse import EventSourceResponse; import aiosqlite; import uvicorn; print(\"ok\")'` exits 0 — all four new deps installed."
    - "`uv run pytest --collect-only apps/api/tests/` exits 0 with `conftest.py + test_smoke.py + fake_adapter.py` all importable; the `app_factory` / `asgi_client` / `aiosqlite_inmemory_db` fixtures are discoverable but lazy-import their producer modules so collection works BEFORE Wave 1-6 land (B3 pattern from Phase 2 Plan 00 Task 3)."
    - "`apps/api/paths.py` exports `USER_HOME`, `DB_PATH`, `BLOBS_DIR`, `SETTINGS_PATH`, `WORKSPACES_DIR`, `JSONL_LOG_PATH` and honours the `PROMPT_OPTIMIZER_HOME` env override (CONTEXT specifics line 324)."
    - "`.gitignore` excludes `.planning/data/` so `routing_decisions.jsonl` (created on first turn in Wave 4) does NOT enter git history (STORE-06 + Phase 1 SECURE-03)."
    - "Phase 1 D-18 import-graph guard test (`pytest src/routing/tests/test_decide_smoke.py -x`) still passes after Wave 0 deps land — adding `fastapi`/`uvicorn`/`sse-starlette`/`aiosqlite` to `pyproject.toml` must NOT pull them into `src.routing.*` import graph."
    - "Phase 2 whole-repo non-live test suite (`uv run pytest -m 'not live' -x`) still passes after Wave 0 deps land — no regression on `apps/api/backends/tests/*`."
    - "`03-VALIDATION.md` frontmatter has `nyquist_compliant: true` and `wave_0_complete: true` set after this plan lands."
  artifacts:
    - path: "pyproject.toml"
      provides: "Phase 3 deps added: `fastapi>=0.115,<1.0`, `uvicorn[standard]>=0.30,<1.0`, `sse-starlette>=2.1,<4.0` (per RESEARCH Open Question 1 widening to accommodate `mcp` 3.x transitive), `aiosqlite>=0.20,<1.0`; dev extra adds `httpx-sse>=0.4,<1.0` for SSE test parsing"
      contains: "fastapi>=0.115,<1.0|uvicorn\\[standard\\]>=0.30,<1.0|sse-starlette>=2.1,<4.0|aiosqlite>=0.20,<1.0|httpx-sse>=0.4,<1.0"
    - path: "uv.lock"
      provides: "Regenerated lockfile with Phase 3 deps; `claude-code-sdk` STILL absent (Phase 2 OSS-06)"
    - path: "apps/api/paths.py"
      provides: "Single-source path constants (USER_HOME, DB_PATH, BLOBS_DIR, SETTINGS_PATH, WORKSPACES_DIR, JSONL_LOG_PATH) per CONTEXT canonical refs line 242"
      contains: "PROJECT_ROOT.*parents\\[2\\]|USER_HOME|DB_PATH|BLOBS_DIR|SETTINGS_PATH|WORKSPACES_DIR|JSONL_LOG_PATH|PROMPT_OPTIMIZER_HOME"
      min_lines: 20
    - path: "apps/api/tests/__init__.py"
      provides: "Package marker for `apps.api.tests`"
    - path: "apps/api/tests/conftest.py"
      provides: "Three core fixtures: `aiosqlite_inmemory_db`, `asgi_client`, `app_factory(adapters_override, settings_override, keystore_override)`. Producer modules (`apps.api.main:create_app`, `apps.api.lifespan`, `apps.api.db.connect.open_db`, `apps.api.db.migrate.up_to_latest`) are LAZILY imported inside the fixtures (try/except ImportError -> pytest.skip) so the conftest is collectable in Wave 0 BEFORE Wave 1-6 modules exist (B3 fix from Phase 2 Plan 00)."
      contains: "aiosqlite_inmemory_db|asgi_client|app_factory|ASGITransport|httpx.AsyncClient|try:|except ImportError|pytest.skip"
    - path: "apps/api/tests/fake_adapter.py"
      provides: "`class FakeStreamingAdapter` implementing `BackendAdapter` Protocol — constructor takes `list[ChatChunk]` + optional `sleep_per_chunk` for heartbeat tests"
      contains: "class FakeStreamingAdapter|async def stream|sleep_per_chunk"
    - path: "apps/api/tests/fixtures/schema_v0_seed.sql"
      provides: "INSERTs 1 thread + 2 messages + 1 routing_decisions row directly into a fresh v0 DB (used by Wave 1 test_migrations.py::test_v0_to_v1_preserves_data per CONTEXT specifics line 346)"
      contains: "INSERT INTO threads|INSERT INTO messages|INSERT INTO routing_decisions"
    - path: "apps/api/tests/test_smoke.py"
      provides: "Wave 0 sanity: imports the four new deps + asserts paths.py constants are well-formed + asserts conftest fixtures collect"
      contains: "fastapi|uvicorn|sse_starlette|aiosqlite|USER_HOME|DB_PATH"
    - path: ".env.example"
      provides: "Enumerate Phase 3 env vars: `OPENROUTER_API_KEY`, `ANTHROPIC_API_KEY`, `COMPUTER_USE_OPT_IN`, `PROMPT_OPTIMIZER_HOME` (optional), `PROMPT_OPTIMIZER_CORS_ORIGINS` (optional)"
      contains: "OPENROUTER_API_KEY|ANTHROPIC_API_KEY|COMPUTER_USE_OPT_IN|PROMPT_OPTIMIZER_HOME|PROMPT_OPTIMIZER_CORS_ORIGINS"
    - path: ".gitignore"
      provides: "Add `.planning/data/` exclusion (forward-compat with STORE-06 JSONL log written in Wave 4)"
      contains: ".planning/data/"
    - path: ".planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md"
      provides: "Mark `nyquist_compliant: true` and `wave_0_complete: true` after Wave 0 lands"
      contains: "nyquist_compliant: true|wave_0_complete: true"
  key_links:
    - from: "apps/api/paths.py"
      to: "PROMPT_OPTIMIZER_HOME env var"
      via: "os.environ.get fallback to Path.home()"
      pattern: "PROMPT_OPTIMIZER_HOME"
    - from: "apps/api/tests/conftest.py"
      to: "apps.api.main:create_app"
      via: "lazy import inside app_factory fixture (Wave 0 compatibility)"
      pattern: "try:\\s+from apps\\.api\\.main import create_app\\s+except ImportError"
    - from: "apps/api/tests/conftest.py"
      to: "apps.api.db.migrate.up_to_latest"
      via: "lazy import inside aiosqlite_inmemory_db fixture"
      pattern: "try:\\s+from apps\\.api\\.db\\.migrate import up_to_latest\\s+except ImportError"
    - from: "apps/api/tests/conftest.py"
      to: "httpx.AsyncClient + ASGITransport"
      via: "asgi_client fixture (D-20 / API-08)"
      pattern: "httpx\\.AsyncClient\\(transport=httpx\\.ASGITransport"
---

<objective>
Wave 0 of Phase 3 — install Python web/storage dependencies, create the central path constants module, and author the test scaffolding (conftest fixtures, fake adapter, migration seed SQL, negative-grep guard) BEFORE any Wave 1-6 implementation lands.

Purpose: Without these contracts in place, Wave 1-6 cannot run in parallel. Wave 0 is the gate that closes the VALIDATION.md "All MISSING references covered" criterion (`nyquist_compliant: true`). The conftest fixtures use Phase 2's B3 lazy-import pattern (try/except ImportError -> pytest.skip) so the file is collectable in Wave 0 BEFORE the Wave 1 db/, Wave 2 lifespan/main, Wave 3 routes/, Wave 4 turn.py, Wave 5 blobs.py, Wave 6 rename.py modules exist — every Phase 3 test file in Waves 1-6 reuses these fixtures.

Output:
  - `pyproject.toml` + regenerated `uv.lock` with 4 new deps (`fastapi`, `uvicorn[standard]`, `sse-starlette`, `aiosqlite`) + 1 dev dep (`httpx-sse`).
  - `apps/api/paths.py` — single source for path constants (D-15 lifespan + D-14 blob cascade + STORE-06 JSONL log all consume).
  - 4 test scaffolding files: `apps/api/tests/{__init__,conftest,fake_adapter}.py`, `apps/api/tests/fixtures/schema_v0_seed.sql`, plus a Wave 0 sanity `test_smoke.py`.
  - `.env.example` (NEW — first time the repo ships this file).
  - `.gitignore` extension for `.planning/data/`.
  - `03-VALIDATION.md` marked `nyquist_compliant: true` and `wave_0_complete: true`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-fastapi-service-persistent-storage/03-CONTEXT.md
@.planning/phases/03-fastapi-service-persistent-storage/03-RESEARCH.md
@.planning/phases/03-fastapi-service-persistent-storage/03-PATTERNS.md
@.planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-VERIFICATION.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-00-PLAN.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-CONTEXT.md
@apps/api/__init__.py
@apps/api/backends/protocol.py
@apps/api/backends/chunks.py
@apps/api/backends/cost.py
@apps/api/backends/tests/conftest.py
@apps/api/backends/tests/test_adapter_contract.py
@pyproject.toml
@.gitignore

<interfaces>
<!-- Types Wave 1-6 will consume from this plan. Wave 1-6 executors should NOT redefine these. -->

From `apps/api/paths.py` (this plan creates):
- `PROJECT_ROOT: Path = Path(__file__).resolve().parents[2]`
- `USER_HOME: Path = Path(os.environ.get("PROMPT_OPTIMIZER_HOME") or (Path.home() / ".prompt-optimizer"))`
- `DB_PATH: Path = USER_HOME / "chat.db"`
- `BLOBS_DIR: Path = USER_HOME / "blobs"`
- `SETTINGS_PATH: Path = USER_HOME / "settings.json"`
- `WORKSPACES_DIR: Path = USER_HOME / "workspaces"`
- `JSONL_LOG_PATH: Path = PROJECT_ROOT / ".planning" / "data" / "routing_decisions.jsonl"`

From `apps/api/tests/fake_adapter.py` (this plan creates):
- `class FakeStreamingAdapter: __init__(self, chunks: list[ChatChunk], *, sleep_per_chunk: float = 0.0); async def stream(self, prompt: str, history: list[Message], options: AdapterOptions) -> AsyncIterator[ChatChunk]`

From `apps/api/tests/conftest.py` (this plan creates):
- Async fixture `aiosqlite_inmemory_db` — yields `aiosqlite.Connection` to `":memory:"` with `up_to_latest()` applied. Closes on teardown.
- Async fixture `asgi_client(app)` — context-manager fixture that wraps `httpx.AsyncClient(transport=httpx.ASGITransport(app=app), base_url="http://test")`.
- Fixture `app_factory(adapters_override=None, settings_override=None, keystore_override=None)` — returns a fresh FastAPI app whose lifespan honors the overrides (i.e., `app.state.adapters = adapters_override or {}`).

From Phase 2 `apps/api/backends/chunks.py` (already exists, imported by `fake_adapter.py`):
- `ChatChunk = Annotated[Union[TextDelta, ToolCall, ToolResult, FileDiff, Screenshot, StreamError, Done], Field(discriminator="type")]`

From Phase 2 `apps/api/backends/protocol.py` (already exists, imported by `fake_adapter.py`):
- `class BackendAdapter(Protocol): async def stream(self, prompt: str, history: list[Message], options: AdapterOptions) -> AsyncIterator[ChatChunk]: ...`
- `@dataclass(frozen=True) class Message: role: str; content: str`
- `@dataclass(frozen=True) class AdapterOptions`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend pyproject.toml + regenerate uv.lock with Phase 3 deps</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (current state — additive only; preserve every existing line)
    - .planning/phases/03-fastapi-service-persistent-storage/03-RESEARCH.md §"Standard Stack" lines 40-79 (4 new deps + 1 optional test dep with verified versions) and §"Open Question 1" lines 1097-1100 (sse-starlette upper bound widened to `<4.0` because `mcp` transitive pins 3.x)
    - .planning/phases/03-fastapi-service-persistent-storage/03-PATTERNS.md §"Configuration & Distribution" line 61 (the exact extension diff for `pyproject.toml`)
    - .planning/phases/03-fastapi-service-persistent-storage/03-CONTEXT.md §"External Dependencies" lines 220-228
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-00-PLAN.md Task 1 lines 213-256 (Phase 2 pyproject.toml extension pattern to mirror)
  </read_first>
  <action>
Add to `[project.dependencies]` in `pyproject.toml` (additive — preserve every existing line): `"fastapi>=0.115,<1.0"`, `"uvicorn[standard]>=0.30,<1.0"`, `"sse-starlette>=2.1,<4.0"` (upper bound widened from CONTEXT D-06's `<3.0` per RESEARCH Open Question 1 — the venv already has 3.x via the `mcp` transitive and the public surface Phase 3 consumes is unchanged), `"aiosqlite>=0.20,<1.0"`.

Add to `[project.optional-dependencies] dev` (preserve existing `pytest`, `pytest-cov`, `pytest-asyncio`, `pytest-timeout` entries): `"httpx-sse>=0.4,<1.0"` for SSE test parsing per RESEARCH §"Standard Stack" line 57 (`aconnect_sse` clean event parsing).

Do NOT add `claude-code-sdk` anywhere (Phase 2 OSS-06; pre-commit hook + CI guard still active).
Do NOT add an `aiosqlitepool` or any connection pooling library (CONTEXT D-15 + RESEARCH Open Question 3 — single shared connection).
Do NOT add `Alembic`, `yoyo-migrations`, `SQLModel`, `SQLAlchemy`, or `databases` (CONTEXT D-01 + D-02 — hand-rolled migration runner; no ORM).

`[tool.hatch.build.targets.wheel] packages = ["src", "apps"]` already covers `apps/api/` from Phase 2 P00 — do NOT extend.
`[tool.pytest.ini_options]` already has `asyncio_mode = "auto"`, `markers = ["live"]`, `testpaths = ["src", "apps"]`, `--import-mode=importlib` — do NOT extend.

After editing, regenerate the lockfile via `uv lock` (NOT `uv sync` — lock only at this step so the lock change is atomic and reviewable). The Wave 1-6 tasks will run `uv sync --locked --all-extras --dev` themselves before any pytest run.

Add the deviation note in this plan's SUMMARY.md (Task 5): "sse-starlette range widened to `>=2.1,<4.0` per RESEARCH Open Question 1; CONTEXT D-06's `<3.0` would have forced a downgrade of `mcp`'s transitive."
  </action>
  <verify>
    <automated>cd "$PWD" && grep -E "^\"fastapi>=0\\.115,<1\\.0\"|^\"uvicorn\\[standard\\]>=0\\.30,<1\\.0\"|^\"sse-starlette>=2\\.1,<4\\.0\"|^\"aiosqlite>=0\\.20,<1\\.0\"" pyproject.toml | wc -l | awk '{ if ($1 < 4) { print "FAIL: missing Phase 3 deps in pyproject.toml ("$1" matches < 4 expected)"; exit 1 } else { print "OK "$1" Phase 3 deps present" } }' && grep -q 'httpx-sse>=0.4,<1.0' pyproject.toml && ! grep -q '"claude-code-sdk"' pyproject.toml && ! grep -q '"claude-code-sdk"' uv.lock && uv lock --check 2>&1 | tail -5 && uv run pytest src/routing/tests/test_decide_smoke.py -x 2>&1 | tail -3</automated>
  </verify>
  <done>
- `pyproject.toml` contains 4 new base dependency lines + 1 new dev dep line (`httpx-sse`).
- `claude-code-sdk` appears in neither `pyproject.toml` nor `uv.lock`.
- `uv lock --check` exits 0 (lockfile in sync with pyproject).
- Phase 1 D-18 import-graph guard still green.
  </done>
  <acceptance_criteria>
- Source: `grep -c '"fastapi>=0.115,<1.0"' pyproject.toml` returns at least 1.
- Source: `grep -c '"uvicorn\[standard\]>=0.30,<1.0"' pyproject.toml` returns at least 1.
- Source: `grep -c '"sse-starlette>=2.1,<4.0"' pyproject.toml` returns at least 1 (note widened upper bound per RESEARCH OQ1).
- Source: `grep -c '"aiosqlite>=0.20,<1.0"' pyproject.toml` returns at least 1.
- Source: `grep -c '"httpx-sse>=0.4,<1.0"' pyproject.toml` returns at least 1 (in dev extra).
- Source: `! grep -q '"claude-code-sdk"' pyproject.toml` exits 0 (deprecated SDK absent).
- Source: `! grep -q '"claude-code-sdk"' uv.lock` exits 0 (deprecated SDK absent from lockfile per Phase 2 OSS-06).
- Behavior: `uv lock --check` exits 0 (pyproject.toml and uv.lock are consistent).
- Behavior: Phase 1 D-18 guard test still passes: `uv run pytest src/routing/tests/test_decide_smoke.py -x` exits 0 (adding new deps must NOT cause `src.routing` to import them).
- Behavior: Phase 2 whole-repo non-live test suite still passes: `uv run pytest -m 'not live' -x 2>&1 | tail -5` exits 0.
- Behavior: `uv run python -c "from fastapi import FastAPI; from sse_starlette.sse import EventSourceResponse; import aiosqlite; import uvicorn; print('ok')"` prints `ok` (4 deps importable).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author apps/api/paths.py + extend .gitignore + create .env.example</name>
  <files>apps/api/paths.py, .gitignore, .env.example</files>
  <read_first>
    - apps/api/__init__.py lines 44-50 (the canonical `PROJECT_ROOT = Path(__file__).resolve().parents[2]` pattern Phase 3 mirrors verbatim)
    - apps/api/backends/openrouter/adapter.py lines 80-105 (module-level path constants + lazy singleton style)
    - .gitignore (current contents — note lines 22-26 already have `chat.db`, `*.db-wal`, `*.db-shm` patterns from Phase 1 SECURE-03)
    - .planning/phases/03-fastapi-service-persistent-storage/03-CONTEXT.md §"Canonical References" line 242 (paths.py role) + §"Storage Layout" lines 262-273 (the `~/.prompt-optimizer/` layout this module encodes)
    - .planning/phases/03-fastapi-service-persistent-storage/03-PATTERNS.md §"Excerpt 1" lines 126-156 (paths.py lift-source)
    - .planning/phases/03-fastapi-service-persistent-storage/03-CONTEXT.md §"Canonical References" line 257 (.env.example contents enumeration)
  </read_first>
  <action>
Write `apps/api/paths.py` with the path constants block. Module docstring follows the Phase 2 `apps/api/__init__.py` style (purpose + Public surface + Cross-refs). Mandatory contents:

```
from __future__ import annotations
import os
from pathlib import Path

PROJECT_ROOT: Path = Path(__file__).resolve().parents[2]
USER_HOME: Path = Path(
    os.environ.get("PROMPT_OPTIMIZER_HOME")
    or (Path.home() / ".prompt-optimizer")
)
DB_PATH: Path = USER_HOME / "chat.db"
BLOBS_DIR: Path = USER_HOME / "blobs"
SETTINGS_PATH: Path = USER_HOME / "settings.json"
WORKSPACES_DIR: Path = USER_HOME / "workspaces"
JSONL_LOG_PATH: Path = PROJECT_ROOT / ".planning" / "data" / "routing_decisions.jsonl"
```

NEVER use `os.path.join` chains in this module (CLAUDE.md `## Conventions / Import Organization` + Phase 2 carry-forward). NEVER add `sys.path.append` (CONTEXT anti-pattern block).

`parents[2]` lift-rationale: `apps/api/paths.py` is two levels below the repo root (same depth as `apps/api/__init__.py`) — `parents[1]` lands at `<repo>/apps`, `parents[2]` at the repo root. This matches the `apps/api/__init__.py:49` constant exactly so downstream code can use either module's `PROJECT_ROOT` interchangeably.

Add a `## Phase 3 storage layout` section to `.gitignore` (after the existing chat.db-pattern lines):

```
# Phase 3 - routing-decisions JSONL log (STORE-06; written at decide-time)
.planning/data/
```

Create `.env.example` (NEW FILE — does not yet exist). Mandatory contents (each line preceded by a one-line `#` comment header per OSS-03 forward-compat — OSS-03 lands in Phase 6 but the file shape is fixed here):

```
# OpenRouter (conversational backends — GPT-5, Claude, Gemini, DeepSeek, Qwen).
OPENROUTER_API_KEY=

# Anthropic (Claude Code SDK + computer-use). Same key for both.
ANTHROPIC_API_KEY=

# Computer-use opt-in. STRICT AND-semantics with settings.computer_use_opt_in (D-12).
# Setting this to "1" alone does NOT enable computer-use — the in-app settings toggle
# must ALSO be on. Defaults to "0" (disabled) on a fresh clone.
COMPUTER_USE_OPT_IN=0

# Override the storage root. Defaults to ~/.prompt-optimizer/ (D-15 + CONTEXT specifics line 324).
# PROMPT_OPTIMIZER_HOME=

# Override CORS allowed origins (comma-separated). Defaults to http://localhost:3000 (OSS-05).
# PROMPT_OPTIMIZER_CORS_ORIGINS=http://localhost:3000
```

`.env.example` MUST NOT contain real key values (Phase 2 SECURE-02 pre-commit hook would block any `sk-` / `sk-ant-` content anyway).
  </action>
  <verify>
    <automated>python -c "from apps.api.paths import PROJECT_ROOT, USER_HOME, DB_PATH, BLOBS_DIR, SETTINGS_PATH, WORKSPACES_DIR, JSONL_LOG_PATH; assert PROJECT_ROOT.is_dir(); assert str(JSONL_LOG_PATH).endswith('.planning/data/routing_decisions.jsonl'); print('OK paths.py')" && grep -q "PROMPT_OPTIMIZER_HOME" apps/api/paths.py && grep -q "Path(__file__).resolve().parents\[2\]" apps/api/paths.py && ! grep -q "sys.path.append" apps/api/paths.py && ! grep -q "os.path.join" apps/api/paths.py && grep -q "^\\.planning/data/" .gitignore && grep -q "OPENROUTER_API_KEY=" .env.example && grep -q "ANTHROPIC_API_KEY=" .env.example && grep -q "COMPUTER_USE_OPT_IN=0" .env.example && grep -q "PROMPT_OPTIMIZER_HOME" .env.example && grep -q "PROMPT_OPTIMIZER_CORS_ORIGINS" .env.example && ! grep -E "(sk-|sk-ant-)" .env.example</automated>
  </verify>
  <done>
- `apps/api/paths.py` exists, importable, exports the 7 constants.
- `.gitignore` excludes `.planning/data/`.
- `.env.example` exists with the 5 documented env vars (no real keys).
- `from __future__ import annotations` is the first import in `apps/api/paths.py`.
  </done>
  <acceptance_criteria>
- Source: `grep -q "from __future__ import annotations" apps/api/paths.py`.
- Source: `grep -q "PROJECT_ROOT: Path = Path(__file__).resolve().parents\\[2\\]" apps/api/paths.py`.
- Source: `grep -q "PROMPT_OPTIMIZER_HOME" apps/api/paths.py`.
- Source: `grep -q "USER_HOME: Path" apps/api/paths.py` AND `grep -q "DB_PATH: Path" apps/api/paths.py` AND `grep -q "BLOBS_DIR: Path" apps/api/paths.py` AND `grep -q "SETTINGS_PATH: Path" apps/api/paths.py` AND `grep -q "WORKSPACES_DIR: Path" apps/api/paths.py` AND `grep -q "JSONL_LOG_PATH: Path" apps/api/paths.py`.
- Source: `! grep -q "sys.path.append" apps/api/paths.py` (anti-pattern: NO sys.path hacks).
- Source: `! grep -q "os.path.join" apps/api/paths.py` (anti-pattern: use pathlib).
- Source: `grep -q "^\\.planning/data/$" .gitignore` (exact line for the JSONL log directory).
- Source: `grep -c "^[A-Z_]*=" .env.example` returns at least 3 (OPENROUTER_API_KEY, ANTHROPIC_API_KEY, COMPUTER_USE_OPT_IN are uncommented).
- Source: `! grep -E "(sk-or-v1-|sk-ant-)" .env.example` exits 0 (no real key shapes in the example file).
- Behavior: `python -c "from apps.api.paths import PROJECT_ROOT, USER_HOME, DB_PATH, BLOBS_DIR, SETTINGS_PATH, WORKSPACES_DIR, JSONL_LOG_PATH; print('ok')"` exits 0 and prints `ok`.
- Behavior: `python -c "import os; os.environ['PROMPT_OPTIMIZER_HOME'] = '/tmp/test-prompt-optimizer'; from apps.api.paths import USER_HOME; assert str(USER_HOME) == '/tmp/test-prompt-optimizer'; print('ok')"` exits 0 (env override honoured).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Author test scaffolding — apps/api/tests/{__init__,conftest,fake_adapter}.py + fixtures/schema_v0_seed.sql</name>
  <files>apps/api/tests/__init__.py, apps/api/tests/conftest.py, apps/api/tests/fake_adapter.py, apps/api/tests/fixtures/schema_v0_seed.sql</files>
  <read_first>
    - apps/api/backends/tests/conftest.py lines 1-100 (canonical fixture-shape mirror; SESSION-scoped data fixtures vs function-scoped fakes; REPO_ROOT computation)
    - apps/api/backends/tests/conftest.py lines 319-375 (the B3 lazy-import pattern: try/except ImportError → pytest.skip — the SAME pattern is used here for `apps.api.main:create_app`, `apps.api.lifespan`, `apps.api.db.connect.open_db`, `apps.api.db.migrate.up_to_latest`)
    - apps/api/backends/tests/test_adapter_contract.py lines 1-60 (parametric + lazy-import discipline; no module-level imports of producer code in test_smoke.py)
    - apps/api/backends/protocol.py (BackendAdapter Protocol + Message + AdapterOptions — what FakeStreamingAdapter implements)
    - apps/api/backends/chunks.py (ChatChunk discriminated union — what FakeStreamingAdapter yields)
    - .planning/phases/03-fastapi-service-persistent-storage/03-RESEARCH.md §"Pattern 6" lines 457-515 (the canonical httpx.AsyncClient + ASGITransport conftest pattern + Pitfall 4 finite-consume requirement)
    - .planning/phases/03-fastapi-service-persistent-storage/03-RESEARCH.md §"Pattern 4" lines 364-383 (aiosqlite_inmemory_db single-connection rationale + Pitfall 2 per-connection isolation)
    - .planning/phases/03-fastapi-service-persistent-storage/03-PATTERNS.md §"Excerpt 7" lines 394-412 (lazy adapter import inside fixture)
    - .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md §"Wave 0 Requirements" lines 69-79 (3 core fixtures + fake adapter + seed SQL + negative-grep step)
    - .planning/phases/03-fastapi-service-persistent-storage/03-CONTEXT.md D-13 (schema shape to seed)
    - .planning/phases/03-fastapi-service-persistent-storage/03-CONTEXT.md specifics line 346 (seed file content: 1 thread + 2 messages + 1 routing_decisions)
  </read_first>
  <action>
Write `apps/api/tests/__init__.py` as an empty package marker (or single-line docstring; mirror `apps/api/backends/tests/__init__.py`).

Write `apps/api/tests/conftest.py` mirroring `apps/api/backends/tests/conftest.py`:

- Module header per Phase 2 conftest style: docstring describing session-scoped vs function-scoped fixtures, B3 lazy-import rationale.
- `from __future__ import annotations`, stdlib imports, `pytest`, `httpx`, `aiosqlite`.
- `REPO_ROOT = os.path.abspath(os.path.join(__file__, "..", "..", "..", ".."))` — same shape as `apps/api/backends/tests/conftest.py` line 29.
- Three core async fixtures:

  1. `aiosqlite_inmemory_db` — async, function-scoped (NOT session — Pitfall 2: every test gets a fresh `:memory:` DB). LAZY-import `apps.api.db.migrate.up_to_latest` and `apps.api.db.connect.open_db` inside the fixture body wrapped in `try: ... except ImportError: pytest.skip("Wave 1 db/ modules not yet implemented")`. Yield the connection; close on teardown.
     - Pragmas applied via `open_db` (which sets WAL, busy_timeout, foreign_keys, synchronous=NORMAL).
     - DO NOT use the `aiosqlitepool` pattern (CONTEXT anti-pattern).
     - `:memory:` per-test rationale: Pitfall 2 — multiple connections to `":memory:"` are separate DBs, but a single shared connection per test is fine.

  2. `asgi_client(app)` — async context-manager fixture (function-scoped). Takes a FastAPI `app` parameter (provided by `app_factory`), returns `httpx.AsyncClient(transport=httpx.ASGITransport(app=app), base_url="http://test")` inside an `async with` block. **NEVER use `fastapi.testclient.TestClient`** (D-20 / API-08; negative-grep guard runs in CI).

  3. `app_factory` — synchronous fixture returning a factory callable. The factory accepts `adapters_override: dict[str, BackendAdapter] | None = None`, `settings_override: dict | None = None`, `keystore_override: KeyStore | None = None`. Inside the factory body, LAZY-import `apps.api.main:create_app` wrapped in `try: ... except ImportError: pytest.skip("Wave 2 main.py not yet implemented")`. The factory builds the app, then monkeypatches `app.state.adapters = adapters_override or {}`, `app.state.settings = settings_override or {...sensible defaults...}`, `app.state.keystore = keystore_override or KeyStore()` BEFORE the test runs (the lifespan honors these overrides instead of computing fresh values).

Every conftest fixture body MUST use the B3 try/except ImportError → pytest.skip pattern for ANY import that depends on Wave 1-6 modules. Specifically:
- `from apps.api.main import create_app` → try/except ImportError → `pytest.skip("Wave 2 main.py not yet implemented")`
- `from apps.api.lifespan import lifespan` → try/except ImportError → `pytest.skip("Wave 2 lifespan.py not yet implemented")`
- `from apps.api.db.connect import open_db` → try/except ImportError → `pytest.skip("Wave 1 db/connect.py not yet implemented")`
- `from apps.api.db.migrate import up_to_latest` → try/except ImportError → `pytest.skip("Wave 1 db/migrate.py not yet implemented")`

Write `apps/api/tests/fake_adapter.py`:

- Module docstring: "Fake `BackendAdapter` for unit tests. Pre-built `list[ChatChunk]` driven; optional `sleep_per_chunk` for heartbeat tests."
- `from __future__ import annotations`, stdlib imports.
- Import `ChatChunk`, `Done` from `apps.api.backends.chunks`; import `BackendAdapter`, `Message`, `AdapterOptions` from `apps.api.backends.protocol`.
- `class FakeStreamingAdapter(BackendAdapter):` — implements the Protocol via duck-typing.
  - `def __init__(self, chunks: list[ChatChunk], *, sleep_per_chunk: float = 0.0): self._chunks = chunks; self._sleep_per_chunk = sleep_per_chunk`
  - `async def stream(self, prompt: str, history: list[Message], options: AdapterOptions) -> AsyncIterator[ChatChunk]`:
    - For each chunk in `self._chunks`: if `self._sleep_per_chunk > 0`, `await asyncio.sleep(self._sleep_per_chunk)`; `yield chunk`.
    - The fake honors the Phase 2 D-04 terminal-Done invariant: tests passing chunk lists MUST include a `Done` at the end. The fake does not synthesize one.
- This class will be used by Wave 4 (`test_turn_streaming.py` for streaming + heartbeat tests via `sleep_per_chunk=0.6` with monkeypatched `DEFAULT_PING_INTERVAL=0.5`) and Wave 6 (`test_rename.py` for the one-shot title-collect pattern).

Write `apps/api/tests/fixtures/schema_v0_seed.sql`. Contents per CONTEXT specifics line 346 — must seed 1 thread + 2 messages + 1 routing_decisions row using ONLY columns that exist in `schema_v0.sql` (which Wave 1 authors per D-13). The seed uses literal IDs / timestamps so Wave 1's `test_v0_to_v1_preserves_data` can assert them post-migration:

```sql
INSERT INTO threads (id, title, created_at, updated_at)
  VALUES ('thr_seed_0001', 'Seed Thread', '2026-01-01T00:00:00Z', '2026-01-01T00:00:00Z');
INSERT INTO messages (id, thread_id, role, content_blocks, text, backend_used, model_used,
                      cost_usd, latency_ms, tokens_in, tokens_out, created_at, status)
  VALUES ('msg_seed_user_0001', 'thr_seed_0001', 'user', '[]', 'hello',
          NULL, NULL, NULL, NULL, NULL, NULL, '2026-01-01T00:00:01Z', 'complete');
INSERT INTO messages (id, thread_id, role, content_blocks, text, backend_used, model_used,
                      cost_usd, latency_ms, tokens_in, tokens_out, created_at, status)
  VALUES ('msg_seed_asst_0001', 'thr_seed_0001', 'assistant', '[]', 'hi back',
          'openrouter', 'openai/gpt-5', 0.0001, 250, 5, 7, '2026-01-01T00:00:02Z', 'complete');
INSERT INTO routing_decisions (id, message_id, task_type, task_confidence, agentic_intent,
                               agentic_confidence, predicted_model, rationale, signals, decided_at)
  VALUES ('rd_seed_0001', 'msg_seed_asst_0001', 'chat', 0.95, 0, 0.10,
          'openai/gpt-5', 'short conversational', '{}', '2026-01-01T00:00:01Z');
```

The seed must NOT reference any v1 columns (would break the round-trip test). The chosen IDs (`thr_seed_0001`, etc.) are 13 chars — collision-free, paste-safe, distinct from production `secrets.token_urlsafe(12)` IDs.
  </action>
  <verify>
    <automated>python -c "from apps.api.tests.fake_adapter import FakeStreamingAdapter; from apps.api.backends.chunks import Done, TextDelta; from apps.api.backends.protocol import AdapterOptions; import asyncio; async def t(): a = FakeStreamingAdapter([TextDelta(text='hi'), Done()]); chunks = [c async for c in a.stream('p', [], AdapterOptions())]; assert len(chunks) == 2; assert chunks[-1].type == 'done'; print('OK fake_adapter'); asyncio.run(t())" && uv run pytest --collect-only apps/api/tests/conftest.py apps/api/tests/fake_adapter.py 2>&1 | tail -10 && ! grep -E "^from apps\\.api\\.main import|^from apps\\.api\\.lifespan import|^from apps\\.api\\.db\\..*import" apps/api/tests/conftest.py && grep -c "try:" apps/api/tests/conftest.py | awk '{ if ($1 < 3) { print "FAIL: expected >=3 try/except blocks (one per lazy import)"; exit 1 } else { print "OK "$1" try blocks" } }' && grep -q "ASGITransport" apps/api/tests/conftest.py && grep -q "asyncio.sleep" apps/api/tests/fake_adapter.py && grep -q "INSERT INTO threads" apps/api/tests/fixtures/schema_v0_seed.sql && grep -q "thr_seed_0001" apps/api/tests/fixtures/schema_v0_seed.sql && grep -c "INSERT INTO messages" apps/api/tests/fixtures/schema_v0_seed.sql | awk '{ if ($1 < 2) { print "FAIL: expected 2 message rows"; exit 1 } else { print "OK 2 messages" } }'</automated>
  </verify>
  <done>
- `apps/api/tests/__init__.py` exists.
- `apps/api/tests/conftest.py` exists with the 3 core fixtures + B3 lazy imports.
- `apps/api/tests/fake_adapter.py` exists with `FakeStreamingAdapter` implementing the Protocol.
- `apps/api/tests/fixtures/schema_v0_seed.sql` exists with 1 thread + 2 messages + 1 routing_decisions row.
- Pytest can collect these files without ImportError.
  </done>
  <acceptance_criteria>
- Source: `grep -q "from __future__ import annotations" apps/api/tests/conftest.py apps/api/tests/fake_adapter.py`.
- Source: `grep -q "ASGITransport" apps/api/tests/conftest.py`.
- Source: `grep -q "httpx.AsyncClient" apps/api/tests/conftest.py`.
- Source: `grep -q "aiosqlite.connect" apps/api/tests/conftest.py`.
- Source: `grep -c "try:" apps/api/tests/conftest.py` returns ≥ 3 (one lazy import per Wave 1/2/3 producer module).
- Source: `grep -c "pytest.skip" apps/api/tests/conftest.py` returns ≥ 3 (the try/except ImportError → pytest.skip triplet).
- Source: `! grep -E "^from apps\\.api\\.main import" apps/api/tests/conftest.py` (NO module-level imports of Wave 2+ producer code; B3 fix).
- Source: `! grep -E "^from apps\\.api\\.lifespan import" apps/api/tests/conftest.py`.
- Source: `! grep -E "^from apps\\.api\\.db\\." apps/api/tests/conftest.py`.
- Source: `grep -q "class FakeStreamingAdapter" apps/api/tests/fake_adapter.py`.
- Source: `grep -q "sleep_per_chunk" apps/api/tests/fake_adapter.py`.
- Source: `grep -q "INSERT INTO threads" apps/api/tests/fixtures/schema_v0_seed.sql`.
- Source: `grep -c "INSERT INTO messages" apps/api/tests/fixtures/schema_v0_seed.sql` returns 2.
- Source: `grep -q "INSERT INTO routing_decisions" apps/api/tests/fixtures/schema_v0_seed.sql`.
- Source: `grep -q "thr_seed_0001" apps/api/tests/fixtures/schema_v0_seed.sql` (seed ID is the literal Wave 1's `test_v0_to_v1_preserves_data` will assert).
- Behavior: `uv run pytest --collect-only apps/api/tests/conftest.py apps/api/tests/fake_adapter.py 2>&1` exits 0 (collection works).
- Behavior: `python -c "from apps.api.tests.fake_adapter import FakeStreamingAdapter; print('ok')"` exits 0.
- Behavior: D-18 guard still green after Wave 0 lands: `uv run pytest src/routing/tests/test_decide_smoke.py -x 2>&1 | tail -3` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Author Wave 0 sanity test (test_smoke.py) + run negative-grep API-08 guard</name>
  <files>apps/api/tests/test_smoke.py</files>
  <read_first>
    - apps/api/paths.py (this plan's Task 2 output — what Wave 0 sanity verifies)
    - apps/api/tests/conftest.py (this plan's Task 3 output — fixture set the smoke test exercises)
    - apps/api/tests/fake_adapter.py (this plan's Task 3 output — class to import)
    - .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md row "3-00-02" line 46 (test_smoke.py expected behavior — Wave 0 sanity test)
    - .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md row "3-00-01" line 45 (the negative-grep guard step API-08 enforces)
    - .planning/phases/03-fastapi-service-persistent-storage/03-PATTERNS.md anti-pattern audit lines 522-543 (the `! grep -rE 'from fastapi.testclient|fastapi\.testclient\.TestClient' apps/api/tests/` guard)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-VERIFICATION.md (Phase 2 closing state — confirms what "green" looks like for the whole-repo suite)
  </read_first>
  <action>
Write `apps/api/tests/test_smoke.py`:

- Module docstring: "Wave 0 sanity test — confirms Phase 3 deps installed, paths.py constants well-formed, conftest fixtures collect."
- `from __future__ import annotations`, `pytest`.
- Test functions (all typed `def test_X() -> None:` per Phase 2 style):

  1. `test_phase_3_deps_importable` — inline `import fastapi; import uvicorn; import aiosqlite; from sse_starlette.sse import EventSourceResponse; from httpx_sse import aconnect_sse`. Assert nothing — pass if no ImportError.

  2. `test_paths_constants_well_formed` — inline `from apps.api.paths import PROJECT_ROOT, USER_HOME, DB_PATH, BLOBS_DIR, SETTINGS_PATH, WORKSPACES_DIR, JSONL_LOG_PATH`. Assert `PROJECT_ROOT.is_dir()`; assert `str(DB_PATH).endswith("chat.db")`; assert `str(BLOBS_DIR).endswith("blobs")`; assert `str(SETTINGS_PATH).endswith("settings.json")`; assert `str(WORKSPACES_DIR).endswith("workspaces")`; assert `str(JSONL_LOG_PATH).endswith(".planning/data/routing_decisions.jsonl")`.

  3. `test_paths_honors_env_override(monkeypatch)` — `monkeypatch.setenv("PROMPT_OPTIMIZER_HOME", "/tmp/po-test")`. Force-reload `apps.api.paths` via `importlib.reload(apps.api.paths)`. Assert `apps.api.paths.USER_HOME == Path("/tmp/po-test")`.

  4. `test_fake_streaming_adapter_yields_chunks` — async test. Inline: `from apps.api.tests.fake_adapter import FakeStreamingAdapter; from apps.api.backends.chunks import TextDelta, Done; from apps.api.backends.protocol import AdapterOptions`. Construct `FakeStreamingAdapter([TextDelta(text="hi"), Done()])`. Iterate; assert 2 chunks; assert last is `Done`.

  5. `test_no_testclient_imports_under_apps_api_tests` — a meta-test that runs `subprocess.run(["grep", "-rE", "from fastapi.testclient|fastapi.testclient.TestClient", "apps/api/tests/"])` and asserts exit code is 1 (no matches found). This is the API-08 / D-20 negative-grep guard. If a future executor adds a `TestClient` import, this test fails.

All tests live in `apps/api/tests/` so they participate in the Phase 3 quick-run suite (`uv run pytest -m 'not live' apps/api/tests -x --timeout=30`).

Do NOT add `@pytest.mark.live` to any of these (Wave 0 sanity tests have zero network/dep cost).

Do NOT use `TestClient` in this test or anywhere under `apps/api/tests/` (the negative-grep guard would catch it; same applies to all Wave 1-6 plans).
  </action>
  <verify>
    <automated>uv run pytest apps/api/tests/test_smoke.py -x --timeout=30 2>&1 | tail -10 && ! grep -rE 'from fastapi.testclient|fastapi\.testclient\.TestClient' apps/api/tests/ && uv run pytest -m 'not live' -x 2>&1 | tail -5</automated>
  </verify>
  <done>
- `apps/api/tests/test_smoke.py` passes 5 sub-tests.
- Negative-grep API-08 guard exits with code 1 (no TestClient imports under `apps/api/tests/`).
- Whole-repo non-live test suite still green after Wave 0 lands.
  </done>
  <acceptance_criteria>
- Behavior: `uv run pytest apps/api/tests/test_smoke.py -x --timeout=30` exits 0 (5 sub-tests pass).
- Behavior: `! grep -rE 'from fastapi.testclient|fastapi\\.testclient\\.TestClient' apps/api/tests/` exits 0 (negative grep — NO TestClient imports anywhere under apps/api/tests/).
- Behavior: `uv run pytest -m 'not live' -x 2>&1 | tail -5` exits 0 (Phase 2 suite still green after Wave 0 lands).
- Source: `grep -q "def test_phase_3_deps_importable" apps/api/tests/test_smoke.py`.
- Source: `grep -q "def test_paths_constants_well_formed" apps/api/tests/test_smoke.py`.
- Source: `grep -q "def test_paths_honors_env_override" apps/api/tests/test_smoke.py`.
- Source: `grep -q "def test_fake_streaming_adapter_yields_chunks" apps/api/tests/test_smoke.py`.
- Source: `grep -q "def test_no_testclient_imports_under_apps_api_tests" apps/api/tests/test_smoke.py`.
- Source: `! grep -q "TestClient" apps/api/tests/test_smoke.py` (the test ABOUT TestClient absence must not itself import TestClient — uses subprocess + grep).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Sign off VALIDATION.md as nyquist-compliant (Wave 0 closes the test-infrastructure gate)</name>
  <files>.planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md</files>
  <read_first>
    - .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md (current frontmatter has `nyquist_compliant: false`, `wave_0_complete: false`, `status: draft`; Validation Sign-Off section at lines 109-118 lists 6 unchecked boxes)
  </read_first>
  <action>
Edit `.planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md` in place to mark Wave 0 complete and the validation contract as nyquist-compliant. Specifically:

1. Change frontmatter:
   - `status: draft` → `status: signed`
   - `nyquist_compliant: false` → `nyquist_compliant: true`
   - `wave_0_complete: false` → `wave_0_complete: true`

2. Update the "Per-Task Verification Map" rows for tasks `3-00-01` and `3-00-02`:
   - Status column: `⬜ pending` → `✅ green`
   - File Exists column: `❌ Wave 0` → `✅ landed`

3. Update the "Validation Sign-Off" section at the bottom:
   - Check all 6 boxes (`[ ]` → `[x]`)
   - Change `**Approval:** pending` to `**Approval:** signed — 2026-05-16`

Preserve every other line of the file verbatim. Use the Read tool to load the file first, then Edit-tool patch the specific lines. Do NOT use Write to overwrite — the file has 118 lines of complex content that must stay intact.
  </action>
  <verify>
    <automated>grep -q "^nyquist_compliant: true$" .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md && grep -q "^wave_0_complete: true$" .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md && grep -q "^status: signed$" .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md && grep -q "\\*\\*Approval:\\*\\* signed" .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md && grep -c "\\[x\\]" .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md | awk '{ if ($1 < 6) { print "FAIL: expected ≥6 checked boxes in Validation Sign-Off"; exit 1 } else { print "OK "$1" boxes checked" } }'</automated>
  </verify>
  <done>
- VALIDATION.md frontmatter: `status: signed`, `nyquist_compliant: true`, `wave_0_complete: true`.
- Approval line: `**Approval:** signed — 2026-05-16`.
- 6 Validation Sign-Off boxes checked.
  </done>
  <acceptance_criteria>
- Source: `grep -q "^nyquist_compliant: true$" .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md`.
- Source: `grep -q "^wave_0_complete: true$" .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md`.
- Source: `grep -q "^status: signed$" .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md`.
- Source: `grep -q "\\*\\*Approval:\\*\\* signed" .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md`.
- Source: `grep -c "\\[x\\]" .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md` returns ≥ 6 (all 6 Validation Sign-Off boxes checked).
- Behavior: `wc -l .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md` returns a count >= 118 (no lines deleted; only frontmatter + boxes flipped).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pyproject.toml → uv.lock` | Lockfile changes commit-staged; Phase 2 pre-commit hook blocks `claude-code-sdk` reintroduction. |
| `.env.example` | Repo-tracked file; MUST NOT contain real `sk-…` / `sk-ant-…` values (Phase 2 SECURE-02 pre-commit grep would block). |
| `.planning/data/` | Gitignored; runtime-created path for STORE-06 JSONL log. |
| `apps/api/tests/conftest.py` lazy imports | Producer modules from Wave 1-6 — try/except ImportError → pytest.skip pattern prevents collection-time failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-Mig | T (Tampering) | `apps/api/tests/fixtures/schema_v0_seed.sql` | accept | Seed file is committed; tampering would only break the migration round-trip test in Wave 1, not production. Severity: LOW. |
| T-03-Disclo (forward) | I (Information Disclosure) | `.env.example` | mitigate | `.env.example` template ships with empty values + comments only; Phase 2 SECURE-02 pre-commit hook blocks any real key shape from entering. |
| T-03-Disclo (forward) | I | `.gitignore` extension for `.planning/data/` | mitigate | Forward-compat with Wave 4's STORE-06 JSONL log; prevents per-turn routing decisions from entering git history. Severity: MEDIUM (decisions include `signals` dict with stage telemetry; not key material, but still sensitive). |
| T-03-Pre-commit | T (Tampering) | `pyproject.toml` Phase 3 deps | mitigate | Phase 2 OSS-06 pre-commit + CI guard still active; any reintroduction of `claude-code-sdk` blocked at commit + at CI lockfile check. |
</threat_model>

<verification>
- `uv lock --check` exits 0 (pyproject.toml + uv.lock consistent).
- `uv run pytest apps/api/tests/test_smoke.py -x --timeout=30` exits 0 (5 sub-tests pass).
- `uv run pytest src/routing/tests/test_decide_smoke.py -x` exits 0 (Phase 1 D-18 guard still green after deps added).
- `uv run pytest -m 'not live' -x` exits 0 (Phase 2 whole-repo suite still green; no regression).
- `! grep -rE 'from fastapi.testclient|fastapi\.testclient\.TestClient' apps/api/tests/` exits 0 (API-08 negative grep clean).
- `python -c "from apps.api.paths import PROJECT_ROOT, USER_HOME, DB_PATH, BLOBS_DIR, SETTINGS_PATH, WORKSPACES_DIR, JSONL_LOG_PATH; print('ok')"` exits 0.
- `python -c "from apps.api.tests.fake_adapter import FakeStreamingAdapter; print('ok')"` exits 0.
- `grep -q "^nyquist_compliant: true$" .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md` (sign-off complete).
- `grep -q "^.planning/data/" .gitignore` (gitignore extension landed).
- `grep -q "OPENROUTER_API_KEY=" .env.example` (env template landed).
</verification>

<success_criteria>
- 4 new base dependencies and 1 new dev dep added to `pyproject.toml`; `uv.lock` regenerated; `claude-code-sdk` still absent.
- `apps/api/paths.py` exists with 7 path constants honouring `PROMPT_OPTIMIZER_HOME` env override.
- `apps/api/tests/{__init__,conftest,fake_adapter}.py` + `apps/api/tests/fixtures/schema_v0_seed.sql` exist; all are collectable by pytest in Wave 0 BEFORE Wave 1-6 modules land (B3 lazy-import pattern).
- `apps/api/tests/test_smoke.py` passes 5 sub-tests; the API-08 negative-grep guard is enforceable.
- `.env.example` exists with the 5 documented env vars (no real key values).
- `.gitignore` excludes `.planning/data/`.
- `03-VALIDATION.md` marked `nyquist_compliant: true` and `wave_0_complete: true`; Validation Sign-Off section signed.
- Phase 1 D-18 import-graph guard still green.
- Phase 2 whole-repo non-live test suite still green.
- All 10 Wave 0 truths from `must_haves.truths` are observable from a CLI / pytest run.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fastapi-service-persistent-storage/03-00-SUMMARY.md` summarizing:
- 5 new files created (`apps/api/paths.py`, `apps/api/tests/__init__.py`, `apps/api/tests/conftest.py`, `apps/api/tests/fake_adapter.py`, `apps/api/tests/fixtures/schema_v0_seed.sql`, `apps/api/tests/test_smoke.py`).
- 3 files modified (`pyproject.toml`, `uv.lock`, `.gitignore`).
- 1 new file in repo root (`.env.example`).
- 1 sign-off update (`03-VALIDATION.md`: nyquist_compliant + wave_0_complete + 6 boxes checked).
- Deviation note: `sse-starlette` range widened to `>=2.1,<4.0` per RESEARCH Open Question 1 (CONTEXT D-06 originally locked `<3.0` but `mcp` transitive pins 3.x).
- Confirmation that Phase 1 D-18 guard still passes.
- Confirmation that Phase 2 whole-repo non-live suite still passes.
- The 4 Wave 0 truths verified at a CLI / pytest level.
</output>
</content>
