---
phase: 03
plan: 02
plan_id: "03-02-app-shell-and-healthz"
type: execute
wave: 2
depends_on: ["03-01-storage-layer"]
files_modified:
  - "apps/api/lifespan.py"
  - "apps/api/main.py"
  - "apps/api/settings.py"
  - "apps/api/routes/__init__.py"
  - "apps/api/routes/health.py"
  - "apps/api/tests/test_health.py"
  - "apps/api/tests/test_cors.py"
autonomous: true
requirements: [API-01, OSS-05]
user_setup: []
must_haves:
  truths:
    - "All Phase 3 source files use `pathlib.Path(__file__).resolve().parents[N]` for path resolution; no `sys.path.append` anywhere (CONTEXT anti-pattern; codebase ARCHITECTURE.md)."
    - "All Phase 3 modules import `apps.api.*` first to trigger `dotenv.load_dotenv()` + `install_redaction_filter()` from `apps/api/__init__.py`; no module re-installs the redaction filter (D-19 / Phase 2 D-11)."
    - "`from apps.api.main import app, create_app` imports succeed; `create_app()` returns a `FastAPI` instance with `title='Prompt-Optimizer API'`, `version='0.1.0'`, and `lifespan` registered."
    - "`apps/api/lifespan.py` defines `@asynccontextmanager async def lifespan(app)` that, when entered, opens the DB, runs migrations, loads joblib artifacts via Phase 1 `_load_default_artifacts()` from `src.routing.decide` (returns the canonical dict with keys `task_type_classifier`, `agentic_intent_classifier`, `model_router`, `model_mapping`), instantiates `KeyStore`, loads settings JSON, sets `app.state.adapters = {}`, caches `app.state.schema_version` (D-15 + discretion line 178)."
    - "`apps/api/settings.py` exports `load_settings_file()`, `write_settings_file(settings)`, `computer_use_enabled(settings)`; `write_settings_file` uses `tmp.write_text(...); tmp.replace(target)` atomic pattern (D-11 + Example 5)."
    - "`computer_use_enabled(settings)` returns True ONLY when `os.environ.get('COMPUTER_USE_OPT_IN') == '1'` AND `settings['computer_use_opt_in'] is True` (D-12 STRICT AND-semantics)."
    - "`GET /api/v1/healthz` returns 200 with body `{status, artifacts_loaded, db_ok, schema_version, adapters: {openrouter, claude_code, computer_use}, version}` where each adapter status is `Literal['ready','missing_key','opt_out','error']` and the check is read-only (NEVER constructs an adapter — D-18)."
    - "CORS middleware is installed with `allow_origins=['http://localhost:3000']` as the default when `PROMPT_OPTIMIZER_CORS_ORIGINS` env var is unset; with the env var set to `http://a.com,http://b.com` the allowlist becomes `['http://a.com', 'http://b.com']`; NEVER `['*']` (OSS-05)."
    - "OPTIONS preflight request with `Origin: http://localhost:3000` returns 200 with header `Access-Control-Allow-Origin: http://localhost:3000`; OPTIONS preflight with `Origin: http://evil.example` does NOT return that origin (browser rejects the response)."
    - "`uvicorn apps.api.main:app` (no actual binding, just `python -c 'from apps.api.main import app'`) imports in <3 seconds on a warm laptop (ROADMAP Phase 3 SC #1; Pitfall 9 — joblib mmap cache makes the second import fast)."
    - "`uv run pytest apps/api/tests/test_health.py apps/api/tests/test_cors.py -x` exits 0 with at least 4 sub-tests covering: artifacts_loaded_once, pragmas_applied, healthz_status_dots (per-backend statuses), cors_preflight_allowed + cors_preflight_rejected + cors_env_override."
  artifacts:
    - path: "apps/api/lifespan.py"
      provides: "`@asynccontextmanager async def lifespan(app: FastAPI)` per D-15 + discretion line 178 — opens DB → migrates → loads artifacts → loads settings → instantiates KeyStore → empty adapter registry → caches schema_version"
      contains: "asynccontextmanager|async def lifespan|app.state.db|app.state.artifacts|app.state.settings|app.state.keystore|app.state.adapters = \\{\\}|app.state.schema_version|await up_to_latest|await open_db|_load_default_artifacts|task_type_classifier|agentic_intent_classifier|model_router|finally:"
      min_lines: 40
    - path: "apps/api/main.py"
      provides: "`create_app() -> FastAPI` factory + module-level `app = create_app()` + CORSMiddleware (OSS-05) + lifespan registration + route inclusion"
      contains: "def create_app|FastAPI\\(lifespan=lifespan|CORSMiddleware|PROMPT_OPTIMIZER_CORS_ORIGINS|allow_origins|allow_credentials=True|include_router\\(health.router\\)|app = create_app\\(\\)"
      min_lines: 30
    - path: "apps/api/settings.py"
      provides: "Settings file I/O + `computer_use_enabled(settings)` STRICT AND-semantics (D-11 + D-12)"
      contains: "def load_settings_file|def write_settings_file|def computer_use_enabled|tmp.replace|computer_use_opt_in|COMPUTER_USE_OPT_IN"
      min_lines: 50
    - path: "apps/api/routes/__init__.py"
      provides: "Package marker for `apps.api.routes`"
    - path: "apps/api/routes/health.py"
      provides: "`GET /api/v1/healthz` per D-18 — adapter-status read-only precheck, never constructs adapters"
      contains: "APIRouter|/api/v1/healthz|app.state.keystore|app.state.settings|app.state.artifacts|app.state.schema_version|computer_use_enabled|status_code=200|Literal\\[.ready., .missing_key., .opt_out., .error.\\]"
    - path: "apps/api/tests/test_health.py"
      provides: "3 sub-tests: artifacts_loaded_once (API-01), pragmas_applied (STORE-01 carry-forward via lifespan), healthz_status_dots (per-backend status enumeration)"
      contains: "test_artifacts_loaded_once|test_pragmas_applied|test_healthz_status_dots"
    - path: "apps/api/tests/test_cors.py"
      provides: "3 sub-tests: cors_preflight_allowed_for_localhost, cors_preflight_rejected_for_evil, cors_env_override"
      contains: "test_cors_preflight_allowed_for_localhost|test_cors_preflight_rejected_for_evil|test_cors_env_override|OPTIONS|Origin"
  key_links:
    - from: "apps/api/lifespan.py"
      to: "apps.api.db.connect.open_db"
      via: "lifespan call at startup"
      pattern: "await open_db\\(DB_PATH\\)"
    - from: "apps/api/lifespan.py"
      to: "apps.api.db.migrate.up_to_latest"
      via: "lifespan call after open_db"
      pattern: "await up_to_latest\\(app\\.state\\.db\\)"
    - from: "apps/api/lifespan.py"
      to: "src.routing.decide._load_default_artifacts"
      via: "lifespan call after migrations — canonical 3-classifier + model_mapping loader (Phase 1 / CONTEXT discretion line 178)"
      pattern: "_load_default_artifacts"
    - from: "apps/api/main.py"
      to: "fastapi.middleware.cors.CORSMiddleware"
      via: "add_middleware with PROMPT_OPTIMIZER_CORS_ORIGINS-honoring origins list"
      pattern: "CORSMiddleware"
    - from: "apps/api/routes/health.py"
      to: "apps.api.settings.computer_use_enabled"
      via: "read-only opt-in check for computer-use adapter status"
      pattern: "computer_use_enabled"
---

<objective>
Wave 2 of Phase 3 — author the FastAPI app shell. This includes the lifespan (D-15 + discretion line 178 ordering: open DB → migrate → load joblib → load settings → KeyStore → empty adapter registry → cache schema_version), the main app constructor with CORS middleware (OSS-05 — explicit `http://localhost:3000`, env override, NEVER wildcard), the settings.py module (D-11 atomic write + D-12 STRICT AND-semantics for computer-use), the rich `/api/v1/healthz` endpoint (D-18 — read-only adapter status enumeration), and the tests that prove all of the above.

Purpose: This is the gate that lets `uvicorn apps.api.main:app` boot. After Wave 2 lands, the server starts, serves `GET /api/v1/healthz`, and reports per-backend status dots BUT has zero actual routes for threads/turn/settings/rename — those land in Wave 3+. The lazy adapter pattern (D-15) means the server boots successfully even when `ANTHROPIC_API_KEY` and `COMPUTER_USE_OPT_IN` are unset.

Output:
  - 5 source files at `apps/api/{lifespan,main,settings}.py` + `apps/api/routes/{__init__,health}.py`.
  - 2 test files at `apps/api/tests/{test_health,test_cors}.py`.
  - The Phase 2 + Phase 3 Waves 0-1 non-live test suites all stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-fastapi-service-persistent-storage/03-CONTEXT.md
@.planning/phases/03-fastapi-service-persistent-storage/03-RESEARCH.md
@.planning/phases/03-fastapi-service-persistent-storage/03-PATTERNS.md
@.planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md
@.planning/phases/03-fastapi-service-persistent-storage/03-00-SUMMARY.md
@.planning/phases/03-fastapi-service-persistent-storage/03-01-SUMMARY.md
@apps/api/__init__.py
@apps/api/paths.py
@apps/api/backends/keystore.py
@apps/api/backends/protocol.py
@apps/api/backends/chunks.py
@apps/api/db/connect.py
@apps/api/db/migrate.py
@apps/api/db/queries.py
@apps/api/tests/conftest.py
@apps/api/tests/fake_adapter.py
@src/demo/demo_router.py

<interfaces>
<!-- Types Wave 3-6 will consume from this plan. Wave 3-6 executors should NOT redefine these. -->

From `apps/api/lifespan.py` (this plan creates):
- `@asynccontextmanager async def lifespan(app: FastAPI)` — D-15 + discretion line 178 ordering.
- Lifespan populates: `app.state.db: aiosqlite.Connection`, `app.state.artifacts: dict[str, Any]`, `app.state.settings: dict`, `app.state.keystore: KeyStore`, `app.state.adapters: dict[Backend, BackendAdapter] = {}`, `app.state.schema_version: int` (per RESEARCH Open Question 5 — cached at startup, never re-read).

From `apps/api/main.py` (this plan creates):
- `def create_app() -> FastAPI` — factory; mounts CORS + lifespan + routers (only `health.router` in Wave 2; later waves grow the include_router list).
- `app = create_app()` — module-level binding for `uvicorn apps.api.main:app`.

From `apps/api/settings.py` (this plan creates):
- `def load_settings_file() -> dict` — reads `SETTINGS_PATH`; returns `_default_settings()` dict if missing.
- `def write_settings_file(settings: dict) -> None` — atomic `tmp.write_text + tmp.replace(target)`.
- `def computer_use_enabled(settings: dict) -> bool` — D-12 STRICT AND.
- `def _default_settings() -> dict` — returns `{"backends_enabled": {"openrouter": True, "claude_code": True, "computer_use": False}, "computer_use_opt_in": False, "default_max_cost_usd": 0.50}`.

From `apps/api/routes/health.py` (this plan creates):
- `router = APIRouter(prefix="/api/v1", tags=["health"])`
- `@router.get("/healthz")` — returns the rich D-18 status payload.

From Phase 1 `src/routing/decide.py` (already exists; imported by lifespan):
- `_load_default_artifacts() -> dict` — the canonical loader Phase 3 reuses per CONTEXT discretion line 178. Returns a dict with the four canonical keys (`task_type_classifier`, `agentic_intent_classifier`, `model_router`, `model_mapping`) that `src.routing.decide.decide()` consumes verbatim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author apps/api/settings.py — load/write atomic + computer_use_enabled STRICT AND</name>
  <files>apps/api/settings.py</files>
  <read_first>
    - apps/api/backends/pricing.py (`from_static` JSON load + module-level `logger`; settings.py mirrors style)
    - apps/api/backends/keystore.py (env-var-aware reads via `os.environ.get`; settings.py mirrors for `computer_use_enabled`)
    - apps/api/backends/computer_use/adapter.py lines 210-223 (the Phase 2 single-gate check Phase 3 EXTENDS via D-12)
    - apps/api/paths.py (Wave 0 — `SETTINGS_PATH` constant)
    - .planning/phases/03-fastapi-service-persistent-storage/03-RESEARCH.md §"Pattern 13" lines 742-783 (D-12 STRICT AND canonical source) and §"Example 5" lines 1054-1065 (atomic JSON write pattern)
    - .planning/phases/03-fastapi-service-persistent-storage/03-CONTEXT.md D-11 (settings file location + atomic write) + D-12 (computer-use AND-semantics) + discretion line 177 (atomic-write tmp pattern)
    - .planning/phases/03-fastapi-service-persistent-storage/03-PATTERNS.md §"Excerpt 8" lines 415-440 (settings.py `computer_use_enabled` extension lift-source)
  </read_first>
  <behavior>
- Test 1: `load_settings_file()` with no file present returns a dict with default keys (`backends_enabled`, `computer_use_opt_in`, `default_max_cost_usd`).
- Test 2: `write_settings_file({...})` followed by `load_settings_file()` round-trips the values.
- Test 3: `write_settings_file({...})` is atomic: creates `tmp = SETTINGS_PATH.with_suffix(".json.tmp")` first, writes content, then `tmp.replace(SETTINGS_PATH)`.
- Test 4: `computer_use_enabled({...})` returns False when env var unset (regardless of `computer_use_opt_in`).
- Test 5: `computer_use_enabled({"computer_use_opt_in": False})` returns False when env var set to `"1"` (STRICT AND requires both).
- Test 6: `computer_use_enabled({"computer_use_opt_in": True})` returns True only when `os.environ["COMPUTER_USE_OPT_IN"] == "1"` is ALSO set.
- Test 7: `computer_use_enabled({"computer_use_opt_in": True})` with env var set to `"true"` (not `"1"`) returns False (strict equality with `"1"`).
  </behavior>
  <action>
Write `apps/api/settings.py`:

- `from __future__ import annotations` first.
- Module docstring per Phase 2 style: purpose ("Non-key settings I/O + computer-use opt-in helper"), Public surface (load_settings_file, write_settings_file, computer_use_enabled, _default_settings), Cross-refs (RESEARCH Pattern 13 + Example 5, CONTEXT D-11 + D-12, PATTERNS Excerpt 8).
- Stdlib imports: `json`, `os`, `logging`. Third-party: none.
- `from apps.api.paths import SETTINGS_PATH`.
- `logger = logging.getLogger(__name__)`.

Functions:

`def _default_settings() -> dict:` returns the canonical default shape:
```
{
    "backends_enabled": {
        "openrouter": True,
        "claude_code": True,
        "computer_use": False,
    },
    "computer_use_opt_in": False,
    "default_max_cost_usd": 0.50,
}
```

`def load_settings_file() -> dict:`:
- If `not SETTINGS_PATH.exists()`: return `_default_settings()`.
- Else: `with open(SETTINGS_PATH, "r", encoding="utf-8") as fh: return json.load(fh)`.

`def write_settings_file(settings: dict) -> None:` per RESEARCH Example 5 + CONTEXT discretion line 177:
- `SETTINGS_PATH.parent.mkdir(parents=True, exist_ok=True)`.
- `tmp = SETTINGS_PATH.with_suffix(SETTINGS_PATH.suffix + ".tmp")`.
- `tmp.write_text(json.dumps(settings, indent=2, sort_keys=True), encoding="utf-8")`.
- `tmp.replace(SETTINGS_PATH)` (atomic on POSIX + Windows when same filesystem; tmp in same directory).

`def computer_use_enabled(settings: dict) -> bool:` per D-12:
- `env_ok = os.environ.get("COMPUTER_USE_OPT_IN") == "1"` (STRICT — only the literal `"1"` enables).
- `setting_ok = bool(settings.get("computer_use_opt_in"))`.
- Return `env_ok and setting_ok`.

NEVER use a single-gate check (CONTEXT anti-pattern: "Do NOT couple computer-use opt-in to env-only").
NEVER use `os.path.join` (use `pathlib`).
NEVER `print(...)` settings content — settings can contain `default_max_cost_usd` which is fine, but the `keys` dict is owned by KeyStore not settings.json (D-11 explicit) so plaintext keys never enter this file.
  </action>
  <verify>
    <automated>python -c "
import os, json, tempfile
from pathlib import Path
# Monkey-patch SETTINGS_PATH for the test
tmp_dir = tempfile.mkdtemp(prefix='po-settings-')
os.environ['PROMPT_OPTIMIZER_HOME'] = tmp_dir
import importlib
import apps.api.paths
importlib.reload(apps.api.paths)
import apps.api.settings
importlib.reload(apps.api.settings)
from apps.api.settings import load_settings_file, write_settings_file, computer_use_enabled, _default_settings
# Test 1 + 2: default + round-trip
defaults = load_settings_file()
assert defaults['backends_enabled']['openrouter'] is True
assert defaults['default_max_cost_usd'] == 0.50
write_settings_file({'foo': 'bar', 'default_max_cost_usd': 0.25})
loaded = load_settings_file()
assert loaded == {'foo': 'bar', 'default_max_cost_usd': 0.25}
# Test 3: atomic write — confirm the tmp file is cleaned up after replace
assert (Path(tmp_dir) / 'settings.json').exists()
assert not (Path(tmp_dir) / 'settings.json.tmp').exists()
# Test 4-7: D-12 STRICT AND
os.environ.pop('COMPUTER_USE_OPT_IN', None)
assert computer_use_enabled({'computer_use_opt_in': True}) is False  # env unset
os.environ['COMPUTER_USE_OPT_IN'] = '1'
assert computer_use_enabled({'computer_use_opt_in': False}) is False  # setting off
assert computer_use_enabled({}) is False  # setting missing
assert computer_use_enabled({'computer_use_opt_in': True}) is True  # both gates
os.environ['COMPUTER_USE_OPT_IN'] = 'true'  # not '1'
assert computer_use_enabled({'computer_use_opt_in': True}) is False  # strict equality
print('OK settings.py D-11 + D-12')
" && grep -q "from __future__ import annotations" apps/api/settings.py && grep -q "computer_use_enabled" apps/api/settings.py && grep -q "tmp.replace" apps/api/settings.py</automated>
  </verify>
  <done>
- `apps/api/settings.py` exists with the 4 documented functions.
- `computer_use_enabled` enforces STRICT AND-semantics (D-12).
- `write_settings_file` uses atomic `tmp.replace(target)` pattern.
  </done>
  <acceptance_criteria>
- Source: `grep -q "from __future__ import annotations" apps/api/settings.py`.
- Source: `grep -q "def load_settings_file" apps/api/settings.py`.
- Source: `grep -q "def write_settings_file" apps/api/settings.py`.
- Source: `grep -q "def computer_use_enabled" apps/api/settings.py`.
- Source: `grep -q "def _default_settings" apps/api/settings.py`.
- Source: `grep -q "tmp.replace" apps/api/settings.py` (atomic-write pattern).
- Source: `grep -q "env_ok and setting_ok" apps/api/settings.py` OR equivalent strict AND expression.
- Source: `grep -q "COMPUTER_USE_OPT_IN" apps/api/settings.py`.
- Source: `grep -q "computer_use_opt_in" apps/api/settings.py`.
- Source: `! grep -q "sys.path.append" apps/api/settings.py`.
- Behavior: All 7 documented tests pass via the inline Python invocation in `<verify>`.
- Behavior: Phase 1 D-18 guard still green: `uv run pytest src/routing/tests/test_decide_smoke.py -x` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author apps/api/lifespan.py + apps/api/main.py (FastAPI app + CORS) + apps/api/routes/{__init__,health}.py</name>
  <files>apps/api/lifespan.py, apps/api/main.py, apps/api/routes/__init__.py, apps/api/routes/health.py</files>
  <read_first>
    - apps/api/__init__.py lines 35-59 (canonical side-effect-at-import — Phase 3 NEVER duplicates `load_dotenv` / `install_redaction_filter`)
    - apps/api/paths.py (Wave 0 — `DB_PATH`, `SETTINGS_PATH`)
    - apps/api/db/connect.py (Wave 1 — `open_db`)
    - apps/api/db/migrate.py (Wave 1 — `up_to_latest`)
    - apps/api/db/queries.py (Wave 1 — `read_schema_version`)
    - apps/api/settings.py (Task 1 output — `load_settings_file`, `computer_use_enabled`)
    - apps/api/backends/keystore.py (Wave 0 Phase 2 — `KeyStore` constructor signature)
    - src/routing/decide.py lines 120-175 (`_load_default_artifacts` — the canonical 3-classifier + model_mapping loader; lifespan calls verbatim per CONTEXT discretion line 178)
    - apps/api/backends/openrouter/adapter.py lines 80-180 (canonical streaming pattern style; lifespan adopts module structure)
    - .planning/phases/03-fastapi-service-persistent-storage/03-RESEARCH.md §"Pattern 1" lines 205-244 (lifespan canonical source), §"Pattern 9" lines 593-631 (CORS canonical source), §"Pattern 12" lines 699-740 (healthz canonical source), §"Example 9" lines 442-477 (defensive lifespan teardown)
    - .planning/phases/03-fastapi-service-persistent-storage/03-CONTEXT.md D-15 (lazy adapter cache + discretion line 178 lifespan order), D-18 (rich healthz adapter status), D-09 (`/api/v1` URL namespace), discretion line 175 (CORS env override)
    - .planning/phases/03-fastapi-service-persistent-storage/03-PATTERNS.md §"Routes" lines 22-26 (route_handler conventions, anti-pattern audit)
  </read_first>
  <behavior>
- Test 1 (`test_artifacts_loaded_once`): On lifespan startup, `_load_default_artifacts` is called exactly once; subsequent requests reuse `app.state.artifacts`. Verify by monkeypatching `apps.api.lifespan._load_default_artifacts` to a counter and asserting `count == 1` after two requests to `/api/v1/healthz`.
- Test 2 (`test_pragmas_applied` — lifespan integration): After lifespan starts, `PRAGMA foreign_keys` returns `1`, `PRAGMA busy_timeout` returns `5000`, on `app.state.db` (verifies D-03 still applied via lifespan path).
- Test 3 (`test_healthz_returns_200_with_required_keys`): `GET /api/v1/healthz` returns 200 with body containing keys: `status`, `artifacts_loaded`, `db_ok`, `schema_version`, `adapters`, `version`. `adapters` is a dict with keys `openrouter`, `claude_code`, `computer_use`.
- Test 4 (`test_healthz_status_dots`): With `OPENROUTER_API_KEY` set + `ANTHROPIC_API_KEY` unset + `COMPUTER_USE_OPT_IN` unset, healthz reports `adapters.openrouter.status == "ready"`, `adapters.claude_code.status == "missing_key"`, `adapters.computer_use.status == "opt_out"`. The overall `status` field is `"degraded"` (≥1 adapter not ready).
- Test 5 (`test_healthz_never_constructs_adapters`): Monkeypatch each adapter class constructor to raise; assert `/api/v1/healthz` returns 200 (the check is read-only — D-18).
- Test 6 (`test_cors_preflight_allowed_for_localhost`): OPTIONS `/api/v1/healthz` with `Origin: http://localhost:3000` + `Access-Control-Request-Method: GET` returns 200 with `Access-Control-Allow-Origin: http://localhost:3000`.
- Test 7 (`test_cors_preflight_rejected_for_evil`): OPTIONS with `Origin: http://evil.example` returns a response that does NOT include `Access-Control-Allow-Origin: http://evil.example` (CORS protects via reflection).
- Test 8 (`test_cors_env_override`): With `PROMPT_OPTIMIZER_CORS_ORIGINS=http://a.com,http://b.com` set, force-reload `apps.api.main`, OPTIONS with `Origin: http://a.com` succeeds with that origin in the response.
  </behavior>
  <action>
Write `apps/api/routes/__init__.py` as a one-line package docstring marker (mirror `apps/api/backends/__init__.py` shape).

Write `apps/api/lifespan.py` per RESEARCH Pattern 1 + CONTEXT D-15 + discretion line 178. Mandatory structure:

- `from __future__ import annotations` first.
- Module docstring: purpose ("FastAPI lifespan — opens DB → runs migrations → loads joblib artifacts → loads settings → instantiates KeyStore → empty adapter registry → caches schema_version"), Public surface (`lifespan`), Cross-refs (RESEARCH Pattern 1 + Example 9, CONTEXT D-15 + discretion line 178, PATTERNS Excerpt 9).
- Stdlib imports: `from contextlib import asynccontextmanager`, `import logging`.
- Third-party: `from fastapi import FastAPI`.
- Local imports: `from apps.api.paths import DB_PATH`, `from apps.api.db.connect import open_db`, `from apps.api.db.migrate import up_to_latest`, `from apps.api.db.queries import read_schema_version`, `from apps.api.backends.keystore import KeyStore`, `from apps.api.settings import load_settings_file`.
- For the artifact loader: `from src.routing.decide import _load_default_artifacts` per CONTEXT discretion line 178 (Phase 1 D-04 canonical loader). NOTE: `src.routing.decide._load_default_artifacts()` returns a dict with the THREE classifier artifacts (`task_type_classifier`, `agentic_intent_classifier`, `model_router`) plus the `model_mapping` JSON config — exactly the shape `decide()` consumes. Do NOT use `src.demo.demo_router.load_joblib_artifacts(path, name)` (that signature loads ONE artifact and returns a single dict; the wrong shape for `decide()`).
- `logger = logging.getLogger(__name__)`.

`@asynccontextmanager async def lifespan(app: FastAPI):` body — ORDER MATTERS per CONTEXT discretion line 178:
1. `app.state.db = await open_db(DB_PATH)` — pragmas applied inside.
2. `await up_to_latest(app.state.db)` — idempotent on already-migrated DBs.
3. `app.state.artifacts = _load_default_artifacts()` — already returns the canonical 4-key dict (`task_type_classifier`, `agentic_intent_classifier`, `model_router`, `model_mapping`) that `decide()` consumes verbatim. No adaptation needed; call site is one line.
4. `app.state.settings = load_settings_file()`.
5. `app.state.keystore = KeyStore(use_keyring=False)` — in-memory primary; optional keyring is a separate concern for Phase 5+.
6. `app.state.adapters = {}` per D-15 — EMPTY adapter registry; lazy-build at first turn (Wave 4).
7. `app.state.schema_version = await read_schema_version(app.state.db)` per RESEARCH Open Question 5 — cache once at startup, never re-read during requests.
8. `logger.info("Lifespan startup complete: schema_version=%d, artifacts_loaded=True", app.state.schema_version)`.
9. `try: yield` — server runs.
10. `finally:` defensive close: `try: await app.state.db.close() except Exception: pass` (mirrors RESEARCH Example 9 defensive teardown pattern from `apps/api/backends/openrouter/adapter.py:352-360`).

Write `apps/api/main.py` per RESEARCH Pattern 9 + CONTEXT D-09 + discretion line 175. Mandatory structure:

- `from __future__ import annotations` first.
- Module docstring: purpose ("FastAPI app constructor; mounts CORS + lifespan + routers"), Public surface (`create_app`, `app`), Cross-refs (RESEARCH Pattern 9, CONTEXT D-09 + D-15 + OSS-05, PATTERNS Routes section).
- Stdlib imports: `import os`, `import logging`.
- Third-party: `from fastapi import FastAPI`, `from fastapi.middleware.cors import CORSMiddleware`.
- Local imports: `from apps.api.lifespan import lifespan`. (No route imports at module top — they go inside `create_app()` to avoid circular import issues if a route ever imports from `apps.api.main`.)

`def create_app() -> FastAPI:`:
- `app = FastAPI(lifespan=lifespan, title="Prompt-Optimizer API", version="0.1.0")`.
- CORS origins per OSS-05 + discretion line 175:
  - `origins_env = os.environ.get("PROMPT_OPTIMIZER_CORS_ORIGINS", "http://localhost:3000")`.
  - `origins = [o.strip() for o in origins_env.split(",") if o.strip()]`.
  - **NEVER fall back to `["*"]`** (OSS-05 explicit; anti-pattern guard).
- `app.add_middleware(CORSMiddleware, allow_origins=origins, allow_credentials=True, allow_methods=["GET", "POST", "PATCH", "DELETE", "OPTIONS"], allow_headers=["Authorization", "Content-Type", "Last-Event-ID"], expose_headers=["X-Request-Id"], max_age=600)`.
- Mount routers (Wave 2 only mounts `health`; Wave 3/4/6 grow the list):
  - `from apps.api.routes import health`.
  - `app.include_router(health.router)`.
- `return app`.

Module-level: `app = create_app()` for `uvicorn apps.api.main:app`.

Write `apps/api/routes/health.py` per RESEARCH Pattern 12 + CONTEXT D-18. Mandatory structure:

- `from __future__ import annotations` first.
- Module docstring: purpose ("GET /api/v1/healthz — rich D-18 status payload with per-adapter status dots"), Public surface (`router`), Cross-refs (RESEARCH Pattern 12, CONTEXT D-18, PATTERNS Excerpt 4 — closed Literal vocabulary).
- Imports: `from typing import Literal`, `from fastapi import APIRouter, Request`, `from apps.api.settings import computer_use_enabled`.
- `AdapterStatus = Literal["ready", "missing_key", "opt_out", "error"]` (PATTERNS Excerpt 4).
- `router = APIRouter(prefix="/api/v1", tags=["health"])`.

`def _adapter_status_for(provider: str, request: Request) -> dict:`:
- Read-only check (NEVER constructs the adapter).
- For each backend, determine status:
  - `openrouter`: `{"status": "ready"}` if `request.app.state.keystore.get("openrouter")` else `{"status": "missing_key", "reason": "OPENROUTER_API_KEY not set"}`.
  - `claude_code`: `{"status": "ready"}` if `request.app.state.keystore.get("anthropic")` else `{"status": "missing_key", "reason": "ANTHROPIC_API_KEY not set"}`.
  - `computer_use`: depends on both `computer_use_enabled(request.app.state.settings)` (the D-12 STRICT AND check) AND `keystore.get("anthropic")`:
    - If neither gate set → `{"status": "opt_out", "reason": "COMPUTER_USE_OPT_IN not set"}`.
    - If env+settings set but anthropic key missing → `{"status": "missing_key", "reason": "ANTHROPIC_API_KEY not set"}`.
    - If all gates set → `{"status": "ready"}`.

`@router.get("/healthz")`:
- `async def healthz(request: Request) -> dict:` body:
  - `adapters = {"openrouter": _adapter_status_for("openrouter", request), "claude_code": _adapter_status_for("claude_code", request), "computer_use": _adapter_status_for("computer_use", request)}`.
  - `artifacts_ok = request.app.state.artifacts is not None`.
  - `db_ok = True` (if we got here, lifespan opened the DB).
  - `schema_version = request.app.state.schema_version` (cached at lifespan; do NOT re-read).
  - `overall = "ok" if all(a["status"] == "ready" for a in adapters.values()) else "degraded"`.
  - Return `{"status": overall, "artifacts_loaded": artifacts_ok, "db_ok": db_ok, "schema_version": schema_version, "adapters": adapters, "version": "0.1.0"}`.

NEVER construct an actual adapter inside healthz (D-18 + anti-pattern audit line 533).
NEVER make live network calls inside healthz (read-only checks only).
NEVER import from `src/routing/*` inside `routes/health.py` (would not violate D-18 from the routing side, but health doesn't need it; lifespan is the only path that touches Phase 1 artifacts).
  </action>
  <verify>
    <automated>python -c "from apps.api.main import create_app, app; from fastapi import FastAPI; assert isinstance(app, FastAPI); assert app.title == 'Prompt-Optimizer API'; assert app.version == '0.1.0'; print('OK app construction')" && python -c "from apps.api.lifespan import lifespan; print('OK lifespan importable')" && python -c "from apps.api.routes.health import router; assert any('healthz' in str(r.path) for r in router.routes); print('OK healthz route registered')" && grep -q "CORSMiddleware" apps/api/main.py && grep -q "PROMPT_OPTIMIZER_CORS_ORIGINS" apps/api/main.py && ! grep -q 'allow_origins=\\[".\\*"\\]' apps/api/main.py && grep -q "Literal\\[.ready., .missing_key., .opt_out., .error.\\]" apps/api/routes/health.py && grep -q "app.state.adapters = {}" apps/api/lifespan.py && grep -q "_load_default_artifacts" apps/api/lifespan.py && grep -q "task_type_classifier" apps/api/lifespan.py && grep -q "agentic_intent_classifier" apps/api/lifespan.py && grep -q "model_router" apps/api/lifespan.py && grep -q "app.state.schema_version" apps/api/lifespan.py</automated>
  </verify>
  <done>
- `apps/api/lifespan.py` exists with the 10-step lifespan body in the correct order per D-15 + discretion line 178.
- `apps/api/main.py` exists with `create_app()` and module-level `app`.
- CORS middleware configured with explicit origin (defaults to `http://localhost:3000`, honours `PROMPT_OPTIMIZER_CORS_ORIGINS`).
- `apps/api/routes/health.py` exists with `GET /api/v1/healthz` returning the D-18 payload.
  </done>
  <acceptance_criteria>
- Source: `grep -q "from __future__ import annotations" apps/api/lifespan.py apps/api/main.py apps/api/routes/health.py`.
- Source: `grep -q "@asynccontextmanager" apps/api/lifespan.py`.
- Source: `grep -q "async def lifespan" apps/api/lifespan.py`.
- Source: `grep -q "app.state.db = await open_db" apps/api/lifespan.py`.
- Source: `grep -q "await up_to_latest" apps/api/lifespan.py`.
- Source: `grep -q "app.state.artifacts" apps/api/lifespan.py`.
- Source: `grep -q "app.state.settings" apps/api/lifespan.py`.
- Source: `grep -q "app.state.keystore" apps/api/lifespan.py`.
- Source: `grep -q "app.state.adapters = {}" apps/api/lifespan.py`.
- Source: `grep -q "app.state.schema_version" apps/api/lifespan.py`.
- Source: `grep -q "finally:" apps/api/lifespan.py` (defensive teardown).
- Source: `grep -q "_load_default_artifacts" apps/api/lifespan.py` (Phase 1 canonical loader per discretion line 178).
- Source: `grep -q "task_type_classifier" apps/api/lifespan.py && grep -q "agentic_intent_classifier" apps/api/lifespan.py && grep -q "model_router" apps/api/lifespan.py` (the three classifier artifact keys present in the lifespan source — defends against the 3-key vs 1-key shape regression).
- Source: `grep -q "def create_app" apps/api/main.py`.
- Source: `grep -q "FastAPI(lifespan=lifespan" apps/api/main.py`.
- Source: `grep -q "CORSMiddleware" apps/api/main.py`.
- Source: `grep -q "PROMPT_OPTIMIZER_CORS_ORIGINS" apps/api/main.py`.
- Source: `grep -q "allow_credentials=True" apps/api/main.py`.
- Source: `! grep -q 'allow_origins=\\[".\\*"\\]' apps/api/main.py` (NEVER wildcard CORS — OSS-05 + anti-pattern).
- Source: `grep -q "app = create_app()" apps/api/main.py` (module-level binding for uvicorn).
- Source: `grep -q "Literal\\[.ready., .missing_key., .opt_out., .error.\\]" apps/api/routes/health.py`.
- Source: `grep -q "/healthz" apps/api/routes/health.py`.
- Source: `grep -q "computer_use_enabled" apps/api/routes/health.py`.
- Source: `! grep -q "OpenRouterAdapter(" apps/api/routes/health.py` (D-18 — NEVER construct adapter in healthz).
- Source: `! grep -q "ClaudeCodeAdapter(" apps/api/routes/health.py`.
- Source: `! grep -q "ComputerUseAdapter(" apps/api/routes/health.py`.
- Source: `! grep -q "sys.path.append" apps/api/lifespan.py apps/api/main.py apps/api/routes/health.py`.
- Behavior: `python -c "from apps.api.main import create_app, app; print(type(app).__name__)"` prints `FastAPI`.
- Behavior: `python -c "from apps.api.routes.health import router; assert any('healthz' in str(r.path) for r in router.routes); print('OK')"` prints `OK`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author test_health.py + test_cors.py — exercise the app via httpx.AsyncClient + ASGITransport</name>
  <files>apps/api/tests/test_health.py, apps/api/tests/test_cors.py</files>
  <read_first>
    - apps/api/tests/conftest.py (Wave 0 — `asgi_client`, `app_factory` fixtures; lazy import for `create_app`)
    - apps/api/main.py (Task 2 — `create_app()` factory)
    - apps/api/routes/health.py (Task 2 — the endpoint shape under test)
    - apps/api/lifespan.py (Task 2 — `app.state.*` keys the tests assert)
    - apps/api/settings.py (Task 1 — `_default_settings`)
    - apps/api/backends/keystore.py (Phase 2 Wave 0 — `KeyStore` constructor for fixture overrides)
    - apps/api/backends/tests/test_adapter_contract.py lines 45-128 (parametric + asyncio + timeout pattern — Phase 3 health tests mirror)
    - .planning/phases/03-fastapi-service-persistent-storage/03-RESEARCH.md §"Pattern 6" lines 457-515 (httpx.AsyncClient + ASGITransport canonical pattern + finite consume + Pitfall 4)
    - .planning/phases/03-fastapi-service-persistent-storage/03-RESEARCH.md §"Pattern 9" lines 593-631 (CORS test patterns)
    - .planning/phases/03-fastapi-service-persistent-storage/03-VALIDATION.md rows 3-02-01 / 3-02-02 / 3-02-03 lines 50-52 (test expectations)
  </read_first>
  <behavior>
- `test_health.py::test_artifacts_loaded_once`: Monkeypatch `apps.api.lifespan._load_default_artifacts` to a counter. Two requests to `/api/v1/healthz` produce counter==1 (lifespan called once; subsequent requests reuse `app.state.artifacts`).
- `test_health.py::test_pragmas_applied`: After lifespan startup, run `PRAGMA foreign_keys` / `PRAGMA busy_timeout` against `app.state.db`; assert FK=1, busy_timeout=5000.
- `test_health.py::test_healthz_returns_200_with_required_keys`: `GET /api/v1/healthz` returns 200; body has keys `status`, `artifacts_loaded`, `db_ok`, `schema_version`, `adapters`, `version`.
- `test_health.py::test_healthz_status_dots`: With `OPENROUTER_API_KEY` env-set, claude_code key absent, COMPUTER_USE_OPT_IN unset → openrouter ready, claude_code missing_key, computer_use opt_out; overall status `degraded`.
- `test_health.py::test_healthz_never_constructs_adapters`: Monkeypatch each adapter class constructor to raise; healthz STILL returns 200. (D-18.)
- `test_cors.py::test_cors_preflight_allowed_for_localhost`: OPTIONS `/api/v1/healthz` with `Origin: http://localhost:3000` + `Access-Control-Request-Method: GET` returns 200 with `Access-Control-Allow-Origin: http://localhost:3000` header.
- `test_cors.py::test_cors_preflight_rejected_for_evil`: OPTIONS with `Origin: http://evil.example` — response does NOT include `Access-Control-Allow-Origin: http://evil.example`.
- `test_cors.py::test_cors_env_override`: With `PROMPT_OPTIMIZER_CORS_ORIGINS=http://a.com,http://b.com` set + reloaded app, OPTIONS with `Origin: http://a.com` returns that origin in response.
  </behavior>
  <action>
Write `apps/api/tests/test_health.py`:

- Module docstring per Phase 2 style.
- `from __future__ import annotations`, `import pytest`, `import httpx`, `from httpx import ASGITransport`.

Test functions:

`async def test_artifacts_loaded_once(monkeypatch, tmp_path) -> None`:
- Monkeypatch `apps.api.paths.USER_HOME` to `tmp_path` so the DB lands in an isolated location. Force-reload `apps.api.lifespan`.
- Create a counter dict: `call_count = {"n": 0}`.
- Monkeypatch `apps.api.lifespan._load_default_artifacts` to a function that increments `call_count["n"]` and returns a sentinel dict mirroring the canonical 4-key shape: `{"task_type_classifier": {}, "agentic_intent_classifier": {}, "model_router": {}, "model_mapping": {}}`.
- Inline-import `create_app` (lazy); build the app; wrap with `httpx.AsyncClient(transport=ASGITransport(app=app), base_url='http://test')`.
- `async with httpx.AsyncClient(...) as client`: call `await client.get("/api/v1/healthz")` TWICE. Assert both return 200.
- Assert `call_count["n"] == 1` (lifespan called once; lifespan is shared across requests for the same app instance).

`async def test_pragmas_applied(monkeypatch, tmp_path) -> None`:
- Build app + asgi_client; trigger lifespan via `await client.get("/api/v1/healthz")`.
- Read `app.state.db`; run `PRAGMA foreign_keys`, `PRAGMA busy_timeout`, `PRAGMA synchronous`.
- Assert `foreign_keys == 1`, `busy_timeout == 5000`, `synchronous == 1`.

`async def test_healthz_returns_200_with_required_keys(monkeypatch, tmp_path) -> None`:
- Build app + client; `resp = await client.get("/api/v1/healthz")`; assert `resp.status_code == 200`.
- `body = resp.json()`; assert `set(body.keys()) >= {"status", "artifacts_loaded", "db_ok", "schema_version", "adapters", "version"}`.
- Assert `set(body["adapters"].keys()) >= {"openrouter", "claude_code", "computer_use"}`.

`async def test_healthz_status_dots(monkeypatch, tmp_path) -> None`:
- `monkeypatch.setenv("OPENROUTER_API_KEY", "sk-or-v1-test")`.
- `monkeypatch.delenv("ANTHROPIC_API_KEY", raising=False)`.
- `monkeypatch.delenv("COMPUTER_USE_OPT_IN", raising=False)`.
- Build app + client; `body = (await client.get("/api/v1/healthz")).json()`.
- Assert `body["adapters"]["openrouter"]["status"] == "ready"`.
- Assert `body["adapters"]["claude_code"]["status"] == "missing_key"`.
- Assert `body["adapters"]["computer_use"]["status"] == "opt_out"`.
- Assert `body["status"] == "degraded"`.

`async def test_healthz_never_constructs_adapters(monkeypatch, tmp_path) -> None`:
- `monkeypatch.setenv("OPENROUTER_API_KEY", "sk-or-v1-test")`.
- Monkeypatch each adapter class constructor to raise `RuntimeError`. Use the import paths the adapters live at (`apps.api.backends.openrouter.adapter.OpenRouterAdapter.__init__`, etc.) — wrap in try/import-error so the test skips if Wave 1 adapters are reorganized in the future.
- Build app + client; `resp = await client.get("/api/v1/healthz")`; assert `resp.status_code == 200`. (Healthz is read-only — it must succeed even if every adapter constructor would raise.)

Write `apps/api/tests/test_cors.py`:

- Module docstring per Phase 2 style.
- `from __future__ import annotations`, `import pytest`, `import httpx`, `from httpx import ASGITransport`.

Test functions:

`async def test_cors_preflight_allowed_for_localhost(monkeypatch, tmp_path) -> None`:
- Build app + client.
- `resp = await client.options("/api/v1/healthz", headers={"Origin": "http://localhost:3000", "Access-Control-Request-Method": "GET", "Access-Control-Request-Headers": "Content-Type"})`.
- Assert `resp.status_code == 200`.
- Assert `resp.headers.get("access-control-allow-origin") == "http://localhost:3000"`.

`async def test_cors_preflight_rejected_for_evil(monkeypatch, tmp_path) -> None`:
- Build app + client.
- `resp = await client.options("/api/v1/healthz", headers={"Origin": "http://evil.example", "Access-Control-Request-Method": "GET"})`.
- Assert `resp.headers.get("access-control-allow-origin") != "http://evil.example"` (CORS protects via reflection — the response does NOT include the evil origin in the allow header).

`async def test_cors_env_override(monkeypatch, tmp_path) -> None`:
- `monkeypatch.setenv("PROMPT_OPTIMIZER_CORS_ORIGINS", "http://a.com,http://b.com")`.
- Force-reload `apps.api.main` via `importlib.reload(apps.api.main)` so the env override is picked up at `create_app()` call.
- Build app + client.
- `resp = await client.options("/api/v1/healthz", headers={"Origin": "http://a.com", "Access-Control-Request-Method": "GET"})`.
- Assert `resp.headers.get("access-control-allow-origin") == "http://a.com"`.

All tests `async def test_X(monkeypatch, tmp_path) -> None:`; use `@pytest.mark.asyncio` for explicitness. NEVER use `TestClient` (D-20 / API-08). Use the Wave 0 `conftest.py` fixtures (`asgi_client`, `app_factory`) where possible — but the lifespan-monkeypatch tests build the app inline via `create_app()` because they need to monkeypatch BEFORE the lifespan starts.
  </action>
  <verify>
    <automated>uv run pytest apps/api/tests/test_health.py apps/api/tests/test_cors.py -x --timeout=30 2>&1 | tail -15 && grep -q "def test_artifacts_loaded_once" apps/api/tests/test_health.py && grep -q "def test_pragmas_applied" apps/api/tests/test_health.py && grep -q "def test_healthz_status_dots" apps/api/tests/test_health.py && grep -q "def test_healthz_never_constructs_adapters" apps/api/tests/test_health.py && grep -q "def test_cors_preflight_allowed_for_localhost" apps/api/tests/test_cors.py && grep -q "def test_cors_preflight_rejected_for_evil" apps/api/tests/test_cors.py && grep -q "def test_cors_env_override" apps/api/tests/test_cors.py && ! grep -q "TestClient" apps/api/tests/test_health.py apps/api/tests/test_cors.py && uv run pytest -m 'not live' -x 2>&1 | tail -5</automated>
  </verify>
  <done>
- `test_health.py` has 5 sub-tests, all pass.
- `test_cors.py` has 3 sub-tests, all pass.
- No use of `TestClient` (API-08 / D-20).
- Whole-repo non-live suite still green.
  </done>
  <acceptance_criteria>
- Behavior: `uv run pytest apps/api/tests/test_health.py -x --timeout=30` exits 0 (5 tests pass).
- Behavior: `uv run pytest apps/api/tests/test_cors.py -x --timeout=30` exits 0 (3 tests pass).
- Source: `grep -q "def test_artifacts_loaded_once" apps/api/tests/test_health.py`.
- Source: `grep -q "def test_pragmas_applied" apps/api/tests/test_health.py`.
- Source: `grep -q "def test_healthz_returns_200_with_required_keys" apps/api/tests/test_health.py`.
- Source: `grep -q "def test_healthz_status_dots" apps/api/tests/test_health.py`.
- Source: `grep -q "def test_healthz_never_constructs_adapters" apps/api/tests/test_health.py`.
- Source: `grep -q "def test_cors_preflight_allowed_for_localhost" apps/api/tests/test_cors.py`.
- Source: `grep -q "def test_cors_preflight_rejected_for_evil" apps/api/tests/test_cors.py`.
- Source: `grep -q "def test_cors_env_override" apps/api/tests/test_cors.py`.
- Source: `! grep -q "TestClient" apps/api/tests/test_health.py apps/api/tests/test_cors.py` (API-08).
- Source: `grep -q "ASGITransport" apps/api/tests/test_health.py apps/api/tests/test_cors.py`.
- Behavior: `uv run pytest -m 'not live' -x 2>&1 | tail -5` exits 0 (full suite green).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `FastAPI app startup → lifespan` | Lifespan runs at server boot; failures during startup must surface clearly. Defensive `finally` block ensures DB close even on exception. |
| `CORS preflight → browser` | Browser enforces CORS based on response headers; server's job is to return the right `Access-Control-Allow-Origin` for known good origins and to NOT return it for evil ones. |
| `healthz → KeyStore + settings` | Read-only check; never constructs adapters (D-18). Avoids any side effects (no API calls, no FS writes, no Playwright instantiation). |
| `lifespan → joblib artifacts` | Phase 1 loaders open `.joblib` files; trust boundary is "the contributor who pushed last" (per codebase CONCERNS.md — joblib trust note carries forward). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-CORS | S (Spoofing) | `apps/api/main.py` `CORSMiddleware` | mitigate | Explicit allowlist (`http://localhost:3000` default; env override via `PROMPT_OPTIMIZER_CORS_ORIGINS`); NEVER `["*"]`. Acceptance: `! grep -q 'allow_origins=\\[".\\*"\\]' apps/api/main.py`. Tests `test_cors_preflight_rejected_for_evil` + `test_cors_env_override` enforce. SEVERITY: HIGH. |
| T-03-LL | I (Information Disclosure) | `apps/api/lifespan.py` joblib artifact load | accept | Artifacts produced by the trained pipeline; trust boundary = "the contributor who pushed last" (codebase CONCERNS.md carry-forward). Lifespan loads artifacts ONCE; never reloads. RESTART required to pick up a re-train (single-process local server). SEVERITY: LOW for v1. |
| T-03-Healthz-side-effect | T (Tampering) | `apps/api/routes/health.py` healthz endpoint | mitigate | Read-only checks only — KeyStore lookup + settings dict + cached `app.state.schema_version`. NEVER constructs adapters (D-18 anti-pattern guard). Test `test_healthz_never_constructs_adapters` enforces by monkeypatching every adapter constructor to raise. SEVERITY: MEDIUM (avoids accidental API calls / sandbox triggers during status polling from UI). |
| T-03-Disclo | I (Information Disclosure) | `apps/api/routes/health.py` healthz response | mitigate | Response contains adapter status (`ready` / `missing_key` / `opt_out` / `error`) but NEVER includes key material. The `missing_key` reason field says only `"OPENROUTER_API_KEY not set"` — never echoes the key shape or value. SEVERITY: LOW. |
| T-03-Pre-commit (forward) | T (Tampering) | `apps/api/settings.py` settings file writes | mitigate | `settings.json` never contains keys (D-11 explicit — keys live in KeyStore). Settings dict round-tripped via atomic write; pre-commit hook from Phase 2 SECURE-02 still blocks any `sk-` leak in a `tests/fixtures/settings_with_key.json` accident. SEVERITY: MEDIUM. |
</threat_model>

<verification>
- `uv run pytest apps/api/tests/test_health.py apps/api/tests/test_cors.py -x --timeout=30` exits 0 (8 sub-tests pass).
- `python -c "from apps.api.main import create_app, app; assert app.title == 'Prompt-Optimizer API'; print('OK')"` exits 0.
- `python -c "from apps.api.lifespan import lifespan; print('OK lifespan')"` exits 0.
- `python -c "from apps.api.routes.health import router; print('OK health router')"` exits 0.
- `python -c "from apps.api.settings import load_settings_file, write_settings_file, computer_use_enabled; print('OK settings')"` exits 0.
- `uv run pytest -m 'not live' -x` exits 0 (Phase 2 + Phase 3 Waves 0-2 all green).
- `uv run pytest src/routing/tests/test_decide_smoke.py -x` exits 0 (D-18 import-graph guard still green — lifespan imports Phase 1 `_load_default_artifacts` from `src.routing.decide` but the routing module does NOT pull FastAPI back in).
- `! grep -q 'allow_origins=\\[".\\*"\\]' apps/api/main.py` (OSS-05 wildcard prohibition).
- `! grep -rE 'from fastapi.testclient' apps/api/tests/` exits 0 (API-08 negative grep clean).
</verification>

<success_criteria>
- 5 source files exist: `apps/api/{lifespan,main,settings}.py` + `apps/api/routes/{__init__,health}.py`.
- 2 test files exist: `apps/api/tests/{test_health,test_cors}.py` with a total of 8 passing tests.
- `uvicorn apps.api.main:app` boots (verified by `python -c "from apps.api.main import app"`); lifespan runs in <3 s (warm laptop).
- `GET /api/v1/healthz` returns 200 with the D-18 payload (overall status, per-adapter status dots, schema_version).
- `computer_use_enabled` enforces STRICT AND-semantics (D-12).
- CORS middleware is configured with explicit `http://localhost:3000` default and `PROMPT_OPTIMIZER_CORS_ORIGINS` env override; NEVER wildcard.
- Phase 1 D-18 import-graph guard still green.
- Phase 2 whole-repo non-live test suite still green.
- All 11 Wave 2 truths from `must_haves.truths` are observable from a CLI / pytest run.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fastapi-service-persistent-storage/03-02-SUMMARY.md` summarizing:
- 5 source files created (lifespan.py, main.py, settings.py, routes/__init__.py, routes/health.py).
- 2 test files created (test_health.py, test_cors.py) with 8 passing sub-tests.
- Lifespan order locked per CONTEXT discretion line 178: open DB → migrate → load artifacts → load settings → KeyStore → adapters={} → cache schema_version.
- CORS default `http://localhost:3000` + env override (NEVER wildcard).
- D-18 healthz read-only adapter status — never constructs adapter.
- D-12 STRICT AND for computer-use opt-in.
- Confirmation that Phase 1 D-18 guard still passes.
- Confirmation that Phase 2 whole-repo non-live suite still passes.
- The 11 Wave 2 truths verified at a CLI / pytest level.
</output>
</content>
