---
phase: 02
plan: 04
plan_id: "02-04-ops-precommit-and-ci"
type: execute
wave: 2
depends_on:
  - "02-01-openrouter-adapter"
  - "02-02-claude-code-adapter"
  - "02-03-computer-use-adapter"
files_modified:
  - ".pre-commit-config.yaml"
  - "scripts/no-secrets.sh"
  - "scripts/no-deprecated-sdk.sh"
  - ".github/workflows/ci.yml"
  - ".github/workflows/live-smoke.yml"
  - ".planning/REQUIREMENTS.md"
autonomous: true
requirements: [SECURE-02, OSS-06]
must_haves:
  truths:
    - "`pre-commit run --all-files` succeeds (clean repo) and the local hooks `no-secrets` + `no-deprecated-claude-code-sdk` are registered (D-09)."
    - "`scripts/no-secrets.sh` blocks `git commit` when staged content contains `sk-ant-AAAAAAAAAAAAAAAAAAAA`, `sk-AAAAAAAAAAAAAAAAAAAA`, or `Bearer AAAAAAAAAAAAAAAAAAAA` (SECURE-02; D-09 regex literal)."
    - "`scripts/no-deprecated-sdk.sh` blocks `git commit` when staged content contains `import claude_code_sdk`, `from claude_code_sdk`, or `\"claude-code-sdk\"`, AND blocks when `uv.lock` contains `\"claude-code-sdk\"` (OSS-06)."
    - "`.github/workflows/ci.yml` extends the Phase 1 workflow with: `uv sync --locked --extra keyring`, `pre-commit run --all-files`, OSS-06 import smoke (`python -c \"from claude_agent_sdk import ClaudeAgentOptions\"`), OSS-06 `claude-code-sdk` absence assertions, and `pytest -m 'not live' apps/api/backends` (D-20)."
    - "Phase 1 `pytest src/` step stays green (the workflow runs both `pytest src/` AND `pytest -m 'not live' apps/api/backends` per D-20)."
    - "The advisory `Routing canary eval` step from Phase 1 stays as-is with `continue-on-error: true` (commit 64a07d2 resolved this; CONTEXT line 240)."
    - "Optional `.github/workflows/live-smoke.yml` is added with manual `workflow_dispatch` + weekly cron trigger, gated by `secrets.OPENROUTER_API_KEY != ''`, with `continue-on-error: true` (D-20)."
    - "`.planning/REQUIREMENTS.md` BACKEND-01 line is updated to include `ToolResult` between `ToolCall` and `FileDiff` per D-02."
    - "`.planning/REQUIREMENTS.md` BACKEND-06 line is verified to already match the locked wording `per-iteration step cap (25 for Claude Code, 15 for computer-use)` per D-15 (planned no-op verification — re-write only if drift exists)."
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "Two local hooks: `no-secrets` + `no-deprecated-claude-code-sdk` (D-09)"
      contains: "repo: local|id: no-secrets|id: no-deprecated-claude-code-sdk|scripts/no-secrets.sh|scripts/no-deprecated-sdk.sh|pass_filenames: false"
    - path: "scripts/no-secrets.sh"
      provides: "Pre-commit script blocking staged secret content (SECURE-02)"
      contains: "git diff --cached|sk-ant-|sk-|Bearer|grep -E"
    - path: "scripts/no-deprecated-sdk.sh"
      provides: "Pre-commit script blocking deprecated SDK imports + uv.lock entries (OSS-06)"
      contains: "claude_code_sdk|claude-code-sdk|uv.lock"
    - path: ".github/workflows/ci.yml"
      provides: "Extended CI per D-20: uv sync, pre-commit, OSS-06 smoke + lockfile grep, dual pytest (src/ + apps/)"
      contains: "uv sync --locked --extra keyring|pre-commit run --all-files|from claude_agent_sdk import ClaudeAgentOptions|claude-code-sdk|pytest -m 'not live' apps/api/backends|continue-on-error: true"
    - path: ".github/workflows/live-smoke.yml"
      provides: "Optional manual + weekly cron live smoke (D-20)"
      contains: "workflow_dispatch|schedule|cron|secrets.OPENROUTER_API_KEY|--live-budget|continue-on-error: true"
    - path: ".planning/REQUIREMENTS.md"
      provides: "BACKEND-01 union updated to add ToolResult; BACKEND-06 verified (no-op if already correct)"
      contains: "TextDelta \\| ToolCall \\| ToolResult \\| FileDiff \\| Screenshot \\| StreamError \\| Done|per-iteration step cap"
  key_links:
    - from: ".pre-commit-config.yaml"
      to: "scripts/no-secrets.sh"
      via: "entry"
      pattern: "entry: scripts/no-secrets.sh"
    - from: ".pre-commit-config.yaml"
      to: "scripts/no-deprecated-sdk.sh"
      via: "entry"
      pattern: "entry: scripts/no-deprecated-sdk.sh"
    - from: ".github/workflows/ci.yml"
      to: "pre-commit run --all-files"
      via: "step"
      pattern: "pre-commit run --all-files"
    - from: ".github/workflows/ci.yml"
      to: "claude_agent_sdk import smoke"
      via: "step"
      pattern: "from claude_agent_sdk import ClaudeAgentOptions"
    - from: ".github/workflows/ci.yml"
      to: "deprecated SDK uv.lock grep"
      via: "step"
      pattern: "grep -q .claude-code-sdk. uv.lock"
---

<objective>
Wave 2 of Phase 2 — install the pre-commit framework hooks (`.pre-commit-config.yaml` + 2 scripts) and extend `.github/workflows/ci.yml` to enforce all Phase 2 invariants on every push. Add an optional `live-smoke.yml` workflow. Reconcile `.planning/REQUIREMENTS.md` BACKEND-01 wording with D-02 and verify BACKEND-06 wording with D-15.

Purpose: SECURE-02 + OSS-06 land here. The triad of (CI smoke + uv.lock grep + pre-commit hook) is the canonical OSS-06 enforcement per RESEARCH Pattern 14. SECURE-02's pre-commit hook is the FIRST line of defense for accidental secret commits; the SECURE-01 logging redaction filter (Wave 0) is the SECOND. This plan runs after all three adapter plans because the CI workflow extension exercises every adapter's tests (`pytest -m 'not live' apps/api/backends`).

Output:
  - 3 new repo-tooling files: `.pre-commit-config.yaml`, `scripts/no-secrets.sh`, `scripts/no-deprecated-sdk.sh`.
  - 2 modified workflow files: `.github/workflows/ci.yml` (extended), `.github/workflows/live-smoke.yml` (new).
  - 1 modified docs file: `.planning/REQUIREMENTS.md` (BACKEND-01 + BACKEND-06 reconciliation).
  - Verification: `pre-commit run --all-files` passes on a clean checkout; CI workflow runs `pytest -m 'not live' apps/api/backends` AND `pytest src/` both green; deliberate secret-paste test triggers the hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backend-adapters-chatchunk-contract/02-CONTEXT.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-PATTERNS.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-00-SUMMARY.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-01-SUMMARY.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-02-SUMMARY.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-03-SUMMARY.md
@.planning/REQUIREMENTS.md
@.github/workflows/ci.yml
@pyproject.toml
@uv.lock

<interfaces>
This plan does not introduce new Python interfaces; the contracts are:

`.pre-commit-config.yaml` schema (pre-commit framework 4.0+):
- `repos: [{repo: local, hooks: [{id, name, entry, language, pass_filenames, stages}]}]`

GitHub Actions workflow schema (`.github/workflows/*.yml`):
- `name`, `on: {push, pull_request, workflow_dispatch, schedule}`, `jobs.{name}.runs-on`, `jobs.{name}.steps: [{name, uses, run, with, if, continue-on-error}]`

REQUIREMENTS.md schema: markdown table-of-contents + bulleted requirements with checkboxes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author pre-commit config + two shell scripts (no-secrets, no-deprecated-sdk)</name>
  <files>.pre-commit-config.yaml, scripts/no-secrets.sh, scripts/no-deprecated-sdk.sh</files>
  <read_first>
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md §"Pattern 11" lines 1559-1619 (full source: pre-commit YAML + both shell scripts — copy verbatim)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-CONTEXT.md D-09 (locked regex: `sk-ant-[A-Za-z0-9_-]{8,}|sk-[A-Za-z0-9]{20,}|Bearer [A-Za-z0-9_.-]{20,}`)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-PATTERNS.md §"Group E ops" lines 838-845 (novel files; use RESEARCH verbatim)
    - uv.lock (verify it does NOT contain `claude-code-sdk` — the no-deprecated-sdk hook will grep this on every commit)
  </read_first>
  <action>
Write `.pre-commit-config.yaml` per RESEARCH Pattern 11 lines 1563-1581 verbatim:
- Top-level `repos:` list with one entry `repo: local`.
- Two hooks under that repo:
  1. `id: no-secrets`, `name: Block secrets in staged content`, `entry: scripts/no-secrets.sh`, `language: script`, `pass_filenames: false`, `stages: [pre-commit]`.
  2. `id: no-deprecated-claude-code-sdk`, `name: Block deprecated claude-code-sdk`, `entry: scripts/no-deprecated-sdk.sh`, `language: script`, `pass_filenames: false`, `stages: [pre-commit]`.

Write `scripts/no-secrets.sh` per RESEARCH lines 1584-1598 verbatim:
- Shebang `#!/usr/bin/env bash`.
- `set -euo pipefail`.
- The locked regex per CONTEXT D-09:
  - Match lines being ADDED in staged content (lines starting with `+`, excluding `+++` header lines).
  - Regex: `(sk-ant-[A-Za-z0-9_-]{8,}|sk-[A-Za-z0-9]{20,}|Bearer [A-Za-z0-9_.-]{20,})`.
  - Pipeline: `git diff --cached --diff-filter=AM | grep -E '^\+[^+]' | grep -E '(...)'`.
- On match: print clear error message including "ERROR: Staged content contains what looks like an API key or bearer token." and a hint "If this is a false positive, remove the literal and use an env-var reference.", exit 1.
- No match → exit 0.

Write `scripts/no-deprecated-sdk.sh` per RESEARCH lines 1601-1618 verbatim:
- Shebang + `set -euo pipefail`.
- Check 1 (staged content): grep added lines for `import claude_code_sdk`, `from claude_code_sdk`, or `"claude-code-sdk"`. On match: print "ERROR: Use 'claude_agent_sdk' (NOT the deprecated 'claude-code-sdk'). See OSS-06 in .planning/REQUIREMENTS.md.", exit 1.
- Check 2 (lockfile): `if [ -f uv.lock ] && grep -q '"claude-code-sdk"' uv.lock; then ... exit 1; fi`.
- Both checks pass → exit 0.

Make both scripts executable: `chmod +x scripts/no-secrets.sh scripts/no-deprecated-sdk.sh`.

DO NOT add any third-party pre-commit repo references (no `repo: https://github.com/...`); pre-commit framework allows mixing local + remote hooks, but Phase 2 keeps it local-only per D-09 (purpose: minimal external dependency).
  </action>
  <verify>
    <automated>ls -la scripts/no-secrets.sh scripts/no-deprecated-sdk.sh | awk '{ print $1, $NF }' | grep -E "x.*no-(secrets|deprecated)" | wc -l | awk '{ if ($1 < 2) { print "FAIL: scripts not executable"; exit 1 } else { print "OK both executable" } }' && grep -q 'id: no-secrets' .pre-commit-config.yaml && grep -q 'id: no-deprecated-claude-code-sdk' .pre-commit-config.yaml && grep -q 'pass_filenames: false' .pre-commit-config.yaml && grep -q 'sk-ant-' scripts/no-secrets.sh && grep -q 'Bearer' scripts/no-secrets.sh && grep -q 'claude_code_sdk' scripts/no-deprecated-sdk.sh && grep -q 'uv.lock' scripts/no-deprecated-sdk.sh && uv run pre-commit run --all-files 2>&1 | tail -10</automated>
  </verify>
  <done>
- `.pre-commit-config.yaml` exists with two local hooks.
- Both scripts exist and are executable (`chmod +x`).
- Locked regex matches RESEARCH Pattern 11.
- `pre-commit run --all-files` passes on a clean checkout (no false positives on Phase 2 source).
  </done>
  <acceptance_criteria>
- Source: `grep -q "repo: local" .pre-commit-config.yaml`.
- Source: `grep -c "id: no-secrets\|id: no-deprecated-claude-code-sdk" .pre-commit-config.yaml` returns 2.
- Source: `grep -q "entry: scripts/no-secrets.sh" .pre-commit-config.yaml` AND `grep -q "entry: scripts/no-deprecated-sdk.sh" .pre-commit-config.yaml`.
- Source: `grep -q "language: script" .pre-commit-config.yaml` AND `grep -q "pass_filenames: false" .pre-commit-config.yaml`.
- Source: `grep -q "sk-ant-\[A-Za-z0-9_-\]{8,}" scripts/no-secrets.sh` AND `grep -q "Bearer \[A-Za-z0-9" scripts/no-secrets.sh` AND `grep -q "sk-\[A-Za-z0-9\]{20" scripts/no-secrets.sh`.
- Source: `grep -q "set -euo pipefail" scripts/no-secrets.sh` AND `grep -q "set -euo pipefail" scripts/no-deprecated-sdk.sh`.
- Source: `grep -q "claude_code_sdk\|claude-code-sdk" scripts/no-deprecated-sdk.sh`.
- Source: `grep -q "uv.lock" scripts/no-deprecated-sdk.sh`.
- File mode: `test -x scripts/no-secrets.sh && test -x scripts/no-deprecated-sdk.sh` (both executable).
- Behavior: `uv run pre-commit run --all-files` exits 0 (no false positives on the existing tree).
- Behavior (deliberate paste test — MANUAL VERIFICATION in `<output>` task summary): `echo "sk-ant-AAAAAAAAAAAAAAAAAAAA" >> deleteme.tmp && git add deleteme.tmp && uv run pre-commit run no-secrets` exits non-zero (the hook blocks). Followed by `git restore --staged deleteme.tmp && rm deleteme.tmp` to clean up. (The CI workflow runs the same command on every push so this is also gated there.)
- Behavior: `! grep -q '"claude-code-sdk"' uv.lock` exits 0 (deprecated SDK absent from lockfile per OSS-06).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend .github/workflows/ci.yml + add optional live-smoke.yml</name>
  <files>.github/workflows/ci.yml, .github/workflows/live-smoke.yml</files>
  <read_first>
    - .github/workflows/ci.yml (current Phase 1 workflow — modify ADDITIVELY only)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md §"Validation Architecture" lines 2058-2086 (test commands + CI gates)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-PATTERNS.md §"Group E" lines 938-1014 (the exact insertion points and step blocks for ci.yml)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-CONTEXT.md D-20 (full CI YAML excerpt — lines 120-132 are the canonical extension)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-PATTERNS.md §"live-smoke.yml" lines 847-855 (workflow_dispatch + cron + budget flag)
  </read_first>
  <action>
Modify `.github/workflows/ci.yml` ADDITIVELY (preserve every existing line; insert new steps as documented).

After the existing `Sync dependencies (locked)` step (line 25-26), insert these steps in order:

```
      - name: Sync with keyring extra (SECURE-04)
        run: uv sync --locked --extra keyring

      - name: Run pre-commit hooks (SECURE-02 + OSS-06)
        run: uv run pre-commit run --all-files

      - name: OSS-06 — claude_agent_sdk import smoke
        run: uv run python -c "from claude_agent_sdk import ClaudeAgentOptions"

      - name: OSS-06 — ensure deprecated claude-code-sdk absent
        run: |
          ! uv run python -c "import claude_code_sdk" 2>/dev/null
          ! grep -q '"claude-code-sdk"' uv.lock
```

After the existing `Pre-fetch NLTK data` step (line 28-30), REPLACE the single `Run full pytest suite` step (line 32-33) with TWO steps:

```
      - name: Phase 1 — src/ tests (D-18 import-graph guard stays green)
        run: uv run pytest -x -q src/

      - name: Phase 2 — apps/api/backends unit tests (no live)
        run: uv run pytest -x -q -m 'not live' apps/api/backends
```

LEAVE the existing `Routing canary eval (advisory)` step at the bottom unchanged — CONTEXT line 240 confirms commit `64a07d2` resolved its `continue-on-error: true` state. Verify by `grep -q 'continue-on-error: true' .github/workflows/ci.yml` after edits.

Write a NEW `.github/workflows/live-smoke.yml` per PATTERNS lines 847-855 + CONTEXT D-20:

```
name: Live smoke (manual + weekly)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1"   # weekly Monday 06:00 UTC

jobs:
  live-smoke:
    runs-on: ubuntu-latest
    if: ${{ secrets.OPENROUTER_API_KEY != '' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - name: Install Python 3.11
        run: uv python install 3.11

      - name: Sync dependencies
        run: uv sync --locked --all-extras --dev

      - name: Live smoke (OpenRouter only)
        continue-on-error: true
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: |
          uv run pytest -m live apps/api/backends/openrouter -x --maxfail=1
```

Do NOT add Anthropic / computer-use live smokes to the cron job (computer-use requires Chromium + COMPUTER_USE_OPT_IN which is a manual-only behavior). Manual `workflow_dispatch` triggers can still run Anthropic smoke when an operator passes the secret.

After editing, validate workflow YAML syntax: `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); yaml.safe_load(open('.github/workflows/live-smoke.yml'))"`.

DO NOT alter Phase 1 steps (`Checkout`, `Install uv`, `Install Python 3.11`, `Sync dependencies (locked)`, `Pre-fetch NLTK data`, `Routing canary eval (advisory)`). The diff is purely additive in the middle of the existing pipeline.
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); yaml.safe_load(open('.github/workflows/live-smoke.yml')); print('OK YAML')" && grep -q "uv sync --locked --extra keyring" .github/workflows/ci.yml && grep -q "pre-commit run --all-files" .github/workflows/ci.yml && grep -q "from claude_agent_sdk import ClaudeAgentOptions" .github/workflows/ci.yml && grep -q "claude-code-sdk" .github/workflows/ci.yml && grep -q "pytest -x -q src/" .github/workflows/ci.yml && grep -q "pytest -x -q -m 'not live' apps/api/backends" .github/workflows/ci.yml && grep -q "continue-on-error: true" .github/workflows/ci.yml && grep -q "workflow_dispatch" .github/workflows/live-smoke.yml && grep -q "schedule:" .github/workflows/live-smoke.yml && grep -q "cron:" .github/workflows/live-smoke.yml && grep -q "secrets.OPENROUTER_API_KEY" .github/workflows/live-smoke.yml</automated>
  </verify>
  <done>
- `.github/workflows/ci.yml` extended with 6 new steps (sync-keyring, pre-commit, OSS-06 smoke, OSS-06 absence, dual pytest split).
- `.github/workflows/live-smoke.yml` created with workflow_dispatch + weekly cron + secret gate + `continue-on-error: true`.
- Both YAML files parse cleanly.
- Phase 1's existing steps (advisory canary etc.) preserved.
  </done>
  <acceptance_criteria>
- Source: `grep -q "name: Sync with keyring extra (SECURE-04)" .github/workflows/ci.yml`.
- Source: `grep -q "name: Run pre-commit hooks (SECURE-02 + OSS-06)" .github/workflows/ci.yml`.
- Source: `grep -q "name: OSS-06 — claude_agent_sdk import smoke" .github/workflows/ci.yml`.
- Source: `grep -q "name: OSS-06 — ensure deprecated claude-code-sdk absent" .github/workflows/ci.yml`.
- Source: `grep -q "from claude_agent_sdk import ClaudeAgentOptions" .github/workflows/ci.yml`.
- Source: `grep -q "name: Phase 1 — src/ tests" .github/workflows/ci.yml` AND `grep -q "name: Phase 2 — apps/api/backends unit tests" .github/workflows/ci.yml`.
- Source: `grep -q "pytest -x -q -m 'not live' apps/api/backends" .github/workflows/ci.yml`.
- Source: `grep -q "uv run python -c \"import claude_code_sdk\"" .github/workflows/ci.yml` OR `grep -q "import claude_code_sdk" .github/workflows/ci.yml` (the absence assertion).
- Source: `grep -q 'grep -q .claude-code-sdk. uv.lock' .github/workflows/ci.yml` (uv.lock absence assertion).
- Source: `grep -q "Routing canary eval" .github/workflows/ci.yml` (Phase 1 step preserved) AND `grep -q "continue-on-error: true" .github/workflows/ci.yml`.
- Source: `grep -q "workflow_dispatch" .github/workflows/live-smoke.yml`.
- Source: `grep -q "cron:" .github/workflows/live-smoke.yml`.
- Source: `grep -q "secrets.OPENROUTER_API_KEY" .github/workflows/live-smoke.yml`.
- Source: `grep -q "continue-on-error: true" .github/workflows/live-smoke.yml`.
- Source: `grep -q "pytest -m live apps/api/backends/openrouter" .github/workflows/live-smoke.yml`.
- Behavior: `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (valid YAML).
- Behavior: `python -c "import yaml; yaml.safe_load(open('.github/workflows/live-smoke.yml'))"` exits 0 (valid YAML).
- Anti-pattern: `! grep -q "pytest -x -q\$" .github/workflows/ci.yml` (the old single `pytest -x -q` step is gone — replaced by the split).
- Behavior: The Phase 1 `Pre-fetch NLTK data` step is still present (`grep -q "Pre-fetch NLTK data" .github/workflows/ci.yml`).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Reconcile .planning/REQUIREMENTS.md BACKEND-01 and BACKEND-06 wording</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — BACKEND-01 at line 22; BACKEND-06 at line 27)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-CONTEXT.md D-02 (BACKEND-01 union grows from 6 to 7 by adding ToolResult), D-15 (BACKEND-06 wording confirmation — already correct on re-read per CONTEXT line 1042-1043)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-PATTERNS.md §"REQUIREMENTS.md modify" lines 1024-1043 (exact before/after diffs)
  </read_first>
  <action>
Read `.planning/REQUIREMENTS.md` and locate BACKEND-01 (currently around line 22):

Current wording:
```
- [ ] **BACKEND-01**: `ChatChunk` discriminated union (`TextDelta | ToolCall | Screenshot | FileDiff | StreamError | Done`) is the single contract between adapters, storage, and UI
```

New wording per CONTEXT D-02 (add `ToolResult` between `ToolCall` and `Screenshot`):
```
- [ ] **BACKEND-01**: `ChatChunk` discriminated union (`TextDelta | ToolCall | ToolResult | FileDiff | Screenshot | StreamError | Done`) is the single contract between adapters, storage, and UI
```

NOTE: CONTEXT D-02 says `TextDelta | ToolCall | ToolResult | FileDiff | Screenshot | StreamError | Done` (ToolResult between ToolCall and FileDiff). Match this exact ordering — see CONTEXT line 269.

Use Edit tool to make the single-line replacement (NOT Write — preserve every other line in REQUIREMENTS.md).

For BACKEND-06: re-read line 27 to verify it already matches `per-iteration step cap (25 for Claude Code, 15 for computer-use)`. CONTEXT line 1042-1043 says "The current file at line 27 matches the locked wording" — this is a NO-OP unless drift has occurred.

If BACKEND-06 wording does NOT match, replace with:
```
- [ ] **BACKEND-06**: Each adapter enforces a hard per-turn USD cap (default $0.50) and per-iteration step cap (25 for Claude Code, 15 for computer-use) at the adapter boundary
```

Do NOT modify any other line in REQUIREMENTS.md (the Traceability table, v2 entries, Out of Scope etc. stay verbatim).
  </action>
  <verify>
    <automated>grep -q "TextDelta | ToolCall | ToolResult | FileDiff | Screenshot | StreamError | Done" .planning/REQUIREMENTS.md && grep -q "per-iteration step cap (25 for Claude Code, 15 for computer-use)" .planning/REQUIREMENTS.md && grep -c "BACKEND-01" .planning/REQUIREMENTS.md | awk '{ if ($1 < 2) { print "FAIL: BACKEND-01 missing from traceability"; exit 1 } else { print "OK BACKEND-01 referenced " $1 " times" } }' && grep -c "BACKEND-06" .planning/REQUIREMENTS.md | awk '{ if ($1 < 2) { print "FAIL"; exit 1 } else { print "OK BACKEND-06 " $1 " times" } }'</automated>
  </verify>
  <done>
- BACKEND-01 line in `.planning/REQUIREMENTS.md` contains the 7-variant union with `ToolResult`.
- BACKEND-06 line contains `per-iteration step cap (25 for Claude Code, 15 for computer-use)`.
- No other lines in REQUIREMENTS.md modified (Traceability table, v2 list etc. intact).
- The Phase 2 implementation in Plans 00-03 was already built against these wordings, so this is a documentation reconciliation — not a behavior change.
  </done>
  <acceptance_criteria>
- Source: `grep -q "TextDelta | ToolCall | ToolResult | FileDiff | Screenshot | StreamError | Done" .planning/REQUIREMENTS.md` (D-02 wording).
- Source: `grep -q "per-iteration step cap (25 for Claude Code, 15 for computer-use)" .planning/REQUIREMENTS.md` (D-15 wording).
- Source: `! grep -q "TextDelta | ToolCall | Screenshot | FileDiff | StreamError | Done" .planning/REQUIREMENTS.md` (old 6-variant wording absent).
- Source: All Phase 1 Complete checkboxes still marked: `grep -cE "ROUTER-0[1-7].*Complete" .planning/REQUIREMENTS.md | awk '{ if ($1 < 7) { print "FAIL: Phase 1 traceability damaged"; exit 1 } else { print "OK" } }'` — actually verify via Traceability table state.
- Source: `wc -l .planning/REQUIREMENTS.md | awk '{ print $1 }'` returns approximately 205 (the same as before, ± 1 line for the BACKEND-01 wording change).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git staged content → repo | Pre-commit hook scans every commit's added lines for secret patterns + deprecated SDK references. Runs in developer's git, NOT in process — local environment, not a code-execution surface. |
| Pull-request push → CI | GitHub Actions runs the same pre-commit hooks + import smoke + lockfile grep. Triple-redundant OSS-06 enforcement. |
| Repo secrets (OPENROUTER_API_KEY etc.) → live-smoke workflow | Manual `workflow_dispatch` or weekly cron; gated by `if: ${{ secrets.OPENROUTER_API_KEY != '' }}` so forks without the secret no-op. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | I (secret commit to repo) | `.pre-commit-config.yaml` + `scripts/no-secrets.sh` | mitigate | Block staged content matching the locked regex (`sk-ant-[A-Za-z0-9_-]{8,}|sk-[A-Za-z0-9]{20,}|Bearer [A-Za-z0-9_.-]{20,}`). Manual verification in Task 1 acceptance criteria via deliberate paste. CI also runs `pre-commit run --all-files`. SEVERITY: HIGH. |
| T-02-08 | T (deprecated SDK re-introduction) | `scripts/no-deprecated-sdk.sh` + CI smoke + uv.lock grep | mitigate | OSS-06 triad per RESEARCH Pattern 14: (a) pre-commit hook blocks staged `claude_code_sdk` imports and lockfile entries; (b) CI runs `python -c "from claude_agent_sdk import ClaudeAgentOptions"` import smoke; (c) CI runs `! grep -q '"claude-code-sdk"' uv.lock`. SEVERITY: MEDIUM. |
| T-02-11 (NEW: cron-driven cost) | D (wallet) | `.github/workflows/live-smoke.yml` weekly cron | accept (with controls) | (a) Gated by `secrets.OPENROUTER_API_KEY != ''` so forks don't run it; (b) `continue-on-error: true` so failures don't cascade; (c) `pytest -m live -x --maxfail=1` so one budget hit aborts; (d) Per-test `max_cost_usd=0.10` (OpenRouter live test contract). Weekly cadence + $0.10/run = ~$0.40/month per repo with the secret set. SEVERITY: LOW. |

Threats T-02-01 (logging key leak — inherited Wave 0), T-02-02 (BYOK disk leak — inherited Wave 0), T-02-03 (cost cap — Wave 1), T-02-04 (cancellation — Wave 1), T-02-05 (workspace leak — Plan 02), T-02-06 (computer-use opt-in — Plan 03) are NOT in this plan's scope but are guarded by the CI gates established here (specifically: the `pytest -m 'not live' apps/api/backends` step verifies the runtime invariants enforced by Plans 00-03).
</threat_model>

<verification>
- `uv run pre-commit run --all-files` exits 0 on a clean checkout.
- A deliberately-staged secret-prefixed file triggers `pre-commit run no-secrets` to exit non-zero (Task 1 acceptance criterion).
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); yaml.safe_load(open('.github/workflows/live-smoke.yml'))"` exits 0 (both workflows parse).
- All CI steps listed in CONTEXT D-20 lines 120-132 are present in `.github/workflows/ci.yml` (verified by grep on each step's name).
- `.planning/REQUIREMENTS.md` BACKEND-01 contains the 7-variant union with `ToolResult`.
- `.planning/REQUIREMENTS.md` BACKEND-06 contains `per-iteration step cap (25 for Claude Code, 15 for computer-use)`.
- `! grep -q '"claude-code-sdk"' uv.lock` exits 0 — deprecated SDK absent from lockfile (OSS-06).
- The CI workflow, when run on this PR, runs `pytest src/` (Phase 1 D-18 guard) AND `pytest -m 'not live' apps/api/backends` (Phase 2 unit + contract suite) AND `pre-commit run --all-files` (SECURE-02 + OSS-06) AND the import smoke (OSS-06) AND the lockfile grep (OSS-06) — all expected green.
</verification>

<success_criteria>
- SECURE-02 satisfied: `.pre-commit-config.yaml` + `scripts/no-secrets.sh` installed and runnable; pre-commit framework blocks staged content matching the locked regex. CI runs the same hooks on every push.
- OSS-06 satisfied: Triad fully in place — (a) pre-commit hook for staged `claude_code_sdk` imports + uv.lock entries, (b) CI import smoke `from claude_agent_sdk import ClaudeAgentOptions`, (c) CI absence assertion `! grep -q '"claude-code-sdk"' uv.lock`. The `pyproject.toml` pin (Wave 0 plan) is the fourth guardrail.
- `.planning/REQUIREMENTS.md` reconciled with CONTEXT D-02 and D-15.
- Optional `live-smoke.yml` workflow ships behind `workflow_dispatch` + weekly cron + secret gate + `continue-on-error: true`.
- The full Phase 2 success criteria (#1-#5) are now verifiable end-to-end on a fresh CI run:
  - SC #1: Per-adapter CLIs work — verified by Plans 01/02/03 acceptance criteria.
  - SC #2: Cost/step caps + cancellation — verified by D-19 contract suite (Wave 0 + Wave 1).
  - SC #3: Logger redaction + BYOK key store — verified by Wave 0 unit tests.
  - SC #4: OSS-06 + watchdog + OpenRouter headers — verified by Plan 04 CI extension + Plan 01 unit tests + Plan 02 watchdog test.
  - SC #5: COMPUTER_USE_OPT_IN + Claude Code workspace + pre-commit hook — verified by Plan 03 opt-in tests + Plan 02 workspace tests + Plan 04 pre-commit setup.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-adapters-chatchunk-contract/02-04-SUMMARY.md` summarizing:
- 3 new ops files (.pre-commit-config.yaml, 2 scripts) + 2 workflow files modified/created.
- Manual deliberate-secret-paste test outcome (paste `sk-ant-AAAA...AAAA` → `git add` → `pre-commit run no-secrets` exits 1).
- Deliberate `claude_code_sdk` import attempt outcome (add line `import claude_code_sdk` to a scratch file → `git add` → `pre-commit run no-deprecated-claude-code-sdk` exits 1).
- Confirmation that CI workflow YAML parses and that on a fresh clone `uv run pre-commit run --all-files` exits 0.
- `.planning/REQUIREMENTS.md` BACKEND-01 + BACKEND-06 reconciliation diff (one-line change in BACKEND-01; no change in BACKEND-06 if already correct).
- Any deviations from RESEARCH Pattern 11 / 14.

After Plan 04 completes: Phase 2 is DONE. The phase verifier (`/gsd-verify-work`) should:
- Confirm all 5 ROADMAP §"Phase 2" success criteria pass.
- Confirm all 15 phase requirement IDs are covered (BACKEND-01..09, SECURE-01/02/04/05, OSS-06).
- Confirm all 20 CONTEXT decisions (D-01..D-20) are present in at least one plan's `must_haves`, task, or acceptance criterion.
- Confirm the D-18 import-graph guard in `src/routing/tests/test_decide_smoke.py` is still green.
</output>
</content>
