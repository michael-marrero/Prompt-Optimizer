---
phase: 02
plan: 01
plan_id: "02-01-openrouter-adapter"
type: execute
wave: 1
depends_on: ["02-00-scaffolding-and-shared-modules"]
files_modified:
  - "apps/api/backends/openrouter/__init__.py"
  - "apps/api/backends/openrouter/__main__.py"
  - "apps/api/backends/openrouter/adapter.py"
  - "apps/api/backends/openrouter/cost.py"
  - "apps/api/backends/openrouter/errors.py"
  - "apps/api/backends/openrouter/tests/__init__.py"
  - "apps/api/backends/openrouter/tests/conftest.py"
  - "apps/api/backends/openrouter/tests/fakes.py"
  - "apps/api/backends/openrouter/tests/test_adapter.py"
  - "apps/api/backends/openrouter/tests/test_live.py"
autonomous: true
requirements: [BACKEND-03, BACKEND-06, BACKEND-07]
must_haves:
  truths:
    - "`OpenRouterAdapter(api_key='')` raises a typed exception (`openai.AuthenticationError` or equivalent) BEFORE the constructor returns (D-19 invariant #6)."
    - "`OpenRouterAdapter(api_key='fake', client_factory=lambda k: FakeOpenAIClient([...]))` constructed; `adapter.stream(prompt='hi', history=[], options=AdapterOptions())` yields at least one `TextDelta` and ends with `Done` (D-19 invariant #1)."
    - "The AsyncOpenAI client constructor receives `base_url='https://openrouter.ai/api/v1'` and `default_headers={'HTTP-Referer': 'https://github.com/marreroii-michael/Prompt-Optimizer', 'X-Title': 'Prompt-Optimizer'}` (BACKEND-03 + CONTEXT discretion line 141 + Pitfall 3)."
    - "Every `client.chat.completions.create()` call sets `stream_options={'include_usage': True}` so the final usage chunk arrives and tracker.record_final_usage overrides the tiktoken pre-flight estimate (Pitfall 1)."
    - "`max_cost_usd=0.000001` with a long generation aborts mid-stream and the chunk list contains `StreamError(code='cost_cap_exceeded', retriable=False)` followed by `Done` (BACKEND-06; D-19 invariant #2)."
    - "Cancelling the consuming task within 50 ms of stream start lands `StreamError(code='cancelled', retriable=True)` + `Done` within 2 s and the underlying httpx response is aborted via `in_flight.close()` (BACKEND-07; D-19 invariant #4)."
    - "Tool-call deltas accumulate by `index`; on `finish_reason == 'tool_calls'` a `ToolCall` chunk is emitted per accumulated index, with `tool_call_id` from `tc_<6-char-base32>` if the provider omits one (CONTEXT specifics line 257)."
    - "`python -m apps.api.backends.openrouter --prompt 'hi'` (with `OPENROUTER_API_KEY` in env or `.env`) writes one JSON line per ChatChunk to stdout, last line has `\"type\":\"done\"` (Phase 2 SC #1)."
  artifacts:
    - path: "apps/api/backends/openrouter/__init__.py"
      provides: "Re-export `OpenRouterAdapter`"
      contains: "from apps.api.backends.openrouter.adapter import OpenRouterAdapter|__all__"
    - path: "apps/api/backends/openrouter/__main__.py"
      provides: "CLI: `python -m apps.api.backends.openrouter --prompt '...'` per Phase 2 SC #1"
      contains: "argparse.ArgumentParser|--prompt|--model|--max-cost-usd|asyncio.run|model_dump_json"
    - path: "apps/api/backends/openrouter/adapter.py"
      provides: "`OpenRouterAdapter` class implementing `BackendAdapter` Protocol (BACKEND-03, BACKEND-06, BACKEND-07)"
      contains: "class OpenRouterAdapter|HTTP_REFERER|X_TITLE|OPENROUTER_BASE_URL|stream_options={\"include_usage\": True}|except asyncio.CancelledError|in_flight.close()|tracker.over_cap()|raise"
      min_lines: 100
    - path: "apps/api/backends/openrouter/cost.py"
      provides: "`OpenRouterCostTracker(CostTracker)` with tiktoken pre-flight + record_final_usage"
      contains: "class OpenRouterCostTracker(CostTracker)|tiktoken|encoding_for_model|record_input_estimate|record_final_usage"
    - path: "apps/api/backends/openrouter/errors.py"
      provides: "`PROVIDER_ERROR_MAP` + `map_provider_error(exc)` translating openai exceptions to StreamError code/retriable"
      contains: "PROVIDER_ERROR_MAP|def map_provider_error|openai.AuthenticationError|openai.RateLimitError|openai.APITimeoutError"
    - path: "apps/api/backends/openrouter/tests/fakes.py"
      provides: "`FakeOpenAIClient`, `FakeAsyncStream`, `FakeChunk`, `FakeChoice`, `FakeDelta`, `FakeUsage` per RESEARCH Common Operation 3"
    - path: "apps/api/backends/openrouter/tests/test_adapter.py"
      provides: "Unit tests: header attribution, stream_options inclusion, cost-cap abort, cancellation, tool-call delta accumulation, missing-key raise"
    - path: "apps/api/backends/openrouter/tests/test_live.py"
      provides: "`@pytest.mark.live` opt-in test against real OpenRouter API"
      contains: "@pytest.mark.live|skipif.*OPENROUTER_API_KEY"
  key_links:
    - from: "apps/api/backends/openrouter/adapter.py"
      to: "openai.AsyncOpenAI"
      via: "_default_client_factory"
      pattern: "AsyncOpenAI\\(\\s*base_url=OPENROUTER_BASE_URL"
    - from: "apps/api/backends/openrouter/adapter.py"
      to: "apps.api.backends.chunks (TextDelta, ToolCall, StreamError, Done)"
      via: "module import + yield"
      pattern: "yield (TextDelta|ToolCall|StreamError|Done)"
    - from: "apps/api/backends/openrouter/adapter.py"
      to: "apps.api.backends.openrouter.cost.OpenRouterCostTracker"
      via: "module import + instantiation"
      pattern: "OpenRouterCostTracker\\("
    - from: "apps/api/backends/openrouter/__main__.py"
      to: "apps.api.backends.openrouter.adapter.OpenRouterAdapter"
      via: "instantiate + iterate stream"
      pattern: "OpenRouterAdapter\\(api_key=.*\\)\\s*\\n.*\\n.*async for chunk in adapter.stream"
    - from: "apps/api/backends/tests/test_adapter_contract.py"
      to: "apps.api.backends.openrouter.adapter.OpenRouterAdapter"
      via: "lazy import in adapter_factory fixture"
      pattern: "openrouter.adapter import OpenRouterAdapter"
---

<objective>
Implement the OpenRouter adapter — async streaming via OpenAI SDK 2.36 pointed at `https://openrouter.ai/api/v1`, ChatChunk-emitting, with per-turn cost cap, 2-second cancellation budget, HTTP-Referer + X-Title attribution headers, and the `stream_options.include_usage` flag set.

Purpose: BACKEND-03 — the OpenRouter adapter is the conversational backend (Claude Sonnet, GPT-5, Gemini, DeepSeek, Qwen, Kimi via OpenRouter). It is the lightest of the three adapters (no tool execution, no subprocess, no browser) and the de-facto contract reference.

Output:
  - 5 source files at `apps/api/backends/openrouter/{__init__,__main__,adapter,cost,errors}.py`.
  - 4 test files at `apps/api/backends/openrouter/tests/{conftest,fakes,test_adapter,test_live}.py`.
  - The Wave 0 D-19 contract suite passes for the `openrouter` parameterization.
  - `python -m apps.api.backends.openrouter --prompt "hi"` is callable end-to-end (with a real key).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backend-adapters-chatchunk-contract/02-CONTEXT.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-PATTERNS.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-00-SUMMARY.md
@apps/api/backends/chunks.py
@apps/api/backends/protocol.py
@apps/api/backends/cost.py
@apps/api/backends/pricing.py
@apps/api/backends/tests/conftest.py
@apps/api/backends/tests/test_adapter_contract.py
@src/routing/decide.py
@src/routing/__main__.py
@config/pricing.json

<interfaces>
<!-- Types imported from Wave 0 shared modules. DO NOT redefine. -->

From `apps/api/backends/chunks.py`:
- `TextDelta(text: str)`, `ToolCall(tool_call_id, tool_name, arguments)`, `Done(tokens_in, tokens_out, cost_usd, latency_ms, routing_signals)`, `StreamError(code, message, retriable)`
- `ChatChunk` is the union type alias used in return annotation

From `apps/api/backends/protocol.py`:
- `class BackendAdapter(Protocol): async def stream(...) -> AsyncIterator[ChatChunk]: ...`
- `Message(role, content)`, `AdapterOptions(model, max_cost_usd, max_steps, cwd, routing_signals)`

From `apps/api/backends/cost.py`:
- `class CostTracker(*, model_id, max_cost_usd, pricing)` with `record_input(n)`, `record_output(n)`, `tokens_in()`, `tokens_out()`, `total()`, `over_cap()`
- `DEFAULT_PER_TURN_COST_USD: Final[float] = 0.50`

From `apps/api/backends/pricing.py`:
- `class PricingTable.from_static(path) -> PricingTable`

From `openai` (external, 2.36+):
- `AsyncOpenAI(base_url, api_key, default_headers) -> client`
- `client.chat.completions.create(model, messages, stream=True, stream_options={"include_usage": True}) -> AsyncIterator[ChatCompletionChunk]`
- `ChatCompletionChunk.choices[0].delta.content: str | None`
- `ChatCompletionChunk.choices[0].delta.tool_calls: list[ChoiceDeltaToolCall] | None`
- `ChatCompletionChunk.choices[0].finish_reason: str | None`
- `ChatCompletionChunk.usage: CompletionUsage | None` (only on final chunk when `include_usage=True`)
- Exception classes: `AuthenticationError`, `RateLimitError`, `APITimeoutError`, `APIStatusError`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author OpenRouterCostTracker + error-mapping + tests-only fakes</name>
  <files>apps/api/backends/openrouter/__init__.py, apps/api/backends/openrouter/cost.py, apps/api/backends/openrouter/errors.py, apps/api/backends/openrouter/tests/__init__.py, apps/api/backends/openrouter/tests/conftest.py, apps/api/backends/openrouter/tests/fakes.py</files>
  <read_first>
    - apps/api/backends/cost.py (the base `CostTracker` class to subclass)
    - apps/api/backends/pricing.py (the `PricingTable.get` API the tracker calls)
    - apps/api/backends/chunks.py (StreamError closed code vocabulary)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md §"Pattern 6" lines 1355-1379 (`OpenRouterCostTracker` source) and §"Common Operation 3" lines 1916-1979 (FakeOpenAIClient full source)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-PATTERNS.md §"openrouter/cost.py" lines 583-606 (`policy.py` style; keyword-only flag; mirror `src/routing/policy.py`)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-PATTERNS.md §"openrouter/errors.py" lines 612-633 (mapping table shape)
    - src/routing/policy.py (module docstring style; stdlib-only imports + own package's config module)
    - src/routing/tests/conftest.py (session-scoped fixture style)
  </read_first>
  <behavior>
- Test 1: `OpenRouterCostTracker(model_id="openai/gpt-5", max_cost_usd=0.50, pricing=PricingTable.from_static("config/pricing.json"))` constructed. `record_input_estimate(prompt="hello", history=[])` increments `tokens_in()` by `len(tiktoken.encoding_for_model("gpt-4").encode("hello"))`.
- Test 2: `tracker.record_output_delta("world")` increments `tokens_out()` by 1 token.
- Test 3: `tracker.record_final_usage(prompt_tokens=100, completion_tokens=50)` overrides the running estimate so `tokens_in() == 100` and `tokens_out() == 50`.
- Test 4: `tracker.over_cap()` returns False at construction; returns True once `record_input` + `record_output` produce a cost exceeding `max_cost_usd` per the static pricing rates.
- Test 5: `map_provider_error(openai.AuthenticationError("bad key", response=None, body=None))` returns `("auth_failed", "bad key", False)`.
- Test 6: `map_provider_error(openai.RateLimitError("429", response=None, body=None))` returns `("rate_limited", <msg>, True)`.
- Test 7: `map_provider_error(ValueError("unrelated"))` returns `("internal_error", "unrelated", False)`.
- Test 8: `FakeOpenAIClient([FakeChunk(choices=[FakeChoice(delta=FakeDelta(content="hi"), finish_reason="stop")]), FakeChunk(choices=[], usage=FakeUsage(prompt_tokens=2, completion_tokens=1))])` exposes `.chat.completions.create(**kw)` which returns a `FakeAsyncStream`; `async for c in stream:` yields the two fake chunks in order; `await stream.close()` is a no-op.
  </behavior>
  <action>
Write `apps/api/backends/openrouter/__init__.py` per PATTERNS line 434-443:
- Module docstring (one-line purpose; "Public surface (BACKEND-03)"; cross-ref to adapter.py).
- `from apps.api.backends.openrouter.adapter import OpenRouterAdapter`
- `__all__ = ["OpenRouterAdapter"]`

Write `apps/api/backends/openrouter/cost.py` per RESEARCH Pattern 6 lines 1355-1377:
- Module docstring (mirror `src/routing/policy.py` style).
- `from __future__ import annotations`.
- Imports: `tiktoken`, `from apps.api.backends.cost import CostTracker`, `from apps.api.backends.pricing import PricingTable`, `from apps.api.backends.protocol import Message`.
- `class OpenRouterCostTracker(CostTracker):` with class-level `_ENCODING = tiktoken.encoding_for_model("gpt-4")` (gpt-4 encoder is "close enough" for non-OpenAI slugs per RESEARCH assumption A6).
- Methods: `record_input_estimate(self, prompt: str, history: list[Message]) -> None` (joins prompt + history.content + encodes + `self.record_input(len(...))`); `record_output_delta(self, text: str) -> None` (encodes and `self.record_output(len(...))`); `record_final_usage(self, prompt_tokens: int, completion_tokens: int) -> None` (overrides `_tokens_in` and `_tokens_out` directly).
- Constructor accepts a `pricing: PricingTable` parameter forwarded to `super().__init__(model_id=..., max_cost_usd=..., pricing=pricing)`. Keyword-only style with `*,` separator.

Write `apps/api/backends/openrouter/errors.py` per PATTERNS line 612-633:
- Module docstring (one-line; cross-ref to D-06 closed code vocabulary).
- Imports: `import openai`.
- `PROVIDER_ERROR_MAP: dict[type[Exception], tuple[str, bool]]` mapping `openai.AuthenticationError → ("auth_failed", False)`, `openai.RateLimitError → ("rate_limited", True)`, `openai.APITimeoutError → ("timeout", True)`, `openai.APIStatusError → ("provider_unavailable", True)`.
- `def map_provider_error(exc: Exception) -> tuple[str, str, bool]`: iterate `PROVIDER_ERROR_MAP`, return `(code, str(exc), retriable)` on first `isinstance` match; default to `("internal_error", str(exc), False)`.
- Only use codes from the closed D-06 vocabulary defined in `apps/api/backends/chunks.py` `StreamError.code` Literal.

Write `apps/api/backends/openrouter/tests/__init__.py` as empty package marker.

Write `apps/api/backends/openrouter/tests/conftest.py`:
- `from __future__ import annotations`, stdlib imports, `pytest`.
- `REPO_ROOT = os.path.abspath(os.path.join(__file__, "..", "..", "..", "..", ".."))` (5 ups: `apps/api/backends/openrouter/tests/conftest.py` → repo root).
- Module-scoped `pricing_table` fixture loading `config/pricing.json`.
- `fake_openai_client` fixture returning a `FakeOpenAIClient` instance pre-loaded with a 2-chunk happy-path sequence.
- (Optional) `fake_openai_factory` fixture returning a callable `lambda api_key: FakeOpenAIClient([...])` for injection.

Write `apps/api/backends/openrouter/tests/fakes.py` per RESEARCH lines 1916-1979 verbatim:
- `@dataclass` classes: `FakeDelta` (content + tool_calls), `FakeChoice` (delta + finish_reason), `FakeUsage` (prompt_tokens + completion_tokens), `FakeChunk` (choices + usage).
- `class FakeAsyncStream` with `__aiter__` returning an async generator over `self._chunks` and `async def close(self): pass`.
- `class FakeChatCompletions` with `async def create(self, **_kw) -> FakeAsyncStream`.
- `class FakeOpenAIClient` exposing `.chat.completions` as a `FakeChatCompletions` instance via the standard `type("Chat", (), {"completions": FakeChatCompletions(chunks)})()` shape.
- All tests in this plan and Wave 0's contract suite consume these fakes — DO NOT inline them anywhere else.

Every file: `from __future__ import annotations`; `logger = logging.getLogger(__name__)` in production modules; type-annotated public functions; snake_case modules per CONVENTIONS.
  </action>
  <verify>
    <automated>python -c "from apps.api.backends.openrouter.cost import OpenRouterCostTracker; from apps.api.backends.pricing import PricingTable; t = OpenRouterCostTracker(model_id='openai/gpt-5', max_cost_usd=0.50, pricing=PricingTable.from_static('config/pricing.json')); t.record_input(100); t.record_output(100); print('OK cost', t.over_cap(), t.total())" && python -c "from apps.api.backends.openrouter.errors import map_provider_error, PROVIDER_ERROR_MAP; import openai; assert len(PROVIDER_ERROR_MAP) >= 4; print('OK errors')" && python -c "from apps.api.backends.openrouter.tests.fakes import FakeOpenAIClient, FakeChunk, FakeChoice, FakeDelta, FakeUsage; c = FakeOpenAIClient([FakeChunk(choices=[FakeChoice(delta=FakeDelta(content='hi'), finish_reason='stop')])]); print('OK fakes')"</automated>
  </verify>
  <done>
- 5 helper modules (`__init__`, `cost`, `errors`, `tests/conftest`, `tests/fakes`) exist.
- `OpenRouterCostTracker` is importable and computes cost correctly.
- `map_provider_error` returns valid D-06 codes for every openai exception class.
- `FakeOpenAIClient` exposes `.chat.completions.create()` and the returned stream yields chunks via `async for`.
  </done>
  <acceptance_criteria>
- Source: `grep -q "class OpenRouterCostTracker(CostTracker)" apps/api/backends/openrouter/cost.py`.
- Source: `grep -q "tiktoken.encoding_for_model" apps/api/backends/openrouter/cost.py`.
- Source: `grep -q "def record_input_estimate" apps/api/backends/openrouter/cost.py` AND `grep -q "def record_output_delta" apps/api/backends/openrouter/cost.py` AND `grep -q "def record_final_usage" apps/api/backends/openrouter/cost.py`.
- Source: `grep -q "PROVIDER_ERROR_MAP" apps/api/backends/openrouter/errors.py` AND `grep -q "def map_provider_error" apps/api/backends/openrouter/errors.py`.
- Source: All 4 mapped exception classes appear: `grep -cE "openai\\.(AuthenticationError|RateLimitError|APITimeoutError|APIStatusError)" apps/api/backends/openrouter/errors.py` returns ≥ 4.
- Source: `grep -q "class FakeOpenAIClient" apps/api/backends/openrouter/tests/fakes.py` AND `grep -q "class FakeAsyncStream" apps/api/backends/openrouter/tests/fakes.py`.
- Source: `grep -q "__all__ = \[\"OpenRouterAdapter\"\]" apps/api/backends/openrouter/__init__.py`.
- Behavior: `python -c "from apps.api.backends.openrouter.cost import OpenRouterCostTracker; from apps.api.backends.pricing import PricingTable; PricingTable.from_static('config/pricing.json')"` exits 0.
- Anti-pattern: `! grep -E "^[^#]*print\(" apps/api/backends/openrouter/cost.py apps/api/backends/openrouter/errors.py` matches no production print statements.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement OpenRouterAdapter (stream method + cancellation + cost cap + tool-call delta accumulation)</name>
  <files>apps/api/backends/openrouter/adapter.py</files>
  <read_first>
    - apps/api/backends/chunks.py (the 7 ChatChunk variants and the closed StreamError code vocabulary)
    - apps/api/backends/protocol.py (`BackendAdapter` Protocol; `Message`; `AdapterOptions`)
    - apps/api/backends/cost.py (`DEFAULT_PER_TURN_COST_USD = 0.50`)
    - apps/api/backends/openrouter/cost.py (`OpenRouterCostTracker` API)
    - apps/api/backends/openrouter/errors.py (`map_provider_error`)
    - apps/api/backends/openrouter/tests/fakes.py (the shape the adapter consumes — duck-typed against `FakeOpenAIClient`)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md §"Pattern 3" lines 472-655 (`OpenRouterAdapter` full source — copy verbatim)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md §"Pattern 7" lines 1381-1418 (CancelledError handling — emit terminal pair then re-raise)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md §"Pitfall 1" lines 1767-1775 (stream_options.include_usage), §"Pitfall 3" lines 1787-1795 (default_headers vs extra_headers)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-CONTEXT.md D-04 (terminal pair invariant), D-06 (StreamError vocabulary), D-11 (env var convention), discretion line 141 (HTTP-Referer + X-Title values)
    - src/routing/decide.py (V7 robustness wrapping; module docstring + import order style)
  </read_first>
  <behavior>
- Test 1: `OpenRouterAdapter(api_key="")` raises `openai.AuthenticationError` BEFORE returning from `__init__` (D-19 invariant #6).
- Test 2: Constructor with `api_key="fake"`, `client_factory=lambda k: FakeOpenAIClient([FakeChunk(choices=[FakeChoice(delta=FakeDelta(content="hi"), finish_reason="stop")]), FakeChunk(choices=[], usage=FakeUsage(prompt_tokens=10, completion_tokens=5))])` — `adapter.stream(prompt="x", history=[], options=AdapterOptions(model="openai/gpt-5"))` yields chunks where the first is `TextDelta(text="hi")` and the last is `Done(tokens_in=10, tokens_out=5, ...)`. (D-19 invariant #1 + Pitfall 1)
- Test 3: With `client_factory` that records the constructor kwargs, the `default_headers` argument contains both `"HTTP-Referer": "https://github.com/marreroii-michael/Prompt-Optimizer"` and `"X-Title": "Prompt-Optimizer"` per Pitfall 3 + CONTEXT discretion line 141. (BACKEND-03)
- Test 4: With a `client_factory` that records `.chat.completions.create` kwargs, the call includes `stream_options={"include_usage": True}` (Pitfall 1).
- Test 5: With `max_cost_usd=0.000001` and a long fake stream emitting many `delta.content` deltas, the chunk list contains `StreamError(code="cost_cap_exceeded", retriable=False)` and ends with `Done` (D-19 invariant #2). The loop must `break` immediately after the cap is detected.
- Test 6: Tool-call delta accumulation — when the fake stream emits two chunks each with `delta.tool_calls=[ChoiceDeltaToolCall(index=0, id=None, function=ChoiceDeltaToolCallFunction(name="weather", arguments='{"city":'))]` and `delta.tool_calls=[ChoiceDeltaToolCall(index=0, function=ChoiceDeltaToolCallFunction(arguments='"Paris"}'))]` then a third chunk with `finish_reason="tool_calls"`, the adapter emits exactly one `ToolCall(tool_name="weather", arguments={"city": "Paris"})`. `tool_call_id` is set to `tc_<6-char-base32>` when the provider doesn't supply one (CONTEXT specifics line 257). Implementation note: use `secrets.choice` over base32 alphabet `"abcdefghijklmnopqrstuvwxyz234567"`.
- Test 7: Cancellation — start the adapter via `task = asyncio.create_task(consume_into_list(...))`, await 50 ms, `task.cancel()`. Within 2 s, the chunk list (if any landed) ends with `Done`. The `try/except asyncio.CancelledError` block MUST emit `StreamError(code="cancelled", retriable=True)` then `Done` then `raise` (Pattern 7). The `finally` block MUST call `in_flight.close()` to abort the underlying httpx stream. (BACKEND-07; D-19 invariant #4)
- Test 8: When `openai.AuthenticationError` is raised mid-stream by the fake, the adapter emits `StreamError(code="auth_failed", retriable=False)` + `Done` and does NOT re-raise.
- Test 9: When `openai.APITimeoutError` is raised mid-stream, the adapter emits `StreamError(code="timeout", retriable=True)` + `Done`.
- Test 10: When `openai.APIStatusError(status_code=429)` is raised, code becomes `"rate_limited"`; for `status_code=503`, code becomes `"provider_unavailable"`.
- Test 11: When an unrelated `RuntimeError` is raised, the adapter emits `StreamError(code="internal_error", retriable=False)` + `Done`. `logger.exception(...)` is called (verify via caplog).
- Test 12: `Done.routing_signals` equals `options.routing_signals` (passed through from `AdapterOptions`).
  </behavior>
  <action>
Implement `apps/api/backends/openrouter/adapter.py` per RESEARCH Pattern 3 (lines 472-655). The source there is the canonical full implementation — read it carefully and reproduce structurally (the planner allows minor identifier renames; the algorithm and error-handling shape MUST match).

Module docstring (mirror `src/routing/decide.py:1-40` style):
- One-line purpose: "OpenRouter adapter — async streaming via OpenAI SDK v1.40+."
- Public surface block: `class OpenRouterAdapter` + `async def stream(prompt, history, options) -> AsyncIterator[ChatChunk]`.
- Pipeline description with 6 numbered stages (pre-flight estimate → create stream → per-chunk loop → tool-call flush → final usage override → terminal Done).

Module-level constants (mirror `src/routing/config.py` SCREAMING_SNAKE_CASE):
- `OPENROUTER_BASE_URL = "https://openrouter.ai/api/v1"`
- `HTTP_REFERER = "https://github.com/marreroii-michael/Prompt-Optimizer"` (project owner per `userEmail` in CLAUDE.md context — replace placeholder if username different).
- `X_TITLE = "Prompt-Optimizer"`

Module-level helper: `def _gen_tool_call_id() -> str:` returns `"tc_" + "".join(secrets.choice("abcdefghijklmnopqrstuvwxyz234567") for _ in range(6))` per CONTEXT specifics line 257.

`OpenRouterAdapter.__init__(self, api_key: str, *, max_cost_usd: float = DEFAULT_PER_TURN_COST_USD, client_factory=None)`:
- Raise `openai.AuthenticationError("OPENROUTER_API_KEY not set", response=None, body=None)` if `not api_key` (D-19 invariant #6 — typed exception BEFORE stream).
- `self._client = (client_factory or self._default_client_factory)(api_key)`.
- `self._max_cost = max_cost_usd`.
- `self._pricing = PricingTable.from_static("config/pricing.json")` cached at the module level OR constructed once per adapter — planner choice; both acceptable. Use `pathlib.Path(__file__).resolve().parents[3] / "config" / "pricing.json"` to resolve the path (5 levels: `apps/api/backends/openrouter/adapter.py` → `apps/api/backends/openrouter` → `apps/api/backends` → `apps/api` → `apps` → repo root, then `/config/pricing.json`). Actually `parents[3]` for `adapter.py` → repo root.

`OpenRouterAdapter._default_client_factory(api_key) -> AsyncOpenAI` static method:
- `AsyncOpenAI(base_url=OPENROUTER_BASE_URL, api_key=api_key, default_headers={"HTTP-Referer": HTTP_REFERER, "X-Title": X_TITLE})` per Pitfall 3 + BACKEND-03.

`async def stream(self, prompt, history, options) -> AsyncIterator[ChatChunk]`:
- Construct `tracker = OpenRouterCostTracker(model_id=options.model or "openai/gpt-5", max_cost_usd=options.max_cost_usd or self._max_cost, pricing=self._pricing)`.
- Call `tracker.record_input_estimate(prompt, history)` (tiktoken pre-flight).
- Build messages list from `history` + `{"role": "user", "content": prompt}`.
- `start_t = asyncio.get_event_loop().time()`.
- `in_flight = None`.
- `try:` block:
  - `in_flight = await self._client.chat.completions.create(model=..., messages=messages, stream=True, stream_options={"include_usage": True})` (Pitfall 1 — `include_usage=True` is MANDATORY).
  - `tool_calls: dict[int, dict] = {}` for tool-call delta accumulation.
  - `async for chunk in in_flight:`:
    - If `chunk.usage is not None`: call `tracker.record_final_usage(chunk.usage.prompt_tokens, chunk.usage.completion_tokens)` and `continue`.
    - If `not chunk.choices`: `continue`.
    - `delta = chunk.choices[0].delta; finish_reason = chunk.choices[0].finish_reason`.
    - If `delta.content`: `tracker.record_output_delta(delta.content)`; `yield TextDelta(text=delta.content)`.
    - If `delta.tool_calls`: for each `tc_delta`, accumulate into `tool_calls[tc_delta.index]` (set `id`, `name`, append `arguments`).
    - If `finish_reason == "tool_calls"`: for each `slot` in `tool_calls.values()`: parse arguments via `json.loads(slot["arguments"])` (fallback to `{"_raw": slot["arguments"]}` on `JSONDecodeError`); `yield ToolCall(tool_call_id=slot["id"] or _gen_tool_call_id(), tool_name=slot["name"], arguments=args)`.
    - If `tracker.over_cap()`: `yield StreamError(code="cost_cap_exceeded", message=f"Cost cap ${tracker.max_cost_usd:.4f} exceeded (used ${tracker.total():.4f}).", retriable=False)` and `break`.
  - After loop: `latency_ms = int((asyncio.get_event_loop().time() - start_t) * 1000)`; `yield Done(tokens_in=tracker.tokens_in(), tokens_out=tracker.tokens_out(), cost_usd=tracker.total(), latency_ms=latency_ms, routing_signals=options.routing_signals)`.
- `except asyncio.CancelledError:` (Pattern 7):
  - `yield StreamError(code="cancelled", message="Stream cancelled by caller.", retriable=True)`.
  - `yield Done(tokens_in=tracker.tokens_in(), tokens_out=tracker.tokens_out(), cost_usd=tracker.total(), latency_ms=..., routing_signals=options.routing_signals)`.
  - `raise` (mandatory in Python 3.11+ — PEP 789).
- `except AuthenticationError as exc:` → `yield StreamError(code="auth_failed", ..., retriable=False); yield Done(...)`.
- `except APITimeoutError as exc:` → `yield StreamError(code="timeout", ..., retriable=True); yield Done(...)`.
- `except APIStatusError as exc:` → `code = "rate_limited" if exc.status_code == 429 else "provider_unavailable"; yield StreamError(code=code, ..., retriable=(code == "rate_limited")); yield Done(...)`.
- `except Exception as exc:` → `logger.exception("OpenRouter adapter internal error"); yield StreamError(code="internal_error", ..., retriable=False); yield Done(...)`. Do NOT re-raise (V7 robustness — let the generator terminate via StopAsyncIteration).
- `finally:` — `if in_flight is not None: try: await in_flight.close() except Exception: pass`.

Mandatory: `from __future__ import annotations` first; `logger = logging.getLogger(__name__)`; type-annotated `async def stream(self, prompt: str, history: list[Message], options: AdapterOptions) -> AsyncIterator[ChatChunk]:`; NEVER `print()` (CONTEXT anti-pattern line 247; redaction filter only intercepts `logging.LogRecord`); NEVER `sys.path.append`.
  </action>
  <verify>
    <automated>python -c "from apps.api.backends.openrouter.adapter import OpenRouterAdapter, OPENROUTER_BASE_URL, HTTP_REFERER, X_TITLE; assert OPENROUTER_BASE_URL == 'https://openrouter.ai/api/v1'; assert X_TITLE == 'Prompt-Optimizer'; print('OK adapter import')" && python -c "import openai; from apps.api.backends.openrouter.adapter import OpenRouterAdapter; raised = False
try: OpenRouterAdapter(api_key='')
except openai.AuthenticationError: raised = True
assert raised, 'expected AuthenticationError on empty api_key'
print('OK missing-key raise')" && grep -q 'stream_options={"include_usage": True}' apps/api/backends/openrouter/adapter.py && grep -q "HTTP-Referer" apps/api/backends/openrouter/adapter.py && grep -q "X-Title" apps/api/backends/openrouter/adapter.py && grep -q "except asyncio.CancelledError" apps/api/backends/openrouter/adapter.py && grep -q "in_flight.close" apps/api/backends/openrouter/adapter.py</automated>
  </verify>
  <done>
- `apps/api/backends/openrouter/adapter.py` exists with `class OpenRouterAdapter` defining `async def stream`.
- Constructor raises `openai.AuthenticationError` on empty api_key.
- `stream_options={"include_usage": True}` literal appears in source (Pitfall 1).
- `HTTP-Referer` and `X-Title` headers appear in source (BACKEND-03).
- `except asyncio.CancelledError` block exists with `raise` re-raise (PEP 789).
- `in_flight.close()` in `finally` block (cancellation propagation).
- Cost cap check uses `tracker.over_cap()` and emits `StreamError(code="cost_cap_exceeded")`.
  </done>
  <acceptance_criteria>
- Source: `grep -q "class OpenRouterAdapter" apps/api/backends/openrouter/adapter.py`.
- Source: `grep -q 'OPENROUTER_BASE_URL = "https://openrouter.ai/api/v1"' apps/api/backends/openrouter/adapter.py`.
- Source: `grep -q "HTTP-Referer" apps/api/backends/openrouter/adapter.py` AND `grep -q "X-Title" apps/api/backends/openrouter/adapter.py` AND `grep -q "Prompt-Optimizer" apps/api/backends/openrouter/adapter.py`.
- Source: `grep -q 'stream_options={"include_usage": True}' apps/api/backends/openrouter/adapter.py` (Pitfall 1).
- Source: `grep -q "default_headers" apps/api/backends/openrouter/adapter.py` (Pitfall 3 — set on constructor, not per-call).
- Source: `grep -q "except asyncio.CancelledError" apps/api/backends/openrouter/adapter.py` (Pattern 7).
- Source: `grep -q "raise$" apps/api/backends/openrouter/adapter.py` (re-raise after cancellation pair).
- Source: `grep -q "in_flight.close" apps/api/backends/openrouter/adapter.py` (httpx abort).
- Source: `grep -q "tracker.over_cap" apps/api/backends/openrouter/adapter.py`.
- Source: `grep -q "cost_cap_exceeded" apps/api/backends/openrouter/adapter.py`.
- Source: `grep -q "tc_" apps/api/backends/openrouter/adapter.py` (tool_call_id generation).
- Source: `grep -q "tool_calls\[tc_delta.index" apps/api/backends/openrouter/adapter.py` OR `grep -q "tool_calls.setdefault" apps/api/backends/openrouter/adapter.py` (delta accumulation by index).
- Source: `grep -q "logger = logging.getLogger" apps/api/backends/openrouter/adapter.py` (CONVENTIONS).
- Source: `grep -q "from __future__ import annotations" apps/api/backends/openrouter/adapter.py`.
- Behavior: `python -c "import openai; from apps.api.backends.openrouter.adapter import OpenRouterAdapter
try: OpenRouterAdapter(api_key='')
except openai.AuthenticationError: print('OK')
else: raise SystemExit(1)"` exits 0.
- Behavior: `python -c "from apps.api.backends.openrouter.adapter import OpenRouterAdapter; import inspect; assert inspect.iscoroutinefunction(OpenRouterAdapter.stream) or inspect.isasyncgenfunction(OpenRouterAdapter.stream) or hasattr(OpenRouterAdapter.stream, '__wrapped__'); print('OK')"`.
- Anti-pattern: `! grep -E "^[^#]*print\(" apps/api/backends/openrouter/adapter.py` matches no production print.
- Anti-pattern: `! grep -q "sys.path" apps/api/backends/openrouter/adapter.py`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author adapter tests + CLI entry + live smoke + verify D-19 contract suite passes for openrouter</name>
  <files>apps/api/backends/openrouter/__main__.py, apps/api/backends/openrouter/tests/test_adapter.py, apps/api/backends/openrouter/tests/test_live.py</files>
  <read_first>
    - apps/api/backends/openrouter/adapter.py (the public surface tests will call)
    - apps/api/backends/openrouter/tests/fakes.py (the test fakes built in Task 1)
    - apps/api/backends/tests/test_adapter_contract.py (the D-19 shared suite — verify it now passes for `openrouter` parameterization)
    - src/routing/__main__.py + src/routing/decide.py lines 530-548 (the CLI `_entrypoint` idiom + argparse skeleton)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md §"Common Operation 2" lines 1873-1914 (per-adapter CLI full source)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-PATTERNS.md §"openrouter/__main__.py" lines 446-489 (CLI shape — `_entrypoint` wrapper indirection so `import` doesn't SystemExit)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-CONTEXT.md discretion line 146 (CLI flag set: `--prompt`, `--max-cost-usd`, `--model`)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-VALIDATION.md "Manual-Only Verifications" lines 89-97 (live test invocation contract)
  </read_first>
  <behavior>
- Test 1: `OpenRouterAdapter(api_key="fake", client_factory=lambda k: FakeOpenAIClient([happy_path_chunks]))` happy-path stream emits `TextDelta(text="hi"), Done(tokens_in=10, tokens_out=5)`. `len(chunks) == 2` and `isinstance(chunks[-1], Done)`.
- Test 2: `_default_client_factory` invoked with `api_key="x"` records its constructor call and asserts `base_url == "https://openrouter.ai/api/v1"` and `default_headers["HTTP-Referer"] == "https://github.com/marreroii-michael/Prompt-Optimizer"` and `default_headers["X-Title"] == "Prompt-Optimizer"`. Use a `monkeypatch` of `openai.AsyncOpenAI` that records construction kwargs to a list.
- Test 3: `.chat.completions.create` is called with `stream_options={"include_usage": True}` — use a `MagicMock`/spy fake to record the call kwargs and assert the literal.
- Test 4: Cost-cap mid-stream — `max_cost_usd=0.000001`, fake stream emits 100 `delta.content` deltas of 100 chars each. Chunk list contains exactly one `StreamError(code="cost_cap_exceeded", retriable=False)` then `Done`. The loop did not consume all 100 fake chunks (i.e., it broke early).
- Test 5: Cancellation timing — wrap `adapter.stream(...)` consumption in `asyncio.create_task(consume())`; await 50 ms; `task.cancel()`; assert within 2 s that `chunks[-1]` (if any chunks landed) is `Done`. Use `@pytest.mark.timeout(2)`.
- Test 6: Tool-call delta accumulation — fake stream emits two `delta.tool_calls` deltas at index 0 then a `finish_reason="tool_calls"` chunk. Output: one `ToolCall` chunk with `arguments == {"city": "Paris"}` and `tool_call_id` starting with `tc_`. Test specifically: `assert chunks[i].tool_call_id.startswith("tc_") and len(chunks[i].tool_call_id) == 9`.
- Test 7: Live smoke at `apps/api/backends/openrouter/tests/test_live.py` — marker `@pytest.mark.live`, `@pytest.mark.skipif(not os.getenv("OPENROUTER_API_KEY"), reason="no OPENROUTER_API_KEY")`. Constructs real adapter, asks `"say hi in exactly one word"` with `max_cost_usd=0.10`, asserts ≥ 1 TextDelta + final Done with `cost_usd > 0`.
- Test 8: CLI smoke — `python -m apps.api.backends.openrouter --prompt "hi" --model openai/gpt-5 --max-cost-usd 0.01` (with `OPENROUTER_API_KEY` in env or `.env`) writes one JSON line per ChatChunk to stdout; the last line satisfies `json.loads(line)["type"] == "done"`. This test runs only when `OPENROUTER_API_KEY` is set (otherwise skipped).
- Test 9: D-19 shared contract suite — `pytest apps/api/backends/tests/test_adapter_contract.py -x -k openrouter` passes all 5 applicable invariants (step cap is skipped for openrouter).
  </behavior>
  <action>
Write `apps/api/backends/openrouter/__main__.py` per RESEARCH Common Operation 2 lines 1873-1914 + PATTERNS line 446-489:
- Module docstring: `"""Package-level CLI: ``python -m apps.api.backends.openrouter``."""`.
- `from __future__ import annotations`.
- Imports: `argparse`, `asyncio`, `os`, `sys`.
- LAZY import: `from apps.api.backends.openrouter.adapter import OpenRouterAdapter` happens INSIDE `async def _run(...)` to avoid SDK side effects on `import apps.api.backends.openrouter.__main__`.
- `async def _run(prompt: str, model: str, max_cost_usd: float, max_steps: int) -> int`:
  - `api_key = os.environ.get("OPENROUTER_API_KEY")`.
  - If `not api_key`: `print("ERROR: set OPENROUTER_API_KEY in env or .env", file=sys.stderr); return 1`.
  - `from apps.api.backends.openrouter.adapter import OpenRouterAdapter` (lazy import).
  - `from apps.api.backends.protocol import AdapterOptions` (lazy).
  - `adapter = OpenRouterAdapter(api_key=api_key, max_cost_usd=max_cost_usd)`.
  - `options = AdapterOptions(model=model, max_cost_usd=max_cost_usd, max_steps=max_steps)`.
  - `async for chunk in adapter.stream(prompt=prompt, history=[], options=options): print(chunk.model_dump_json(), flush=True)`.
  - `return 0`.
- `def main(argv: list[str] | None = None) -> int`: parser with `--prompt` required, `--model` default `"openai/gpt-5"`, `--max-cost-usd` default `0.50`, `--max-steps` default `25`. `return asyncio.run(_run(args.prompt, args.model, args.max_cost_usd, args.max_steps))`.
- `def _entrypoint() -> None: raise SystemExit(main())` (PATTERNS WR-07 fix — indirection).
- `if __name__ == "__main__": _entrypoint()`.

Write `apps/api/backends/openrouter/tests/test_adapter.py`:
- Comment-block header: `# Plan 01 Task 3 — OpenRouter adapter unit tests (BACKEND-03, BACKEND-06, BACKEND-07). Six invariants:` enumerate the 6 tests from Test 1-6 above + tests 8-12 from Task 2 behavior.
- `from __future__ import annotations`, stdlib + `pytest` + `pytest.mark.asyncio` imports.
- Inline imports inside each test function.
- Tests:
  1. `test_happy_path_emits_textdelta_and_done` — uses `fake_openai_client` fixture, asserts shape.
  2. `test_default_headers_set_on_client_constructor` — `monkeypatch` `openai.AsyncOpenAI` to record kwargs; assert headers (Pitfall 3 regression).
  3. `test_stream_options_include_usage` — spy fake records `.create()` kwargs (Pitfall 1 regression).
  4. `test_cost_cap_aborts_mid_stream` — `max_cost_usd=0.000001`, long fake; assert chunk list contains `StreamError(code="cost_cap_exceeded")` and ends with `Done` (BACKEND-06).
  5. `test_cancellation_within_2s` — `@pytest.mark.timeout(2)`, task cancel pattern; assert terminal `Done` (BACKEND-07).
  6. `test_tool_call_delta_accumulation` — fake emits two `tool_calls` deltas at same index, then `finish_reason="tool_calls"`; assert one `ToolCall` with accumulated args.
  7. `test_missing_key_raises` — `OpenRouterAdapter(api_key="")` raises `openai.AuthenticationError`.
  8. `test_authentication_error_emits_auth_failed` — mid-stream `AuthenticationError` → `StreamError(code="auth_failed")` + `Done`.
  9. `test_timeout_emits_retriable_timeout` — mid-stream `APITimeoutError` → `StreamError(code="timeout", retriable=True)` + `Done`.
  10. `test_rate_limit_429_emits_retriable` — mid-stream `APIStatusError(status_code=429)` → `StreamError(code="rate_limited", retriable=True)` + `Done`.
  11. `test_503_emits_provider_unavailable` — mid-stream `APIStatusError(status_code=503)` → `StreamError(code="provider_unavailable")` + `Done`.
  12. `test_routing_signals_passed_through` — `AdapterOptions(routing_signals={"task_type": "coding"})` produces `Done.routing_signals == {"task_type": "coding"}`.

Write `apps/api/backends/openrouter/tests/test_live.py`:
- Comment-block header: `# Plan 01 Task 3 — OpenRouter live smoke (BACKEND-03 + VALIDATION.md "Manual-Only Verifications"). Opt-in.`
- `from __future__ import annotations`, imports.
- `@pytest.mark.live` + `@pytest.mark.skipif(not os.getenv("OPENROUTER_API_KEY"), reason="no key")` on the single async test.
- `test_live_streams_one_word_response` — constructs real `OpenRouterAdapter` from `os.environ["OPENROUTER_API_KEY"]`, `max_cost_usd=0.10`, prompts "say hi in exactly one word"; asserts `any(isinstance(c, TextDelta) for c in chunks)`, `isinstance(chunks[-1], Done)`, `chunks[-1].cost_usd > 0`, `chunks[-1].cost_usd < 0.10`.

After implementation: run `uv run pytest -m 'not live' apps/api/backends/openrouter/` and assert all unit tests pass. Then run `uv run pytest -m 'not live' apps/api/backends/tests/test_adapter_contract.py -k openrouter` and assert the parametric `openrouter` cases all pass (5 cases — step cap skipped).
  </action>
  <verify>
    <automated>uv run pytest -m 'not live' apps/api/backends/openrouter/tests/test_adapter.py -x -q 2>&1 | tail -10 && uv run pytest -m 'not live' apps/api/backends/tests/test_adapter_contract.py -k openrouter -x -q 2>&1 | tail -10 && python -c "from apps.api.backends.openrouter.__main__ import main; print('OK CLI import')" && uv run pytest src/routing/tests/test_decide_smoke.py -x -q</automated>
  </verify>
  <done>
- 12 unit tests in `test_adapter.py` pass.
- The 5 D-19 invariants applicable to openrouter pass via `pytest -k openrouter` against `test_adapter_contract.py`.
- `apps/api/backends/openrouter/__main__.py` is importable without SystemExit and exposes `main(argv) -> int`.
- `test_live.py` is collected (skipped without `OPENROUTER_API_KEY`).
- Phase 1 D-18 guard still green.
  </done>
  <acceptance_criteria>
- Behavior: `uv run pytest -m 'not live' apps/api/backends/openrouter/tests/test_adapter.py -x` exits 0.
- Behavior: `uv run pytest -m 'not live' apps/api/backends/tests/test_adapter_contract.py -k openrouter -x` exits 0 (5 cases pass: happy_path, cost_cap, cancellation_within_2s, done_always_lands, missing_api_key; step_cap skipped via `pytest.skip`).
- Behavior: `uv run pytest --collect-only apps/api/backends/openrouter/tests/test_live.py` exits 0 (collectable; runs only with `-m live` and `OPENROUTER_API_KEY` set).
- Source: `grep -q "@pytest.mark.live" apps/api/backends/openrouter/tests/test_live.py` AND `grep -q "skipif.*OPENROUTER_API_KEY" apps/api/backends/openrouter/tests/test_live.py`.
- Source: `grep -q "def _entrypoint" apps/api/backends/openrouter/__main__.py` (WR-07 indirection).
- Source: `grep -q "asyncio.run" apps/api/backends/openrouter/__main__.py` AND `grep -q "model_dump_json" apps/api/backends/openrouter/__main__.py`.
- Source: `grep -q -- "--prompt" apps/api/backends/openrouter/__main__.py` AND `grep -q -- "--model" apps/api/backends/openrouter/__main__.py` AND `grep -q -- "--max-cost-usd" apps/api/backends/openrouter/__main__.py`.
- Source: `grep -q "@pytest.mark.timeout(2)" apps/api/backends/openrouter/tests/test_adapter.py` (cancellation test).
- Source: `grep -q "stream_options" apps/api/backends/openrouter/tests/test_adapter.py` (Pitfall 1 regression).
- Source: `grep -q "HTTP-Referer" apps/api/backends/openrouter/tests/test_adapter.py` (Pitfall 3 regression).
- Source: `grep -q "tc_" apps/api/backends/openrouter/tests/test_adapter.py` (tool-call ID accumulation test).
- Behavior: Phase 1 D-18 guard still green: `uv run pytest src/routing/tests/test_decide_smoke.py -x` exits 0.
- Behavior: `python -m apps.api.backends.openrouter --help 2>&1 | grep -E -- "--prompt|--model|--max-cost-usd"` matches all three flags (the CLI accepts them).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user prompt → OpenRouter API | Adapter forwards untrusted prompt text verbatim. Cost-cap + step-cap are the abuse mitigations. |
| OpenRouter response → ChatChunk | Pydantic v2 validation on every emitted chunk; closed Literal vocabulary on `StreamError.code`. |
| OPENROUTER_API_KEY → AsyncOpenAI client | Key flows through `KeyStore`/env → constructor; redaction filter scrubs from any accidental log line; `default_headers` does NOT include the key. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | D (DoS — wallet) | `OpenRouterAdapter.stream` cost cap | mitigate | `tracker.over_cap()` checked after every `TextDelta` emit; cap-exceeded path emits `StreamError(code="cost_cap_exceeded", retriable=False)` + `Done` and `break`s the loop. Test 4 in Task 3 regression. SEVERITY: HIGH. |
| T-02-04 | D (DoS — runaway) | `OpenRouterAdapter.stream` cancellation | mitigate | `except asyncio.CancelledError`: emit terminal pair, `raise`. `finally`: `await in_flight.close()` aborts httpx. 2-second budget enforced by `@pytest.mark.timeout(2)` on D-19 invariant #4 (Pattern 7). SEVERITY: HIGH. |
| T-02-01 | I (key leak) | `OpenRouterAdapter._default_client_factory` | mitigate (inherited) | Adapter never logs key. `AsyncOpenAI` SDK uses `Authorization: Bearer` header which is set inside the SDK, not exposed in logs. SECURE-01 redaction filter (installed at `import apps.api`) catches any accidental log. SEVERITY: HIGH (inherited mitigation from Wave 0). |
| T-02-08 | T (deprecated SDK) | `apps/api/backends/openrouter/*` | mitigate (inherited) | This adapter uses `openai>=1.40,<3.0` (not claude-*). OSS-06 triad (Plan 04) ensures `claude-code-sdk` isn't reintroduced, but this adapter does not import claude-related SDKs. SEVERITY: MEDIUM (inherited). |

Threats T-02-02 (BYOK disk leak), T-02-05 (workspace leak), T-02-06 (computer-use opt-in), T-02-07 (secret commit) are NOT in this plan's scope — owned by Wave 0 / Wave 1 (Claude Code, computer-use) / Wave 2 plans.
</threat_model>

<verification>
- `uv run pytest -m 'not live' apps/api/backends/openrouter/tests/ -x` exits 0.
- `uv run pytest -m 'not live' apps/api/backends/tests/test_adapter_contract.py -k openrouter -x` exits 0 (5/6 invariants pass; step_cap skipped per `pytest.skip("not applicable to openrouter")`).
- `python -m apps.api.backends.openrouter --help` prints the argparse help with `--prompt`, `--model`, `--max-cost-usd`, `--max-steps` flags.
- `python -m apps.api.backends.openrouter --prompt "hi"` exits 1 with stderr `ERROR: set OPENROUTER_API_KEY` when no env var is set.
- `uv run pytest src/routing/tests/test_decide_smoke.py -x` exits 0 — Phase 1 D-18 import-graph guard still green (Phase 2 `openai`/`anthropic`/etc. additions do NOT leak into `src.routing`).
</verification>

<success_criteria>
- BACKEND-03 satisfied: OpenRouter adapter streams ChatChunk via OpenAI SDK pointed at `https://openrouter.ai/api/v1` with HTTP-Referer + X-Title headers set on the AsyncOpenAI constructor (not per-call).
- BACKEND-06 satisfied: Per-turn USD cap (`tracker.over_cap()` checked after every chunk emit; default $0.50 from `DEFAULT_PER_TURN_COST_USD`). Step cap N/A for OpenRouter (single round-trip; D-19 invariant #3 skips this adapter).
- BACKEND-07 satisfied: `aclose()` / task cancel propagates to `in_flight.close()` within 2 s (D-19 invariant #4 enforces).
- D-19 shared contract suite passes 5/6 invariants for `openrouter` parameterization (1 skipped).
- Phase 2 SC #1 satisfied for OpenRouter: `python -m apps.api.backends.openrouter --prompt "..."` writes one ChatChunk JSON line per chunk to stdout; last line `"type":"done"`.
- Pitfall 1 (`stream_options.include_usage`), Pitfall 3 (`default_headers` on constructor) have explicit regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-adapters-chatchunk-contract/02-01-SUMMARY.md` summarizing:
- 5 source files + 4 test files created.
- Constants chosen: `HTTP_REFERER`, `X_TITLE`, `OPENROUTER_BASE_URL`.
- D-19 invariant pass count for openrouter (expect 5 pass + 1 skip).
- Live test instructions (`OPENROUTER_API_KEY=… uv run pytest -m live apps/api/backends/openrouter`).
- Any deviations from RESEARCH Pattern 3.
</output>
</content>
</invoke>