---
phase: 02
plan: 00
plan_id: "02-00-scaffolding-and-shared-modules"
type: execute
wave: 0
depends_on: []
files_modified:
  - "pyproject.toml"
  - "uv.lock"
  - "apps/__init__.py"
  - "apps/api/__init__.py"
  - "apps/api/backends/__init__.py"
  - "apps/api/backends/protocol.py"
  - "apps/api/backends/chunks.py"
  - "apps/api/backends/keystore.py"
  - "apps/api/backends/logging_filter.py"
  - "apps/api/backends/pricing.py"
  - "apps/api/backends/cost.py"
  - "apps/api/backends/tests/__init__.py"
  - "apps/api/backends/tests/conftest.py"
  - "apps/api/backends/tests/test_chunks.py"
  - "apps/api/backends/tests/test_logging_filter.py"
  - "apps/api/backends/tests/test_keystore.py"
  - "apps/api/backends/tests/test_pricing.py"
  - "apps/api/backends/tests/test_adapter_contract.py"
  - "config/pricing.json"
  - ".planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md"
autonomous: true
requirements: [BACKEND-01, BACKEND-02, SECURE-01, SECURE-04]
must_haves:
  truths:
    - "`import apps.api` runs `dotenv.load_dotenv()` and `install_redaction_filter()` as side effects at import time."
    - "`TypeAdapter(ChatChunk).validate_python({'type': 'text_delta', 'text': 'hi'})` returns a `TextDelta` instance whose `model_dump_json()` round-trips back through `validate_json` to the same object (D-01)."
    - "`ChatChunk` Pydantic union exposes exactly 7 discriminator literals: `text_delta | tool_call | tool_result | file_diff | screenshot | stream_error | done` (D-02)."
    - "`logging.getLogger().info('auth: %s', 'sk-ant-AAAAAAAAAAAAAAAAAAAA')` produces a log line containing `***REDACTED-ANTHROPIC***` and not containing `sk-ant-` (SC #3)."
    - "`KeyStore(use_keyring=True)` raises `RuntimeError` mentioning `uv sync --extra keyring` when the `keyring` extra is absent (D-10)."
    - "`KeyStore().get('openrouter')` returns the value of `OPENROUTER_API_KEY` from `os.environ` when memory + keyring are empty."
    - "`PricingTable.from_static('config/pricing.json').get('unknown-slug')` returns the `_default` entry (D-17)."
    - "`PricingTable._merge_openrouter_snapshot({'data': [{'id': 'openai/gpt-5', 'pricing': {'prompt': '0.0000025', 'completion': '0.00001'}}]})` updates the in-memory entry to `{'input_per_mtok': 2.5, 'output_per_mtok': 10.0}` (Pitfall 6)."
    - "`apps/api/backends/tests/test_adapter_contract.py` is collected by pytest with 6 parametric tests × 3 adapter ids = 18 test cases (one `pytest.skip` on `test_step_cap_aborts[openrouter]`) — these initially error because the adapter modules in Wave 1 don't exist yet, but the file is syntactically valid and importable. Adapter imports are LAZY inside the `adapter_factory` fixture (wrapped in try/except ImportError → `pytest.skip(...)`) so the file is collectable before Wave 1 adapters exist."
    - "The Phase 1 D-18 import-graph guard (`pytest src/routing/tests/test_decide_smoke.py`) still passes after this plan adds new dependencies — `import src.routing.decide` does not pull `pydantic`, `openai`, `anthropic`, `claude_agent_sdk`, `playwright`, `httpx`, `keyring`, `tiktoken`, or `python_dotenv` into `sys.modules`."
    - "`.planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md` `## Open Questions` section header is renamed to `## Open Questions (RESOLVED)`; each of the 4 numbered questions has a `RESOLVED:` title prefix and a `**Resolution:** ...` sentence appended after its `Recommendation` line. The literal token `RESOLVED` appears at least 5 times in the file (Dimension 11 gate)."
  artifacts:
    - path: "pyproject.toml"
      provides: "Phase 2 base dependencies, optional keyring extra, pytest asyncio mode, `apps` package in hatch wheel"
      contains: "pydantic>=2.6,<3.0|openai>=1.40,<3.0|anthropic>=0.40,<1.0|claude-agent-sdk>=0.1.80,<0.2|playwright>=1.45,<2.0|python-dotenv>=1.0,<2.0|tiktoken>=0.7,<1.0|httpx>=0.27,<1.0|pre-commit>=4.0,<5.0|pytest-asyncio>=0.24,<2.0|pytest-timeout>=2.3,<3.0|keyring>=24,<26|asyncio_mode = \"auto\""
    - path: "uv.lock"
      provides: "Regenerated lockfile with Phase 2 deps; does NOT contain `claude-code-sdk`"
    - path: "apps/__init__.py"
      provides: "Top-level `apps` package marker (empty/docstring only)"
      min_lines: 1
    - path: "apps/api/__init__.py"
      provides: "Process-import-time side effects: `load_dotenv()` + `install_redaction_filter()` (D-11, SECURE-01)"
      min_lines: 10
    - path: "apps/api/backends/__init__.py"
      provides: "Empty package marker"
    - path: "apps/api/backends/protocol.py"
      provides: "`BackendAdapter` Protocol, `Message` frozen dataclass, `AdapterOptions` frozen dataclass"
      contains: "class BackendAdapter(Protocol)|@dataclass(frozen=True)|class Message|class AdapterOptions"
    - path: "apps/api/backends/chunks.py"
      provides: "Pydantic v2 discriminated union: `TextDelta`, `ToolCall`, `ToolResult`, `FileDiff`, `Screenshot`, `StreamError`, `Done`, type alias `ChatChunk`, `chat_chunk_adapter` `TypeAdapter`"
      contains: "class TextDelta(BaseModel)|class ToolCall(BaseModel)|class ToolResult(BaseModel)|class FileDiff(BaseModel)|class Screenshot(BaseModel)|class StreamError(BaseModel)|class Done(BaseModel)|Field(discriminator=\"type\")|chat_chunk_adapter = TypeAdapter"
    - path: "apps/api/backends/keystore.py"
      provides: "`KeyStore` in-memory + optional `keyring` (D-10, SECURE-04)"
      contains: "class KeyStore|_HAS_KEYRING|SERVICE_NAME"
    - path: "apps/api/backends/logging_filter.py"
      provides: "`RedactionFilter` subclass + `install_redaction_filter()` (SECURE-01)"
      contains: "class RedactionFilter(logging.Filter)|def install_redaction_filter|SECRET_PATTERNS"
    - path: "apps/api/backends/pricing.py"
      provides: "`PricingTable` with static-load + async OpenRouter refresh (D-17)"
      contains: "class PricingTable|def from_static|async def refresh_from_openrouter|_merge_openrouter_snapshot"
    - path: "apps/api/backends/cost.py"
      provides: "`CostTracker` base class + `DEFAULT_PER_TURN_COST_USD = 0.50`"
      contains: "class CostTracker|DEFAULT_PER_TURN_COST_USD: Final[float] = 0.50"
    - path: "apps/api/backends/tests/conftest.py"
      provides: "Shared fakes (`fake_openai`, `fake_claude_sdk_client`, `fake_anthropic`, `fake_screen`) and parametric `adapter_factory` fixture for D-19. Adapter imports are LAZY (try/except ImportError → pytest.skip) so the fixture is collectable in Wave 0 before Wave 1 adapters land."
    - path: "apps/api/backends/tests/test_chunks.py"
      provides: "BACKEND-01 schema test — round-trip + discriminator + frozen variants"
    - path: "apps/api/backends/tests/test_logging_filter.py"
      provides: "SC #3 redaction regression — `sk-`, `sk-ant-`, `Bearer …` rewritten before handler sees record"
    - path: "apps/api/backends/tests/test_keystore.py"
      provides: "SECURE-04 keystore tests — memory primary, env fallback, lazy keyring import"
    - path: "apps/api/backends/tests/test_pricing.py"
      provides: "D-17 pricing tests — static load + `_default` fallback + Pitfall 6 per-Mtok conversion"
    - path: "apps/api/backends/tests/test_adapter_contract.py"
      provides: "D-19 shared parametric contract suite (6 invariants × 3 adapter ids). Adapter classes are imported LAZILY inside the `adapter_factory` fixture in conftest.py (NOT at module level) so the file is collectable in Wave 0."
    - path: "config/pricing.json"
      provides: "13 model entries + `_default` per D-17"
      contains: "openai/gpt-5|anthropic/claude-opus-4-7|anthropic/claude-sonnet-4-6|claude-agent-sdk|computer-use-2025-11-24|openrouter/auto|_default"
    - path: ".planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md"
      provides: "Open Questions section reconciled with RESOLVED status markers per Dimension 11 gate (B2 fix)"
      contains: "## Open Questions (RESOLVED)|RESOLVED:|**Resolution:**"
  key_links:
    - from: "apps/api/__init__.py"
      to: "apps/api/backends/logging_filter.install_redaction_filter"
      via: "module-level call at import"
      pattern: "install_redaction_filter\\(\\)"
    - from: "apps/api/__init__.py"
      to: "dotenv.load_dotenv"
      via: "module-level call at import"
      pattern: "load_dotenv\\(\\)"
    - from: "apps/api/backends/tests/test_adapter_contract.py"
      to: "apps.api.backends.openrouter.adapter.OpenRouterAdapter"
      via: "lazy import inside adapter_factory fixture (Wave 0 compatibility)"
      pattern: "try:\\s+from apps.api.backends.openrouter.adapter import OpenRouterAdapter\\s+except ImportError: pytest.skip\\(.*\\)"
    - from: "apps/api/backends/tests/test_adapter_contract.py"
      to: "apps.api.backends.claude_code.adapter.ClaudeCodeAdapter"
      via: "lazy import inside adapter_factory fixture (Wave 0 compatibility)"
      pattern: "try:\\s+from apps.api.backends.claude_code.adapter import ClaudeCodeAdapter\\s+except ImportError: pytest.skip\\(.*\\)"
    - from: "apps/api/backends/tests/test_adapter_contract.py"
      to: "apps.api.backends.computer_use.adapter.ComputerUseAdapter"
      via: "lazy import inside adapter_factory fixture (Wave 0 compatibility)"
      pattern: "try:\\s+from apps.api.backends.computer_use.adapter import ComputerUseAdapter\\s+except ImportError: pytest.skip\\(.*\\)"
    - from: "apps/api/backends/keystore.py"
      to: "keyring"
      via: "lazy try/except optional import"
      pattern: "try:\\s+import keyring as _keyring"
    - from: "apps/api/backends/pricing.py"
      to: "httpx.AsyncClient"
      via: "async refresh from https://openrouter.ai/api/v1/models"
      pattern: "httpx\\.AsyncClient"
---

<objective>
Wave 0 of Phase 2 — install the Python dependencies, build the `apps/api/backends/` shared module surface (Protocol, ChatChunk Pydantic union, KeyStore, RedactionFilter, PricingTable, CostTracker), and author every test that does NOT depend on the three adapter implementations.

Purpose: Without these contracts in place, Wave 1's three adapter plans cannot run in parallel — they all import `ChatChunk`, `BackendAdapter`, `CostTracker`, `DEFAULT_PER_TURN_COST_USD`, and the redaction filter. Wave 0 also lands the D-19 shared parametric contract suite (`test_adapter_contract.py`) as a stub that initially errors on missing adapter modules; once Wave 1 plans complete, this file is the single test contract every adapter must satisfy. Adapter classes used by the contract suite are imported LAZILY inside `conftest.py`'s `adapter_factory` fixture (try/except ImportError → `pytest.skip`) so the file is collectable in Wave 0 BEFORE the Wave 1 adapter modules exist — module-level imports of `OpenRouterAdapter`/`ClaudeCodeAdapter`/`ComputerUseAdapter` are STRICTLY FORBIDDEN in this file.

Output:
  - Updated `pyproject.toml` + regenerated `uv.lock` with Phase 2 deps (no `claude-code-sdk`).
  - 7 source files at `apps/api/__init__.py`, `apps/api/backends/{protocol,chunks,keystore,logging_filter,pricing,cost}.py`.
  - 6 test files at `apps/api/backends/tests/{conftest,test_chunks,test_logging_filter,test_keystore,test_pricing,test_adapter_contract}.py`.
  - 1 config file at `config/pricing.json`.
  - 1 documentation update: `.planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md` Open Questions section reconciled to RESOLVED status (B2 fix).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-CONTEXT.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-PATTERNS.md
@.planning/phases/02-backend-adapters-chatchunk-contract/02-VALIDATION.md
@.planning/phases/01-router-brain-foundation/01-VERIFICATION.md
@src/routing/schema.py
@src/routing/__main__.py
@src/routing/policy.py
@src/routing/config.py
@src/routing/tests/conftest.py
@src/routing/tests/test_schema.py
@src/routing/tests/test_decide_smoke.py
@src/routing/tests/test_rule_cascade.py
@src/demo/demo_router.py
@pyproject.toml
@config/model_mapping.json

<interfaces>
<!-- Key types Wave 1 adapters will import from this plan. Wave 1 executors should NOT redefine these. -->

From `apps/api/backends/protocol.py` (this plan creates):
- `Backend = Literal["openrouter", "claude_code", "computer_use"]` (mirror Phase 1 `src/routing/schema.py:33`)
- `@dataclass(frozen=True) class Message: role: str; content: str`
- `@dataclass(frozen=True) class AdapterOptions: model: str | None = None; max_cost_usd: float | None = None; max_steps: int | None = None; cwd: str | None = None; routing_signals: dict | None = None`
- `class BackendAdapter(Protocol): async def stream(self, prompt: str, history: list[Message], options: AdapterOptions) -> AsyncIterator[ChatChunk]: ...`

From `apps/api/backends/chunks.py` (this plan creates):
- `class TextDelta(BaseModel): type: Literal["text_delta"] = "text_delta"; text: str`
- `class ToolCall(BaseModel): type: Literal["tool_call"] = "tool_call"; tool_call_id: str; tool_name: str; arguments: dict[str, Any]`
- `class ToolResult(BaseModel): type: Literal["tool_result"] = "tool_result"; tool_call_id: str; content: str | dict[str, Any]; is_error: bool = False`
- `class FileDiff(BaseModel): type: Literal["file_diff"] = "file_diff"; tool_call_id: str; path: str; diff: str; operation: Literal["create", "edit", "delete"]`
- `class Screenshot(BaseModel): type: Literal["screenshot"] = "screenshot"; step: int; image_b64: str | None = None; image_ref: str | None = None; image_format: Literal["png", "jpeg"] = "png"`
- `class StreamError(BaseModel): type: Literal["stream_error"] = "stream_error"; code: Literal["cost_cap_exceeded","step_cap_exceeded","cancelled","rate_limited","auth_failed","provider_unavailable","timeout","validation_error","internal_error"]; message: str; retriable: bool`
- `class Done(BaseModel): type: Literal["done"] = "done"; tokens_in: int | None = None; tokens_out: int | None = None; cost_usd: float | None = None; latency_ms: int | None = None; routing_signals: dict[str, Any] | None = None`
- `ChatChunk = Annotated[Union[TextDelta, ToolCall, ToolResult, FileDiff, Screenshot, StreamError, Done], Field(discriminator="type")]`
- `chat_chunk_adapter = TypeAdapter(ChatChunk)`

From `apps/api/backends/cost.py` (this plan creates):
- `DEFAULT_PER_TURN_COST_USD: Final[float] = 0.50`
- `class CostTracker:` with `__init__(self, *, model_id: str, max_cost_usd: float, pricing: PricingTable)`, `record_input(n: int)`, `record_output(n: int)`, `tokens_in() -> int`, `tokens_out() -> int`, `total() -> float`, `over_cap() -> bool`

From `apps/api/backends/keystore.py` (this plan creates):
- `class KeyStore: __init__(self, *, use_keyring: bool = False); get(provider: str) -> str | None; set(provider: str, key: str) -> None`
- `SERVICE_NAME: Final[str] = "prompt-optimizer"`
- `_ENV_MAP = {"openrouter": "OPENROUTER_API_KEY", "anthropic": "ANTHROPIC_API_KEY"}`

From `apps/api/backends/logging_filter.py` (this plan creates):
- `class RedactionFilter(logging.Filter): def filter(self, record) -> bool`
- `def install_redaction_filter() -> None` (idempotent on root logger)
- `SECRET_PATTERNS = [(re.compile(r"sk-ant-[A-Za-z0-9_-]{8,}"), "***REDACTED-ANTHROPIC***"), (re.compile(r"sk-[A-Za-z0-9_-]{20,}"), "***REDACTED-OPENAI***"), (re.compile(r"Bearer\s+[A-Za-z0-9_.\-]{20,}"), "Bearer ***REDACTED***")]`

From `apps/api/backends/pricing.py` (this plan creates):
- `class PricingTable: __init__(self, table: dict); from_static(cls, path) -> PricingTable; get(model_id: str) -> dict[str, float]; async refresh_from_openrouter(cache_path) -> None; _merge_openrouter_snapshot(snapshot: dict) -> None`
- `OPENROUTER_MODELS_URL = "https://openrouter.ai/api/v1/models"`
- `CACHE_TTL_SECONDS = 24 * 60 * 60`

From `src/routing/schema.py` (Phase 1 — already exists, DO NOT MODIFY):
- `Backend = Literal["openrouter", "claude_code", "computer_use"]`
- `@dataclass(frozen=True) class RoutingDecision: backend; model_or_agent; rationale; confidence; signals`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend pyproject.toml + regenerate uv.lock with Phase 2 base dependencies</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (current state — only modify additively, preserve every existing line)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md §"Standard Stack" lines 130-213 (verified version pins) and §"Installation" lines 166-200 (canonical `[project.dependencies]` + `[project.optional-dependencies]` block)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-PATTERNS.md §"Group F" lines 862-935 (the exact extension diff for `pyproject.toml`)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-CONTEXT.md D-10 (keyring optional extra), D-11 (python-dotenv), D-20 (CI dep set)
  </read_first>
  <action>
Add the following entries to `[project.dependencies]` in `pyproject.toml` (additive — preserve every existing line): `"pydantic>=2.6,<3.0"`, `"openai>=1.40,<3.0"`, `"anthropic>=0.40,<1.0"`, `"claude-agent-sdk>=0.1.80,<0.2"`, `"playwright>=1.45,<2.0"`, `"python-dotenv>=1.0,<2.0"`, `"httpx>=0.27,<1.0"`, `"tiktoken>=0.7,<1.0"`, `"pre-commit>=4.0,<5.0"`.

Add to `[project.optional-dependencies] dev` (preserve existing `pytest`, `pytest-cov` entries): `"pytest-asyncio>=0.24,<2.0"`, `"pytest-timeout>=2.3,<3.0"`.

Create a NEW `[project.optional-dependencies] keyring = ["keyring>=24,<26"]` block.

Change `[tool.hatch.build.targets.wheel] packages = ["src"]` to `packages = ["src", "apps"]` per D-07.

Extend `[tool.pytest.ini_options]`: change `testpaths = ["src"]` to `testpaths = ["src", "apps"]`; add `asyncio_mode = "auto"` per D-20 + RESEARCH line 2065; add `markers = ["live: hits real provider APIs (BYOK required); skipped by default"]` per D-18.

Do NOT add `claude-code-sdk` anywhere (OSS-06; the pre-commit hook in Plan 04 will guard this).

After editing, regenerate the lockfile via `uv lock` (NOT `uv sync` — lock only at this step so the lock change is atomic and reviewable). The Wave 1 adapter tasks will run `uv sync --locked --all-extras --dev` themselves before any pytest run.
  </action>
  <verify>
    <automated>cd "$PWD" && grep -E "pydantic|openai|anthropic|claude-agent-sdk|playwright|python-dotenv|httpx|tiktoken|pre-commit|pytest-asyncio|pytest-timeout|keyring" pyproject.toml | wc -l | awk '{ if ($1 < 11) { print "FAIL: missing deps in pyproject.toml ("$1" matches < 11 expected)"; exit 1 } else { print "OK" } }' && grep -q 'asyncio_mode = "auto"' pyproject.toml && grep -q 'packages = \["src", "apps"\]' pyproject.toml && ! grep -q '"claude-code-sdk"' pyproject.toml && ! grep -q '"claude-code-sdk"' uv.lock && uv lock --check 2>&1 | tail -5</automated>
  </verify>
  <done>
- `pyproject.toml` contains 11 new dependency lines (9 base + 2 dev) and 1 new optional-extra group (`keyring`).
- `[tool.hatch.build.targets.wheel] packages = ["src", "apps"]`.
- `[tool.pytest.ini_options]` contains both `asyncio_mode = "auto"` and a `live` marker.
- `claude-code-sdk` appears in neither `pyproject.toml` nor `uv.lock`.
- `uv lock --check` exits 0 (lockfile in sync with pyproject).
  </done>
  <acceptance_criteria>
- Source: `grep -c '^"pydantic>=2.6' pyproject.toml` returns at least 1 (and similarly for openai, anthropic, claude-agent-sdk, playwright, python-dotenv, httpx, tiktoken, pre-commit, pytest-asyncio, pytest-timeout).
- Source: `grep -q 'keyring = \["keyring>=24,<26"\]' pyproject.toml` returns 0 (line exists).
- Source: `grep -q 'asyncio_mode = "auto"' pyproject.toml` returns 0.
- Source: `grep -q 'packages = \["src", "apps"\]' pyproject.toml` returns 0.
- Source: `! grep -q '"claude-code-sdk"' pyproject.toml` exits 0 (deprecated SDK absent).
- Source: `! grep -q '"claude-code-sdk"' uv.lock` exits 0 (deprecated SDK absent from lockfile per OSS-06).
- Behavior: `uv lock --check` exits 0 (pyproject.toml and uv.lock are consistent).
- Behavior: The Phase 1 D-18 guard test still passes: `uv run pytest src/routing/tests/test_decide_smoke.py -x` exits 0 (adding new deps must NOT cause `src.routing` to import them).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create shared module surface (apps/api/__init__.py, chunks, protocol, keystore, logging_filter, pricing, cost) + config/pricing.json</name>
  <files>apps/__init__.py, apps/api/__init__.py, apps/api/backends/__init__.py, apps/api/backends/protocol.py, apps/api/backends/chunks.py, apps/api/backends/keystore.py, apps/api/backends/logging_filter.py, apps/api/backends/pricing.py, apps/api/backends/cost.py, config/pricing.json</files>
  <read_first>
    - src/routing/schema.py (Phase 1 — frozen-dataclass + module-docstring style to mirror; DO NOT modify)
    - src/routing/policy.py (Phase 1 — module-docstring + Final-constant style to mirror; DO NOT modify)
    - src/routing/config.py (path-discovery preamble — Phase 2 uses pathlib version instead per CONTEXT line 245)
    - src/demo/demo_router.py lines 35-85 (`load_json` + `load_joblib_artifacts` — FileNotFoundError remediation-text style to mirror)
    - config/model_mapping.json (sibling config; flat-dict-keyed-by-slug schema + `OTHER` reserved key)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md §"Pattern 1" lines 330-417 (ChatChunk full Pydantic schema), §"Pattern 10" lines 1472-1525 (RedactionFilter full source), §"Pattern 12" lines 1623-1681 (KeyStore full source), §"Pattern 6" lines 1211-1330 (CostTracker + PricingTable full source), §"Pattern 2" lines 425-465 (Protocol + Message + AdapterOptions)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-PATTERNS.md §"Group A" lines 60-262 (per-file analog and divergence guidance)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-CONTEXT.md D-01..D-06 (ChatChunk contract), D-10..D-11 (keystore + dotenv), D-14 (Screenshot fields), D-17 (pricing schema)
  </read_first>
  <behavior>
- Test 1: `import apps.api` runs `dotenv.load_dotenv()` and `apps.api.backends.logging_filter.install_redaction_filter()` exactly once (idempotent on subsequent imports). (D-11, SECURE-01)
- Test 2: `TypeAdapter(ChatChunk).validate_python({"type": "text_delta", "text": "hi"})` returns `TextDelta(type="text_delta", text="hi")`. `model_dump_json()` round-trips through `validate_json`. (BACKEND-01)
- Test 3: Every variant has its `type` Literal correctly defaulted so `TextDelta(text="x")`, `Done()`, `StreamError(code="cancelled", message="m", retriable=True)`, etc. work without specifying `type`. (D-01)
- Test 4: `ChatChunk` union has exactly 7 members in this order: TextDelta, ToolCall, ToolResult, FileDiff, Screenshot, StreamError, Done. (D-02)
- Test 5: `Screenshot(step=1, image_b64="abc")` has `image_ref=None` and `image_format="png"` by default. (D-14)
- Test 6: `Done(routing_signals={"task_type": "coding"})` round-trips through `model_dump_json` / `validate_json` preserving the dict.
- Test 7: `StreamError(code="invalid_code", message="m", retriable=False)` raises `pydantic.ValidationError` (closed Literal vocabulary). (D-06)
- Test 8: `install_redaction_filter()` called twice attaches exactly one `RedactionFilter` to the root logger. Re-import of `apps.api` does not duplicate.
- Test 9: Calling `logging.getLogger("test").info("auth: Bearer sk-ant-AAAAAAAAAAAAAAAAAAAA")` after `install_redaction_filter()` produces `caplog.text` containing `***REDACTED-ANTHROPIC***` and NOT containing `sk-ant-` or the raw bearer suffix. (SECURE-01)
- Test 10: Calling `logger.info("k=%s url=%s", "sk-proj-abcdefghijklmnopqrstuvwxyz", "https://x")` produces caplog containing `***REDACTED-OPENAI***` and not `sk-proj-` (Pitfall 8 — `record.args` must be cleared).
- Test 11: `KeyStore().get("openrouter")` returns `os.environ["OPENROUTER_API_KEY"]` when memory + keyring empty; uses `monkeypatch.setenv` to set the env var.
- Test 12: `KeyStore().set("openrouter", "fakekey"); KeyStore().get("openrouter") == "fakekey"` (memory primary).
- Test 13: `KeyStore(use_keyring=True)` raises `RuntimeError` mentioning `uv sync --extra keyring` when `apps.api.backends.keystore._HAS_KEYRING` is patched to False.
- Test 14: `PricingTable.from_static("config/pricing.json").get("openai/gpt-5")` returns the entry; `.get("unknown-slug")` returns the `_default` entry. (D-17)
- Test 15: `PricingTable._merge_openrouter_snapshot({"data": [{"id": "openai/gpt-5", "pricing": {"prompt": "0.0000025", "completion": "0.00001"}}]})` mutates the table so `.get("openai/gpt-5") == {"input_per_mtok": 2.5, "output_per_mtok": 10.0}` — i.e., the per-token decimal string is multiplied by 1_000_000 (Pitfall 6).
- Test 16: `CostTracker(model_id="openai/gpt-5", max_cost_usd=0.50, pricing=table).total()` returns 0.0 initially; after `record_input(1_000_000)` returns ~2.5 (input rate × 1.0); `over_cap()` is False; after `record_output(1_000_000)` returns ~12.5 (input + output), `over_cap()` is True.
  </behavior>
  <action>
Write `apps/__init__.py` and `apps/api/backends/__init__.py` as single-line docstring or empty package markers (mirror `src/routing/__init__.py`).

Write `apps/api/__init__.py` per RESEARCH Pattern 10 install + D-11. At module top after `from __future__ import annotations` and stdlib imports: call `dotenv.load_dotenv()` (no-op if `.env` missing), then `install_redaction_filter()` (imported from `apps.api.backends.logging_filter`). Use `pathlib.Path(__file__).resolve().parents[1]` for `PROJECT_ROOT` per CONTEXT line 245 (NOT `os.path.dirname` chain).

Write `apps/api/backends/protocol.py` exactly per RESEARCH Pattern 2 (lines 425-465). Mandatory: `from __future__ import annotations`; `Backend = Literal["openrouter", "claude_code", "computer_use"]` (mirrors Phase 1 `src/routing/schema.py:33`); `@dataclass(frozen=True) class Message`; `@dataclass(frozen=True) class AdapterOptions` with `model: str | None = None; max_cost_usd: float | None = None; max_steps: int | None = None; cwd: str | None = None; routing_signals: dict | None = None`; `class BackendAdapter(Protocol): async def stream(...) -> AsyncIterator[ChatChunk]: ...`. Stdlib + `apps.api.backends.chunks` imports only — no provider SDK leak into the contract module.

Write `apps/api/backends/chunks.py` exactly per RESEARCH Pattern 1 (lines 330-417). Mandatory 7 BaseModels with `type: Literal[...]` defaulted. The `code` Literal in `StreamError` MUST contain exactly these 9 values per D-06: `"cost_cap_exceeded"`, `"step_cap_exceeded"`, `"cancelled"`, `"rate_limited"`, `"auth_failed"`, `"provider_unavailable"`, `"timeout"`, `"validation_error"`, `"internal_error"`. `Done` MUST carry `routing_signals: dict[str, Any] | None = None` per CONTEXT specifics line 267. `Screenshot` MUST carry both `image_b64` and `image_ref` (Phase 2 only sets `image_b64`) per D-14. Define `ChatChunk = Annotated[Union[TextDelta, ToolCall, ToolResult, FileDiff, Screenshot, StreamError, Done], Field(discriminator="type")]` and `chat_chunk_adapter = TypeAdapter(ChatChunk)`.

Write `apps/api/backends/keystore.py` exactly per RESEARCH Pattern 12 (lines 1623-1681). The lazy `try: import keyring as _keyring; _HAS_KEYRING = True; except ImportError: _HAS_KEYRING = False` pattern is mandatory per D-10. `SERVICE_NAME: Final[str] = "prompt-optimizer"`. `_ENV_MAP = {"openrouter": "OPENROUTER_API_KEY", "anthropic": "ANTHROPIC_API_KEY"}`. Constructor with `*` keyword-only flag `use_keyring: bool = False`. Raise `RuntimeError("use_keyring=True but the 'keyring' extra is not installed. Run: uv sync --extra keyring")` if `use_keyring=True` and `_HAS_KEYRING is False`. `get(provider)` order: memory → keyring (if enabled and installed) → env via `_ENV_MAP`. `set(provider, key)` populates memory and (if keyring enabled) calls `keyring.set_password(SERVICE_NAME, provider, key)`.

Write `apps/api/backends/logging_filter.py` exactly per RESEARCH Pattern 10 (lines 1472-1525). `SECRET_PATTERNS = [(re.compile(r"sk-ant-[A-Za-z0-9_-]{8,}"), "***REDACTED-ANTHROPIC***"), (re.compile(r"sk-[A-Za-z0-9_-]{20,}"), "***REDACTED-OPENAI***"), (re.compile(r"Bearer\s+[A-Za-z0-9_.\-]{20,}"), "Bearer ***REDACTED***")]`. The `filter(self, record)` method MUST call `record.getMessage()`, apply every pattern's `.sub(...)`, then set `record.msg = msg` AND `record.args = ()` per Pitfall 8. `install_redaction_filter()` is idempotent (checks `root.filters` for existing `RedactionFilter` instance) and ALSO attaches to every existing handler.

Write `apps/api/backends/pricing.py` exactly per RESEARCH Pattern 6 (lines 1262-1330). Constants: `OPENROUTER_MODELS_URL = "https://openrouter.ai/api/v1/models"`, `CACHE_TTL_SECONDS = 24 * 60 * 60`. `PricingTable.__init__(self, table: dict[str, dict[str, float]])`. `@classmethod from_static(cls, path)` calls `open(path, "r", encoding="utf-8")` and `json.load`. `.get(model_id)` returns `self._table.get(model_id) or self._table["_default"]`. `async refresh_from_openrouter(cache_path)` does mtime-based 24h cache check, then `async with httpx.AsyncClient(timeout=10.0)` GET; on success cache to disk + merge; on `httpx.HTTPError` `logger.warning("OpenRouter pricing refresh failed: %s — using static table.", exc)` and return. `_merge_openrouter_snapshot(snapshot)` iterates `snapshot["data"]`, for each entry with `pricing.prompt` and `pricing.completion` defined, converts the decimal string to a per-Mtok float via `float(p) * 1_000_000` (Pitfall 6).

Write `apps/api/backends/cost.py` exactly per RESEARCH Pattern 6 lines 1211-1259 per D-16 (shared CostTracker interface — each adapter subclasses). `DEFAULT_PER_TURN_COST_USD: Final[float] = 0.50` per CONTEXT specifics line 260. `class CostTracker` with `__init__(self, *, model_id, max_cost_usd, pricing)`. Methods: `record_input(n)`, `record_output(n)`, `tokens_in() -> int`, `tokens_out() -> int`, `total() -> float` (returns `_final_cost_override` if set, else compute from pricing rates × tokens / 1_000_000), `over_cap() -> bool`.

Write `config/pricing.json` with the 13 model entries + `_default` per RESEARCH lines 1334-1351. Exact required keys: `openai/gpt-5`, `openai/gpt-5-chat`, `qwen/qwen3-235b-a22b-2507`, `qwen/qwen3-235b-a22b-thinking-2507`, `deepseek/deepseek-v3.1-terminus`, `deepseek/deepseek-chat-v3-0324`, `moonshotai/kimi-k2-0905`, `google/gemini-2.5-flash`, `openrouter/auto`, `anthropic/claude-opus-4-7`, `anthropic/claude-sonnet-4-6`, `claude-agent-sdk`, `computer-use-2025-11-24`, `_default`. The `_default` value MUST be `{"input_per_mtok": 5.00, "output_per_mtok": 20.00}` per CONTEXT specifics line 266.

Every file MUST have `from __future__ import annotations` as the first import. Every module MUST use `logger = logging.getLogger(__name__)` (NOT `print()`) per CONVENTIONS + SECURE-01 anti-pattern (RESEARCH line 1721). Every module MUST have a module docstring per PATTERNS §"Module docstring style" (one-line purpose; "Public surface" with requirement IDs; 2-3 paragraphs of contract; cross-refs to RESEARCH / CONTEXT). NEVER add `sys.path.append` (CONTEXT line 244).
  </action>
  <verify>
    <automated>python -c "from apps.api.backends.chunks import TextDelta, ToolCall, ToolResult, FileDiff, Screenshot, StreamError, Done, ChatChunk, chat_chunk_adapter; td = TextDelta(text='hi'); assert td.type == 'text_delta'; restored = chat_chunk_adapter.validate_json(td.model_dump_json()); assert isinstance(restored, TextDelta); assert restored.text == 'hi'; print('OK chunks')" && python -c "from apps.api.backends.protocol import BackendAdapter, Message, AdapterOptions, Backend; assert AdapterOptions().model is None; print('OK protocol')" && python -c "from apps.api.backends.keystore import KeyStore, SERVICE_NAME, _ENV_MAP; assert SERVICE_NAME == 'prompt-optimizer'; print('OK keystore')" && python -c "from apps.api.backends.logging_filter import RedactionFilter, install_redaction_filter, SECRET_PATTERNS; assert len(SECRET_PATTERNS) == 3; print('OK filter')" && python -c "from apps.api.backends.pricing import PricingTable; t = PricingTable.from_static('config/pricing.json'); assert t.get('openai/gpt-5')['input_per_mtok'] == 2.50; assert t.get('unknown-slug') == t.get('_default-fallback'); print('OK pricing')" && python -c "from apps.api.backends.cost import CostTracker, DEFAULT_PER_TURN_COST_USD; assert DEFAULT_PER_TURN_COST_USD == 0.50; print('OK cost')" && python -c "import apps.api; import logging; logging.getLogger('x').info('Bearer sk-ant-AAAAAAAAAAAAAAAAAAAA'); print('OK side-effect import')"</automated>
  </verify>
  <done>
- All 9 module files exist and are importable.
- `config/pricing.json` contains the 14 required keys (13 model entries + `_default`).
- `import apps.api` runs `load_dotenv()` and `install_redaction_filter()` at import.
- Pydantic v2 round-trip works for all 7 ChatChunk variants.
- Side-effect tests in Task 3 will validate these contracts at the unit level.
  </done>
  <acceptance_criteria>
- Source: `grep -c "class TextDelta\|class ToolCall\|class ToolResult\|class FileDiff\|class Screenshot\|class StreamError\|class Done" apps/api/backends/chunks.py` returns ≥ 7.
- Source: `grep -q 'Field(discriminator="type")' apps/api/backends/chunks.py`.
- Source: `grep -q 'chat_chunk_adapter = TypeAdapter' apps/api/backends/chunks.py`.
- Source: `grep -q "from __future__ import annotations" apps/api/backends/protocol.py apps/api/backends/chunks.py apps/api/backends/keystore.py apps/api/backends/logging_filter.py apps/api/backends/pricing.py apps/api/backends/cost.py apps/api/__init__.py` (all 7 files have the future import).
- Source: `grep -q "load_dotenv()" apps/api/__init__.py` AND `grep -q "install_redaction_filter()" apps/api/__init__.py`.
- Source: `grep -q "DEFAULT_PER_TURN_COST_USD: Final\[float\] = 0.50" apps/api/backends/cost.py`.
- Source: `grep -q "SERVICE_NAME: Final\[str\] = .prompt-optimizer." apps/api/backends/keystore.py`.
- Source: `grep -q "_HAS_KEYRING" apps/api/backends/keystore.py`.
- Source: `grep -q "record.args = ()" apps/api/backends/logging_filter.py` (Pitfall 8 fix).
- Source: `grep -q "OPENROUTER_MODELS_URL" apps/api/backends/pricing.py`.
- Source: `grep -q '"_default"' config/pricing.json` AND `grep -q '"openai/gpt-5"' config/pricing.json` AND `grep -q '"anthropic/claude-opus-4-7"' config/pricing.json` AND `grep -q '"anthropic/claude-sonnet-4-6"' config/pricing.json` AND `grep -q '"claude-agent-sdk"' config/pricing.json` AND `grep -q '"computer-use-2025-11-24"' config/pricing.json`.
- Behavior: `python -c "import apps.api"` exits 0 (side-effect import works without ImportError).
- Behavior: `python -c "from apps.api.backends.chunks import chat_chunk_adapter; chat_chunk_adapter.validate_python({'type': 'text_delta', 'text': 'hi'})"` exits 0.
- Behavior: `python -c "from apps.api.backends.chunks import StreamError; StreamError(code='not_a_code', message='m', retriable=False)" 2>&1 | grep -q "ValidationError"` (closed Literal vocabulary enforced).
- Behavior: D-18 guard still green: `uv run pytest src/routing/tests/test_decide_smoke.py -x` exits 0.
- Anti-pattern: `! grep -rq "sys.path.append" apps/` exits 0 (no sys.path hacks).
- Anti-pattern: `! grep -E "^[^#]*print\(" apps/api/backends/*.py` matches no production print statements (test files exempt).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author shared tests (test_chunks, test_logging_filter, test_keystore, test_pricing) + conftest with fakes + D-19 contract suite stub</name>
  <files>apps/api/backends/tests/__init__.py, apps/api/backends/tests/conftest.py, apps/api/backends/tests/test_chunks.py, apps/api/backends/tests/test_logging_filter.py, apps/api/backends/tests/test_keystore.py, apps/api/backends/tests/test_pricing.py, apps/api/backends/tests/test_adapter_contract.py</files>
  <read_first>
    - src/routing/tests/conftest.py (`_load_joblib_or_skip` pattern, `pytest.importorskip` style, session-scoped fixtures)
    - src/routing/tests/test_schema.py (inline imports, type-annotated `-> None`, `pytest.raises` style, `set(parsed.keys()) == {...}` assertion shape)
    - src/routing/tests/test_decide_smoke.py (forbidden-modules guard style — the analog for parametric contract suites)
    - src/routing/tests/test_rule_cascade.py lines 18-26 (`@pytest.fixture(scope="module")` + JSON-load fixture)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md §"Validation Architecture" lines 2056-2232 (full D-19 contract test source — clone STRUCTURE only; per the key_links above and B3 fix, adapter imports MUST be lazy inside `adapter_factory`, NOT at module level)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md §"Common Operation 3" lines 1916-1979 (FakeOpenAIClient + FakeAsyncStream shape)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md §"Pattern 10 regression test" lines 1527-1551 (caplog-based redaction test)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-PATTERNS.md §"Group A" lines 282-431 (per-test-file conventions and analog imports)
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-VALIDATION.md "Wave 0 Requirements" lines 61-83 (which test files are required and what each must cover)
    - This plan's `key_links` block (frontmatter) — adapter imports in `test_adapter_contract.py` MUST be LAZY (try/except ImportError → pytest.skip) inside the `adapter_factory` fixture. Module-level `from apps.api.backends.<backend>.adapter import <Backend>Adapter` is strictly forbidden in this file.
  </read_first>
  <behavior>
Same Tests 1-16 from Task 2 `<behavior>` are codified in this task. Add:
- Test 17: `apps/api/backends/tests/test_adapter_contract.py` is collected by pytest with 6 parametric tests × 3 adapter ids (one `pytest.skip` for `test_step_cap_aborts[openrouter]`). After this task, the file should pass `pytest --collect-only` (importable) but fail individual tests with `ModuleNotFoundError: apps.api.backends.openrouter.adapter` until Wave 1 plans land. Wave 1 acceptance is "the contract suite passes when its adapter is included; no failure for the OTHER adapters' missing modules" (use `pytest.importorskip` per-adapter inside the `adapter_factory` fixture so importing the file doesn't error). CRITICAL: This file MUST NOT have module-level imports of `OpenRouterAdapter` / `ClaudeCodeAdapter` / `ComputerUseAdapter`. All three adapter classes are imported LAZILY inside the `adapter_factory` fixture in `conftest.py` (try/except ImportError → `pytest.skip(...)`).
- Test 18: `apps/api/backends/tests/conftest.py` provides session-scoped fixtures `fake_openai`, `fake_claude_sdk_client`, `fake_anthropic`, `fake_screen`, plus `adapter_factory` parametric factory fixture. The `adapter_factory` fixture wraps each `from apps.api.backends.<backend>.adapter import <Backend>Adapter` in a try/except ImportError that calls `pytest.skip(f"<backend> adapter not yet implemented")` — this is the Wave 0 compatibility shim per B3 fix.
- Test 19: `pytest apps/api/backends/tests -x -m 'not live'` runs all SHARED unit tests (test_chunks, test_logging_filter, test_keystore, test_pricing) and the contract-suite collection step without errors. The contract suite's 18 parametric cases will skip until Wave 1 adapters exist.
  </behavior>
  <action>
Write `apps/api/backends/tests/__init__.py` as an empty package marker (or single-line docstring).

Write `apps/api/backends/tests/conftest.py` mirroring `src/routing/tests/conftest.py` style:
- `from __future__ import annotations`, stdlib imports, `pytest`.
- Session-scoped `pricing_table` fixture loading `config/pricing.json` via `PricingTable.from_static` (use `os.path.join(REPO_ROOT, "config", "pricing.json")` with `REPO_ROOT = os.path.abspath(os.path.join(__file__, "..", "..", "..", ".."))`).
- Session-scoped `key_store` fixture returning `KeyStore()`.
- Fakes:
  - `fake_openai` fixture returning a `FakeOpenAIClient` instance constructed from the Common Operation 3 shape (RESEARCH lines 1916-1979). The fake exposes `.chat.completions.create(**kw) -> FakeAsyncStream` and the stream yields a fixed sequence of `FakeChunk(choices=[FakeChoice(delta=FakeDelta(content="hello"), finish_reason="stop")], usage=None)` followed by `FakeChunk(choices=[], usage=FakeUsage(prompt_tokens=10, completion_tokens=5))`.
  - `fake_claude_sdk_client` fixture returning an object with `connect()`, `query(prompt=...)`, `receive_response()` (async iterator yielding one `AssistantMessage`-like value with a `TextBlock`-like content and one `ResultMessage`-like value with `total_cost_usd=0.001`), `interrupt()`, `disconnect()` as awaitable mocks. The fake satisfies the duck-type interface ClaudeCodeAdapter uses.
  - `fake_anthropic` fixture returning an object with `.beta.messages.stream(**kw)` returning an async context manager whose `__aenter__` yields an object iterable as `async for event in stream:` producing one `content_block_delta` event with `delta.type == "text_delta"` and one `content_block_stop` event, plus `get_final_message()` returning an object with `usage.input_tokens=10`, `usage.output_tokens=5`, `stop_reason="end_turn"`, `content=[]`.
  - `fake_screen` fixture returning an object with `start()`, `screenshot()` returning `b"\x89PNG\r\n..."`, `left_click(x,y)`, `type_text(t)`, `press_key(k)`, `scroll(...)`, `goto(url)`, `aclose()` as awaitable mocks.
- `adapter_factory` fixture per RESEARCH lines 2110-2132 with the three-way `if backend == "openrouter": import OpenRouterAdapter ... if backend == "claude_code": ... if backend == "computer_use": monkeypatch.setenv("COMPUTER_USE_OPT_IN", "1") ...` shape. Wrap each `from apps.api.backends.<backend>.adapter import <Backend>Adapter` in a try/except ImportError that calls `pytest.skip(f"<backend> adapter not yet implemented")` so the file is importable in Wave 0 (B3 fix — frontmatter `key_links` mandate lazy import).

Write `apps/api/backends/tests/test_chunks.py` mirroring `src/routing/tests/test_schema.py`:
- Comment-block header per PATTERNS line 287-305: `# Plan 00 Task 3 — BACKEND-01 ChatChunk schema test (D-01, D-02, D-06, D-14).` enumerate the test contracts.
- Tests for: 7-variant union (one test per variant validating type Literal + required fields + defaults), `chat_chunk_adapter.validate_python` discriminator routing, `chat_chunk_adapter.validate_json` round-trip, `Done(routing_signals={...}).model_dump_json()` preserves the dict, `StreamError(code="bad_code")` raises `pydantic.ValidationError`.
- Type-annotated `def test_X() -> None:` signatures. Inline `from apps.api.backends.chunks import TextDelta` inside each test function.

Write `apps/api/backends/tests/test_logging_filter.py` per RESEARCH lines 1527-1551:
- `caplog`-based test for each of the three secret patterns (sk-ant, sk-, Bearer). Each test asserts the pattern's text is replaced and the replacement marker appears.
- One test for `record.args = ()` interpolation safety (Pitfall 8): `logger.info("key=%s url=%s", "sk-proj-abcdefghijklmnopqrstuvwxyz", "https://x")` and assert `***REDACTED-OPENAI***` in caplog AND `sk-proj-` NOT in caplog.
- One test for idempotence: call `install_redaction_filter()` twice, assert exactly one `RedactionFilter` instance in `logging.getLogger().filters`.

Write `apps/api/backends/tests/test_keystore.py` per PATTERNS line 390-393:
- Test memory primary (`set` then `get` returns same value).
- Test env fallback via `monkeypatch.setenv("OPENROUTER_API_KEY", "envkey"); assert KeyStore().get("openrouter") == "envkey"`.
- Test `use_keyring=True` raises when `_HAS_KEYRING` is False (monkeypatch the module attribute).
- Test that the `RuntimeError` message contains `uv sync --extra keyring`.
- Test unknown provider returns None.

Write `apps/api/backends/tests/test_pricing.py` per PATTERNS line 393-399:
- Test `PricingTable.from_static("config/pricing.json")` loads without error.
- Test `.get("openai/gpt-5")` returns the static entry with `input_per_mtok == 2.50`.
- Test `.get("nonexistent-slug")` returns the `_default` entry with `input_per_mtok == 5.00`.
- Test `_merge_openrouter_snapshot` correctly converts decimal-string-per-token to per-Mtok float (Pitfall 6 regression): feed `{"data": [{"id": "openai/gpt-5", "pricing": {"prompt": "0.0000025", "completion": "0.00001"}}]}` and assert `table.get("openai/gpt-5") == {"input_per_mtok": 2.5, "output_per_mtok": 10.0}`.
- Async test (decorated `@pytest.mark.asyncio` even though `asyncio_mode="auto"` for explicitness) for cache-hit behavior using a `tmp_path` fixture: write a fake snapshot to `tmp_path / "cache.json"` with the right mtime, call `await table.refresh_from_openrouter(cache_path)`, assert no HTTP call was made (use `monkeypatch` to patch `httpx.AsyncClient` to a fake that records calls; assert call count is 0).

Write `apps/api/backends/tests/test_adapter_contract.py` per RESEARCH lines 2088-2232 (STRUCTURE only):
- Comment-block header: `# Plan 00 Task 3 — D-19 shared parametric adapter contract suite. Six invariants × three adapters = 18 cases.` Enumerate the 6 invariants.
- Module-level imports of `Done`, `StreamError`, `TextDelta` from chunks; `AdapterOptions` from protocol. **DO NOT import the three adapter classes at module level** — they are imported lazily inside the `adapter_factory` fixture (per the Wave 0 stub strategy above AND per the B3 key_links mandate) so this file's collection works before Wave 1 adapters exist. The B3 fix is enforced by the frontmatter `key_links` block; any reviewer running `grep "from apps.api.backends.openrouter.adapter import OpenRouterAdapter" apps/api/backends/tests/test_adapter_contract.py` MUST get zero hits.
- `ADAPTER_PARAMS = [pytest.param("openrouter", id="openrouter"), pytest.param("claude_code", id="claude_code"), pytest.param("computer_use", id="computer_use")]`.
- Six tests parametrized across all three:
  1. `test_happy_path_terminates_with_done` — asserts ≥1 TextDelta and `isinstance(chunks[-1], Done)`.
  2. `test_cost_cap_aborts` — `max_cost_usd=0.000001`, prompt `"x" * 1000`, assert `StreamError(code="cost_cap_exceeded")` chunk present and last chunk is `Done`.
  3. `test_step_cap_aborts` — `pytest.skip("not applicable")` for openrouter; for the other two, `max_steps=1`, assert `StreamError(code="step_cap_exceeded")` chunk present and last is `Done`.
  4. `test_cancellation_within_2_seconds` — `@pytest.mark.timeout(2)`; start a consumer task, cancel after 50 ms, assert (if any chunks landed) last is `Done`.
  5. `test_done_always_lands` — happy path, `assert isinstance(chunks[-1], Done)`.
  6. `test_missing_api_key_raises_before_stream` — adapter-specific raise (openrouter: `OpenRouterAdapter(api_key="")` raises; claude_code: `monkeypatch.delenv("ANTHROPIC_API_KEY"); ClaudeCodeAdapter()` raises; computer_use: `monkeypatch.setenv("COMPUTER_USE_OPT_IN", "1"); ComputerUseAdapter(api_key="")` raises). The fixture handles lazy imports so each test gets the appropriate adapter class from the fixture parameter — not from a module-level import.

Every test file: `from __future__ import annotations` after the comment block; inline imports inside test functions; type-annotated `def test_X() -> None:`; `@pytest.mark.asyncio` on every async test (explicit even though `asyncio_mode="auto"`).
  </action>
  <verify>
    <automated>uv run pytest apps/api/backends/tests/test_chunks.py apps/api/backends/tests/test_logging_filter.py apps/api/backends/tests/test_keystore.py apps/api/backends/tests/test_pricing.py -x -q 2>&1 | tail -20 && uv run pytest --collect-only apps/api/backends/tests/test_adapter_contract.py 2>&1 | grep -E "test_happy_path|test_cost_cap|test_step_cap|test_cancellation|test_done_always|test_missing_api" | wc -l | awk '{ if ($1 < 6) { print "FAIL: contract suite did not collect 6 test functions"; exit 1 } else { print "OK collected " $1 " test functions" } }' && ! grep -E "^from apps\.api\.backends\.(openrouter|claude_code|computer_use)\.adapter import" apps/api/backends/tests/test_adapter_contract.py && uv run pytest src/routing/tests/test_decide_smoke.py -x -q</automated>
  </verify>
  <done>
- All 5 shared unit tests (chunks, logging_filter, keystore, pricing, conftest+fakes) pass.
- D-19 contract suite file is collected without ImportError (it skips per-adapter when the adapter module doesn't yet exist).
- `test_adapter_contract.py` has ZERO module-level imports of `OpenRouterAdapter` / `ClaudeCodeAdapter` / `ComputerUseAdapter` (B3 fix — verified by negative grep).
- Phase 1 D-18 guard still green.
- Wave 1 plans can now import shared modules + the contract suite imports the adapter classes via the lazy try/except idiom in conftest fixtures.
  </done>
  <acceptance_criteria>
- Behavior: `uv run pytest apps/api/backends/tests/test_chunks.py -x` exits 0.
- Behavior: `uv run pytest apps/api/backends/tests/test_logging_filter.py -x` exits 0.
- Behavior: `uv run pytest apps/api/backends/tests/test_keystore.py -x` exits 0.
- Behavior: `uv run pytest apps/api/backends/tests/test_pricing.py -x` exits 0.
- Behavior: `uv run pytest --collect-only apps/api/backends/tests/test_adapter_contract.py 2>&1 | grep -Ec "test_happy_path|test_cost_cap|test_step_cap|test_cancellation|test_done_always|test_missing_api"` returns 6 (one collected definition per invariant).
- Source: `grep -q "ADAPTER_PARAMS = \[" apps/api/backends/tests/test_adapter_contract.py`.
- Source: `grep -q "@pytest.mark.timeout(2)" apps/api/backends/tests/test_adapter_contract.py` (cancellation invariant).
- Source: `grep -q "from __future__ import annotations" apps/api/backends/tests/conftest.py apps/api/backends/tests/test_chunks.py apps/api/backends/tests/test_adapter_contract.py`.
- Source: `grep -q "pytest.skip" apps/api/backends/tests/conftest.py` (lazy adapter import skip pattern in the `adapter_factory` fixture).
- Source (B3 enforcement): `! grep -E "^from apps\.api\.backends\.(openrouter|claude_code|computer_use)\.adapter import" apps/api/backends/tests/test_adapter_contract.py` exits 0 (NO module-level adapter imports in the contract suite file).
- Source (B3 enforcement): `grep -E "(try:|except ImportError|pytest\.skip)" apps/api/backends/tests/conftest.py | wc -l` returns >= 6 (at least one try/except/skip triple per adapter inside the `adapter_factory` fixture).
- Source: `grep -q "***REDACTED-ANTHROPIC***" apps/api/backends/tests/test_logging_filter.py`.
- Source: `grep -q "record.args" apps/api/backends/tests/test_logging_filter.py` OR `grep -q "%s.*sk-proj" apps/api/backends/tests/test_logging_filter.py` (Pitfall 8 regression).
- Source: `grep -q "_merge_openrouter_snapshot" apps/api/backends/tests/test_pricing.py` (Pitfall 6 regression).
- Behavior: Phase 1 D-18 guard still passes: `uv run pytest src/routing/tests/test_decide_smoke.py -x` exits 0.
- Behavior: `uv run pytest -m 'not live' apps/api/backends/tests/ -x 2>&1 | tail -5` — the 4 unit-test files all pass; the contract suite reports ModuleNotFoundError on per-adapter tests (expected until Wave 1).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Reconcile RESEARCH.md Open Questions with RESOLVED status (B2 fix)</name>
  <files>.planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md</files>
  <read_first>
    - .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md lines 2014-2035 (the current `## Open Questions` section with 4 numbered questions, each with a `Recommendation:` line but no `RESOLVED:` marker)
    - The B2 checker issue: Dimension 11 gate requires explicit `RESOLVED` markers on the section header AND on each individual question; current state has only `Recommendation:` lines.
  </read_first>
  <action>
Read `.planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md` and locate the `## Open Questions` section (starts at approximately line 2014).

Edit the file in place to:

1. **Rename the section header** from `## Open Questions` to `## Open Questions (RESOLVED)`.

2. **For each of the 4 numbered questions**, transform the title and append a `**Resolution:**` sentence after each `Recommendation:` line using these dispositions:

   - **Q1** (OpenRouter `/api/v1/models` rate limits) — change title from `1. **OpenRouter \`/api/v1/models\` — confirmed unauthenticated**` to `1. **RESOLVED: OpenRouter \`/api/v1/models\` — confirmed unauthenticated**`. After the existing `Recommendation:` line append a NEW line `**Resolution:** Accept 24h cache; revisit if 429s appear (defer to operations).`
   
   - **Q2** (Anthropic SDK `usage.cache_*_tokens` field names) — change title from `2. **Anthropic SDK \`usage.cache_*_tokens\` fields exact names**` to `2. **RESOLVED: Anthropic SDK \`usage.cache_*_tokens\` fields exact names**`. Append `**Resolution:** Defensive \`getattr\` is the locked approach (RESEARCH Pattern 5 line numbers).`
   
   - **Q3** (Phase 2 `Done.cost_usd` precedence) — change title from `3. **Phase 2 \`Done.cost_usd\` precedence with mixed authority sources**` to `3. **RESOLVED: Phase 2 \`Done.cost_usd\` precedence with mixed authority sources**`. Append `**Resolution:** Defer authority precedence to live smoke; defensive code stands until then.`
   
   - **Q4** (claude-agent-sdk message content shape) — change title from `4. **claude-agent-sdk message content shape for ToolResult blocks**` to `4. **RESOLVED: claude-agent-sdk message content shape for ToolResult blocks**`. Append `**Resolution:** \`getattr(msg, "content", [])\` + isinstance dispatch is the locked approach (Pattern 4); live smoke confirms shape.`

3. **Preserve every other line** of the file verbatim (sections above and below Open Questions, including the `Environment Availability` and `Validation Architecture` sections that follow).

Use the Read tool to load the current file (only the relevant range — lines 2010-2040 is enough), then use Write to overwrite the file with the modified content. Be careful to preserve every byte outside the Open Questions section.
  </action>
  <verify>
    <automated>grep -q "## Open Questions (RESOLVED)" .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md && grep -c "RESOLVED" .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md | awk '{ if ($1 < 5) { print "FAIL: expected ≥5 RESOLVED occurrences (1 header + 4 question titles), got " $1; exit 1 } else { print "OK " $1 " RESOLVED occurrences" } }' && grep -c "\*\*Resolution:\*\*" .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md | awk '{ if ($1 < 4) { print "FAIL: expected ≥4 Resolution lines (one per question), got " $1; exit 1 } else { print "OK " $1 " Resolution lines" } }' && grep -q "Defensive \`getattr\` is the locked approach" .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md && grep -q "live smoke confirms shape" .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md</automated>
  </verify>
  <done>
- `## Open Questions (RESOLVED)` is the new section header.
- All 4 numbered questions have `RESOLVED:` prefix in their title.
- Each question has a `**Resolution:**` line appended after its `Recommendation:` line.
- The literal token `RESOLVED` appears at least 5 times (1 header + 4 titles).
- The literal token `**Resolution:**` appears exactly 4 times (one per question).
- No other section in the file is modified.
  </done>
  <acceptance_criteria>
- Source: `grep -q "## Open Questions (RESOLVED)" .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md`.
- Source: `grep -c "RESOLVED" .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md` returns >= 5.
- Source: `grep -c "\*\*Resolution:\*\*" .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md` returns >= 4.
- Source: `grep -q "Accept 24h cache; revisit if 429s appear" .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md` (Q1 resolution).
- Source: `grep -q "Defensive \`getattr\` is the locked approach" .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md` (Q2 resolution).
- Source: `grep -q "Defer authority precedence to live smoke" .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md` (Q3 resolution).
- Source: `grep -q "live smoke confirms shape" .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md` (Q4 resolution).
- Behavior: `wc -l .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md | awk '{ print $1 }'` returns a count >= the original line count (Resolution lines are appended; no lines deleted).
- Anti-pattern: `! grep -q "^## Open Questions$" .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md` (old header without "(RESOLVED)" suffix is absent).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process import → root logger | Untrusted log strings (incl. caller-supplied prompts, env-var-rendered values) cross here. Redaction filter mutates `record.msg + record.args` before any handler. |
| `KeyStore.get` → env / keyring | Returns plaintext key material; must never be logged via `print()` or `logger.info("key: %s", k)` (filter catches the latter). |
| `pricing.refresh_from_openrouter` → public HTTPS endpoint | No auth required; data is integer/string pricing only. Treat response as untrusted: defensive `try/except` around JSON decode; warning-and-fallback on `httpx.HTTPError`. |
| `pyproject.toml → uv.lock` | Lockfile changes commit-staged; pre-commit hook (Plan 04) blocks `claude-code-sdk` reintroduction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I (Information Disclosure) | `apps/api/backends/logging_filter.py` `RedactionFilter` | mitigate | Pattern 10 + Pitfall 8: mutate `record.msg = msg` AND set `record.args = ()` so formatters don't re-interpolate the raw key from `%s` args. Test 9 + Test 10 in this plan enforce. SEVERITY: HIGH. |
| T-02-02 | I | `apps/api/backends/keystore.py` `KeyStore` | mitigate | D-10 + Pattern 12: `keyring` is an optional extra, lazy-imported. `set()` writes to keyring only if `use_keyring=True`. Default constructor (`KeyStore()`) keeps keys in memory only. Test 13 in this plan enforces the missing-extra raise. SEVERITY: HIGH. |
| T-02-03 (this plan's scope) | D (Denial of Service — wallet) | `apps/api/backends/cost.py` `CostTracker` | mitigate | `DEFAULT_PER_TURN_COST_USD = 0.50`; `over_cap()` is the gate every adapter checks per-chunk in Wave 1. Test 16 + Pitfall 6 regression (Test 15) ensure the per-Mtok math is correct so caps actually trip. SEVERITY: HIGH. |
| T-02-08 | T (Tampering) | `pyproject.toml`, `uv.lock` | mitigate | `claude-code-sdk` MUST be absent from both files. Plan 04 adds the pre-commit + CI grep. Acceptance criteria here verify by `grep -v` in both files. SEVERITY: MEDIUM. |

Threats T-02-04 (cancellation deadlock), T-02-05 (workspace leak), T-02-06 (computer-use accidental enable), T-02-07 (secret committed), are owned by Wave 1 / Wave 2 plans.
</threat_model>

<verification>
- All three Wave 1 adapter plans must be able to run `from apps.api.backends.chunks import ChatChunk, TextDelta, ToolCall, ToolResult, FileDiff, Screenshot, StreamError, Done`, `from apps.api.backends.protocol import BackendAdapter, Message, AdapterOptions`, `from apps.api.backends.cost import CostTracker, DEFAULT_PER_TURN_COST_USD`, and `from apps.api.backends.pricing import PricingTable` without error.
- `uv run pytest -m 'not live' apps/api/backends/tests/test_chunks.py apps/api/backends/tests/test_logging_filter.py apps/api/backends/tests/test_keystore.py apps/api/backends/tests/test_pricing.py` exits 0.
- `uv run pytest --collect-only apps/api/backends/tests/test_adapter_contract.py` exits 0 (collection works; per-adapter tests skip until Wave 1).
- `uv run pytest src/routing/tests/test_decide_smoke.py -x` exits 0 (Phase 1 D-18 import-graph guard still green).
- `grep -c '"claude-code-sdk"' uv.lock` returns 0 (deprecated SDK absent).
- `python -c "import apps.api; import logging; logging.getLogger('x').info('Bearer sk-ant-AAAAAAAAAAAAAAAAAAAA')"` runs without raising and (under caplog in the regression test) produces a redacted line.
- `grep -q "## Open Questions (RESOLVED)" .planning/phases/02-backend-adapters-chatchunk-contract/02-RESEARCH.md` (B2 fix verified).
- `! grep -E "^from apps\.api\.backends\.(openrouter|claude_code|computer_use)\.adapter import" apps/api/backends/tests/test_adapter_contract.py` (B3 fix verified — adapter imports are lazy inside `adapter_factory`).
</verification>

<success_criteria>
- Phase 1 D-18 import-graph guard still green after Phase 2 dep additions.
- 9 shared source files exist at `apps/api/__init__.py`, `apps/__init__.py`, and `apps/api/backends/{__init__,protocol,chunks,keystore,logging_filter,pricing,cost}.py`.
- 6 test files exist and pass (the 4 shared unit tests pass; the contract suite collects but defers per-adapter tests to Wave 1).
- `config/pricing.json` exists with the 13 model entries + `_default`.
- `uv lock` is in sync with `pyproject.toml` (regenerated; passes `uv lock --check`).
- The four Wave 0 truths from `must_haves.truths` are observable from a CLI / pytest run.
- B3 fix verified: `test_adapter_contract.py` has zero module-level adapter imports; `conftest.py`'s `adapter_factory` fixture wraps each adapter import in try/except ImportError → `pytest.skip(...)`.
- B2 fix verified: `02-RESEARCH.md` Open Questions section is renamed to `## Open Questions (RESOLVED)` with each of 4 questions carrying a `RESOLVED:` title prefix and a `**Resolution:**` sentence appended.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-adapters-chatchunk-contract/02-00-SUMMARY.md` summarizing:
- Dependencies added (with verified versions).
- 9 source files + 6 test files created (one-line description each).
- Pydantic schema commit hash + a representative chunk JSON line.
- `config/pricing.json` entry count and `_default` fallback.
- Confirmation that Phase 1 D-18 guard still passes.
- B3 fix outcome: `test_adapter_contract.py` has zero module-level adapter imports.
- B2 fix outcome: `02-RESEARCH.md` Open Questions reconciled with RESOLVED markers (1 header + 4 questions + 4 Resolution lines).
- Any deviations from RESEARCH.md patterns (none expected; flag if `claude-agent-sdk` version pin needed to shift).
</output>
</content>
</invoke>