---
phase: 01-router-brain-foundation
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/task_classifier/build_agentic_dataset.py
  - src/task_classifier/tests/test_agentic_intent.py
  - data_processed/agentic_intent_seeds.csv
  - data_processed/agentic_intent_synthesized.csv
  - data_processed/agentic_intent_negatives.csv
  - data_processed/agentic_intent_training.csv
autonomous: false
requirements:
  - ROUTER-01
must_haves:
  truths:
    - "data_processed/agentic_intent_training.csv exists, has exactly 4 columns (text, label, source, dataset), and ~1,000 rows balanced 500 agentic / 500 conversational (D-07)."
    - "Positives come from the union of: ~30 hand-written seeds (D-05) anchored to the README golden-path examples + ~470 LLM-synthesized paraphrases produced via a one-time offline expansion using the developer's own API key from OUTSIDE this codebase (D-05, RESEARCH Assumption A3)."
    - "Negatives come from LLMRouterBench non-tool-use rows mined out of data_processed/flat_records.csv (D-06)."
    - "`python -m src.task_classifier.build_agentic_dataset --check` exits 0 when the output CSV is well-formed (balanced, correct columns, no NaN in label) and non-zero otherwise — used by CI and by Plan 04 to gate training."
    - "Hand-audit checkpoint after synthesis: developer reviews 100% of synthesized rows; expected drop rate 5-15% per Anthropic synthetic-data norms (RESEARCH §Pattern 3 Step 3)."
  artifacts:
    - path: "src/task_classifier/build_agentic_dataset.py"
      provides: "argparse-driven CSV concatenator that takes seeds + synthesized + negatives inputs and writes the balanced training CSV"
      contains: "def main"
    - path: "data_processed/agentic_intent_seeds.csv"
      provides: "~30 hand-written agentic seed prompts (committed; anchors LLM expansion)"
      contains: "text,label,source,dataset"
    - path: "data_processed/agentic_intent_synthesized.csv"
      provides: "~470 LLM-synthesized paraphrastic variations of seeds (committed; produced once-offline by developer)"
      contains: "text,label,source,dataset"
    - path: "data_processed/agentic_intent_negatives.csv"
      provides: "~500 conversational/non-agentic prompts mined from LLMRouterBench (committed)"
      contains: "text,label,source,dataset"
    - path: "data_processed/agentic_intent_training.csv"
      provides: "Concatenated, shuffled, balanced training CSV (~1,000 rows, columns: text, label, source, dataset)"
      contains: "text,label,source,dataset"
  key_links:
    - from: "src/task_classifier/build_agentic_dataset.py"
      to: "data_processed/agentic_intent_seeds.csv"
      via: "--seeds-input CLI flag"
      pattern: "seeds-input"
    - from: "src/task_classifier/build_agentic_dataset.py"
      to: "data_processed/flat_records.csv"
      via: "When --negatives-input is omitted, mine from flat_records.csv directly using the filter from RESEARCH §Pattern 3 Step 4"
      pattern: "flat_records"
---

<objective>
Assemble the binary `agentic_intent_training.csv` dataset that Plan 04 will train against. Per CONTEXT D-05/06/07, positives are ~30 hand-written seeds + ~470 LLM-synthesized paraphrastic variations (the LLM expansion is a ONE-TIME OFFLINE step by the developer using their own API key from outside this codebase per RESEARCH Assumption A3), and negatives are ~500 conversational rows mined from LLMRouterBench's `flat_records.csv`. Target: ~1,000 rows total, balanced 500/500.

Purpose: Plan 04 (`train_agentic_intent.py`) requires this CSV to produce `models/agentic_intent_classifier.joblib`. Without an audited training dataset, the classifier — and therefore Phase 1 Success Criterion #2 — is impossible.

Output: Committed seed/synthesized/negatives CSVs in `data_processed/`, a concatenated `agentic_intent_training.csv`, and a `build_agentic_dataset.py` pipeline script with `--check` mode for CI/preflight use.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-router-brain-foundation/01-CONTEXT.md
@.planning/phases/01-router-brain-foundation/01-RESEARCH.md
@.planning/phases/01-router-brain-foundation/01-PATTERNS.md
@.planning/phases/01-router-brain-foundation/01-VALIDATION.md
@.planning/phases/01-router-brain-foundation/01-01-SUMMARY.md
@CLAUDE.md
@src/data/build_classifier_dataset.py
@src/task_classifier/build_question_type.py
</context>

<interfaces>
The data pipeline script pattern is established. The executor should copy the argparse + logging shape from `src/data/build_classifier_dataset.py:191-228` (per 01-PATTERNS.md lines 498-548). Key existing helpers to reuse:

From src/data/build_classifier_dataset.py (canonical pipeline-script shape):
- `parse_args(argv)` — argparse with --input, --output, -v flags
- `main(argv)` — returns int; 0 success, 2 input-not-found, 1 other failures
- `_setup_logging(verbosity)` — basicConfig with %(asctime)s | %(levelname)-7s | %(message)s
- `PROJECT_ROOT = Path(__file__).resolve().parents[2]` — pipeline scripts use pathlib

LLMRouterBench dataset families to EXCLUDE for negatives (tool-use / agentic rows that would poison the conversational class):
- Per RESEARCH §Pattern 3 Step 4: exclude any `dataset` in `{"tau2", "tau", "tool", "agent", "humaneval", "livecodebench", "mbpp", "swe-bench"}`.
- Per RESEARCH §Pattern 3 Step 4: include rows whose `dataset` is in `{"gsm8k", "math", "mmlu", "mmlu_pro", "simpleqa", "arenahard_creative_writing"}` plus other non-tool-use datasets present in flat_records.csv.

CSV column contract (locked):
- `text` (str) — the prompt
- `label` (str) — exactly `"agentic"` or `"conversational"`
- `source` (str) — one of `seed`, `synthesized`, `llmbench`
- `dataset` (str) — for negatives: the LLMRouterBench dataset slug; for seeds/synthesized: `"hand-written"` or `"llm-expansion"`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author the 30 hand-written agentic seed prompts</name>
  <read_first>
    - .planning/phases/01-router-brain-foundation/01-RESEARCH.md (section "Pattern 3, Step 1" lines 514-522 — covers the three sub-buckets: ~10 build/edit prompts → Claude Code intent, ~10 browse/click prompts → computer-use intent, ~10 multi-step prompts that are clearly agentic)
    - .planning/phases/01-router-brain-foundation/01-CONTEXT.md (D-05 verbatim; the README golden-path examples: "build me a finance app" → Claude Code, "open this URL and check the price" → computer-use; CONTEXT line 161 — fallback target literal string)
    - ReadMe.md (existing golden-path mention; current README references the same three demo prompts the seeds anchor on)
    - data_processed/classifier_training.csv (only schema reference; do NOT use it for label content)
  </read_first>
  <action>
    1. Create `data_processed/agentic_intent_seeds.csv` with header line `text,label,source,dataset`.
    2. Author 30 high-quality rows, all with `label = "agentic"`, `source = "seed"`, `dataset = "hand-written"`. Distribution per D-05 / RESEARCH §Pattern 3 Step 1:
       - ~10 build/edit prompts anchored to "build me a finance app" — variations like `"build me a small finance dashboard"`, `"write a CLI tool that summarizes my AWS bill"`, `"create a Python script that scrapes Hacker News headlines"`, `"refactor my Flask app to use FastAPI"`, `"implement a binary search tree with delete()"`, `"add unit tests for the auth module"`, `"fix the off-by-one bug in pagination.py"`, `"edit my Dockerfile to use Python 3.12"`, `"rewrite this regex to be O(n)"`, `"generate a sqlx schema for a todo list app"`.
       - ~10 browse/click prompts anchored to "open this URL and check the price" — `"open https://news.ycombinator.com and summarize the top 5 stories"`, `"navigate to amazon.com and find a charger under $20"`, `"click the subscribe button on this newsletter page"`, `"fill out the contact form at example.com/contact"`, `"submit my resume to the careers page at acme.io"`, `"browse to my Gmail inbox and report unread count"`, `"visit github.com/microsoft/vscode and tell me the star count"`, `"download the latest invoice from stripe.com"`, `"scrape https://example.com/products and list prices"`, `"check if my flight on united.com is delayed"`.
       - ~10 multi-step prompts that combine reasoning + action — `"open my repo, find all TODO comments, and write a summary report"`, `"build a Streamlit app that fetches weather from openweathermap.org and plots the forecast"`, `"refactor my data pipeline to read from S3 and write to BigQuery"`, `"create a GitHub Action that runs pytest on every PR"`, `"investigate why the prod API returns 500s and fix the root cause"`, `"write a script that monitors my CPU usage and alerts via webhook"`, `"set up a cron job that backs up my notes folder nightly"`, `"port my React app from CRA to Vite"`, `"add OAuth2 login to my Express server using passport"`, `"convert my Jupyter notebook to a reusable Python module"`.
    3. Each row must be properly CSV-quoted (commas, quotes, newlines escaped per RFC 4180; the executor SHOULD use Python's `csv.writer` to write the file rather than hand-typing the CSV).
    4. Run a sanity check: `uv run python -c "import csv; rows=list(csv.DictReader(open('data_processed/agentic_intent_seeds.csv'))); assert len(rows) == 30, len(rows); assert all(r['label']=='agentic' for r in rows); assert all(r['source']=='seed' for r in rows); print('ok')"`.
  </action>
  <verify>
    <automated>uv run python -c "import csv; rows=list(csv.DictReader(open('data_processed/agentic_intent_seeds.csv'))); assert 28 &lt;= len(rows) &lt;= 32, len(rows); assert all(r['label']=='agentic' for r in rows); assert all(r['source']=='seed' for r in rows); assert all(r['dataset']=='hand-written' for r in rows); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `data_processed/agentic_intent_seeds.csv` exists
    - Source: First line of file is exactly `text,label,source,dataset`
    - Behavior: `csv.DictReader` reads between 28 and 32 rows (target 30; tolerance for minor adjustments during authoring)
    - Behavior: All rows have `label == "agentic"`, `source == "seed"`, `dataset == "hand-written"`
    - Behavior: Rows include at least one prompt containing the literal substring `build` (build/edit bucket)
    - Behavior: Rows include at least one prompt containing the literal substring `open` AND `https?://` (browse bucket)
    - Behavior: No row has an empty `text` field
  </acceptance_criteria>
  <done>
    Seeds CSV committed with ~30 high-quality agentic prompts spanning the three required sub-buckets.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Developer runs offline LLM expansion to produce ~470 paraphrastic variations</name>
  <read_first>
    - .planning/phases/01-router-brain-foundation/01-RESEARCH.md (section "Pattern 3, Step 2" lines 524-533; section "Assumptions Log" A3 line 1104 — manual one-time operation; section "Pitfall 2" lines 916-926 — drift expectations)
    - .planning/phases/01-router-brain-foundation/01-CONTEXT.md (D-05 verbatim)
    - data_processed/agentic_intent_seeds.csv (the input to the LLM expansion — must exist from Task 1)
  </read_first>
  <what-built>
    The agent CANNOT perform the LLM expansion call from within its sandbox — per RESEARCH Assumption A3 and the architectural constraint that `src/routing/` must be HTTP-library-free (D-18), the LLM expansion is intentionally a one-time MANUAL step run by the developer in their own shell, using their own LLM API key, OUTSIDE this codebase. This keeps the routing brain dependency-free while still producing ~470 synthesized rows.
  </what-built>
  <how-to-verify>
    1. In your OWN terminal (NOT the agent's sandbox), use whichever LLM you prefer (Claude Opus 4.7, GPT-5, or any strong chat model) with the following prompt template:

       ```
       I am building a training dataset for an "agentic intent" binary classifier. Here are 30 hand-written examples of AGENTIC prompts (build/edit code, browse the web, multi-step actions). For each seed, generate 15 paraphrastic variations that preserve the agentic intent but vary in: verb diversity, prompt length (5 tokens to 200 tokens), phrasing style (terse vs. polite), and domain spread.

       Output ONLY a CSV with header `text,label,source,dataset` where label="agentic", source="synthesized", dataset="llm-expansion".

       Seed prompts:
       <paste data_processed/agentic_intent_seeds.csv here>
       ```

    2. Save the LLM's output as `data_processed/agentic_intent_synthesized.csv`. Target row count: 450-490 rows (30 seeds × ~15 variations ≈ 450; allow ±40).
    3. Hand-audit (RESEARCH §Pattern 3 Step 3): scroll through the file in a CSV-aware viewer (VS Code with CSV preview, Numbers, or Excel). Delete any row that:
       - Is no longer agentic (e.g., became a pure information request)
       - Is degenerate (empty, single-token, all punctuation, hallucinated foreign language unintentionally)
       - Duplicates an existing seed verbatim
       - Mentions a specific seed identifier (looks like it leaked the LLM's reasoning)
       Expected drop: 5-15% per Anthropic synthetic-data norms. If you drop >25%, the LLM session quality was low — re-run.
    4. Final target after audit: ~470 rows (range 400-490).
    5. Save the prompt template you used as a comment block at the top of `src/task_classifier/build_agentic_dataset.py` (Task 3 will create that file) for reproducibility per RESEARCH Open Question 3.
  </how-to-verify>
  <acceptance_criteria>
    - Source: `data_processed/agentic_intent_synthesized.csv` exists
    - Source: First line of file is exactly `text,label,source,dataset`
    - Behavior: `uv run python -c "import csv; rows=list(csv.DictReader(open('data_processed/agentic_intent_synthesized.csv'))); assert 400 &lt;= len(rows) &lt;= 490, len(rows); assert all(r['label']=='agentic' for r in rows); assert all(r['source']=='synthesized' for r in rows); print(len(rows))"` exits 0 and prints a row count in [400, 490]
    - Behavior: At least 80% of rows are unique (no near-duplicate spam) — verified by `uv run python -c "import csv; texts=[r['text'] for r in csv.DictReader(open('data_processed/agentic_intent_synthesized.csv'))]; assert len(set(texts)) / len(texts) &gt;= 0.80, (len(set(texts)), len(texts))"` exiting 0
  </acceptance_criteria>
  <resume-signal>Type "synthesized done" with the final row count, or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Build the negatives miner + the combined training CSV in build_agentic_dataset.py</name>
  <read_first>
    - src/data/build_classifier_dataset.py (full file, 232 lines — canonical argparse-driven pipeline-script shape per CLAUDE.md "Module Design" and 01-PATTERNS.md lines 498-577)
    - data_processed/classifier_training.csv (small file; LFS pointer — confirm schema)
    - .planning/phases/01-router-brain-foundation/01-RESEARCH.md (section "Pattern 3 Step 4" lines 539-545 — negatives mining filter; section "Pattern 3 Step 5" lines 547-549 — concat + shuffle)
    - .planning/phases/01-router-brain-foundation/01-PATTERNS.md (section "src/task_classifier/build_agentic_dataset.py" lines 494-576 — the analog file and pattern excerpts)
    - data_processed/agentic_intent_seeds.csv (from Task 1)
    - data_processed/agentic_intent_synthesized.csv (from Task 2)
    - CLAUDE.md (section "Logging" + "Module Design" — pipeline scripts use `logging` + `argparse`; section "Naming Patterns" — `_setup_logging` and snake_case modules)
  </read_first>
  <action>
    1. Create `src/task_classifier/build_agentic_dataset.py`. Match the shape of `src/data/build_classifier_dataset.py` (argparse + logging + `main(argv) -> int`). Top-of-file docstring per 01-PATTERNS.md lines 550-566 documents the CLI invocation. Below the docstring, include a comment block with the EXACT LLM prompt template used by the developer in Task 2 for reproducibility.
    2. Module-level constants (use pathlib per CLAUDE.md "Path discovery via `__file__`" for pipeline scripts):
       - `PROJECT_ROOT = Path(__file__).resolve().parents[2]`
       - `DATA_PROCESSED_DIR = PROJECT_ROOT / "data_processed"`
       - `DEFAULT_SEEDS = DATA_PROCESSED_DIR / "agentic_intent_seeds.csv"`
       - `DEFAULT_SYNTHESIZED = DATA_PROCESSED_DIR / "agentic_intent_synthesized.csv"`
       - `DEFAULT_NEGATIVES = DATA_PROCESSED_DIR / "agentic_intent_negatives.csv"`
       - `DEFAULT_FLAT_RECORDS = DATA_PROCESSED_DIR / "flat_records.csv"` (input for the miner)
       - `DEFAULT_OUTPUT = DATA_PROCESSED_DIR / "agentic_intent_training.csv"`
    3. Function `mine_negatives_from_flat_records(flat_records_path: Path, target_count: int, random_state: int = 42) -> pd.DataFrame`:
       - Read `flat_records.csv` via `pd.read_csv(flat_records_path)`.
       - The agentic/tool-use datasets to EXCLUDE per RESEARCH lines 540-541: `{"tau2", "tau", "tool", "agent", "humaneval", "livecodebench", "mbpp", "swe-bench"}`. Filter rows where `df["dataset"].str.lower().str.contains(<each>)` is False for ALL exclusions.
       - Drop rows with empty `origin_query`.
       - Stratified sample by `dataset` to avoid math-only bias — group by `dataset`, take up to `target_count // n_datasets` per group, then top-up randomly if under-target.
       - Return DataFrame with columns: `text` (the `origin_query` value), `label = "conversational"`, `source = "llmbench"`, `dataset` (preserve the original LLMRouterBench dataset slug).
    4. Function `parse_args(argv)`:
       - `--seeds-input` (path; default `DEFAULT_SEEDS`)
       - `--synthesized-input` (path; default `DEFAULT_SYNTHESIZED`)
       - `--negatives-input` (path; default `DEFAULT_NEGATIVES` — but if it does NOT exist, the script automatically mines from `--flat-records-input` and writes the result to `DEFAULT_NEGATIVES`)
       - `--flat-records-input` (path; default `DEFAULT_FLAT_RECORDS`)
       - `--output` (path; default `DEFAULT_OUTPUT`)
       - `--negatives-target-count` (int; default 500)
       - `--random-seed` (int; default 42)
       - `--check` (action="store_true") — does NOT write anything; only validates the output CSV at `--output` and exits 0 if balanced and well-formed, non-zero otherwise. Used by CI per 01-VALIDATION.md.
       - `-v / --verbose` (action="count", default=1) — same logging-verbosity pattern as `build_classifier_dataset.py:104-114`.
    5. Function `main(argv) -> int`:
       - If `--check` flag: read `--output`, assert columns are exactly `{"text", "label", "source", "dataset"}`, assert both labels present, assert row count >= 800, assert label balance is within 0.55/0.45 either side (i.e., neither class is more than 55% of the data). On any failure, log error and return 1. On success, log info and return 0.
       - Otherwise: load seeds + synthesized (require they exist; raise FileNotFoundError with the canonical multi-line remediation hint per CLAUDE.md "Error Handling" if missing). If `--negatives-input` exists, load it; else call `mine_negatives_from_flat_records` and persist to `DEFAULT_NEGATIVES`.
       - Concatenate the three DataFrames, shuffle via `.sample(frac=1.0, random_state=args.random_seed).reset_index(drop=True)`.
       - Validate balance: log a warning if the agentic:conversational ratio is outside [0.45, 0.55].
       - Write to `args.output` via `to_csv(index=False)`.
       - Print `Saved <output_path>` per CLAUDE.md "Logging" "After every saved file, print `Saved <thing> to: <path>`".
       - Return 0.
    6. `if __name__ == "__main__": sys.exit(main())` at the bottom.
    7. Run the script: `uv run python -m src.task_classifier.build_agentic_dataset` (uses defaults). Inspect output: ~1000 rows; near-balanced; columns correct.
    8. Run with `--check`: `uv run python -m src.task_classifier.build_agentic_dataset --check`. Confirm exit 0.
    9. Implement the test in `src/task_classifier/tests/test_agentic_intent.py` by REMOVING the `pytest.skip(...)` from Plan 01's scaffold AND adding ONE single test for now: `test_dataset_csv_well_formed()` that reads `data_processed/agentic_intent_training.csv` and asserts the same conditions as `--check`. The MODEL training test will be added by Plan 04 (this file is multi-plan; Plan 04 will add the second test).
  </action>
  <verify>
    <automated>uv run python -m src.task_classifier.build_agentic_dataset --check &amp;&amp; uv run pytest src/task_classifier/tests/test_agentic_intent.py::test_dataset_csv_well_formed -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/task_classifier/build_agentic_dataset.py` exists and is a valid Python module
    - Source: file contains the literal substring `argparse` (pipeline-script convention)
    - Source: file does NOT contain `input(` (training-script REPL convention is forbidden here — this is a pipeline script per CLAUDE.md anti-pattern in 01-PATTERNS.md line 1286)
    - Source: file contains `--check` flag definition
    - Source: file contains a comment block with the EXACT LLM prompt template used in Task 2
    - Source: `data_processed/agentic_intent_negatives.csv` exists after first run
    - Source: `data_processed/agentic_intent_training.csv` exists after first run
    - Behavior: `uv run python -m src.task_classifier.build_agentic_dataset --check` exits 0
    - Behavior: `uv run python -c "import csv; rows=list(csv.DictReader(open('data_processed/agentic_intent_training.csv'))); n=len(rows); n_ag=sum(1 for r in rows if r['label']=='agentic'); n_co=sum(1 for r in rows if r['label']=='conversational'); assert 800 &lt;= n &lt;= 1100, n; assert 0.45 &lt;= n_ag / n &lt;= 0.55, (n_ag, n_co, n); print(f'rows={n} ag={n_ag} co={n_co}')"` exits 0 and prints balanced counts
    - Behavior: `uv run python -c "import csv; rows=list(csv.DictReader(open('data_processed/agentic_intent_training.csv'))); cols=set(rows[0].keys()); assert cols == {'text', 'label', 'source', 'dataset'}, cols; print(cols)"` exits 0 and prints exactly `{'text', 'label', 'source', 'dataset'}`
    - Test: `uv run pytest src/task_classifier/tests/test_agentic_intent.py::test_dataset_csv_well_formed -x -q` exits 0
  </acceptance_criteria>
  <done>
    `agentic_intent_training.csv` exists with ~1,000 rows balanced 500/500; `--check` mode passes; full pytest suite stays green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer's external LLM session → committed CSV | The synthesized CSV is hand-pasted from an offline LLM call; quality is human-audited but the LLM session itself is outside the codebase |
| flat_records.csv → mined negatives | `flat_records.csv` is an LLMRouterBench-derived file produced by `src/data/flatten_raw_jsons.py`; assumed trusted (same repo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-DS-1 | Tampering (training data) | `agentic_intent_synthesized.csv` (LLM-generated) | mitigate | RESEARCH §Pattern 3 Step 3 mandates 100% human audit of synthesized rows with a 5-15% expected drop rate. Task 2's `<acceptance_criteria>` includes a uniqueness check ≥ 80% to catch low-quality LLM sessions. The LLM prompt template is committed inline in `build_agentic_dataset.py` for reproducibility. |
| T-01-DS-2 | Information disclosure | LLM prompt template / API key | accept | The developer's LLM API key is used in their own shell outside this repo per RESEARCH Assumption A3; key never touches the codebase. The committed prompt template is non-secret. SECURE-03 `.gitignore` (from Plan 01) blocks `.env` if the developer accidentally drops a key into a project-local file. |
| T-01-DS-3 | Tampering (label flip) | seeds + synthesized CSVs | accept | Every row is hand-authored or hand-audited; Plan 04's classifier training will reveal mislabeled clusters via per-class precision/recall numbers. No automated label-flip detection in Phase 1. |
</threat_model>

<verification>
1. All 4 CSVs exist in `data_processed/`: `agentic_intent_seeds.csv`, `agentic_intent_synthesized.csv`, `agentic_intent_negatives.csv`, `agentic_intent_training.csv`.
2. Final `agentic_intent_training.csv` has ~1,000 rows, balanced 500/500 (tolerance ±55%).
3. `uv run python -m src.task_classifier.build_agentic_dataset --check` exits 0.
4. `uv run pytest -x -q` stays green (the only new test is `test_dataset_csv_well_formed` which exercises the same conditions as `--check`).
</verification>

<success_criteria>
- Plan 04 (`train_agentic_intent.py`) can read `data_processed/agentic_intent_training.csv` and have an immediately usable balanced binary dataset (`text` column for TF-IDF, `label` column as the target).
- CI's `build_agentic_dataset --check` step gates Wave 2 — if a future commit corrupts the CSV (wrong columns, imbalance, NaN labels), CI fails before training is attempted.
</success_criteria>

<output>
After completion, create `.planning/phases/01-router-brain-foundation/01-03-SUMMARY.md` documenting:
- Final row counts: total, agentic, conversational, plus per-source breakdown (seed/synthesized/llmbench).
- Distribution of negatives across LLMRouterBench `dataset` slugs (so Plan 04 / 05 / 07 know whether math, MMLU, or creative-writing dominates the conversational class).
- The committed LLM prompt template used in Task 2 (verbatim) so Plan 07's canary-eval design can avoid generating prompts that overlap with the synthesized set.
- Drop rate observed during the Task 2 hand audit (5-15% expected; anything outside this range is a flag for Plan 04).
</output>
