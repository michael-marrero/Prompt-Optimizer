---
phase: 01-router-brain-foundation
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - src/task_classifier/train_agentic_intent.py
  - src/task_classifier/tests/test_agentic_intent.py
  - models/agentic_intent_classifier.joblib
  - evaluation/agentic_intent_plots/
autonomous: true
requirements:
  - ROUTER-01
must_haves:
  truths:
    - "models/agentic_intent_classifier.joblib exists and loads via load_joblib_artifacts() from src/demo/demo_router.py:35 unmodified — same canonical 5-key dict shape (Pitfall 4 / Success Criterion #2)."
    - "The saved `model` field is a `CalibratedClassifierCV(FrozenEstimator(LogisticRegression(...)))` — calibrated probabilities for the binary head per ROUTER-03 + RESEARCH §Pattern 1 (sklearn 1.6+ FrozenEstimator pattern)."
    - "On the 20% held-out test split, classification_report prints per-class precision/recall/F1 for both `agentic` and `conversational` labels; macro-F1 ≥ 0.80 is the soft target (RESEARCH-recommended; tunable per Open Question 1)."
    - "Training script follows the canonical training-script REPL `input()` pattern with 'train' / 'load' modes (CLAUDE.md Module Design); not argparse."
    - "Feature stack matches the existing classifiers: FeatureUnion[word_tfidf(1-2gram), char_tfidf(3-5gram)] ⊕ csr_matrix(StandardScaler.transform(numeric)) ⊕ LogisticRegression(max_iter=1500, class_weight='balanced', solver='saga', C=2.0, n_jobs=-1) (D-08; reuses the established stack verbatim)."
  artifacts:
    - path: "src/task_classifier/train_agentic_intent.py"
      provides: "Training script for the binary agentic-intent classifier"
      contains: "def train_agentic_intent_classifier"
    - path: "models/agentic_intent_classifier.joblib"
      provides: "Calibrated binary LogisticRegression artifact with the canonical 5-key dict shape ({model, vectorizer, scaler, label_encoder, feature_columns})"
      contains: "model"
    - path: "evaluation/agentic_intent_plots/"
      provides: "Confusion matrix and any other plots (dpi=300; tight_layout; close)"
      contains: "confusion_matrix.png"
  key_links:
    - from: "models/agentic_intent_classifier.joblib"
      to: "src/demo/demo_router.py:35 load_joblib_artifacts"
      via: "Same 5-key dict shape — KeyError-on-missing validator passes unmodified"
      pattern: "load_joblib_artifacts"
    - from: "train_agentic_intent.py"
      to: "src/feature_extraction/Feature_extractor.py"
      via: "PromptFeatureExtractor.extract() now includes the 5 new agentic features (Plan 02)"
      pattern: "PromptFeatureExtractor"
    - from: "train_agentic_intent.py"
      to: "data_processed/agentic_intent_training.csv"
      via: "Training input — text + label columns"
      pattern: "agentic_intent_training.csv"
---

<objective>
Train the binary agentic-intent classifier mandated by ROUTER-01. Reuses the canonical sklearn TF-IDF + handcrafted-feature stack from `train_task_classifier_robust.py` (per CLAUDE.md and 01-PATTERNS.md), with a `CalibratedClassifierCV(FrozenEstimator(...))` wrapper for calibrated probabilities (per ROUTER-03 + RESEARCH §Pattern 1, the post-sklearn-1.6 idiom). Persists `models/agentic_intent_classifier.joblib` in the canonical 5-key dict shape so the existing `load_joblib_artifacts()` validator at `src/demo/demo_router.py:35` accepts it unmodified.

Purpose: Phase 1 Success Criterion #2 — `models/agentic_intent_classifier.joblib` must exist, must load via the canonical validator, and must produce P/R numbers on a held-out test split. Without this artifact, `decide()` (Plan 06) cannot run the rule cascade for `backend = "claude_code"` vs `"computer_use"` vs `"openrouter"`.

Output: Trained calibrated binary classifier, saved to `models/agentic_intent_classifier.joblib`, with classification report logged at the end of training and a confusion-matrix plot under `evaluation/agentic_intent_plots/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-router-brain-foundation/01-CONTEXT.md
@.planning/phases/01-router-brain-foundation/01-RESEARCH.md
@.planning/phases/01-router-brain-foundation/01-PATTERNS.md
@.planning/phases/01-router-brain-foundation/01-VALIDATION.md
@.planning/phases/01-router-brain-foundation/01-02-SUMMARY.md
@.planning/phases/01-router-brain-foundation/01-03-SUMMARY.md
@CLAUDE.md
@src/task_classifier/train_task_classifier_robust.py
</context>

<interfaces>
Canonical 5-key joblib artifact dict (lifted from src/demo/demo_router.py:35-60 via 01-PATTERNS.md):
```python
artifacts = {
    "model": calibrated,           # CalibratedClassifierCV(FrozenEstimator(LogisticRegression(...)))
    "vectorizer": vectorizer,      # FeatureUnion([word_tfidf, char_tfidf])
    "scaler": scaler,              # StandardScaler fitted on numeric features
    "label_encoder": label_encoder, # LabelEncoder over {agentic, conversational}
    "feature_columns": feature_columns,  # list of numeric column names (incl. the 5 new agentic features)
}
joblib.dump(artifacts, "models/agentic_intent_classifier.joblib")
```

Required-keys check the validator runs (src/demo/demo_router.py:51-58):
```python
required_keys = ["model", "vectorizer", "scaler", "label_encoder", "feature_columns"]
for key in required_keys:
    if key not in artifacts:
        raise KeyError(f"{artifact_name} is missing required key: {key}")
```

Canonical TF-IDF + handcrafted stack (from train_task_classifier_robust.py:367-413 per 01-PATTERNS.md lines 358-415):
```python
vectorizer = FeatureUnion([
    ("word_tfidf", TfidfVectorizer(lowercase=True, ngram_range=(1, 2), min_df=2, max_df=0.95, max_features=10000)),
    ("char_tfidf", TfidfVectorizer(lowercase=True, analyzer="char_wb", ngram_range=(3, 5), min_df=2, max_features=6000)),
])
X_train_tfidf = vectorizer.fit_transform(X_text_train)
scaler = StandardScaler()
X_train_num_scaled = scaler.fit_transform(X_num_train)
X_train_combined = hstack([X_train_tfidf, csr_matrix(X_train_num_scaled)])
model = LogisticRegression(max_iter=1500, class_weight="balanced", solver="saga", C=2.0, n_jobs=-1)
model.fit(X_train_combined, y_train)
```

Canonical FrozenEstimator calibration wrapper (RESEARCH §Pattern 1 lines 382-421; sklearn 1.6+):
```python
from sklearn.calibration import CalibratedClassifierCV
from sklearn.frozen import FrozenEstimator

# After fitting `model` above:
X_train_only, X_calib, y_train_only, y_calib = train_test_split(
    X_train_combined, y_train, test_size=0.25, random_state=42, stratify=y_train,
)
calibrated = CalibratedClassifierCV(FrozenEstimator(model), method="sigmoid")
calibrated.fit(X_calib, y_calib)
# Replace ONLY the "model" field; preserve all other artifact keys verbatim.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create train_agentic_intent.py with TF-IDF stack + FrozenEstimator calibration</name>
  <read_first>
    - src/task_classifier/train_task_classifier_robust.py (full file, 609 lines — the analog; per 01-PATTERNS.md every section maps 1-to-1)
    - .planning/phases/01-router-brain-foundation/01-RESEARCH.md (section "Pattern 1: Calibration via FrozenEstimator" lines 372-431; section "Pattern 3: Agentic-Intent Classifier" lines 461-562; section "Pitfall 1" lines 905-913 — `cv="prefit"` is deprecated; section "Pitfall 3" lines 928-936 — calibrate on a fresh slice; section "Common Pitfalls — Pitfall 4" lines 938-946 — preserve the canonical 5-key dict shape)
    - .planning/phases/01-router-brain-foundation/01-PATTERNS.md (section "src/calibration/calibrate.py (or modifications to existing train scripts)" lines 354-446; section "src/task_classifier/train_agentic_intent.py" lines 449-490)
    - .planning/phases/01-router-brain-foundation/01-CONTEXT.md (D-07 verbatim — target ~1,000 prompts balanced 500/500; D-08 verbatim — feature stack matches existing + 5 new agentic features)
    - data_processed/agentic_intent_training.csv (the input — schema: text, label, source, dataset)
    - .planning/phases/01-router-brain-foundation/01-03-SUMMARY.md (read for actual row counts, drop rate, and dataset distribution from the audit)
    - CLAUDE.md (section "Naming Patterns" — snake_case modules, SCREAMING_SNAKE_CASE constants; section "Module Design" — training scripts use `input()`-prompt for train/load mode; section "Function Design" — heavy use of keyword arguments at call sites; section "Persistence Patterns" — `joblib.dump(artifacts_dict, output_path)`)
  </read_first>
  <action>
    1. Create `src/task_classifier/train_agentic_intent.py`. Mirror the file shape of `src/task_classifier/train_task_classifier_robust.py` line by line, with the following deltas:

    2. Path setup constants (use `os.path.join` per CLAUDE.md "Import Organization" — training scripts stay on os.path.join):
       - `CURRENT_DIR = os.path.dirname(os.path.abspath(__file__))`
       - `PROJECT_ROOT = os.path.abspath(os.path.join(CURRENT_DIR, "..", ".."))`
       - `SRC_DIR = os.path.join(PROJECT_ROOT, "src", "feature_extraction")`
       - `DATA_PROCESSED_DIR = os.path.join(PROJECT_ROOT, "data_processed")`
       - `INPUT_CSV = os.path.join(DATA_PROCESSED_DIR, "agentic_intent_training.csv")`
       - `MODELS_DIR = os.path.join(PROJECT_ROOT, "models")`
       - `MODEL_PATH = os.path.join(MODELS_DIR, "agentic_intent_classifier.joblib")`
       - `EVALUATION_DIR = os.path.join(PROJECT_ROOT, "evaluation")`
       - `PLOTS_DIR = os.path.join(EVALUATION_DIR, "agentic_intent_plots")`
       - `os.makedirs(MODELS_DIR, exist_ok=True)` and `os.makedirs(PLOTS_DIR, exist_ok=True)`.
       - `if SRC_DIR not in sys.path: sys.path.append(SRC_DIR)` then `from Feature_extractor import PromptFeatureExtractor` (canonical sys.path injection per CLAUDE.md Anti-Patterns "Per-script `sys.path` injection" — flagged but kept; broad migration is deferred per CONTEXT `<deferred>`).

    3. Imports (mirror robust.py top imports):
       - stdlib: `os`, `sys`
       - third-party: `pandas as pd`, `numpy as np`, `joblib`, `matplotlib.pyplot as plt`
       - sklearn: `LabelEncoder`, `StandardScaler`, `LogisticRegression`, `TfidfVectorizer`, `FeatureUnion`, `train_test_split`, `accuracy_score`, `f1_score`, `classification_report`, `confusion_matrix`, `ConfusionMatrixDisplay`, `precision_recall_fscore_support`
       - sklearn calibration: `CalibratedClassifierCV` from `sklearn.calibration`, `FrozenEstimator` from `sklearn.frozen`
       - scipy: `csr_matrix`, `hstack` from `scipy.sparse`

    4. Function `get_numeric_feature_columns(df: pd.DataFrame) -> list`:
       - Mirror `train_task_classifier_robust.py:65-85`. The deny-list for the agentic-intent dataset is different — use:
         `columns_to_remove = ["text", "label", "source", "dataset"]`
         (these are the 4 string columns from the agentic CSV, NOT the dozen-plus benchmark columns from the original).
       - Iterate `df.columns`; keep numeric-dtype columns NOT in `columns_to_remove`. Return the list.

    5. Function `build_text_input(df: pd.DataFrame) -> pd.Series`:
       - Returns `df["text"].fillna("").astype(str)`. (No Stage-2 router smuggling format here; this is a binary head trained on raw prompts.)

    6. Function `build_numeric_features(df: pd.DataFrame, extractor: PromptFeatureExtractor, feature_columns: list) -> pd.DataFrame`:
       - For each row of `df`, call `extractor.extract(row["text"])` to produce a feature dict. (NOTE: This is the 5-new-features-extended dict from Plan 02.)
       - Build a DataFrame from the list of dicts. `.fillna(0)`.
       - Return `df_numeric[feature_columns]`.

    7. Function `train_agentic_intent_classifier(df: pd.DataFrame, extractor: PromptFeatureExtractor)`:
       - Mirror `train_task_type_classifier` from `train_task_classifier_robust.py:338-449`.
       - Required-column check at the top: `if "text" not in df.columns: raise ValueError("CSV must contain a 'text' column.")` and same for `label` (per CLAUDE.md Error Handling pattern).
       - Drop rows with empty `text` or NaN `label`.
       - LabelEncoder on `label` → `y`.
       - First feature pass: build numeric DataFrame on the FULL dataset to discover `feature_columns` via `get_numeric_feature_columns`. (Reason: numeric columns come from extractor output, which is uniform across rows; we need the column names BEFORE the train/test split.)
       - Build `text_data = build_text_input(df)` and `numeric_features = build_numeric_features(df, extractor, feature_columns)`.
       - `train_test_split(text_data, numeric_features, y, test_size=0.2, random_state=42, stratify=y)` → `X_text_train, X_text_test, X_num_train, X_num_test, y_train, y_test`.
       - Build `vectorizer = FeatureUnion([word_tfidf, char_tfidf])` per `<interfaces>` block (verbatim hyperparams from train_task_classifier_robust.py:373-388: word ngram_range=(1,2), min_df=2, max_df=0.95, max_features=10000; char analyzer="char_wb", ngram_range=(3,5), min_df=2, max_features=6000).
       - `X_train_tfidf = vectorizer.fit_transform(X_text_train)`; `X_test_tfidf = vectorizer.transform(X_text_test)`.
       - `scaler = StandardScaler()`; `X_train_num_scaled = scaler.fit_transform(X_num_train)`; `X_test_num_scaled = scaler.transform(X_num_test)`.
       - `X_train_num_sparse = csr_matrix(X_train_num_scaled)`; same for test.
       - `X_train_combined = hstack([X_train_tfidf, X_train_num_sparse])`; same for test.
       - Base classifier: `model = LogisticRegression(max_iter=1500, class_weight="balanced", solver="saga", C=2.0, n_jobs=-1)`. Fit on `X_train_combined, y_train`.
       - Calibration step (CRITICAL — Pitfall 1 + Pitfall 3): carve a calibration slice FROM the training data only:
         `X_train_only, X_calib, y_train_only, y_calib = train_test_split(X_train_combined, y_train, test_size=0.25, random_state=42, stratify=y_train)`.
         (Note: per RESEARCH §Pattern 1 note at line 404, FrozenEstimator means the base classifier is NOT refit — it was trained on the full X_train_combined; we only need a fresh slice for calibration.)
         `calibrated = CalibratedClassifierCV(FrozenEstimator(model), method="sigmoid")`. `calibrated.fit(X_calib, y_calib)`.
         (Do NOT pass `cv="prefit"` — Pitfall 1 — the wrapper auto-detects FrozenEstimator since 1.6.)
       - Print metrics block per CLAUDE.md "Logging" + 01-PATTERNS.md "Metric reporting block" (uses `calibrated.predict(X_test_combined)` for `y_pred`):
         ```
         print("\\nAgentic Intent Classifier Results")
         print("---------------------------------")
         print(f"Accuracy: {accuracy_score(y_test, y_pred):.4f}")
         print(f"Macro F1: {f1_score(y_test, y_pred, average='macro'):.4f}")
         print(f"Weighted F1: {f1_score(y_test, y_pred, average='weighted'):.4f}")
         print("\\nClassification Report:")
         print(classification_report(y_test, y_pred, target_names=label_encoder.classes_, zero_division=0))
         ```
       - Confusion-matrix plot saved to `os.path.join(PLOTS_DIR, "confusion_matrix.png")` at `dpi=300` with `plt.tight_layout()` and `plt.close()` per CLAUDE.md "Plotting conventions". Print `Saved confusion matrix to: <path>`.
       - Return: `calibrated, vectorizer, scaler, label_encoder, feature_columns` (5-tuple).

    8. Function `save_classifier_artifacts(*, model, vectorizer, scaler, label_encoder, feature_columns, output_path=MODEL_PATH)`:
       - Mirror `save_router_artifacts` shape from `train_model_router.py:370-391` per 01-PATTERNS.md lines 922-949, but DROP `target_column` (binary head; not a multi-target router).
       - `artifacts = {"model": model, "vectorizer": vectorizer, "scaler": scaler, "label_encoder": label_encoder, "feature_columns": feature_columns}`
       - `joblib.dump(artifacts, output_path)`.
       - Print `Saved agentic intent classifier artifacts to:\\n{output_path}`.

    9. Function `load_classifier_artifacts(model_path=MODEL_PATH)`:
       - Mirror the loader at `train_model_router.py:394-430`. `if not os.path.exists(model_path): raise FileNotFoundError(...)` with the canonical multi-line "Run this script in 'train' mode first." remediation. Validate the same 5 required keys; `KeyError` per missing key.
       - Return `(model, vectorizer, scaler, label_encoder, feature_columns)` 5-tuple.

    10. `main()`:
        - Mirror `train_task_classifier_robust.py:553-580` REPL pattern. Print banner; prompt `train` / `load`. Train branch: load CSV, run `train_agentic_intent_classifier`, call `save_classifier_artifacts`. Load branch: call `load_classifier_artifacts`, print summary. Invalid mode: print and return.

    11. `if __name__ == "__main__": main()`.

    12. Run training: `echo train | uv run python -m src.task_classifier.train_agentic_intent` (the script's `input()` prompt accepts piped input).

    13. Verify the saved artifact loads via the existing demo validator: `uv run python -c "import sys, os; sys.path.insert(0, 'src/demo'); sys.path.insert(0, 'src/feature_extraction'); from demo_router import load_joblib_artifacts; a = load_joblib_artifacts('models/agentic_intent_classifier.joblib', 'agentic_intent_classifier.joblib'); assert set(a.keys()) >= {'model', 'vectorizer', 'scaler', 'label_encoder', 'feature_columns'}; print('keys ok:', sorted(a.keys()))"`. Expect `keys ok: ['feature_columns', 'label_encoder', 'model', 'scaler', 'vectorizer']`.
  </action>
  <verify>
    <automated>echo train | uv run python -m src.task_classifier.train_agentic_intent &amp;&amp; uv run python -c "import joblib; a = joblib.load('models/agentic_intent_classifier.joblib'); req = {'model', 'vectorizer', 'scaler', 'label_encoder', 'feature_columns'}; assert set(a.keys()) &gt;= req, sorted(a.keys()); from sklearn.calibration import CalibratedClassifierCV; assert isinstance(a['model'], CalibratedClassifierCV), type(a['model']); print('artifact ok; model type:', type(a['model']).__name__)"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/task_classifier/train_agentic_intent.py` exists
    - Source: file contains `from sklearn.frozen import FrozenEstimator`
    - Source: file contains `from sklearn.calibration import CalibratedClassifierCV`
    - Source: file does NOT contain the substring `cv="prefit"` or `cv='prefit'` (Pitfall 1)
    - Source: file contains `LogisticRegression(max_iter=1500, class_weight="balanced", solver="saga", C=2.0, n_jobs=-1)` (canonical hyperparams)
    - Source: file's `save_classifier_artifacts` function builds a dict with EXACTLY 5 keys: `model`, `vectorizer`, `scaler`, `label_encoder`, `feature_columns` — no `target_column`, no renamed `model` key (Pitfall 4)
    - Source: file uses `input()` for train/load mode toggle (training-script convention) — `grep "input(" src/task_classifier/train_agentic_intent.py` matches at least one line
    - Source: file does NOT contain `argparse` (training scripts don't use argparse per CLAUDE.md Module Design)
    - Behavior: `models/agentic_intent_classifier.joblib` exists after training
    - Behavior: `uv run python -c "import joblib; a = joblib.load('models/agentic_intent_classifier.joblib'); assert set(a.keys()) &gt;= {'model','vectorizer','scaler','label_encoder','feature_columns'}; print('ok')"` exits 0
    - Behavior: Loaded `model` is a `CalibratedClassifierCV` instance (not bare `LogisticRegression`)
    - Behavior: Loaded `label_encoder.classes_` equals `array(['agentic', 'conversational'])` (alphabetical)
    - Behavior: `evaluation/agentic_intent_plots/confusion_matrix.png` exists
    - CLI: Training run prints a "Classification Report:" block with per-class precision/recall for both `agentic` and `conversational`
  </acceptance_criteria>
  <done>
    Trained calibrated binary classifier saved with the canonical 5-key shape; loads via the existing demo validator unchanged; classification report printed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add classifier-level test asserting load + predict_proba + per-class P/R</name>
  <read_first>
    - src/task_classifier/tests/test_agentic_intent.py (the file already contains `test_dataset_csv_well_formed` from Plan 03; this task ADDS more tests, does NOT replace)
    - src/task_classifier/train_agentic_intent.py (from Task 1 — to know the exact load_classifier_artifacts signature)
    - .planning/phases/01-router-brain-foundation/01-VALIDATION.md (Wave 0 / 2 — the test file is owned by Plan 04 for the classifier-level assertions)
    - .planning/phases/01-router-brain-foundation/01-RESEARCH.md (section "Pitfall 4" lines 938-946 — preserve canonical 5-key shape; this test is the regression guard)
  </read_first>
  <action>
    1. Open `src/task_classifier/tests/test_agentic_intent.py`.
    2. After the existing `test_dataset_csv_well_formed` test (preserve it untouched), add three new tests:

    3. `test_artifact_loads_with_canonical_5_keys()`:
       - Load `models/agentic_intent_classifier.joblib` with `joblib.load`.
       - Assert keys `{"model", "vectorizer", "scaler", "label_encoder", "feature_columns"}` are all present.
       - Assert `target_column` is NOT in keys (binary head; absence is the contract).
       - Assert `label_encoder.classes_` is sorted as `['agentic', 'conversational']`.

    4. `test_calibrated_predict_proba_shape()`:
       - Load the artifact via `joblib.load`.
       - Build a tiny test input: `text = "build me a CLI tool"`. Use `PromptFeatureExtractor.extract(text)` then assemble the same TF-IDF + scaled-numeric + hstack pipeline used by training (mirror lines 100-135 of `src/demo/demo_router.py:predict_task_type` — for a binary head, just adapt to one prompt).
       - Call `model.predict_proba(combined)`. Assert shape `(1, 2)` and that the row sums to ≈1.0 (allow ±0.01 tolerance for floating point).
       - Assert that for the agentic-leaning prompt above, `model.predict_proba(combined)[0][label_encoder.transform(["agentic"])[0]] > 0.5`. (NOTE: this is a soft assertion — if the trained model puts higher weight on `conversational` for this string, the assertion fails and signals a training-data issue from Plan 03.)

    5. `test_held_out_macro_f1_meets_soft_target()`:
       - This test is a SOFT regression guard. Reload the training CSV via `pd.read_csv("data_processed/agentic_intent_training.csv")`. Re-run the same `train_test_split(test_size=0.2, random_state=42, stratify=y)` so the test split is deterministic.
       - For each row in the test split, build features and call `model.predict(combined)`. Compare to ground truth via `f1_score(y_test, y_pred, average="macro")`.
       - Assert macro-F1 ≥ 0.75 (RESEARCH-recommended soft floor; the target is 0.80 but the test bar is set lower to absorb cross-platform float drift). Use `pytest.fail(f"macro-F1 {f1:.4f} below 0.75 floor — check training data quality")` if violated.

    6. Add a top-of-file docstring noting that these tests require both Plan 03's CSV AND Plan 04's trained artifact to be present; if `models/agentic_intent_classifier.joblib` is missing, all three new tests should `pytest.skip("Run train_agentic_intent.py first")`.
  </action>
  <verify>
    <automated>uv run pytest src/task_classifier/tests/test_agentic_intent.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/task_classifier/tests/test_agentic_intent.py` contains all 4 test function names: `test_dataset_csv_well_formed`, `test_artifact_loads_with_canonical_5_keys`, `test_calibrated_predict_proba_shape`, `test_held_out_macro_f1_meets_soft_target`
    - Source: file does NOT contain `pytest.skip(` at module level (only inside test bodies, conditional on artifact presence)
    - Test: `uv run pytest src/task_classifier/tests/test_agentic_intent.py -x -q` exits 0
    - Test: All 4 tests pass (no skips when the artifact is present)
  </acceptance_criteria>
  <done>
    Four tests cover dataset shape, artifact shape, predict_proba shape, and held-out macro-F1 floor; all green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| training CSV → joblib pickle | Pickled artifact embeds the trained model state and feature pipeline; calls to `joblib.load` deserialize arbitrary Python objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-AG-1 | Tampering | `models/agentic_intent_classifier.joblib` | accept | Phase 1 joblib artifacts are produced and consumed by the same codebase. Documented in Plan 06's threat model (T-01-05 from RESEARCH §Security) and in the future ReadMe Security section (Phase 6 OSS-04). No code-level mitigation in this plan. |
| T-01-AG-2 | DoS via training-data poisoning | `data_processed/agentic_intent_training.csv` | accept | Plan 03's `--check` mode in `build_agentic_dataset.py` validates schema and balance before training. Class-weight=balanced absorbs minor imbalances. No defense against an attacker who edits the CSV directly — same trust model as every other training CSV in the repo. |
</threat_model>

<verification>
1. `models/agentic_intent_classifier.joblib` exists and loads with the canonical 5-key shape.
2. `model` field is a `CalibratedClassifierCV` instance.
3. Held-out macro-F1 ≥ 0.75 (4-test pytest module green).
4. `evaluation/agentic_intent_plots/confusion_matrix.png` saved.
5. Full pytest suite green: `uv run pytest -x -q`.
</verification>

<success_criteria>
**Phase Success Criterion #2 (this plan delivers it in full):** `models/agentic_intent_classifier.joblib` exists, loads via the same artifact-dict shape as the existing classifiers, and a held-out test reports precision/recall on agentic vs. conversational prompts.

**Internal:**
- Plan 06's `decide()` can call `agentic_intent_classifier.predict_proba(features)` to get `[P(agentic), P(conversational)]` and route per the D-01 rule cascade.
- Plan 07's `evaluate_routing.py` can compute per-stage ECE on the agentic head's `predict_proba` output (calibrated probabilities are meaningful per ROUTER-03).
</success_criteria>

<output>
After completion, create `.planning/phases/01-router-brain-foundation/01-04-SUMMARY.md` documenting:
- Final classification-report numbers (precision/recall/F1 per class) on the held-out test split.
- Macro-F1 and weighted-F1 numbers; flag if macro-F1 < 0.80 (RESEARCH-recommended soft target).
- The `feature_columns` list saved into the artifact (so Plan 06 can confirm the 5 new agentic features ARE included; if they are NOT, that signals Plan 02's extension didn't propagate properly).
- Whether `method="sigmoid"` was used or if the executor switched to `method="isotonic"` (RESEARCH Open Question 1 — sigmoid is the default; switch only if ECE > 0.10 on Plan 07's reliability-diagram output).
</output>
