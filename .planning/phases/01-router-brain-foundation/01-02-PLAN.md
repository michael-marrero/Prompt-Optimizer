---
phase: 01-router-brain-foundation
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/feature_extraction/Feature_extractor.py
  - src/feature_extraction/text_inputs.py
  - src/feature_extraction/tests/test_agentic_features.py
  - src/feature_extraction/tests/test_text_inputs.py
autonomous: true
requirements:
  - ROUTER-01
must_haves:
  truths:
    - "PromptFeatureExtractor.extract(prompt) returns a dict whose keys include the 5 new agentic-intent feature names verbatim: imperative_verb_count, has_url, has_file_path, has_code_fence, has_action_keyword (per D-08)."
    - "The 5 new features are reachable in the public extract() method, NOT only in a private helper — Plan 04 (agentic-intent classifier) reads them from extract()."
    - "Existing classifiers continue to load via load_joblib_artifacts() unchanged — adding fields to extract() does not break inference because build_numeric_features at src/demo/demo_router.py:101-105 trims to feature_columns (RESEARCH §Pattern 3 lines 503-511)."
    - "src/feature_extraction/text_inputs.py exposes the canonical Stage-2 router text input (build_router_text_input_series for DataFrame form + build_router_text_input_single for single-prompt form), eliminating the duplication between train_model_router.py and demo_router.py (per D-anti-pattern in 01-PATTERNS.md line 1278)."
  artifacts:
    - path: "src/feature_extraction/Feature_extractor.py"
      provides: "PromptFeatureExtractor.extract() now returns 5 additional fields (imperative_verb_count, has_url, has_file_path, has_code_fence, has_action_keyword)"
      contains: "_agentic_features"
    - path: "src/feature_extraction/text_inputs.py"
      provides: "Centralized Stage-2 router text input format helpers"
      contains: "build_router_text_input_single"
    - path: "src/feature_extraction/tests/test_agentic_features.py"
      provides: "Per-feature unit tests with explicit input → expected output assertions for the 5 new features"
  key_links:
    - from: "Feature_extractor.py:_agentic_features"
      to: "Feature_extractor.py:extract"
      via: "extract() body invokes features.update(self._agentic_features(text)) after _constraint_features"
      pattern: "self._agentic_features"
    - from: "Feature_extractor.py:_agentic_features"
      to: "_ensure_nltk_sentence_tokenizer"
      via: "Imperative-verb counting calls sent_tokenize which requires the punkt_tab download guard"
      pattern: "_ensure_nltk_sentence_tokenizer"
---

<objective>
Extend `PromptFeatureExtractor` with the 5 new agentic-intent handcrafted features mandated by CONTEXT D-08 (`imperative_verb_count`, `has_url`, `has_file_path`, `has_code_fence`, `has_action_keyword`) so they are available to every classifier that consumes `PromptFeatureExtractor.extract()`. Concurrently, lift the duplicated Stage-2 router text-input format from `train_model_router.py` and `demo_router.py` into a new `src/feature_extraction/text_inputs.py` module so Plan 06 (`src/routing/decide.py`) does not duplicate it a third time.

Purpose: Plans 04 (agentic-intent classifier training) and 05 (calibration retrain — Option A: re-train existing two heads on the extended feature set) both read from the extended `extract()`. Plan 06 (`decide.py`) imports both helpers from `text_inputs.py`. This plan is the foundation that unblocks Wave 2.

Output: Modified `Feature_extractor.py` with `_agentic_features` method, new `text_inputs.py` module, and unit tests asserting per-feature behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-router-brain-foundation/01-CONTEXT.md
@.planning/phases/01-router-brain-foundation/01-RESEARCH.md
@.planning/phases/01-router-brain-foundation/01-PATTERNS.md
@.planning/phases/01-router-brain-foundation/01-VALIDATION.md
@.planning/phases/01-router-brain-foundation/01-01-SUMMARY.md
@CLAUDE.md
@src/feature_extraction/Feature_extractor.py
</context>

<interfaces>
Key shapes the executor needs (extracted from existing code per 01-PATTERNS.md):

From src/feature_extraction/Feature_extractor.py:30-360 (current PromptFeatureExtractor):
```python
class PromptFeatureExtractor:
    def __init__(self):
        # Keyword groups stored on the instance (code_keywords, math_keywords, ..., reasoning_keywords)
        ...

    def extract(self, text: str) -> dict:
        text = _safe_text(text)
        features = {}
        features.update(self._basic_text_features(text))
        features.update(self._symbol_features(text))
        features.update(self._keyword_features(text))
        features.update(self._complexity_features(text))
        features.update(self._constraint_features(text))
        return features

    def _basic_text_features(self, text: str) -> dict:
        # First call site of sent_tokenize; calls _ensure_nltk_sentence_tokenizer() at line ~130
        ...

    def _keyword_features(self, text: str) -> dict:
        # Returns dict like {"has_code_keywords": 0|1, "code_keyword_count": int, ...}
        ...
```

From src/model_router/train_model_router.py:125-155 (current build_text_input on the DataFrame side):
```python
def build_text_input(df: pd.DataFrame) -> pd.Series:
    origin_query = df["origin_query"].fillna("").astype(str)
    if "question_type" in df.columns:
        question_type = df["question_type"].fillna("unknown").astype(str)
    else:
        question_type = pd.Series(["unknown"] * len(df), index=df.index)
    if "keyword_question_type" in df.columns:
        keyword_question_type = df["keyword_question_type"].fillna("unknown").astype(str)
    else:
        keyword_question_type = pd.Series(["unknown"] * len(df), index=df.index)
    return origin_query + " task_type_" + question_type + " keyword_type_" + keyword_question_type
```

From src/demo/demo_router.py:110-129 (current build_model_router_text_input on the single-prompt side):
```python
def build_model_router_text_input(prompt: str, question_type: str, keyword_question_type: str = "unknown") -> pd.Series:
    combined_text = (
        str(prompt)
        + " task_type_"
        + str(question_type)
        + " keyword_type_"
        + str(keyword_question_type)
    )
    return pd.Series([combined_text])
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _agentic_features to PromptFeatureExtractor</name>
  <read_first>
    - src/feature_extraction/Feature_extractor.py (full file — to see _basic_text_features, _keyword_features, _complexity_features, _constraint_features patterns; the extract() orchestrator at lines ~101-109; _ensure_nltk_sentence_tokenizer at lines 8-18; _safe_text at lines 21-28)
    - .planning/phases/01-router-brain-foundation/01-RESEARCH.md (section "Pattern 3 — Agentic-Intent Classifier" lines 461-501 for the verbatim _agentic_features body; section "Common Pitfalls — Pitfall 5" lines 948-963 for NLTK guard reuse; section "Security Domain" line 1231 — bound input length to 50,000 chars before regex to mitigate ReDoS)
    - .planning/phases/01-router-brain-foundation/01-PATTERNS.md (section "src/feature_extraction/Feature_extractor.py (MODIFIED — +5 agentic features)" lines 580-652 for the insertion-point and pattern-mirror guidance)
    - .planning/phases/01-router-brain-foundation/01-CONTEXT.md (D-08 verbatim — the 5 feature names are LOCKED: imperative_verb_count, has_url, has_file_path, has_code_fence, has_action_keyword)
    - CLAUDE.md (section "Conventions — Naming Patterns" — internal helpers prefixed with single underscore; section "How scikit-learn / pandas / numpy Are Used" — `.fillna(0)` for numeric features)
  </read_first>
  <behavior>
    - Test 1: `extractor.extract("write a Python function for fizzbuzz")` returns a dict whose `imperative_verb_count >= 1` (the imperative "write" is the first word).
    - Test 2: `extractor.extract("explain how OAuth works")` returns a dict whose `imperative_verb_count == 0` ("explain" is not in the imperative-verbs set per D-08; or if explain IS added, document why; per RESEARCH lines 472-478 the locked list is the 26-verb set).
    - Test 3: `extractor.extract("open https://example.com and click subscribe")` returns `has_url == 1`, `has_action_keyword == 1`, and `imperative_verb_count >= 1`.
    - Test 4: `extractor.extract("see /usr/local/bin/foo for details")` returns `has_file_path == 1`.
    - Test 5: `extractor.extract("```python\\nprint(1)\\n```")` returns `has_code_fence == 1`.
    - Test 6: `extractor.extract("hello")` returns ALL 5 new keys with falsy/zero values (proves keys are always present, not conditional).
    - Test 7: `extractor.extract("")` and `extractor.extract(None)` both return ALL 5 new keys without raising (V7 Error Handling per RESEARCH §Security line 1233).
    - Test 8: `extractor.extract("a" * 100_000)` completes in under 1 second on the test runner — proves the 50k-char input bound described in RESEARCH §Security line 1231 actually applies (executor MUST add `text = text[:50_000]` at the top of `_agentic_features` before any `re.search` call).
    - Test 9: A regression assertion that `extract("hello")` still returns ALL the previously-existing keys from `_basic_text_features`, `_symbol_features`, `_keyword_features`, `_complexity_features`, `_constraint_features` (proves the addition is purely additive).
  </behavior>
  <action>
    1. Open `src/feature_extraction/Feature_extractor.py`.
    2. After the existing `_constraint_features` method, add a new private method `_agentic_features(self, text: str) -> dict`. The body MUST:
       - First line: `text = text[:50_000]` (RESEARCH §Security line 1231 mitigation against ReDoS — bound input length BEFORE any regex).
       - Second line: `text_lower = text.lower()`.
       - Define the locked imperative-verbs set (per D-08 + RESEARCH lines 472-478): `{"build", "make", "create", "write", "edit", "refactor", "fix", "implement", "add", "remove", "delete", "update", "rewrite", "open", "browse", "click", "navigate", "visit", "fill", "submit", "scrape", "fetch", "download", "install", "run"}`.
       - Call `_ensure_nltk_sentence_tokenizer()` (the existing module-level guard from line 11).
       - Compute `imperative_verb_count = sum(1 for s in sent_tokenize(text) if s.strip().split() and s.strip().split()[0].lower() in imperative_verbs)`.
       - Compute `has_url = 1 if re.search(r"https?://\S+", text) else 0` (simple non-backtracking pattern).
       - Compute `has_file_path = 1 if re.search(r"(/[^\s/]+)+|[A-Za-z]:\\\\", text) else 0` — note the slight simplification from the RESEARCH-suggested pattern to avoid catastrophic backtracking (anchored `/`-paths require at least one non-slash non-space char).
       - Compute `has_code_fence = 1 if "```" in text else 0` (simple substring; no regex).
       - Compute the locked action-keywords set (per RESEARCH line 491): `{"build", "open", "scrape", "fill", "click", "edit", "refactor"}`. `has_action_keyword = 1 if any(kw in text_lower for kw in action_keywords) else 0`.
       - Return the 5-key dict using EXACTLY those names: `{"imperative_verb_count": ..., "has_url": ..., "has_file_path": ..., "has_code_fence": ..., "has_action_keyword": ...}`.
    3. Update the existing `extract(self, text: str) -> dict` method (currently at ~line 101) to add ONE NEW LINE after `features.update(self._constraint_features(text))`: `features.update(self._agentic_features(text))`.
    4. Do NOT modify `_safe_text`, `_ensure_nltk_sentence_tokenizer`, or any existing feature method.
    5. Implement test assertions in `src/feature_extraction/tests/test_agentic_features.py` — REMOVE the `pytest.skip(...)` line from Plan 01's scaffolding and write the 9 tests from `<behavior>`. Test fixture: `extractor = PromptFeatureExtractor()` at module level (no NLTK download in pytest collection — first `extract()` call lazy-downloads via the existing guard).
    6. Naming: `_agentic_features` follows snake_case + leading-underscore-for-private convention (CLAUDE.md "Naming Patterns"). Type hint: `def _agentic_features(self, text: str) -> dict:`. Position the method between `_constraint_features` (currently last) and any module trailer code.
  </action>
  <verify>
    <automated>uv run pytest src/feature_extraction/tests/test_agentic_features.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "_agentic_features" src/feature_extraction/Feature_extractor.py` returns at least 2 (definition + call from extract())
    - Source: `grep "imperative_verb_count" src/feature_extraction/Feature_extractor.py` matches at least one line
    - Source: `grep "has_url" src/feature_extraction/Feature_extractor.py` matches at least one line
    - Source: `grep "has_file_path" src/feature_extraction/Feature_extractor.py` matches at least one line
    - Source: `grep "has_code_fence" src/feature_extraction/Feature_extractor.py` matches at least one line
    - Source: `grep "has_action_keyword" src/feature_extraction/Feature_extractor.py` matches at least one line
    - Source: `grep "text\[:50_000\]\|text = text\[:50000\]" src/feature_extraction/Feature_extractor.py` matches at least one line (ReDoS mitigation)
    - Source: `grep "_ensure_nltk_sentence_tokenizer" src/feature_extraction/Feature_extractor.py` matches at least 2 lines (existing call site + new call from `_agentic_features`)
    - Behavior: `uv run python -c "from src.feature_extraction.Feature_extractor import PromptFeatureExtractor; e = PromptFeatureExtractor(); d = e.extract('open https://x.com'); assert d['has_url'] == 1 and d['imperative_verb_count'] >= 1, d"` exits 0
    - Behavior: `uv run python -c "from src.feature_extraction.Feature_extractor import PromptFeatureExtractor; e = PromptFeatureExtractor(); d = e.extract(''); assert {'imperative_verb_count', 'has_url', 'has_file_path', 'has_code_fence', 'has_action_keyword'} <= set(d), d"` exits 0
    - Test: `uv run pytest src/feature_extraction/tests/test_agentic_features.py -x -q` exits 0 with 9 tests passing
  </acceptance_criteria>
  <done>
    PromptFeatureExtractor.extract() returns the 5 new keys for any input; ReDoS-safe; tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lift Stage-2 router text input into src/feature_extraction/text_inputs.py</name>
  <read_first>
    - src/model_router/train_model_router.py (lines 125-155 for the DataFrame-form `build_text_input`)
    - src/demo/demo_router.py (lines 110-129 for the single-prompt `build_model_router_text_input`)
    - .planning/phases/01-router-brain-foundation/01-PATTERNS.md (section "src/feature_extraction/text_inputs.py (NEW — planner discretion)" lines 656-709 supplies the verbatim code to lift; "Anti-Patterns to AVOID" line 1278 mandates this consolidation)
    - .planning/phases/01-router-brain-foundation/01-RESEARCH.md (section "Anti-Patterns to Avoid" line 853)
    - CLAUDE.md (section "Naming Patterns" — `snake_case` modules; section "Module Design" — no `__all__` lists)
  </read_first>
  <behavior>
    - Test 1: `build_router_text_input_single("write code", "coding", "code")` returns a `pd.Series` of length 1 whose only value is the literal string `"write code task_type_coding keyword_type_code"`.
    - Test 2: `build_router_text_input_single("hi")` (using default `keyword_question_type="unknown"`, `question_type` REQUIRED so this is a TypeError-or-explicit) — actually the existing signature defaults `question_type` is NOT defaulted. Pass `("hi", "general")` and assert the result equals `pd.Series(["hi task_type_general keyword_type_unknown"])`.
    - Test 3: `build_router_text_input_series(pd.DataFrame({"origin_query": ["a", "b"], "question_type": ["coding", "math"], "keyword_question_type": ["code", "math"]}))` returns a `pd.Series` equal to `pd.Series(["a task_type_coding keyword_type_code", "b task_type_math keyword_type_math"])`.
    - Test 4: `build_router_text_input_series(pd.DataFrame({"origin_query": ["x"]}))` (no question_type / keyword_question_type columns) returns `pd.Series(["x task_type_unknown keyword_type_unknown"])` — proves the missing-column fallback to `"unknown"`.
    - Test 5: NaN handling — `build_router_text_input_series(pd.DataFrame({"origin_query": [None], "question_type": [None]}))` returns a series whose value is `" task_type_unknown keyword_type_unknown"` (origin_query empty, question_type filled with "unknown" via .fillna("unknown")).
  </behavior>
  <action>
    1. Create `src/feature_extraction/text_inputs.py` with two public functions:
       - `build_router_text_input_series(df: pd.DataFrame) -> pd.Series` — DataFrame form. Body lifted verbatim from `src/model_router/train_model_router.py:125-155` (see `<interfaces>` block). Use `.fillna("").astype(str)` on `origin_query` and `.fillna("unknown").astype(str)` on the two type columns; emit a `pd.Series(["unknown"] * len(df), index=df.index)` when a column is missing.
       - `build_router_text_input_single(prompt: str, question_type: str, keyword_question_type: str = "unknown") -> pd.Series` — single-prompt form. Body lifted verbatim from `src/demo/demo_router.py:110-129`.
       Both functions concatenate using the EXACT format `"<query> task_type_<qt> keyword_type_<kqt>"` so the existing `model_router.joblib` vectorizer's tokenization keeps producing the same `task_type_<X>` and `keyword_type_<X>` tokens as before — this is load-bearing.
    2. Add a module docstring at the top: `"""Centralized Stage-2 router text input format.\\n\\nLifted from src/model_router/train_model_router.py:125-155 and\\nsrc/demo/demo_router.py:110-129 to eliminate duplication. Both call sites\\nare updated by this plan to import from here."""`.
    3. Import-only: `import pandas as pd`. No other dependencies.
    4. Implement test assertions in `src/feature_extraction/tests/test_text_inputs.py` (NEW file — Plan 01 did not scaffold this since text_inputs is a new module discovered at planning time). Tests cover the 5 behaviors above using `pd.testing.assert_series_equal` for the series-comparison assertions.
    5. Do NOT update `train_model_router.py` or `demo_router.py` import sites in this task — that is Task 3 below (separated so a failure in lift logic does not poison the original sites).
  </action>
  <verify>
    <automated>uv run pytest src/feature_extraction/tests/test_text_inputs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/feature_extraction/text_inputs.py` exists
    - Source: file contains the literal substring `def build_router_text_input_series`
    - Source: file contains the literal substring `def build_router_text_input_single`
    - Source: file contains the literal substring `task_type_` (the format token)
    - Source: file contains the literal substring `keyword_type_`
    - Source: file's only top-level imports are `import pandas as pd` (no sklearn, no scipy, no joblib)
    - Behavior: `uv run python -c "from src.feature_extraction.text_inputs import build_router_text_input_single; s = build_router_text_input_single('write code', 'coding', 'code'); assert s.iloc[0] == 'write code task_type_coding keyword_type_code', s.iloc[0]"` exits 0
    - Test: `uv run pytest src/feature_extraction/tests/test_text_inputs.py -x -q` exits 0 with 5 tests passing
  </acceptance_criteria>
  <done>
    text_inputs.py exposes both helpers with the canonical format string; tests green; original call sites still untouched (Task 3 will swap them).
  </done>
</task>

<task type="auto">
  <name>Task 3: Switch existing call sites to import from text_inputs.py (no behavior change)</name>
  <read_first>
    - src/model_router/train_model_router.py (lines 1-50 for the import block; lines 125-155 for the function being replaced; lines 600-660 for the call sites that invoke `build_text_input` during training to confirm the rename works)
    - src/demo/demo_router.py (lines 1-30 for imports; lines 110-129 for the function being replaced; lines 200-240 for the call sites)
    - .planning/phases/01-router-brain-foundation/01-PATTERNS.md (section "Anti-Patterns to AVOID" line 1278; section "src/feature_extraction/text_inputs.py" lines 656-709)
    - CLAUDE.md (section "Import Organization" — sys.path injection convention; section "Anti-Patterns" — `sys.path` duplication is flagged but EXISTING sites stay untouched per the no-broad-rewrite guidance in CONTEXT `<deferred>`)
  </read_first>
  <action>
    1. In `src/model_router/train_model_router.py`:
       - At the top of the file, after the existing `import` block but before the `from Feature_extractor import …` line, add: `from src.feature_extraction.text_inputs import build_router_text_input_series`. (Note: this import works because `python -m src.model_router.train_model_router` is the canonical invocation per CLAUDE.md "Module Design"; the existing `sys.path.append` mechanism in this file remains unchanged for `Feature_extractor`.)
       - Delete the existing `def build_text_input(df: pd.DataFrame) -> pd.Series:` block at lines 125-155.
       - Replace every call site that invoked `build_text_input(df)` with `build_router_text_input_series(df)` (likely 1-3 sites within `train_model_router.py`).
    2. In `src/demo/demo_router.py`:
       - At the top of the file, after the existing `from Feature_extractor import PromptFeatureExtractor` line (which depends on `sys.path` injection), add: `from src.feature_extraction.text_inputs import build_router_text_input_single`. The `sys.path.append` for `Feature_extractor` stays — this is a planner-allowed exception per CONTEXT `<code_context>` line 151 (broad migration deferred).
       - Delete the existing `def build_model_router_text_input(prompt, question_type, keyword_question_type="unknown") -> pd.Series:` block at lines 110-129.
       - Replace every call site that invoked `build_model_router_text_input(...)` with `build_router_text_input_single(...)` (likely 1-2 sites within `demo_router.py`).
    3. Confirm both files still import / parse and any direct `python -m src.model_router.train_model_router` or `python src/demo/demo_router.py` invocation imports without error — this is a static check via `uv run python -c "import importlib; importlib.import_module('src.model_router.train_model_router'); print('train ok')"` and similar for demo_router (note: `demo_router.py` uses `sys.path.append` so direct import via `src.demo.demo_router` should still work because the package marker `__init__.py` is missing in `src/demo/` — check `ls src/demo/__init__.py` first; if absent, leave it absent and rely on the existing `python src/demo/demo_router.py` invocation form for verification).
    4. Do NOT make any other change to either file in this task.
  </action>
  <verify>
    <automated>uv run python -c "from src.feature_extraction.text_inputs import build_router_text_input_series, build_router_text_input_single; print('imports ok')" &amp;&amp; uv run python -c "import ast; src=open('src/model_router/train_model_router.py').read(); assert 'def build_text_input' not in src, 'old function still defined in train_model_router'; print('train_model_router clean')" &amp;&amp; uv run python -c "src=open('src/demo/demo_router.py').read(); assert 'def build_model_router_text_input' not in src, 'old function still defined in demo_router'; print('demo_router clean')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep "def build_text_input" src/model_router/train_model_router.py` returns nothing (function removed)
    - Source: `grep "def build_model_router_text_input" src/demo/demo_router.py` returns nothing (function removed)
    - Source: `grep "from src.feature_extraction.text_inputs import" src/model_router/train_model_router.py` matches at least one line
    - Source: `grep "from src.feature_extraction.text_inputs import" src/demo/demo_router.py` matches at least one line
    - Source: `grep -c "build_router_text_input_series" src/model_router/train_model_router.py` returns at least 2 (import + at least one call site)
    - Source: `grep -c "build_router_text_input_single" src/demo/demo_router.py` returns at least 2 (import + at least one call site)
    - Behavior: `uv run python -c "from src.feature_extraction.text_inputs import build_router_text_input_series, build_router_text_input_single; print('ok')"` exits 0 and prints `ok`
    - Behavior: `uv run python -c "import sys; sys.path.insert(0, 'src/feature_extraction'); import importlib; m = importlib.import_module('src.model_router.train_model_router'); print(hasattr(m, 'build_router_text_input_series'))"` exits 0
    - Behavior: Full pytest suite stays green — `uv run pytest -x -q` exits 0
  </acceptance_criteria>
  <done>
    Both legacy call sites import from `text_inputs.py`; old function definitions removed; suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → PromptFeatureExtractor.extract(prompt) | Untrusted prompt text crosses into regex matching and string operations inside `_agentic_features` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-FE-1 | Denial of Service | `_agentic_features` regex matching (URLs, file paths) | mitigate | Bound input length to 50,000 chars at the top of `_agentic_features` (RESEARCH §Security line 1231). Use simple non-backtracking patterns: `https?://\S+` for URLs (no nested quantifiers), and a non-greedy `(/[^\s/]+)+` for file paths (each segment is bounded by non-slash non-space characters). Test #8 in Task 1 verifies 100k-char input completes in under 1 second. |
| T-01-FE-2 | Denial of Service | `sent_tokenize` on adversarial input | mitigate | NLTK's `punkt_tab` tokenizer is bounded by the same 50,000-char input cap applied before the call. No additional mitigation required. |
</threat_model>

<verification>
1. `PromptFeatureExtractor.extract("…")` returns the 5 new keys for every input including empty string and None.
2. `text_inputs.py` exists with both helpers; both legacy call sites import from it.
3. Full pytest suite passes: `uv run pytest -x -q`.
4. `_agentic_features` is reachable from `extract()` (verified by Test 9 in Task 1's behavior block).
5. No regressions to existing classifiers — they still load via `joblib.load(…)` with the unchanged 5-key artifact dict (no model file is touched in this plan).
</verification>

<success_criteria>
- Plan 04 (agentic-intent classifier training) can call `PromptFeatureExtractor.extract(prompt)` and receive the 5 new agentic features in the returned dict without any further extension to `Feature_extractor.py`.
- Plan 05 (calibration retrain — Option A) can re-train `task_type_classifier` and `model_router` on the extended feature set; `feature_columns` will grow by 5 in those artifacts.
- Plan 06 (`src/routing/decide.py`) can import `build_router_text_input_single` from `src.feature_extraction.text_inputs` instead of re-implementing the format string a third time (eliminates the anti-pattern flagged in 01-PATTERNS.md line 1278).
</success_criteria>

<output>
After completion, create `.planning/phases/01-router-brain-foundation/01-02-SUMMARY.md` documenting:
- The exact set of imperative-verb tokens in the locked list (so Plan 04 can mention them in the agentic-intent training docstring).
- Whether `_agentic_features` was inserted at the planned position (after `_constraint_features`) or required reordering.
- Any other call sites of `build_text_input` / `build_model_router_text_input` discovered during Task 3 that are NOT in `train_model_router.py` or `demo_router.py` (so Plan 06 knows whether `decide.py` is actually the third site or if there are more).
</output>
