# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/

# Virtual environments
.venv/
venv/
env/

# Environment / secrets
.env
.env.local
.env.*.local

# Local databases (Phase 3 lands these; this list is forward-compatible per SECURE-03)
*.db
*.db-journal
*.db-wal
*.db-shm
chat.db

# IDE
.idea/
.vscode/

# macOS
.DS_Store

# Internal planning artifacts — local-only (PLAN/SUMMARY/VERIFICATION per phase, milestone archives, etc.)
.planning/

# Stray top-level node_modules (e.g., .vite cache from one-off tool runs)
/node_modules/

# Claude Code local permissions (machine-local; never shared)
.claude/settings.local.json

# Already-committed paths to LEAVE alone
# (this file does NOT exclude data_processed/, models/, evaluation/ — those are LFS-tracked)

# Next.js (Phase 4)
apps/web/node_modules/
apps/web/.next/
apps/web/coverage/
apps/web/playwright-report/
apps/web/test-results/
apps/web/.env.local

# pnpm content-addressable cache (pnpm 11 default in-repo store)
.pnpm-store/

# inspect-ai eval logs (Phase 10 EVAL harness — `.eval` / `inspect view` log dir)
logs/
